---
id: T-327
title: W0 — KPI temizliği: 6 e2e artığı + üreteç düzeltmesi + T-047 teyidi
epic:
sprint:
status: todo
assignee: qa-engineer
created: 2026-08-28
updated: 2026-08-28
touches:
  - collmind.backend/test/
  - collmind.backend/src/database/seeds/
migration_seq:
---

## `Faz-2` `W0` — sıralamanın **en başı** (`Z62 §3`)

### Bulgu `[ÖLÇÜLDÜ]` 2026-08-28
```
main.kpis   33 toplam / 30 aktif
            kpi_group='Test' → 6 kalem, HEPSİ is_active=TRUE, created_at 2026-08-16
            E2E_KPILOCK_{FRESH,MIXED,NOVERSION,RACE,STALE,XTENANT}_<epoch>
GERÇEK ürün 27 toplam / 24 aktif
```

⇒ *"Aktif KPI"* sayan **her ölçüm** bugün **`+6` şişik** — ve `W2` eşlemesi bu
evrenle yapılırsa **`42×30`** olur; doğrusu **`42×24`**.

## İş
1. **Altı artığı öldür** — canlı DB'den. ⚠️ Bunlar **tenant verisi değil**, test
   artefaktı; `created_at` ve `kpi_group='Test'` ile **kesin** ayrılıyorlar.
   ⛔ Silmeden önce **hangi tenant'a ait olduklarını** ölç; başka tenant varsa DUR.
2. ⛔ **ÜRETECİ BUL VE DÜZELT** — asıl iş bu. Bir e2e suite'i `kpis`'e yazıyor ve
   temizlemiyor. İki meşru şekil: `afterAll` temizliği · ya da `kpi_group='Test'`
   satırlarının **hiç yaratılmaması** (fixture yerine mevcut KPI kullanmak).
   **Hangisini seçtiğini gerekçesiyle yaz.**
3. **`T-047` teyidi** — evren artık `48/48` ve `kpis` **içinde**. Üreteç
   düzeltilmeden bir tam e2e koşumu invaryantı **kırmızı** vermeli.
   ⛔ **REPRODÜKSİYON ZORUNLU, iki çıktı:**
   ```
   düzeltme ÖNCESİ  tam e2e → T-047 KIRMIZI (kpis delta > 0)
   düzeltme SONRASI tam e2e → T-047 YEŞİL
   ```
   ⚠️ Eğer **öncesi de yeşil çıkarsa** bu bir bulgudur: ya suite bugün o KPI'ları
   yaratmıyor (artıklar **eski** bir koşumdan), ya invaryant `kpis`'i saymıyor.
   **Hangisi olduğunu ÖLÇ** ve raporla — sessizce geçme.

## ⛔ DUR listesi
- Ürün KPI'larına (`Test` dışı 27 kalem) **dokunma**
- `kpi_group` şemasını değiştirme
- Migration yazma (`migration_seq` yok)
- `W2` eşlemesine girme — bu tur **yalnız temizlik**

## Acceptance
- [ ] `main.kpis` `kpi_group='Test'` → **0 satır**, DB'den doğrulandı
- [ ] Üreteç düzeltildi, **seçim gerekçeli**
- [ ] `T-047` reprodüksiyonu **iki çıktılı** (ya da öncesi-yeşil bulgusu raporlandı)
- [ ] Tam `npm run test:e2e` → `T-047 PASS`, `kpis` delta **0**
- [ ] `CLAUDE.md §4.2` Done listesi tam
