---
id: T-382
title: "`T-375` re-review artıkları — beş 🟡, ikisi \"mekanizma doğru, ÖLÇÜMÜ yok\""
epic:
sprint:
status: todo
assignee: qa-engineer
created: 2026-09-07
updated: 2026-09-07
touches:
  - collmind.backend/src/modules/modes/actuals-first/agreement/agreement.service.ts
  - collmind.backend/src/modules/modes/planning-first/plan/plan.service.ts
  - collmind.backend/test/optimistic-locking.e2e-spec.ts
  - collmind.backend/test/on-invoice-ledger-invariants.e2e-spec.ts
  - collmind.backend/src/database/migrations/1828000000000-BackfillAgreementCategoryFromForecastingUnit.ts
---

## Nasıl doğdu
`T-375` dalgası **PUSH'A UYGUN** hükmüyle indi; re-review beş `🟡` bıraktı. Hiçbiri finansal
bir yanlış **değer** üretmiyor — ama ikisi bu projede en pahalıya mal olan sınıfın üyesi:
**mekanizma doğru, ölçümü yok.**

## ⭐ ÖNCE BU İKİSİ (review'ın kendi sıralaması)

### `Y3` — `S1`'in yedi yeni guard'ının **SIFIRI** pinli, ve fixture'lar durumu **YOK ETTİ**
`plan.service.ts:939,952,1438,1447,1690,1702` · `approval-workflow.service.ts:78,88` ·
`agreement.service.ts:820-831`

Bu dalga *"kanal/kategori kodu boşsa açık hata"* guard'larını ekledi. Ölçüm:
`grep "çözümlenemedi" src test | grep spec` → yalnız **on-invoice** kardeşi pinli
(`on-invoice.service.spec.ts:346`, üstelik *"bugün canlı veride tetiklenemeyen dal"* notuyla —
**doğru şekil**). Yeni yedi guard'ın **hiçbiri** pinli değil.

⛔ **Ve daha kötüsü:** unit spec'lerde yapılan tek şey guard'ı **KARŞILAMAK** oldu —
`agreement.service.spec.ts:265-269,480-484,533-537` fixture'lara `category: {code:'HAIR_CARE'}`
eklendi; `plan.service.spec.ts:450,1172` beklentiye `'DAIRY'` eklendi.

> ### Bu **`§2.7 #4`**'tür: *kanıt kurulumu, ölçülmek istenen durumu yok eder.*
> ### Guard'ın **ateşlediğini** gösteren tek bir assertion yok — yani guard yarın sessizce
> ### kaldırılsa **hiçbir test kırılmaz.**

**Yapılacak:** on-invoice emsalindeki gibi mock-tabanlı birer pin (relation `undefined` →
`400` + mesaj). ⛔ Ve her pin **mutasyonla** sınanır: guard kaldırılınca kırmızı, ve kırmızı
bir **assertion** olmalı.

### `Y2` — `B1` pinleri `İKİSİ BİRDEN değişiyor` senaryosunu **hiç ölçmüyor**
`test/optimistic-locking.e2e-spec.ts:1596-1651`

Üç pin ayırt edici (negatif · pozitif · yalnız-`fuId`). Ama ölçüldü:
`grep "patch(\`/agreements" -A6 test/*.ts | grep fuId` → **tek** eşleşme.
Yani `{ fuId, categoryId }`'nin **birlikte** gönderildiği hiçbir test yok — ne negatif ne pozitif.

⇒ Kodun *"taze çekilmiş FU × dto kategorisi"* dalı (`agreement.service.ts:527-531`'in **her iki
tarafının aynı anda `dto`'dan geldiği TEK yol**) **hiç koşmuyor**. Meşru *"anlaşmayı başka bir
FU+kategoriye taşı"* akışı da pinsiz.

**Yapılacak:** pozitif (`{fuId: FU_WELLA_HC_500ML, categoryId: HAIR_CARE}` → `200`) **ve**
negatif (`{fuId: FU_WELLA_HC_500ML, categoryId: CAT-SAC-BOYASI}` → `400`) pin.

## Kalan üç `🟡`

### `Y1` — `effectiveFu` `null` olabilir ⇒ kodlu `400` değil, ham `500`
`agreement.service.ts:525-531` → `:88` (`fu.genericUnit?.categoryId` — `fu`'nun **kendisi**
korumasız). Üretimde `null` gelebilir, iki ölçülmüş yoldan:
```
ForecastingUnit SOFT-DELETE'li (base.entity @DeleteDateColumn) · fu.service.ts:113 softRemove
  · @Delete(':id') ucu CANLI  ⇒ relation join soft-delete'liyi dışlar ⇒ forecastingUnit NULL
FK_agreements_fu_id  confdeltype='n' (ON DELETE SET NULL) · fu_id is_nullable=YES
```
⚠️ **Aynı dosya kardeş yolda bunu ZATEN BİLİYOR:** `:383` opsiyonel zincir + `logger.warn` +
fail-closed. Yani bilgi var, **yeni yola taşınmamış**.
Yön fail-closed (yazma reddediliyor, para hareket etmiyor) — ama `§2.5`: hata **kodlu ve
adlandırılmış** olmalı. `S7`'nin `create()`'e kazandırdığı üç-durum ayrımı `update()`'e de gelmeli.

### `Y4` — `S3`'ün iddiası gereğinden **geniş**, fixture'a kırılgan bağlı
`test/on-invoice-ledger-invariants.e2e-spec.ts:288-293` — ayırt etme gücünün **tamamı**
`Budget envelope bulunamadı` öbeği + `ERROR_PERIOD`'da; `DISTRIBUTOR` ve `CAT-SAC-BOYASI`
literalleri **hiçbir alternatifi elemiyor**. Arıza yönü **kırmızı** (sessiz yeşil değil), yani
kabul edilebilir — ama iddia **bedava** gevşetilebilir: regex ile öbek + dönem, kanal/kategori
**sabitlerden türetilerek**.

### `Y5` — `1828`'in **satır içi** yorumu çürütülmüş iddiayı hâlâ taşıyor
`1828000000000-*.ts:151-153` — dosya başı `S6` ile düzeltildi, satır içi yorum eski hükmü
**tekrarlıyor** ve düzeltilmiş başlığa **atıf veriyor**. `DISIPLIN`: *"yorum kirliliği iki
yönde birden yanıltır."* İki cümle.

## 📌 Ayrıca kayda (bu task'ın kapsamı değil, ama ölçüldü)
```
1827  "bağ" ölçümü İKİ tabloda, seed ÜÇ tabloda ⇒ aynı "bağ" tanımının İKİ EVRENİ var
      yön fail-closed (0/0 → throw) ⇒ veri riski yok
1827  tenant predicate YOK (WHERE code = ANY($1)) ⇒ çok-tenant'lı ortamda aynı kodlu
      BAŞKA bir tenant'ın zarfını kapatır. Bugün dar/legacy kapsam.
1828:104  `undivable` → `underivable` (yazım)
```

## Acceptance
- [ ] `Y3` ve `Y2` **önce** — ikisi de "mekanizma doğru, ölçümü yok"
- [ ] Her yeni pin **mutasyonla** sınanır; kırmızı bir **assertion** olmalı (derleme hatası değil)
- [ ] `Y1` kodlu bir `400` döner ve üç durum `update()`'te de ayrılır
- [ ] `Y4`/`Y5` iddiaları taşıdıkları şeye eşitlenir
