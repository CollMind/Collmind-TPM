---
id: T-341
title: Parser beyaz listesi ÜSTEL GÖSTERİMİ reddediyor — çok küçük/büyük ara değer sessizce null
epic:
sprint:
status: review
assignee: backend-engineer
created: 2026-08-30
updated: 2026-08-31
touches:
  - collmind.backend/src/modules/shared/kpi-engine/formula-parser.service.ts
  - collmind.backend/src/modules/shared/kpi-engine/formula-parser.service.spec.ts
---

## Açıklama — `T-334` kapanış turunda ÖLÇÜLDÜ, düzeltilmedi, **PİNLENDİ**
```
String(1e-7) === '1e-7'
beyaz liste  /^[0-9+\-*\/().]+$/   'e' KABUL ETMİYOR   ⇒ null
ayırt edici:  0.000001   ÇALIŞIYOR
              0.0000001  null            ← eşik: |v| < 1e-6  ya da  |v| >= 1e21
```
⛔ Bir **ara değer** bu aralığa düştüğü an, hesaplanabilir bir KPI **sessizce `null`** olur —
`§2.5`. Ve `null` bağımlı KPI'lara **yayılır** (`GP_ROI_PCT`, RAG).

⚠️ **Bu kusur `T-334`'ün getirdiği DEĞİL** — `B1`/`B1b` ile aynı ailede, aynı satırda yaşıyor.
`T-334` turu onu **ölçtü ve bugünkü hâliyle PİNLEDİ**, ki düzeltildiği gün **bilerek kırılsın**.

## Çözüm adayları (ölçülerek seçilir)
```
(a) beyaz listeye üstel gösterim eklenir     ⚠️ güvenlik kapısı GENİŞLER — dikkat
(b) yerine koyma toFixed/BigInt benzeri SABİT gösterim üretir
(c) değer sayısal olarak taşınır, dizgeye hiç girmez (parser yeniden yazılır)
```
⛔ **(a) bir GÜVENLİK kapısını genişletir** — `DISIPLIN`: *"'güvenlik' gerekçeleri en az
sorgulananlardır"*, ve bu sefer ters yön: kapıyı **gevşetirken** aynı titizlik gerekir.
`e` kabul edilirse `1e400` → `Infinity` gibi yollar **ölçülmeli**.

## Acceptance Criteria
- [ ] Mevcut pin (bugünkü `null` davranışı) **bilerek** kırılır ve yeni davranışa çevrilir
- [ ] Beyaz liste değişiyorsa **güvenlik yüzü yeniden ölçülür** (kod çalıştırma yolu yok mu)
- [ ] ⛔ **Denetlenen dizge = değerlendirilen dizge** eşitliği **korunur** (`T-334 B1` dersi)
- [ ] Eşik değerlerin **her iki yanı** pinlenir (`1e-6` / `1e-7` · `1e20` / `1e21`)

## Kaynak
`T-334` kapanış turu · review `B1`/`B1b` · `docs/DISIPLIN.md`

---

## ✅ KAPANDI (2026-08-31, `DALGA 2b`) — seçilen aday: **`(b)`, ama `toFixed` DEĞİL**

### Aday seçimi ÖLÇÜMLE yapıldı — TL'nin `(b)` önerisi kısmen ÇÜRÜTÜLDÜ

| aday | ölçüm | karar |
|---|---|---|
| **(a)** beyaz listeye `e` | `1e-400` beyaz listeden **GEÇER** ve **sessizce `0`** olur; sıfıra-bölme deseni onu görmez ⇒ dürüst `null`'ın yerine **yanlış sayı**. `1e400` → `Infinity` (yalnız çıkıştaki `isFinite` yakalar) | ⛔ **RED** |
| **(b) `toFixed`** *(TL'nin somut önerisi)* | `(5e-324).toFixed(20) === '0.00000000000000000000'` ⇒ **SESSİZ SIFIR**. Ve `(1e21).toFixed(2) === '1e+21'` ⇒ **BÜYÜK tarafı hiç düzeltmiyor** | ⛔ **RED — iki uçta da batıyor** |
| **(b) metinsel açılım** | `String(v)` zaten round-trip garantili en kısa gösterim; ondalık noktayı kaydırmak basamakları değiştirmez | ✅ **SEÇİLDİ** |
| **(c)** sayısal taşıma | parser yeniden yazımı; `DALGA 2b` kapsamının dışında, ve `(b)` sorunu kayıpsız çözüyor | ⏭️ gereksiz |

**Kayıpsızlık ölçümü:** `1e-320`..`1e+320`, **5.000.000 + 17** örnek →
round-trip ihlali **0**, beyaz liste ihlali **0**, çıktı karakter evreni **`-.0123456789`**,
en uzun çıktı **327** karakter.

> **TL'nin gerekçesi doğruydu, aracı yanlıştı.** *"Beyaz liste hiç genişlemesin,
> `denetlenen = değerlendirilen` korunsun"* — ikisi de sağlandı. Ama `toFixed` o
> gerekçeyi **taşıyamıyordu**: `§2.5`'in tam merkezinde iki sessiz sıfır üretiyor.

### Randevu-pini KIRILDI — sıra doğru
```
1  taban koşum            exit 0   (25 test yok, 22 vardı — pin YEŞİL)
2  RED-PROBE eklendi      exit 1   "Expected 1e-7 / Received null"   ← kusur GÖRÜLDÜ
3  düzeltme                        toFixedNotation + tek substituteDependencies
4  eski pin KIRILDI       exit 1   "Received: 1e-7"  ← pin bilerek kırıldı
5  pin YENİDEN yazıldı    exit 0   eşiğin İKİ YANI + 5e-324 + 1e20/1e21 + negatif üstel
```

### Acceptance Criteria
- [x] Mevcut pin bilerek kırıldı ve yeni davranışa çevrildi
- [x] Beyaz liste **DEĞİŞMEDİ** ⇒ güvenlik yüzü genişlemedi (yine de karşı-örnek tarandı, aşağıda)
- [x] `denetlenen dizge = değerlendirilen dizge` korundu (`safeEval` yine `sanitized` üzerinde)
- [x] Eşiğin her iki yanı pinli: `1e-6`/`1e-7` · `1e20`/`1e21` (+ `5e-324`, negatif üstel)
