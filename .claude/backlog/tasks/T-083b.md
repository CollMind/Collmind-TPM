---
id: T-083b
title: "Yetim anahtar ÖNLEMESİ — pasifleştirme, kullanan planlardaki anahtarı da temizler"
epic:
sprint:
status: todo
assignee: backend-engineer
priority: P3
created: 2026-08-05
updated: 2026-08-05
touches:
  - collmind.backend/src/modules/master-data/mechanic/mechanic.service.ts
  - collmind.backend/src/modules/modes/planning-first/plan/plan.repository.ts
migration_seq:
---

## Bağlam

[[T-083]]'ün **önleme** yarısı. Tedavi yarısı [[T-083a]] — o önce gitti ve **doğru mesajı**
getirdi (planı açmadı; bkz. T-083a'daki kapsam düzeltmesi). Bu task yeni yetimlerin doğmasını
engeller.

## ⚠️ ZORUNLU TASARIM GEREKSİNİMİ: temizlik GEÇMİŞE DE uygulanabilir olmalı

T-083a mesajı düzeltti ama planı açmadı; bu task geleceği koruyor. **İkisi birlikte de "zaten
kilitlenmiş bir planı açma" işini kapsamıyor.**

Ürün sahibi kararı (2026-08-05): **bunun için ayrı task AÇILMAZ.** Gerekçe iki katlı:

1. `main.plan_fus` bugün 0 satır — kurtarılacak plan yok, aciliyeti sıfır.
2. **Daha önemlisi:** bu task'ın temizlik mekanizması (bir mekaniğin anahtarlarını planlardan
   kaldırmak) kurtarma için de tam olarak aynı işi yapar. Ayrı task açmak, **aynı yeteneğin iki
   implementasyonu** riskini doğurur — bu reponun yedi kayıtlı vakası (iki submit yolu, iki
   lumpsum dağıtımı, iki CSV parser, üç kez yazılmış scope mantığı).

**Bu yüzden gereksinim tasarıma yazılıyor:**

> Temizlik, pasifleştirme akışına gömülü bir yan etki DEĞİL, **ayrı çağrılabilir bir yetenek**
> olarak yazılır. Pasifleştirme onu çağırır; mevcut yetim anahtarları temizlemek gerektiğinde
> **aynı yetenek** (bir bakım komutu / admin ucu) tekrar çağrılır.

Yani "bir mekaniğin anahtarlarını planlardan kaldır" tek bir yerde yaşar ve iki tetikleyicisi
olur. Ters tasarım — temizliği `update`/`remove` gövdesine gömmek — geçmişi kurtarmak
gerektiğinde ikinci bir implementasyon yazmaya zorlar.

**Bugün aciliyeti düşük:** `main.plan_fus` 0 satır (ölçüldü 2026-08-05), yani henüz yetim
üretebilecek veri yok. Ama ilk gerçek kullanımda çıkar.

## Karar: engelleme de uyarı da değil — TEMİZLİK (ürün sahibi, 2026-08-05)

İkili soru (engelle mi / uyar mı) yanıltıcıydı:

- **Engelleme yanlış:** bir mekanik gerçekten kullanımdan kalkıyorsa, admin'in onu
  pasifleştirememesi doğru değil. Admin kilitlenir.
- **Uyarı yetersiz:** yetim üretmeye devam eder, yalnız sesli yapar.

**Üçüncü yol:** pasifleştirme, o mekaniği kullanan planlardaki anahtarları **da temizler.**

1. Admin `DELETE /mechanics/:id` veya `PATCH {isActive:false}` yapar
2. Sistem etkilenen planları bulur, `plan_fus.tactics` JSONB'sinden o anahtarı kaldırır
3. Admin'e **kaç planın etkilendiği** raporlanır

Böylece pasifleştirme atomik olarak **tutarlı bir duruma** geçirir: mekanik gitti, ona ait
girdiler de gitti. Yetim doğmaz, admin engellenmez.

### Bedeli — saklanmıyor

**Planner'ın girdiği bir değer, onun haberi olmadan siliniyor.** Bu gerçek bir maliyet.

Ama alternatifler daha kötü, ve mevcut durumdan da kötü değil: o değer bugün **zaten hesaba
katılmıyor** (mekanik pasif), yalnızca planı kilitliyor. Silmek, kilitli tutmaktan iyidir.

## İki zorunlu koşul

- [ ] **Audit'e yazılır** — hangi plan, hangi anahtar, **hangi değer**, hangi admin.
      `AdminAuditService` zaten var (`src/common/services/admin-audit.service.ts`), yenisi
      yazılmaz. Değerin kaydedilmesi kritik: geri alınması gerekirse tek kaynak bu.
- [ ] **Aynı transaction'da** — pasifleştirme ile temizlik ayrılırsa yarı durum oluşur
      (mekanik pasif, anahtarlar duruyor = tam olarak önlemeye çalıştığımız hâl). Bu projenin
      tekrarlayan sınıfı.

## ⚠️ EK VAKA: yetim anahtarın KOLON yarısı hâlâ sessizce düşüyor

`code-reviewer` (T-083a review, S2). `spend-calculation.service.ts:761-763`:

```ts
const mech = byCode.get(code);
const raw = mech ? readEnteredRaw(pmv, mech) : undefined;
if (raw != null) put(code, raw);
```

Mekanik pasifleştirilmişse `mech` undefined → `plan_mechanic_values` satırı **hatasız, logsuz
atlanıyor** ve harcama sessizce eksik hesaplanıyor. CLAUDE.md §2.5'in "sessizce atlamak"
yasağı.

**T-083a'nın getirdiği bir kusur değil** — öncesinde de böyleydi. Ama T-083a'dan sonra
asimetri **bilinçli** okunur hâle geliyor: JSONB katmanı artık gürültülü (`MECHANIC_DEACTIVATED`
fırlatıyor), kolon katmanı hâlâ sessiz. Aynı yetim anahtar, hangi katmandan okunduğuna göre
farklı davranış görüyor.

`main.plan_mechanic_values` bugün boş olduğu için aciliyeti düşük. Bu task'ın temizliği kolon
tarafını da kapsamalı — yoksa önleme yalnız yarısını çözer.

## ⚠️ ÖLÇÜM ÖNCE — katman sınırı sorusu açık, kod yazmadan konuşulacak

`PATCH isActive` ve `DELETE` yolları bugün **plan verisine hiç dokunmuyor.** Onlara plan yazma
sorumluluğu eklemek bir katman sınırı sorusu doğuruyor:

> **Mekanik servisi plan verisini değiştirmeli mi, yoksa bir domain event mi?**

- **Doğrudan yazma:** basit, tek transaction kolay. Ama `MechanicService` → `PlanRepository`
  bağımlılığı master-data modülünü planning-first moduna bağlar; modül sınırı ihlali olabilir.
- **Domain event:** sınırı korur, ama aynı transaction koşulu event'lerle zorlaşır (event
  handler ayrı transaction'da koşarsa tam olarak yasakladığımız yarı durum oluşur).

**Bu karar `architect` ile konuşulmalı ve ürün sahibine getirilmeli. Kod yazmadan önce.**

Ölçülecekler:
- [ ] Kaç plan/FU etkilenir tipik bir pasifleştirmede? (Bugün 0; tasarım için üst sınır gerekir)
- [ ] `MechanicService` bugün başka bir modülün repository'sine dokunuyor mu? (Emsal var mı)
- [ ] Aynı transaction gerçekten sağlanabiliyor mu her iki tasarımda?

## Acceptance Criteria

- [ ] Pasifleştirme sonrası o mekaniğe ait hiçbir yetim anahtar kalmıyor (her iki yol:
      `PATCH isActive:false` **ve** `DELETE`)
- [ ] Temizlik ve pasifleştirme **tek transaction** — biri başarısız olursa ikisi de geri alınır
- [ ] Audit kaydı plan, anahtar, **değer** ve admin'i taşıyor
- [ ] Admin'e etkilenen plan sayısı dönüyor
- [ ] Katman sınırı kararı verilmiş ve **kod yorumunda gerekçelendirilmiş**
- [ ] e2e: mekanik pasifleştirilir → etkilenen planın `tactics`'inde anahtar yok → plan hâlâ
      düzenlenebilir ve submit edilebilir (kilit yok)

## Done tanımı
- [ ] Testler yeşil (unit + e2e), 3× reset'siz
- [ ] `npm run guards` exit 0 · `money-float.sh --ratchet` exit 0
- [ ] `code-reviewer` onayı
- [ ] Üretim çağrı yolu var
- [ ] `touches:` güncel
