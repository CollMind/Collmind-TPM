---
id: T-334
title: FORMÜL-KANON DÜZELTMESİ — TO/NIV kavram-ayrıştırma + GP tabanı + off-invoice tabanı
epic:
sprint:
status: review
assignee: data-engineer
created: 2026-08-30
updated: 2026-08-30
touches:
  - collmind.backend/src/database/migrations/1818000000000-FormulaCanonTurnoverNivSplit.ts  # YENİ
  - collmind.backend/src/database/seeds/kpi.seed.ts
  - collmind.backend/src/database/seeds/kpi.seed.spec.ts
  - collmind.backend/src/database/seeds/mechanic.seed.ts
  - collmind.backend/src/modules/master-data/kpi/kpi.service.ts
  - collmind.backend/src/common/kpi/roi-denominator.ts
  - collmind.backend/src/modules/shared/spend-calculation/spend-calculation.service.ts
  - collmind.backend/src/modules/shared/spend-calculation/spend-calculation.service.spec.ts
  - collmind.backend/src/modules/shared/spend-calculation/dto/spend-breakdown.dto.ts
  - collmind.backend/src/modules/shared/kpi-engine/formula-parser.service.ts
  - collmind.backend/src/modules/shared/kpi-engine/formula-parser.service.spec.ts
  - collmind.backend/src/modules/shared/kpi-engine/kpi-engine.service.ts  # review N1 — YALNIZ YORUM
  - collmind.backend/src/modules/shared/lta/lta-calculation.service.ts  # YALNIZ ŞERH
  - collmind.backend/src/modules/modes/planning-first/plan/plan.service.ts
  - collmind.backend/test/formula-canon-turnover-niv-and-rag-quadrant.e2e-spec.ts  # YENİ
  - collmind.backend/test/lta-lifecycle-bond-and-base-chain.e2e-spec.ts  # pin güncellendi
  - collmind.frontend/src/components/features/plans/PlanningGridEnhanced.tsx
migration_seq: 1818000000000
---

## Neden bloklu
`W2 DALGA-B` (`T-293`+`T-291`) koşuyor. `B`'nin **LTA-taban-pini** bu task'ın **zeminini**
hazırlıyor. `B` kapanmadan başlamaz. *(`Z65 §7`)*

## Açıklama — `Z65` hükmü, **ÜÇ KALEM TEK PAKET**

⛔ **Üçü ayrı inerse ÜÇ migration turu olur.** Aynı formül-katmanı ⇒ **tek tur, tek pin-seti.**

### `1` · `Q2` — KAVRAM-AYRIŞTIRMA *(geri-al DEĞİL)*
Kaynak-zincirin üçü de (Excel formül sözlüğü · KPI-Library · BRD) hemfikir:
```
TO  = GSV − TotalSpend(on+off)      iki AYRI kavram
NIV = GSV − Spend_On                ikisi de MEŞRU, ikisi de GEREKLİ
```
- **NIV kendi kodlarıyla DOĞAR:** `BASE_NIV` · `PLANNED_NIV` · `INCR_NIV`
  *(`1781`'in yazdığı formüller **zaten bunlar** — yalnız yanlış ada yazılmış)*
- **TO kodları gerçek TO semantiğine DÖNER** — `1780`'in formülleri
  *(Excel'le birebir olduğu `A0'`da ölçüldü)*
- **Frontend'in İKİ kopyası aynı dalgada hizalanır:**
  `PlanningGridEnhanced.tsx:218` (`getSkuCellValue`) · `:561` (`getFuCellValue`)

### `2` · `Q3` — GP TABANI **`TO`**
`GP = Turnover − COGS` (Excel-kanonik). Bugün beş kalem NIV tabanlı:
`BASE_GP · PLANNED_GP · INCR_GP · GP_ROI_PCT · GP_MARGIN_PCT`
⚠️ **RAG renkleri DEĞİŞECEK — bu bir DÜZELTMEDİR, regresyon değil.**
Pin bunu **"beklenen-değişim" listesiyle** taşır.

### `3` · `Q5` — OFF-INVOICE TABANI **`NIV`**, `LTA_Off` **DÜŞÜLMEZ**
Excel + BRD hemfikir: `CPPOff = NIV × pct`. Kod `LTA_Off`'u da düşüyor ⇒ taban küçülüyor
⇒ ROI iyimser. **`T-291`'in kardeşi.**

## ⛔ ACİLİYET — `plans = 0` PENCERESİ
```
BUGÜN   migration BEDELSİZ (plan-verisi YOK; yalnız kpi-TANIM tablosu)
SONRA   ilk gerçek müşteri planı ⇒ VERİ-MİGRATION'ına döner
```
**Veri-sıfır penceresi kapanmadan inmeli.** *(`Z65 §1b`)*

## Acceptance Criteria
- [ ] `TO` ve `NIV` **ayrı kpi kodları**; her ikisi de canlı ve formülü kaynağa birebir
- [ ] GP ailesi (5 kalem) `TO` tabanına bağlı
- [ ] off-invoice tabanı `NIV` (`LTA_Off` düşülmüyor)
- [ ] frontend **iki kopyası** backend'le hizalı — sapma **ölçülerek** gösterildi
- [ ] pin: **"beklenen-değişim" listesi** taşır (RAG renkleri dahil), ve fixture
      `T-273` ailesine karşı **kalıcı değer** taşır (`plans=0` ⇒ yol bugün koşmuyor)
- [ ] migration üç durum ayrımı (`1805`+ deseni), **dalların en az ikisi ampirik**
- [ ] `Section_05` / referans belge `F12` izleri yazıldı

## ⚠️ SINIF-NOTU (hüküm DEĞİL, kayıt — `Z65 §1a`)
> **Frontend formül HESAPLAMAZ; motor SONUCUNU gösterir.**
Grid'in kendi formül-hesabı taşıması **temizlik-listesine** sınıf-notu olarak girer.

## Kaynak
`docs/brd-v2/04_KARAR_KAYDI.md` `Z65` · `docs/research/A0_KAVRAM_ESLEME_RAPORU.md`
`docs/DISIPLIN.md`: *"bir listeden düşen kavram yok olmaz"* · *"sapmaların yönü rastgele
değilse sistematik bir basınç var"*

---

# ⛔ `Z66` HÜKÜMLERİ — KAPSAM KESİNLEŞTİ (**9 + 1 kalem**)

## `Q6` · ROI PAYDASI — **DEĞER DEĞİL, OKUNAN KALEM**
```
TOTAL_PLANNED_SPEND    OLDUĞU GİBİ KALIR   ← bütçe/plan.totalSpend besliyor; bütçe TOTAL ister
ROI paydası            AYRI KALEM          ← INCR_PROMO_SPEND sınıfı
                       yalnız promo · LTA HARİÇ · INCREMENTAL  (Z62 §6-3)
tanım                  TEK NOKTA: src/common/kpi/roi-denominator.ts
```
> **Finansal yayılım SIFIR** — bütçe yolu dokunulmaz, yalnız ROI'nin **okuma adresi** değişir.

📌 Excel'in *"incremental-total-incl-LTA-delta"* tanımı **`F12` farkı** olarak kayda
girer; **tenant-konfigür ekseni** yazılı ⇒ yarın bir tenant'ın **seçeneği** olur.
✅ `docs/decisions/0011-*.md`'ye `F12` notu **İŞLENDİ** (*"kalem bölündü"*).

## `Q7` · RAG — **İKİ-EKSEN KADRAN**
```
Red     iTO ≤ 0
Amber   iTO > 0  ∧  iGP ≤ 0      ← "satış var, kâr yok" — silinmemesi gereken durum
Green   ikisi de > 0
```
⛔ Canlı tek-eksense **`eşleşen-sapmalı`**.
⛔ **FIXTURE İKİ-EKSENLİ KURULUR: DÖRT KADRAN VAKASI, HÜCRE BAŞINA BİR.**

## `Q8` · KALEM `22` **PAKETE GİRER** ⇒ **9 + 1 = 10 kalem**
`PLANNED_LTA_OFF` tabanı (`spend-calculation.service.ts:197`):
```
kod     (plannedGsv − plannedLtaOnInv) × pct        ← promo-on DÜŞÜLMEMİŞ ⇒ taban BÜYÜK
Excel   PlannedNIV = GSV − TotalSpendOn             ⇒ spend BÜYÜK ⇒ ROI KÖTÜMSER
```
> **Paket gerekçesi *"iyimserlik"* DEĞİL, FORMÜL-KANON — ve kanon YÖN-AGNOSTİKTİR.**
> Aynı formül katmanında ikinci bir migration turu açmak **maliyetin kendisi**.

⇒ `Z65 §6`'ya **karşı-örnek satırı** eklenir; *"sistematik iyimserlik"* **"BASKIN YÖN"**e yumuşar.

## ⛔ PİN-SETİNE EKLENDİ — **beklenen-değişim listesi**
```
test/lta-lifecycle-bond-and-base-chain.e2e-spec.ts:436
  expect(kpis.BASE_TO.value).toBeCloseTo(baseGsv - expectedOn, 2)
  ⇒ T-334 bu beklentiyi GSV − (on+off)'a çevirir. KIRMIZIYA DÖNMESİ BEKLENENDİR.
+ RAG renk değişimleri (Q7)
+ GP ailesi: METİN DEĞİŞMEZ ("GP = TO − COGS" zaten kanonik) ama DEĞER DEĞİŞİR
  ⚠️ diff'e bakan "GP'ye dokunulmamış" der — beklenen-değişim listesi OPSİYONEL DEĞİL
```

## KAPSAM DIŞI (hüküm)
`Q9` UOM (`İlke 1`: tüketicisiz inşa yok ⇒ `Faz-2-aday`) · `Q12` `weighted_avg` ad-borcu
(davranış **doğru**; ad-borcu paketi **eşleme-sonrası tek dokunuş**) · `50`/`52`'nin
kanon-içi çatışması **`Q6`/`Q7` ile ÇÖZÜLDÜ**.


---

# ✅ UYGULAMA RAPORU — `migration 1818000000000` (2026-08-30, `data-engineer`)

## Beklenen-değişim listesi — **ÖLÇÜLMÜŞ önce/sonra**
Fixture: `FU-E2E-GRID-SINGLE-SKU` (tek SKU, `price=100`, `cogs=60`),
`baseVol=1000` · `planVol=1200` · LTA `on %7 / off %2` · `CPP_ON %10` ·
`CPP_OFF %5`. **ÖNCE** değerleri `HEAD` kodunun izole bir `git worktree`'sinde
+ migration `revert` edilmiş DB ile ÖLÇÜLDÜ; **SONRA** aynı uçtan.

| kalem | ÖNCE | SONRA | fark | sebep |
|---|---|---|---|---|
| `BASE_TO` | `93.000,00` | `91.140,00` | `−1.860` | `Q2` — `BASE_LTA_OFF` artık düşülüyor |
| `BASE_NIV` | *(kalem YOK)* | `93.000,00` | yeni | `Q2` — eski `BASE_TO`'nun değeri |
| `PLANNED_TO` | `100.440,00` | `93.409,20` | `−7.030,80` | `Q2` — off-invoice toplamı düşülüyor |
| `PLANNED_NIV` | *(kalem YOK)* | `100.440,00` | yeni | `Q2` |
| `INCR_NIV` / `INCR_TO` | *(kalem YOK)* | `7.440` / `2.269,20` | yeni | `Q2` (+`Q7` ekseni) |
| `PLANNED_LTA_OFF` | `2.232,00` | `2.008,80` | `−223,20` | `Q8` — taban `NIV` (promo-on düşüldü) |
| off-invoice promo (`CPP_OFF`) | `4.910,40` | `5.022,00` | `+111,60` | `Q5` — taban `NIV`, `LTA_Off` düşülmüyor |
| `TOTAL_PLANNED_SPEND` | `26.702,40` | `26.590,80` | `−111,60` | `Q5`+`Q8` **net** (`−223,20 + 111,60`) |
| `PLANNED_ON_INVOICE_SPEND` | `19.560,00` | `19.560,00` | **`0`** | on-invoice yolu dokunulmadı |
| `BASE_GP` | `33.000,00` | `31.140,00` | `−1.860` | `Q3` — **formül metni DEĞİŞMEDİ** |
| `PLANNED_GP` | `28.440,00` | `21.409,20` | `−7.030,80` | `Q3` — metin değişmedi |
| `INCR_GP` | `−4.560,00` | `−9.730,80` | `−5.170,80` | `Q3` türevi |
| `GP_MARGIN_PCT` | `28,3154` | `22,9198` | `−5,4` puan | `Q3` türevi |
| `INCR_PROMO_SPEND` | *(kalem YOK)* | `16.182,00` | yeni | `Q6` — ROI paydası |
| `GP_ROI_PCT` | `−17,0771` | `−60,1335` | `−43,06` puan | `Q3` + `Q6` |
| **RAG** (`plans.rag_status`) | `RED` | `RED` | — | bu hücrede renk **değişmedi** |
| ⭐ **RAG — LTA-ONLY PLANLAR** | renk **VAR** | **`null` (RENKSİZ)** | sınıf-değişimi | `Q6` — payda `0` ⇒ ROI `null` ⇒ RAG `null` |
| `plans.total_spend` | `26.702,40` | `26.590,80` | `−111,60` | `Q5`+`Q8` (**`Q6` DEĞİL**) |
| `plans.on_invoice_spend` | `19.560,00` | `19.560,00` | **`0`** | — |
| `plans.off_invoice_spend` | `7.142,40` | `7.030,80` | `−111,60` | `Q5`+`Q8` |

## ⭐ `S1` — LTA-ONLY PLANLAR ROI'Yİ **VE RAG RENGİNİ** KAYBEDER (ürün-görünür)

⛔ Bu, beklenen-değişim listesinin **ilk yazımında EKSİKTİ** (review `S1` yakaladı);
AC açıkça *"RAG renkleri dahil"* diyordu.

```
payda = INCR_PROMO_SPEND  (yalnız promo, LTA hariç)
promo mekaniği OLMAYAN bir plan  ⇒  payda TAM 0
                                 ⇒  GP_ROI_PCT = null   (sıfıra bölme → null, §2.3)
                                 ⇒  ragStatus  = null   (kpi-engine: value===null ⇒ renk YOK)
```
| | ÖNCE (payda `TOTAL_PLANNED_SPEND`) | SONRA (payda `INCR_PROMO_SPEND`) |
|---|---|---|
| LTA'sı olan, promosu **olmayan** plan | payda `>0` ⇒ ROI **var**, RAG **renkli** | payda `0` ⇒ ROI `null`, RAG **RENKSİZ** |

**Bu bir hata değil, hükmün doğrudan sonucudur** — *"promo harcaması olmayan bir planın
promo-ROI'si yoktur"*. Ama **bütün bir plan sınıfı ekranda renksiz kalır** ⇒ ürün-görünür.
**Pinlenmiştir:** kadran `HÜCRE 4` (`cppOn=0 · cppOff=0`) `liveRag: null` sabitiyle bunu
bilinçli olarak sabitler.

⚠️ Karar noktası ürün sahibinde: *renksiz mi kalsın, yoksa LTA-only planlar için ayrı bir
gösterim mi olsun?* — `T-334` kapsamında **değiştirilmedi**, yalnız kayda geçti.

## `Q6` — FİNANSAL YAYILIM SIFIR (kanıt)
```
plans.total_spend farkı  = −111,60
Q8 katkısı               = −223,20   (PLANNED_LTA_OFF)
Q5 katkısı               = +111,60   (CPP_OFF)
toplam                   = −111,60   ⇒ Q6 katkısı = 0,00  (birebir kapanıyor)
```
+ `TOTAL_PLANNED_SPEND` KPI satırı (`order 9`, `external`) **dokunulmadı** ·
`plan.service.ts:2933` (bütçe/spend-tipi türetimi) hâlâ onu okuyor ·
`INCR_PROMO_SPEND`'in **tek yazıcısı** context enjeksiyonu, **tek okuyucusu**
`GP_ROI_PCT` formülü (ölçüldü, `rg`).

## RAG (`Q7`) — ÖLÇÜLDÜ, **UYGULANMADI**, `eşleşen-sapmalı`
Canlı model **tek eksen** (`kpi-engine.determineRagStatus`, `GP_ROI_PCT ≥20/≥10`).
Kadranın dört hücresi **fixture olarak kuruldu**; `HÜCRE 2`'de kanon `AMBER`,
canlı `RED` ⇒ *"satış var, kâr yok"* durumu bugün **siliniyor**.
⛔ **Model değişimi ürün sahibi hükmü bekliyor** (`Z66 §2`) — `§2.4` gereği DURULDU.

## `T-334` KAPSAMI DIŞINDA BULUNAN, DÜZELTİLEN İKİ KUSUR
1. **`formula-parser` sessiz `null`** — `PLANNED_GP - BASE_GP` iki operand da
   negatifken `-1200--1500` üretiyor, JS postfix `--` sanıp `SyntaxError`
   fırlatıyor ⇒ `catch` ⇒ **`null`**. `INCR_GP` + `GP_ROI_PCT` + RAG birlikte
   kayboluyordu. Kadran `HÜCRE 4` bunu ortaya çıkardı.
2. **`mechanics.CPP_OFF_PCT.calculation_formula`** metni eski (sapmalı) tabanı
   anlatıyordu; `mechanic.seed.ts` var olan satırı **güncellemiyor** (ölçüldü)
   ⇒ metin migration'a alındı (üç durum ayrımıyla).

## AÇIK BIRAKILAN — ürün sahibi/Team Lead kararı gerekiyor
- `Q7` RAG kadranı (yukarı).
- **`POST /lta-agreements/calculate/planned-spend`** (`LTACalculationService`)
  `Q8` sapmasını **taşımaya devam ediyor**: metodun imzasında promo bağlamı yok
  ⇒ düzeltme mimari (`Z66 §6` onaylı *"çift LTA implementasyonu"* task'ı).
  Uç HTTP'den erişilebilir, bugün tüketicisi yok (`T-267`). Koda şerh yazıldı.


---

# 🔁 REVIEW KAPANIŞ TURU (2026-08-30) — `B1`/`B1b` + `S1`–`S4` + `N1`/`N2`

## `B1`/`B1b` — parser düzeltmesi **YENİDEN YAZILDI**: yerine koyma **parantezli**
İlk düzeltme (`const evaluable = expression`) **iki kusur birden** taşıyordu; ikisi de
reprodüksiyonla **ölçüldü**, sonra kapatıldı:

| girdi | İLK DÜZELTME | ŞİMDİ | not |
|---|---|---|---|
| `PLANNED_GP-BASE_GP` (`-1200`,`-1500`) — **boşluksuz** | `null` ⛔ kusur AÇIK | **`300`** | `B1b` — düzeltme boşluğa bağımlıydı |
| `PLANNED_GP - BASE_GP` (aynı değerler) | `300` | `300` | — |
| `"1 // 2\n+ 5"` | **`6`** ⛔ sessiz YANLIŞ SAYI | **`null`** | `B1` — `//` yorum oldu, ifadenin yarısı düştü |
| `"1 000"` (poz. kontrol) | `null` | **`1000`** | ayrışma **iki yönde de** gerçekti |
| `String(1e-7)` = `"1e-7"` | `null` | `null` | ⚠️ **DEĞİŞMEDİ** — ayrı sınıf, aşağı bkz. |

**Düzeltme:** `parseExpression` **ve** `parseConditional` yerine koymayı `` `(${value})` ``
ile sarmalar; `evaluable` **geri alındı**, eval yine **`sanitized`** üzerinde.
⇒ (a) boşluktan bağımsız · (b) operatör bitişikliğinden yeni token doğmaz · (c) **beyaz
listenin denetlediği dizge ile değerlendirilen dizge YİNE AYNI** (`§2.7`).

**Üç mutasyon, üç ayrı pin, üçü de izole** *(kopyala → uygula → SATIRI BAS → ölç → geri
yükle → `shasum -a 256 -c`: hepsi `OK`)*:
| mutasyon | kırılan pin |
|---|---|
| iki kanalda da parantezi kaldır | `B1b · İKİ NEGATİF operand — BOŞLUKTAN BAĞIMSIZ` |
| eval'i boşluklu dizgeye çevir | `B1 · // + newline SESSİZ YANLIŞ SAYI üretmez` |
| **yalnız `parseConditional`**'ın parantezi | `B1 · conditional yolu da parantezli` |

⚠️ `conditional` pini ilk yazımda **ayırt etmiyordu** (`IF(A > B, …)` — `>` iki değeri
zaten ayırır, `--` bitişikliği hiç doğmaz). Ayırt eden şekle çevrildi:
`IF(PLANNED_GP - BASE_GP > 0, 1, 2)`.

**Yan etki ölçüldü (varsayılmadı):** parantez `/(0)` ürettiği için metin-tabanlı
sıfıra-bölme deseni artık **eşleşmiyor**; sonuç **değişmiyor** (`Infinity` → `!isFinite`
→ `null`), ve desen **ölü değil** — `(5)/0` gibi LİTERAL bölen hâlâ yakalanıyor. Üçü de
pinlendi. Güvenlik kapısı kapalı (harf/dizge/property → `null`).

## ⚠️ YENİ TASK ADAYI — **ÜSTEL GÖSTERİM SESSİZ `null` ÜRETİYOR** (`T-334` DIŞI)
```
String(1e-7)  === '1e-7'   ·  beyaz liste `e` KABUL ETMİYOR  ⇒  null
ayırt edici:  0.000001 → ÇALIŞIYOR   ·   0.0000001 → null
```
Yeterince küçük (`<1e-6`) ya da yeterince büyük (`≥1e21`) bir **ara değer** KPI'ı sessizce
düşürür. **Düzeltilmedi** (kapsam dışı), ama **bugünkü hâliyle pinlendi** ki düzeltildiği
gün bilerek kırılsın. ⇒ Team Lead'e task olarak bildirildi.

## `S2` — `basePromoSpend` iddiası **KALDIRILDI**
`baseTotalSpend − baseLtaOn − baseLtaOff` ifadesi tabanı `lta`'ya sabitleyen satırlardan
**mekanik olarak türüyordu** ve daima `0`'dı; *"tabana promo girerse kendiliğinden doğru
kalır"* iddiası **yanlıştı**. İfade kaldırıldı (`incrementalPromoSpend = promoOn +
promoOff`), yerine **dürüst bağ** yazıldı: *"taban promo alırsa `SEVIYE 4` VE burası
BİRLİKTE değişmek zorundadır."* Aynı düzeltme DTO docstring'inde ve `:545`'teki
`plannedPromoNiv(baseGsv, baseLtaOnInv, 0)` yorumunda da yapıldı.

## `S3` — `Q7` pini artık **ÜRÜNÜ** ölçüyor
Testteki `roi >= 20 ? … : …` **yeniden uygulaması kaldırıldı** (`§2.7 #8`). Her hücre
artık **LİTERAL** bir `liveRag` sabiti taşıyor ve motorun `ragStatus`'ü ona karşı
okunuyor. Totolojik son test, **motordan** `INCR_TO`/`INCR_GP`/`ragStatus` okuyan bir
ayrışma testine dönüştürüldü (`kanon AMBER` ↔ `canlı RED`).
**Mutasyonla kanıtlandı:** `determineRagStatus` → `return 'AMBER'` yapıldığında **dört
test birden** kırmızıya döndü (`HÜCRE 4` hariç — orada değer zaten `null`, ve bu tutarlı).

## `S4` — `down()`'ın `kpis` yarısına da **üç durum ayrımı**
`down()` artık `classifyRows` ile sınıflandırıyor: hedef hâl ⇒ geri al · `PRE` hâli ⇒
**NO-OP** · başka her şey ⇒ **İPTAL**; ve geri almadan sonra `PRE` hâli **katalogdan
okunarak** assert ediliyor. Aynı dosyanın iki yarısı artık **aynı disiplinde**.

## `N1` / `N2`
`N1` `kpi-engine.service.ts`'in beş yorumu paydayı `TOTAL_PLANNED_SPEND` diye
anlatıyordu ⇒ payda-agnostik metne çevrildi + *"bu motor hangi paydanın kullanıldığını
BİLMEZ; tek kanonik yer `roi-denominator.ts`"* notu eklendi (`5 → 1` geçiş, kalan tek
geçiş o notun kendisi).
`N2` off-promo için doğrudan pozitif kontrol eklendi (`promoOff > 0`, `promoOn > 0`,
`PLANNED_LTA_OFF > 0`) — `Q5`/`Q8` pinlerinin körlüğü artık **iki koşulla** kapalı.
