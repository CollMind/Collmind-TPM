---
id: T-215
title: "gri→yeşil sızıntısı: `|| 'GREEN'` kaldırılır + `coverageRatio` DTO'ya (backend yarısı)"
epic: E-001
sprint: S-001
status: todo
assignee: backend-engineer
priority: P1
created: 2026-08-14
updated: 2026-08-14
touches:
  - collmind.backend/src/modules/shared/finance-reporting/finance-reporting.service.ts
  - collmind.backend/src/modules/shared/finance-reporting/dto/
migration_seq:
---

## Bulgu — `INV-N-004`, ve motorun kasıtlı `null`'ı sahteleştiriliyor

```
finance-reporting.service.ts:583   ragStatus: plan.ragStatus || 'GREEN'
finance-reporting.service.ts:633   ragStatus: plan.ragStatus || 'GREEN'
```

İkisi de **canlı** `@Get` rotalarında (`finance-reporting.controller.ts`, beş rota).

⚠️ **Üretici taraf DOĞRU:** `kpi-engine.service.ts`'in iki roll-up'ı `fullCoverage` ile
korunuyor (`T-177`), yani kısmi kapsamada `ragStatus = null` **kasıtlı**. Kusur yalnız
okuma tarafında.

**Kapsam ölçüldü (2026-08-14, altı şekil):** `|| 'GREEN'` **2** · `?? 'GREEN'` 0 ·
`|| RagStatus.*` 0 · düz varsayılan 0 · `= 'GREEN'` 3 (**ikisi korumalı üretici, biri DTO
enum üyesi**) · frontend 0. **`T-093` deseni tekrarlamadı — kapsam gerçekten 2.**

📌 **Bugünkü veriyle bu ÇOĞUNLUK durumu:** `COGS 4/170` — panelde görünen yeşillerin
neredeyse hepsi aslında *"hesaplanamadı"*. Karar `3.9`: *"en tehlikeli türden, çünkü tam
da güven beyanını sahteleştiriyor."*

## Yapılacak
- [ ] İki `|| 'GREEN'` kaldırılır — `null` **geçirilir** (`K-2.4.22a1`: taşıyıcı `null`,
      `GRAY` diye bir değer **eklenmez** — üçüncü bir temsil doğururdu)
- [ ] DTO `ragStatus`'u `null` kabul eder
- [ ] **`coverageRatio` yanıta eklenir** — bugün `kpi-engine` üretiyor ve JSONB'ye yazıyor
      ama DTO'da **0**, frontend'de **0** geçiş. `GRİ` durumu rozetsiz kalıyor (`K-2.4.22a`)

## Kabul — ihlal ekle, kırılsın
```
kapsama < 1 iken tam-kapsama paleti dönen bir yol   → test KIRMIZI
kapsama oranı yanıtta yok                           → test KIRMIZI
```
Testi `qa-engineer` yazar; **kabul şartını sen ölç** ve komut+çıktı raporla.

## İlgili
`INV-N-004` · `K-2.4.22a`/`a1`/`b`/`c` · [[T-177]] (kapsama oranını üreten iş) ·
[[T-216]] (arayüz yarısı) · [[T-217]] (eksik listesi)
