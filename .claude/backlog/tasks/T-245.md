---
id: T-245
title: scope'ta tekrarlı çift → 500 (yakalanmamış 23505) ya da sessiz çift satır
epic: E-001
sprint: S-001
status: todo
assignee: backend-engineer
created: 2026-08-20
updated: 2026-08-20
touches:
  - collmind.backend/src/modules/user/user.service.ts
  - collmind.backend/src/modules/user/dto/create-user.dto.ts
---

## Açıklama

`T-241` ikinci tur review'unun `A2` bulgusu. **Ayrı sınıf** (DTO/servis doğrulaması) —
`A1`/`A7`'nin denetim ailesiyle ilgisi yok.

`resolveScopeRowsToWrite` **dedupe yapmıyor**. Ve `UQ_user_scopes_user_cpl_category`
`(user_id, cpl_id, category_id)` üzerinde, **`NULLS NOT DISTINCT` YOK** (katalogdan
ölçüldü):

```
scope:[{A,B},{A,B}]      ikisi de DOLU  → 23505 → yakalanmamış → 500
scope:[{A,null},{A,null}] NULL içeriyor → UNIQUE ayırt ETMEZ → SESSİZCE iki
                                          özdeş satır yazılır
```

⚠️ İki farklı sonuç, ve **ikisi de yanlış**: biri kullanıcıya `500` gösteriyor
(teşhis netliği yok — `K-2.2.11a`), diğeri sessizce yineleniyor.

📌 `B1` kapısı bunu **görmüyor** — o yalnız `{null,null}` çiftine bakıyor.

## Acceptance Criteria

- [ ] Tekrarlı çift **açık `400`** ile reddedilir — **sessiz dedupe DEĞİL**
      (`§2.5` ruhu: girdi belirsizse uydurma, sor). ⚠️ Alternatif (`approval-policy
      .service.ts:98` `wrapCheckViolation` deseniyle `23505` → `400`) **ikinci**
      tercih: o, hatayı DB'den öğrenir; girdi doğrulaması **daha erken** olmalı
- [ ] Kanonik anahtar `NULL`'ı da kapsar: `${cplId ?? '∅'}|${categoryId ?? '∅'}` —
      yoksa `{A,null}` tekrarı yine kaçar
- [ ] **İki girdi, iki çıktı:** tekrarlı çift → `400` · farklı çiftler → `201`
- [ ] ⚠️ Aynı kontrol `T-242`'nin (kapsam güncelleme) yolunda da gerekli —
      `resolveScopeRowsToWrite` **yeniden kullanılmalı**, ikinci kopya yazılmamalı
      (`İlke 4`; bugün tek çağıran, `grep` ile doğrulandı)

## Done tanımı

- [ ] Testler yeşil (unit + ilgili e2e)
- [ ] `npm run guards` exit 0 · `money-float --ratchet` exit 0
- [ ] `code-reviewer` onayı
- [ ] `touches:` güncel
