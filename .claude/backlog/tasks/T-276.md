---
id: T-276
title: 🔴 SAHTE SELF — `GET /approvals/pending` `userId`'yi alıyor, `WHERE`'e koymuyor
epic: E-001
sprint: S-001
status: blocked
assignee: architect
created: 2026-08-23
updated: 2026-08-23
touches:
  - collmind.backend/src/modules/shared/approval/approval.repository.ts
  - collmind.backend/src/modules/shared/approval/approval.service.ts
---

## Bulgu — `ADIM3_FAZB_PLAN §5 ÖLÇÜM 2`'nin uyardığı sınıf

Swagger: *"pending approval requests **for current user**"*. Kod:

```
controller   findPending(@TenantId() t, @CurrentUser('id') userId)   ← kimlik ALINIYOR
service      approval.service.ts:263  findPendingForUser(userId, tenantId)  ← TAŞINIYOR
repository   approval.repository.ts:77
               where: { tenantId, status: PENDING, deletedAt: IsNull() }
                                     ⛔ userId WHERE'DE YOK — parametre KULLANILMIYOR
```

`userId` bir parametre olarak **alınıyor, taşınıyor, ve atılıyor**.

### Davranışsal (architect, fixture kurulup silindi)

```
fixture      requested_by_id = admin@wella.com olan bir PENDING kayıt
FINANCE token GET /approvals/pending    →  adet 1 · requestedById = ADMIN'in id'si  ⛔
POZ.KONTROL   GET /approvals/my-requests →  adet 0  (yüklemi OLAN kardeş,
                                             AYNI fixture, AYNI token)
```

Pozitif kontrol ayrımı kanıtlıyor: fixture ayırt edici, desen çalışıyor, **yüklem yok**.

📌 `ADIM3_FAZB_PLAN §5`: *"yüklem yoksa SINIF SAHTE — 'kendi kaydı' diye adlandırılan şey
aslında 'herkesin kaydı'dır."*

## ⛔ DUR — DOĞRU YÜKLEM BİR ÜRÜN KARARIDIR

```
(a) "bana ATANMIŞ onaylar"     →  approverId / approval_levels içindeki sıradaki onaycı
(b) "benim AÇTIĞIM onaylar"    →  requestedById   (ama bu zaten /my-requests)
(c) "görebildiğim kapsamdaki"  →  AccessScopeService — bir SELF yüklemi DEĞİL
```

⚠️ **Ölçüm bir cevap veremez** (`§2.4`): `Z18`'in `READ_OWN` tanımı `(a)` ile `(b)`'yi
ayırmıyor, ve `approval_levels` jsonb'si bugün **`0` satır** — semantiği koddan
okunamıyor.

📌 `(b)` seçilirse uç `/my-requests`'in **ikizi** olur (`İlke 4`) — o hâlde soru
*"bu uç neden var?"*a döner.

## Kardeş bulgu — aynı sınıf, okuma yönü

`GET /approvals/:id` · `@Roles(5/5)` · `findById(id, tenantId)` — **sahiplik sorulmuyor**.
`B1`'in konusu, ama aynı ailede.

## Acceptance Criteria
- [ ] Doğru yüklem **ürün sahibi kararıyla** belirlendi ve karar defterine bağlandı
- [ ] Yüklem `WHERE`'e girdi
- [ ] **Davranışsal, iki girdi:** iki farklı kullanıcı → **farklı** sonuç, poz.kontrollü
- [ ] `(b)` seçilirse: `/my-requests` ile `İlke 4` çakışması **ayrıca** ele alındı
- [ ] `GET /approvals/:id` kardeşi **ayrıca** değerlendirildi (ya bu turda, ya `B1`'e bağlı)


---

## 2026-08-23 — KARAR VERİLDİ (`Z27`): **`(a)` onaycı perspektifi**

```
(a) "benim onayımı bekleyenler"             ✅ SEÇİLDİ
(b) "benim gönderdiklerimden bekleyenler"   ⛔ RET — /my-requests'in KOPYASI (İlke 4)
```

⚠️ **Ve `(a)` bir `SELF` yüklemi DEĞİL** — *"onay kademesinde ben varım"* yüklemi.
`SELF` kovasına **girmez**; `rol + kapsam` kesişimidir.

**Ürün dayanağı:** `PLAN_BUTCE_NETLESTIRME` `netleştirme-1(c)` — *"onay kuyruğunda zarf
bazlı toplam talep"*; onay kuyruğu **onaycıya** gösterilen bir yüzey.

### Bugünkü ASGARİ DOĞRU uygulama

Tam çözüm **şablon çözümlemesi + `K-2.5.12-R` tek-hat kuralı** ister — o **`Faz 2` işi**.
Bugün:

```
kullanıcının rolü  ==  isteğin MEVCUT KADEME rolü
        ∧
kapsam kesişimi    !=  ∅
```

✅ **Doğrulama:** `FINANCE`'in `ADMIN`'in kaydını görmesi **bu yüklemle de düşer**.

### ⛔ SIRA — bu task BEKLER

Ürün sahibi kararı: `T-275` **şimdi** (karar-bağımsız `P0`), `T-276` **bekler**.

> *"Yanlış yüklemle inen bir 'düzeltme', doğru yüklem geldiğinde İKİNCİ KEZ açılır."*

Yürüme sırası: `T-275` → `SELF` karar kaydı → dekoratör+guard+`B4` kriterleri **tek
turda** → **`T-276`** → `B3` → `B4`.

### Fixture borcu — `Z25` tablosuna girdi

```
KOŞUL       approval_levels DOLDUĞUNDA
TETİKLEYEN  şablon motoru (Faz 2)
DURUM       ⏳ KOŞUL — aktif izlenir
```

📌 `(a)`/`(b)` ayrımının koddan okunamaması bir **ölçüm sınırı değil, bir VERİ
YOKLUĞUDUR** (`approval_levels` `0` satır) — *"verinin yokluğu örter"* alt sınıfı.
