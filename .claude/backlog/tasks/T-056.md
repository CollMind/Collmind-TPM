---
id: T-056
title: Submit yolu yakınsaması — tipli on/off rezervasyon canlı /submit'e taşınır
epic: E-001
sprint: S-001
status: in-progress
assignee: architect
created: 2026-08-02
updated: 2026-08-02
---

## ⚠️ 2026-08-02 — task yeniden çerçevelendi
Hazırlık sırasında ölçüldü: **frontend `/submit-for-approval`'ı hiç çağırmıyor**
(kaynakta sıfır referans); tek submit yolu `/plans/:id/submit` → `PlanService#submit` →
**TOTAL kova**. Yani on/off makinesinin tamamı ([[T-019]], [[T-048]], [[T-053]], [[T-019b]])
üründen **erişilemez** durumda.

Ürün sahibi kararı: **tipli rezervasyon canlı `/submit`'e taşınır**, `/submit-for-approval`
deprecate edilir → `docs/decisions/0005-submit-yolu-yakinsamasi.md`.

Bu yüzden task artık "5 çağrı yerini split-farkındalı yap" değil, **submit yolu yakınsaması**.
Kalan tipsiz çağrı yerleri (`plan.service.ts:910`, `:1019`,
`agreement-transaction.service.ts:142`, `on-invoice.service.ts:439`) → [[T-057]].

## Açıklama (özgün, kısmen geçerli)
[[T-019b]] split ucunu ekledi, ama `findEnvelopeByDimensions`'ı **`spendType` vermeden** çağıran
5 yer bölünmüş bir boyutta iki tipli satır arasında ayrım yapamıyor. T-019b bunu
`SPEND_TYPE_REQUIRED_FOR_SPLIT_DIMENSION` guard'ıyla **gürültülü hataya** çevirdi
(karar: ürün sahibi, 2026-08-02) — yani sessiz mis-attribution yapısal olarak imkânsız, **ama**
split çağrılan bir boyutta bu yollar hata verir. Bu task o yolları gerçekten tipli dünyaya taşır.

**Bu task kapanana kadar `POST /budget/envelopes/:id/split` üretimde kullanılmamalıdır.**

## Çağrı yerleri
| Yer | Ne yapıyor | Bölünmüş dünyada ne olmalı? |
|---|---|---|
| `plan.service.ts:810` | TOTAL kova rezervasyonu — kod yorumunda **"frontend'in canlı rotası"** | 🔴 **Tasarım kararı gerekiyor:** legacy tek-tutar rezervasyonu tipli dünyaya nasıl eşlenir? En olası: [[T-052]]'nin `buildMechanicValues` türetimini kullanıp on/off'a ayırmak — yani bu yolu `submitForApproval` ile aynı hesaba bağlamak |
| `plan.service.ts:1019` | auto-create-on-approve — **tek zarf** yaratıyor | Bölünmüş boyutta **çift** yaratmalı (tasarım §4 tuzağı) |
| `plan.service.ts:910` | bütçe durumu sorgusu (okuma) | Tip bazlı kırılım döndürmeli (§5.6 `bySpendType`) |
| `agreement-transaction.service.ts:142` | ledger kaydı | Agreement'ın `spend_type`'ına göre çözülmeli; BOTH/NULL → ADR Karar 3 (400) |
| `on-invoice.service.ts:439` | ledger kaydı | Adı gereği ON_INVOICE; **varsayma, doğrula** |

## Tuzaklar
- `plan.service.ts:810` en riskli madde: canlı UI rotası. Değiştirmeden önce iki kanonik yolun
  ([[T-052]]) aynı sonucu ürettiği kanıtı korunmalı.
- Tek türetim noktası — tip çözümünü her çağrı yerinde ayrı yazma ([[T-049]]/[[T-052]]/[[T-053]]).
- §5.7 mimari kuralı: on/off çözümü `SpendCalculationService`'in sorumluluğu; `shared/budget`
  bu kararı yeniden uygulamaz.

## İlgili
- Ön koşul: [[T-019b]] · Kardeşleri: [[T-054]] (seed), [[T-055]] (unsplit)
- Tasarım: `docs/analysis/0008-on-off-invoice-envelope-design.md` §4, §5.5, §5.6, §5.7


## Adım 1 — DONE (2026-08-02): F1 net-tabanlı kova keşfi

**F1 (tasarım 0009):** `commitAllReservedForPlan` kova keşfi **ham RESERVE satırı varlığına**
bakıyordu, net'e değil. Reject append-only ledger'da RESERVE'ü değiştirmez, ayrı RELEASE yazar →
net'i sıfırlanmış TOTAL kovası yeniden keşfedilip **hayalet COMMIT** üretiyordu.

**Kırmızı önce üretildi** — çapraz yol (`/submit` → reject → return-to-draft →
`/submit-for-approval` → approve):
```
COMMIT ON_INVOICE        10000.00
COMMIT OFF_INVOICE        4500.00
COMMIT (spend_type=NULL) 14500.00   ← hayalet
```
Fix sonrası yalnız iki tipli COMMIT. [[T-033]] ve [[T-053]]'ün üçüncü kardeşi: *ham satır varlığı ≠ net*.

**Tek türetim noktası (Team Lead review'ünde geri gönderildi):** ilk teslimde `computeBucketNet`
eklenmiş ama `reserveForPlan` hâlâ kendi inline reduce'unu yapıyordu — aynı kuralın iki kopyası,
yani [[T-053]]'ü doğuran durumun ta kendisi. Ölçüm ikisinin **eşdeğer olmadığını** gösterdi
(`netOutstanding` ayrıca zarfa göre süzüyordu), bu yüzden körü körüne birleştirilmedi: kapsam
opsiyonel `envelopeId` parametresine dönüştürüldü ve inline reduce kaldırıldı.

## 🔴 Kanıtlanmamış koruma bulundu ve kapatıldı
Mutasyon (b) — `computeBucketNet`'e verilen zarf kapsamını yok say — **hiçbir testi kırmadı**
(unit 585/585, e2e 228/228, ikisi de exit 0). Yani `reserveForPlan`'ın "aynı kova, farklı zarf"
ayrımı kodda vardı ama **hiçbir şey onu korumuyordu**. Kod okumasıyla görülemezdi; yalnız
mutasyonla ortaya çıktı.

Ertelenmedi, hemen kapatıldı — çünkü [[T-019b]]'nin split + re-home'u bu senaryoyu **artık
ulaşılabilir** kılıyor (bir plan eski ve yeni zarfta aynı kovada RESERVE bırakabiliyor).
Eklenen odaklı unit test: aynı plan + aynı kova, `envelope-B` net>0, hedef `envelope-A` net=0 →
`reserveForPlan` **yeni RESERVE yazmalı**. E2E'ye kurulmadı (dev fixture'da her kanal/dönem tek
zarfa çözülüyor; zorlamak yapay olur ve invaryant zeminini kirletirdi).

## Team Lead bağımsız doğrulama
- **Mutasyon (a)** (F1 net filtresi geri alındı): `tsc` temiz, **JEST EXIT=1**,
  `Expected: 0, Received: 1` (hayalet COMMIT geri geldi)
- **Mutasyon (b)** (zarf kapsamı etkisiz): **ilk denemem GEÇERSİZDİ** — regex'im `tx.true`
  üretti, derleme kırıldı, `Tests: 0 total` çıktı (bu oturumda üçüncü kez düştüğüm tuzak;
  "0 total kanıt değildir"). Tip-güvenli tekrar: `tsc` **temiz kalarak** EXIT=1,
  1 failed / 26; geri alınca 26/26 EXIT=0
- unit **586/586** EXIT=0 · e2e **228/228** × 3 ardışık reset'siz koşum, üçünde de EXIT=0
- [[T-047]] genişletilmiş invaryantı üç koşumda PASS · `ENV-2026-NKA-Q1` 75.000 sabit
- A16/A17/A18 üçü de yeşil — [[T-033]]/[[T-053]]/F1 korumaları gevşetilmedi

**Kalan adımlar (tasarım 0009 §6):** 2) `checkPlanBudgetAvailability` yükseltmesi ·
3) `reserveTypedForPlan` · 4) recalc on/off kolonları · **5) 🔴 canlı `/submit` yakınsaması** ·
6) approve auto-create tipli çift · 7) deprecation + ikiz testler


## Adım 2 — DONE (2026-08-02): `checkPlanBudgetAvailability` yükseltmesi (saf taşıma)

`approval-workflow.service.ts`'teki private `checkBudgetAvailability`, `shared/budget`'a
`checkPlanBudgetAvailability` olarak taşındı. Gerekçe: adım 3'te `/submit` de aynı kontrolü
kullanacak; iki mode-servisi private metot paylaşamaz, kopyalamak **ikinci türetim noktası**
demektir (adım 1'de `computeBucketNet` ile birebir aynı sorun).

**Saf taşıma disiplini korundu:** davranış birebir aynı, dönüş tipi değişmedi. Tasarımın önerdiği
`unsplitSharedEnvelope` alanı **bilinçli eklenmedi** — saf taşımayı kirletmek, adım 5'te bir şey
bozulduğunda sebebi ayırt etmeyi imkânsızlaştırırdı.

**Bağımlılık yönü kanıtlandı:** `shared/budget` içinde `modes/`'a tek bir import yok
(`grep` ile doğrulandı); `approval-workflow` → `shared/budget` yönü korunuyor.

**Mock değişikliği incelendi (Team Lead şüphesi):** ajan `approval-workflow.service.spec.ts`'in
iki katmanlı mock kurulumunu literal değerlerle değiştirmişti — bu, korumayı buharlaştırabilecek
türden bir değişiklik. Ölçüm: yeni 5 test yalnız `BudgetRepository`'yi mock'layıp
`service.checkPlanBudgetAvailability(...)`'i **gerçekten çağırıyor**, yani kural gerçek kodla
test ediliyor; koruma buharlaşmamış, doğru modüle taşınmış.

## Team Lead bağımsız doğrulama
- **Mutasyon (Team Lead kendi koştu):** UNSPLIT birleşik kuralın kalbi (`onInvoiceAmount +
  offInvoiceAmount` → yalnız `onInvoiceAmount`) bozuldu → `tsc` **TEMİZ kalarak** JEST **EXIT=1**,
  `Expected: false, Received: true` (1 failed / 31). Geri alınca temiz.
- unit **591/591** EXIT=0 (586 + 5 yeni)
- e2e **228/228 × 3 ardışık reset'siz koşum** (flag kapalı/açık/kapalı), **üçünde de EXIT=0**,
  [[T-047]] genişletilmiş invaryantı üçünde PASS, `ENV-2026-NKA-Q1 = 75.000` sabit

**F2 bu adımda ÇÖZÜLMEDİ (tasarım gereği):** `getBudgetSummary` hâlâ `this.dataSource` kullanıyor,
`manager` almıyor → availability okumaları çağıranın açık transaction'ını görmüyor. Yeni konumda
JSDoc'a not düşüldü. ADR 0004 Karar 2'nin "yazımdan önce kontrol" kapısı tek koruma katmanı olmaya
devam ediyor. Ayrı karar gerektiriyor.
