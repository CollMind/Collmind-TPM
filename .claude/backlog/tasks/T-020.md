---
id: T-020
title: Actuals (satış gerçekleşen) modülü portu — TTM actuals → CTPM
epic: E-001
sprint: S-001
status: done
assignee: backend-engineer
created: 2026-06-24
updated: 2026-06-24
---

## Sonuç
Akış: architect (tasarım) → backend (impl) → code-review → backend (fix).
Tasarım: `docs/analysis/0002-actuals-port-design.md`.

- Yeni `modes/actuals-first/sales-actuals/` modülü (controller/service/repository + lookup/validation servisleri), 2 entity + migration 1785, jenerik `common/services/csv-parser.service.ts`, decimal transformer, seed + Wella CSV fixture'ları.
- 5 endpoint (upload/batches/batch/rows/summary) — **silme endpoint'i YOK** (immutable).
- **Kapsam sıkı:** yalnızca ingestion+saklama+okuma. TTM'in claim generation / CAP policy / ledger dispatch'i port EDİLMEDİ (ledger çift-sayım riski).
- **Hard delete yok:** replacement = versiyonlama (`replacedByBatchId`), partial unique index + `23505→409`.
- **Actuals KPI'ı beslemez** (gerekçe tasarım §3; baseline türetme → [[T-024]] blocked).
- **Ledger sınırı 5 katman + DB kanıtı:** yasak kolon 0, `v_budget_summary` actuals'ı görmüyor, SA-E2E-06 upload öncesi/sonrası bütçe birebir aynı.
- 386 unit + 68 e2e; Wella verisi yüklü (2026-01: 400.000+600.000, 2026-02: 500.000).

## Çözülen bulgular
- Implementasyon sırasında yakalandı: yeni ACTIVE batch, eskisi REPLACED'e çevrilmeden insert edilince partial unique `23505` → repository `markReplacedStatus` + `linkReplacement` olarak bölündü.
- Code-review blocker: e2e dev DB'de çöp bırakıyordu (9 batch/52 satır) → `cleanupSalesActuals` helper + mevcut çöp tek seferlik temizlendi (3 ardışık koşum doğrulandı).
- Code-review: `getBatch` 400 → **404**; e2e'deki `[400,404]` maskeleyen assertion `.expect(404)`'e sıkılaştırıldı.

## Bilinen risk / takip
- E2E cleanup stratejisi `fiscal_period LIKE '2027-%'` — **2027 takvim yılından önce gözden geçirilmeli** (gerçek üretim verisiyle çakışır). Not: [[T-021]].
- Kapsam dışı: [[T-021]] (parser/entity kaydı/DTO Swagger), [[T-022]] (recognition-exceptions), [[T-023]] (plan-vs-actual), [[T-024]] (baseline, blocked), [[T-025]] (frontend).

## Açıklama
T-010 sırasında tespit edildi: CTPM'de actuals/sales/baseline entity'si **hiç yok**.
Wella `actuals_*.csv` (CPL × Kategori × Kanal aylık satış agregası: gross/net/discount)
gidecek yer bulamıyor. `ledger-entry` harcamadır, `agreement-transaction` hakediştir —
ikisi de satış cirosu değil. TTM'de `apps/api/src/actuals/` modülü mevcut.

Bu, actuals-first modunun eksik ayağı: gerçekleşen satış verisi olmadan baseline/
recognition akışı ve gerçek-veri raporlaması kurulamaz.

## Kapsam
- Actuals entity + migration (boyutlar: tenant, cpl, category, channel, period; gross/net/discount)
- Ingestion: CSV import (Wella actuals_YYYY-MM.csv) + replacement semantiği (aynı dönem yeniden import)
- TTM `recognition-exceptions` karşılığı değerlendirilir (kapsam kararı architect'te)
- KPI/baseline ilişkisi: actuals BASE_VOL/baseline'ı besliyor mu — netleştirilecek
- RBAC + tenant scope + immutable audit; her port'a e2e (port kuralı)

## Acceptance Criteria
- [ ] Actuals entity + migration; tenant-scoped, boyutlar CPL/Category/Channel/Period
- [ ] Ingestion servisi + endpoint; idempotent/replacement davranışı tanımlı ve test edilmiş
- [ ] Wella actuals_2026-01/02.csv gerçek veriyle yüklenir (seed veya import)
- [ ] Ledger/spend ile karıştırılmaz (satış ≠ harcama); KPI ilişkisi netleştirilmiş
- [ ] Unit + e2e yeşil; BRD: tenant izolasyonu, audit, dinamik formül korunur

## İlgili
- Tespit: [[T-010]] (Wella demo dataset) · gap audit: [[T-009]]
- TTM kaynak: `apps/api/src/actuals/` (controller, service, replacement.spec, recognition-exceptions)
- CTPM hedef: `collmind.backend/src/modules/modes/actuals-first/`
- Veri: `Wella/Test CSV/Veriler/actuals_2026-01.csv`, `actuals_2026-02.csv`
