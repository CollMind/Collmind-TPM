---
id: T-285
title: route-cell-map — göçen rotalar hücre haritasından SESSİZCE düşüyor
epic:
sprint:
status: todo
assignee: backend-engineer
created: 2026-08-25
updated: 2026-08-25
touches:
  - collmind.backend/scripts/analysis/route-cell-map.py
migration_seq:
---

## Açıklama

`route-cell-map.py:310` yalnız `@Roles` taşıyan rotaları alıyor:

```python
if len(c)<8 or c[4]!='1': continue
```

⇒ `W1`'de göçen **üç** rota (`admin/audit-log` ×2 · `notifications/:id/read`) bugün
hücre haritasında **YOK** (ölçüldü 2026-08-25, `code-reviewer` `N-3`: çıktıda
`ADMIN_READ`/`NOTIFICATION_WRITE` satırı **sıfır**).

`reconcile()` bilinçli olarak toplam-çıpası tutmuyor (*"ELLE YAZILMIŞ SAYI YOK"* —
kendi içinde **doğru** bir karar), yani bu düşüş **hiçbir kapıya** çarpmıyor.

## Neden bir sorun

`W2`–`W8` ilerledikçe hücre üyeliğinin **kanıt tabanı küçülür**, ve **hiçbir şey
ayrılan rotanın DOĞRU hücreye vardığını mutabakata sokmaz.** `G5` yalnız `@Roles`
taşıyan rotaları `EXPECT` ile çakıştırıyor — göçen rota `G5`'in de dışına çıkıyor.

⚠️ Sonuç: göç ilerledikçe **`G5`'in kapsamı erir**. `211 → 0` yolunda son rota
göçtüğünde `G5` **hiçbir şeyi** ölçmüyor olacak — ve bu, `Z29`'un *"bir kapı,
ölçümün BAŞARISINI hata sayamaz"* tuzağının **ters** hâli: kapı, başarı ilerledikçe
**sessizce boşalıyor**.

## Bugün zararsız

Üç rota elle `cell_for()`'a sorulup doğrulandı (`code-reviewer`, `MEKANIK`):
`admin/audit-log` → `ADMIN_READ` · `notifications/:id/read` → `NOTIFICATION_WRITE`.
Ve `@Roles`→yetenek eşleşmesi `git show HEAD:` ile karşılaştırıldı, **birebir**.

## Acceptance Criteria

- [ ] Harita `col5 || col9` üzerinden kuruluyor (göçen rota **düşmüyor**)
- [ ] Göçen rotalar için `G5`, `@Roles` yerine **`@RequireCapability` argümanını**
      hücreyle çakıştırıyor — yani göçün **doğru hücreye** vardığı mutabakata giriyor
- [ ] `MUTABAKAT` çıktısı göçen/göçmemiş **ayrımını basıyor** (kapsam görünür kalsın)
- [ ] Mutasyonla kanıt: bir rotanın `@RequireCapability` argümanı yanlış hücreye
      çevrilince `G5` **kırmızıya dönüyor**

## Ne zaman

**`W3`'ten önce.** `W1`+`W2` sonrası kayıp hâlâ küçük (≤ ~12 rota); `W7`/`W8`'de
`64`+ rota haritadan düşmüş olur ve geriye dönük doğrulama pahalılaşır.
