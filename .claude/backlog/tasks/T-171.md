---
id: T-171
title: "`GrandTotals` ROI hedefini sabit tutuyor (20.0) — kardeş okuyucu konfigürasyondan alıyor"
epic: E-001
sprint: S-001
status: done
assignee: frontend-engineer
priority: P2
created: 2026-08-10
updated: 2026-08-31
touches:
  - collmind.frontend/src/components/features/plans/GrandTotals.tsx
migration_seq:
---

## Açıklama

`GrandTotals.tsx:66`:

```ts
const targetRoi = 20.0; // Default target ROI (will be configurable from KPI config)
```

Bu sabit iki yerde **karar veriyor**: `:137` sayının rengini (kırmızı/nötr), `:142` gösterilen
hedef metnini.

**Kardeş okuyucu zaten düzeltilmiş** (`plan.service.ts:2809`, B-1 fix'i):

```ts
// B-1: Target ROI from GP_ROI_PCT KPI config (ragGreenThreshold) — NOT hardcoded.
const gpRoiKpi = await this.kpiEngine.getKpiConfig(tenantId, 'GP_ROI_PCT');
```

ve `PlanAnalysis.tsx:118` bu konfigüre değeri okuyor.

> **Sabit olan, sürekli görünen paneldedir; konfigüre olan bir alt sayfada.**
> `§7.1`'in T-083a vakasıyla aynı şekil: düzeltme yapıldı, kardeş yol açıkta kaldı, ve
> açıkta kalan daha görünür olandı.

`CLAUDE.md §2.3`: *"RAG: hardcoded threshold YASAK; sadece KPI konfigürasyonundan."*

**Ölçüm:** `docs/analysis/0051-brd-okuma-turu-33-whatif-ve-roi-tuketimi.md §2`

## Yapılacak

1. Backend'in `gpRoiPerformance.targetRoi`'sini bu panele de taşı (mevcut bir uç var mı önce
   **ara** — `§7`).
2. Fallback gerekiyorsa **sabit yazma**: hedef okunamıyorsa hedef **gösterme** ve renk kararı
   verme (`§2.5` — bilinmezliği bir yargıya çevirme; bkz. [[T-172]]).
3. `20.0` sabitinin başka okuyucusu var mı — **grep çıktısıyla** yanıtla (`§7.1`).

## Acceptance Criteria
- [ ] `GrandTotals`'ta sayısal ROI hedefi sabiti yok
- [ ] Hedef okunamadığında renk/karşılaştırma **yapılmıyor** (uydurulmuş hedefle değil)
- [ ] Kardeş okuyucu taraması yapıldı ve sonucu task'a yazıldı

## Notlar
[[T-172]] ile **aynı dosyaya** dokunuyor → paralel değil, **sıralı** koştur.
[[T-163]] aynı dosyanın `:64` yorumunu da değiştirecek.

## ✅ KAPANDI — `DALGA 0` (2026-08-31)
**Kapatan hüküm:** `T-344` (`B3` ölümü — hardcode `%20`).
**Kanıt (birinci elden):** `GrandTotals.tsx:5` `import { useTargetRoiThreshold }`; `:144` `const targetRoiThreshold = useTargetRoiThreshold();`
`:136` eski satırın `F12` izi: *"Eski satır: `const targetRoi = 20.0;`"* — silinmemiş, **üstü çizilmiş**.
