---
id: T-361
title: getBatchRows SINIRSIZ — sayfalama yok, jsonb raw ile yanıt boyutu üst sınırsız
epic:
sprint:
status: todo
assignee: backend-engineer
created: 2026-09-03
updated: 2026-09-03
touches:
  - collmind.backend/src/modules/master-data/baseline-volume/baseline-volume.controller.ts
  - collmind.backend/src/modules/master-data/baseline-volume/baseline-volume.repository.ts
---

## Açıklama

`code-reviewer` bulgusu `S6` (2026-09-03, `BL-4a` review'u).

`GET /baseline-volumes/batches/:batchId/rows` → `find({ where, order })` — **limit yok**.

```
YAZMA yolu   CHUNK_SIZE = 500 ile parçalayarak INSERT  ⇒ binlerce satırlı batch BEKLENEN ŞEKİL
OKUMA ucu    her satırın `raw` JSONB'sini de döndürüyor ⇒ yanıt boyutu ÜST SINIRSIZ
varsayılan   filtreler OPSİYONEL ⇒ FİLTRESİZ çağrı varsayılan yoldur
```

## Yapılacak
- [ ] Sayfalama (`limit`/`offset` ya da cursor) — varsayılan bir limit **zorunlu**
- [ ] `raw` alanının her satırda dönmesi gerekli mi — teşhis için `remediation` + anahtarlar
      yetiyorsa `raw` opsiyonel bir bayrağa bağlansın
- [ ] Sınırın **aşıldığı** durumda sessiz kesme YASAK (`§2.5`) — toplam sayı + devam bilgisi

## Done tanımı
- [ ] `docker ps --filter "label=com.docker.compose.project=tpm"` boş
- [ ] Testler yeşil, `npm run guards` exit 0
- [ ] `code-reviewer` onayı
