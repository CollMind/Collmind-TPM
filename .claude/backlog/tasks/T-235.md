---
id: T-235
title: Kapsam filtresi 5 rolün 4'ünde kapalı — ve bayrağın kapanış şartının SAĞLAYICISI yok
epic: E-001
sprint: S-001
status: in-progress
assignee: architect
created: 2026-08-17
updated: 2026-08-18
touches:
  - collmind.backend/src/modules/shared/access-scope/access-scope.service.ts
  - collmind.backend/src/modules/shared/access-scope/access-scope.service.spec.ts
  - collmind.backend/src/database/seeds/user-scope.seed.ts
  - collmind.backend/.env.example        # ⛔ Team Lead'in izni YOK — metin: docs/verification/T235_SCOPE_FLAG_ENV_ORNEGI.md
  - docs/verification/ADIM3_OLCUM_2_KAPSAM_MEKANIZMASI.md
  - docs/verification/T235_OLCUM_1_VE_3.md
---

## Açıklama

`ADIM 3` ölçüm 2'de bulundu (`docs/verification/ADIM3_OLCUM_2_KAPSAM_MEKANIZMASI.md`).

**Mekanizma doğru ve tekil** — `AccessScopeService`, `T-028d` kopyaları taşımış,
`İlke 4` gerilimi yok. Sorun mekanizmada değil, **kapsamının kapalılığında**:

```
UNRESTRICTED_ROLES = { ADMIN, FINANCE, READONLY }   kod sabiti, koşulsuz
PLANNER  &&  !scopeEnforcementEnabled               UNRESTRICTED
CATEGORY_MANAGER                                     buildScope()   ← TEK gerçek daraltma
```

Yani **`5` rolün `1`'i** kapsam filtresine tabi. Ve `SCOPE_ENFORCEMENT_ENABLED`
**hiçbir yerde tanımlı değil** (`.env` 0 anahtar · `.env.example` yok · compose yok) —
varsayılan `false`.

### ⛔ Asıl kalem: şartın sağlayıcısı yok

Bayrak bir unutma değil, `T-028c`'nin bilinçli kararı. Kapanış şartı
`access-scope.service.ts` başlığında yazılı:

> *"prod/UAT'de backfill migration'ı doğrulanana kadar bu YIKICI olur"*

Ama CTPM bugün **yalnız lokal geliştirme ortamında koşuyor; deploy edilmiş
staging/production yok** (`CLAUDE.md §1`). Şart karşılanamıyor değil — **karşılayacak
ortam yok**, ve bayrak `2026-07-28`'den beri kendiliğinden açılamaz durumda.

📌 Bu, aynı turda bulunan **ikinci** aynı-şekilli kalem. Birincisi `report-only`
(`0073` Soru 3): *"envanter fiili trafikte doğrulanır"* → fiili trafik yok.
**İkisi de doğru yazılmış, ikisi de var olmayan bir ortama adresli.**

Ve `K-2.6.9` sapmayı zaten kaydetmişti (*"filtre bugün bir ayarla kapalı"*) —
**adresi ve kapanamama sebebi kayıtta yoktu.**

## Acceptance Criteria

- [x] ✅ **`SCOPE_ENFORCEMENT_ENABLED` `.env.example`'a YAZILDI** (2026-08-18).
      Önce **engel kaldırıldı**: `Read(./**/.env.*)` deseni `.env.example`'ı da
      kapsıyordu — ürün sahibi kararıyla **daraltıldı** (şablonlar hariç, gerçek
      ortam dosyaları korumada). Karar + kabul kanıtı:
      `docs/decisions/ENV_KORUMA_DARALTMASI.md`.
      Koruma **daraldı, kalkmadı** — `.env` okuma **hâlâ reddediliyor** (ölçüldü).
- [x] ✅ **KAPANIŞ ŞARTI REVİZE EDİLDİ** (2026-08-18) — eski şartın sağlayıcısı yoktu:

      ```
      ESKİ   ŞART       "prod/UAT'de backfill doğrulanana kadar"
             SAĞLAYICI  prod/UAT ortamı
             DURUM      ⛔ YOK → kilit, 2026-07-28'den beri

      YENİ   ŞART       migration 1792 backfill'i doğrulanmış VE PLANNER rolündeki
                        HİÇBİR kullanıcı kapsam satırsız değil
             SAĞLAYICI  lokal dev DB + e2e ortamı        ← VAR
             DURUM      ✅ BUGÜN SAĞLANIYOR — ölçüldü, satırsız PLANNER: 0
      ```

      ⚠️ **Şart sağlandı, ama bayrağı AÇMAK ayrı bir karardır** — ve etkisi ölçüldü:

      | kullanıcı | satır | kapsanan CPL | toplam CPL |
      |---|---|---|---|
      | `planner@wella.com` | 11 | **11** | 29 |
      | `planner2@wella.com` | 17 | **17** | 29 |

      Bayrak açılırsa iki planlamacı `29/29` CPL'den `11/29` ve `17/29`'a **daralır**.
      Satırların hepsi `category_id NULL` (yalnız CPL boyutu), yani kategori daralması
      olmaz. **Bu, `K-2.6.9`'un istediği davranıştır** — ama davranışsal bir değişiklik
      olduğu için ürün sahibi kararıdır.

- [ ] ⛔ **BAYRAK BUGÜN AÇILMIYOR** — ürün sahibi kararı, 2026-08-18. **Ve gerekçe
      kilidin kendisinde değil, ETKİSİNDE:**

      > *"Bayrağı açmak, henüz bütünlüğü sağlanmamış bir veriye davranışsal bir kapı
      > bağlamak olur — ve yönü fail-closed, yani hata **erişim kaybı** olarak
      > görünür."*

      Kapsam tablosunun bütünlüğü **tam değil**:

      ```
      T-237 ✅  115 öksüz satır DAHA YENİ temizlendi + category_id FK'si eklendi
      T-238 ⛔  user_scopes.channel_id HÂLÂ FK'siz
      ```

      **SIRA:** [[T-238]] (kalan FK) → **sonra** bayrak.

      📌 **Kilit açıldı, uygulama bir adım daha bekliyor — ve o adım ADRESLİ.**
      Bu, *"sağlayıcısı olmayan şart"* sınıfına **geri düşmez**: şartın sağlayıcısı
      var ve sağlanıyor; bekleyen şey bir ortam değil, **bir task**.

      ⚠️ Bayrak açıldığında `K-2.6.9`'un *"❌ ölçülmüş sapma: filtre bugün bir ayarla
      kapalı"* notu **`F12` deseniyle güncellenmeli** — sapma kapanmış olur.
- [x] ✅ **ÖLÇÜLDÜ + KARARA BAĞLANDI** (2026-08-17) — `docs/verification/T235_OLCUM_1_VE_3.md`.
      Ölçüm: `ADMIN`/`FINANCE`/`READONLY`'nin scope satırı **`0`**, ve kod aynı sıfırı
      **tam erişim** diye okuyor — `K-2.6.8a` (*"boş kapsam = erişim yok; tüm veriye
      erişim **açık bir joker atamasıyla** verilir"*) tam tersini söylüyor. **Mekanizma ters.**

      **KARARLAR (ürün sahibi):**

      | rol | karar | gerekçe |
      |---|---|---|
      | `YÖNETİCİ` | **kalır** | `K-2.6.4`: *"tanımlar, kural yönetimi"* — tenant genelinde |
      | `FİNANS` | **kalır** | `K-2.6.4`: *"transfer, mutabakat, içe aktarma"* — tenant geneli işler. **Bir transfer iki zarf arasında olur; kapsam kısıtı onu bölerdi** |
      | `İZLEYİCİ` | ⛔ **ÇIKAR** | `K-2.6.4c`: *"`İZLEYİCİ` bir **izleme yetenekleri setidir**, bir 'salt-okur bayrağı' DEĞİL."* Kapsamı hiçbir yerde *"her şey"* diye yazılmamış → **kapsam ataması gerekir** |

      ⚠️ **Ve üçü de KOD SABİTİ olarak kalmaz.** `K-2.6.8a` *"açık bir joker atamasıyla"*
      diyor: `UNRESTRICTED` bir **seed satırı** olmalı (`{cplId: null, categoryId: null}`),
      bir kod dalı değil.

      > O zaman mekanizma tersine dönmez: **boş kapsam gerçekten erişim yok, ve joker
      > görünür.** `buildScope` zaten joker satırı `UNRESTRICTED`'a çeviriyor
      > (`hasUnrestrictedRow`) — yani mekanizma **hazır**, eksik olan yalnız satırlar.

### ⛔ SIRA BAĞLAYICI (ürün sahibi, 2026-08-17) — seed ÖNCE, kod dalı SONRA

```
YANLIŞ SIRA   kod dalı kaldır  →  satırsız READONLY HİÇBİR ŞEY görmez
DOĞRU SIRA    seed satırları yaz  →  SONRA kod dalını kaldır
```

**Ve `İZLEYİCİ`'nin kapsamı = JOKER SATIR** (ürün sahibi kararı):

| şık | sonuç |
|---|---|
| **joker satır** ✅ | bugünkü davranışı **korur**, ama `K-2.6.8a`'ya **uygun** — erişim artık *açık* |
| dar kapsam ❌ | davranış değişir, ve **kim belirler?** — o zaten `K-2.6.9`'un konusu, tenant sonra ayarlar |

> `K-2.6.8a`'nın istediği tam bu: *"tüm veriye erişim, **açık bir joker atamasıyla**
> verilir."*

📌 **Ve `T-028c`'nin kilit tuzağı BURADA YOK — çünkü sağlayıcı var: seed.** Şart bu
yüzden **ölçülebilir** ve var olan bir ortama adresli:

```
ŞART        İZLEYİCİ rolündeki HER kullanıcının bir kapsam satırı var
SAĞLAYICI   seed                        ← VAR
DURUM       ✅ ölçülebilir — bir erteleme değil, bir adım
```

- [x] ✅ **ADIM 1 BİTTİ** (`a25e820`, 2026-08-18) — `WILDCARD_SCOPE_ROLES` role bağlı
      yazıyor (sabit e-postaya değil): `ADMIN 1` · `FINANCE 2` · `READONLY 1` = **4**
      joker satır. `PLANNER`/`CATEGORY_MANAGER` **0** — dar kapsamları bozulmadı.
      Seed'e **DUR guard'ı gömüldü**: wildcard-rollü bir kullanıcının zaten DAR bir
      satırı varsa loud throw (sessiz kapsam genişletmesi yok).
      ⚠️ İdempotentlik **DB'den gelmiyor** — `UQ_user_scopes_user_cpl_category`
      `NULLS NOT DISTINCT` taşımıyor; seed açık `IS NULL` SELECT'ine dayanıyor.
      Team Lead ölçtü: seed iki kez koştu, **delta `0`**.
- [x] ✅ **ADIM 2 BİTTİ** (`ff1f85f`, 2026-08-18) — `UNRESTRICTED_ROLES` = `{ADMIN,
      FINANCE}`. Kapı ölçüldü: tek `READONLY` kullanıcısının satırı var **ve joker**.
      Team Lead mutasyonla doğruladı (`READONLY` geri kondu → yeni bloğun **iki testi
      de kırmızı**, geri alma `shasum -c`) — blok kör değil.
- [x] ✅ **BEKLENEN KIRMIZI doğru şekilde giderildi** (`ff1f85f`) — `it.each` listesinden
      `READONLY` çıktı, kısa devre assertion'ı `ADMIN`/`FINANCE` için **korundu**; kısa
      devre geri konarak susturulmadı. Aşağıdaki uyarı **tarihsel kayıt** olarak duruyor:
      `access-scope.service.spec.ts:51-56` bugün şunu pinliyor —
      ```
      it.each([ADMIN, FINANCE, READONLY]) → UNRESTRICTED
        expect(userScopeRepo.find).not.toHaveBeenCalled()   ← KISA DEVRE assertion'ı
      ```
      Kısa devre kalkınca `find` **çağrılacak** ve bu assertion **kırılacak**.
      Kırılması **beklenen ve doğrudur** — ama kabul listesinde yazılı değilse bir
      *"regresyon"* sanılıp kısa devre geri konabilir. Testin yeni hâli: aynı üç rol
      **joker satırdan** `UNRESTRICTED` dönmeli, ve **satırsız** bir `READONLY`
      `SCOPED{pairs:[]}` (yani hiçbir şey görmemeli) — iki girdi, iki çıktı
- [x] ✅ Davranış **testle pinlendi** — `access-scope.service.spec.ts` `T-235 ADIM 2`
      describe'ı: joker satır → `UNRESTRICTED` **ve `find` ÇAĞRILIR** · satır yok →
      `SCOPED{pairs:[]}`. İki girdi, iki çıktı.

### ⚠️ KALAN ASİMETRİ — `ADMIN`/`FİNANS` (bilinçli, bu turda değil)

Bugün o iki rol için **iki mekanizma birden** var:

```
kod dalı      UNRESTRICTED_ROLES kısa devresi   → satırları HİÇ okumadan UNRESTRICTED
joker satır   ADIM 1'de yazıldı                 → okunsaydı da UNRESTRICTED verirdi
```

İkisi **aynı sonucu** veriyor, yani hangisinin kanonik olduğu bugün **ölçülemez** —
`İlke 4`'ün sessiz hâli. `K-2.6.8a` (*"açık joker atamasıyla"*) uzun vadede **satırı**
kanonik yapıyor.

- [ ] **ADIM 3 (gelecek)** — `ADMIN`/`FİNANS` kod dalları da kalkar, `UNRESTRICTED_ROLES`
      sabiti **tümüyle boşalır** ve silinir. Kapı `ADIM 2`'nin aynısı: her iki roldeki
      **her** kullanıcının joker satırı olduğu assert edilir. ⚠️ Ve `spec:51`'in
      `it.each` bloğu o turda **tümüyle** kalkar — beklenen kırmızı, şimdiden yazılı
- [ ] Bayrak açıldığında kırılan/kırılmayan davranış bir **teste** bağlanır — bugün
      `access-scope.service.spec.ts` bayrağı `'true'` ile koşuyor, yani **KAPALI
      davranış** ayrıca sınanmalı (spec'in kendi yorumu bunu söylüyor)

## Done tanımı

- [ ] Testler yeşil (unit + ilgili e2e)
- [ ] `npm run guards` yeşil — exit 0
- [ ] `bash scripts/guards/money-float.sh --ratchet` exit 0
- [ ] `code-reviewer` onayı
- [ ] **Üretim çağrı yolu var mı?** — `AccessScopeService` altı tüketiciden çağrılıyor
      (`plan.repository` · `agreement.repository` · `approval-workflow` · `dashboard` ·
      `settlement-summary` · `finance-reporting.budget-variance`), yol **var**
- [ ] `touches:` gerçekte dokunulan dosyalarla güncel
