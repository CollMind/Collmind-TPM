---
id: T-288
title: route-cell-map.py bir KAPI ama hiçbir kapı yoluna bağlı değil
epic:
sprint:
status: done
assignee: backend-engineer
created: 2026-08-25
updated: 2026-08-25
touches:
  - collmind.backend/scripts/guards/run-all.sh
  - collmind.backend/scripts/analysis/route-cell-map.py
migration_seq:
---

## Açıklama

`route-cell-map.py` **altı kapı** taşıyor (`G1`·`G2`·`G3`·`G4`·`G5`·`G6` + `W1`
uyarısı) ve ihlalde **`exit 2`** veriyor — ama `npm run guards` onu
**ÇAĞIRMIYOR**. Dosya kendi docstring'inde bunu dürüstçe yazıyor:

> *"KAPSAM: bu dosya `scripts/analysis/` altında ve `run-all.sh`/`npm run guards`
> KAPSAMAZ. Yani operatörün ELLE koşturduğu bir kapıdır."*

⛔ **`§`: *"Doğrulama bir KAPIDIR — durdurmuyorsa doğrulama değildir."*** Ve
zincirleme kuralı: *"self-test dosyası VAR ama hiçbir gerçek kapı yolu onu
ÇAĞIRMIYORSA"* ihlal.

## ⛔ Aciliyeti bir VAKA ile ölçüldü (`W3`, 2026-08-25)

Bu turda **tam olarak bu sınıf** gerçekleşti: `roles_for`'un dosya-geneli geri
düşüşü **başka bir rotanın `@Roles`'unu göçenlere atfediyordu** — kanonik
üreticinin **veri uydurması**.

```
W1/W2'de GÖRÜNMEDİ   o controller'larda @Roles hiç kalmamıştı
W3'te ÇIKTI          controller KARMA (GET /users bilinçli göçmedi)
YAKALAYAN            bir kapı DEĞİL — Team Lead'in çıktıya bakması
```

⇒ **Her kısmi dalgada tekrarlayabilir**, ve bugün onu **hiçbir otomatik kapı
görmüyor**. `W4`–`W8`'in hepsi karma olabilir.

📌 Ve bir yan bulgu (`W3` review): commit'li TSV **`W2` boyunca bir dalga
bayattı** (`tenant` rotalarını hâlâ `ROLES` gösteriyordu) — **bunu gören kapı da
yok**. Yani iki ayrı sessiz sapma, aynı boşluktan.

## Acceptance Criteria

- [ ] `route-cell-map.py` `run-all.sh`'a bağlandı (ratchet'lerin zincirleme
      deseniyle — `exit 2` = SETUP HATASI taksonomisine **uygun** kod seçimi;
      `single-mechanism`'in `2`/`3` ayrımı emsal)
- [ ] **TSV DRIFT KAPISI:** commit'li `B3A_EK3_ROTA_HUCRE_ESLEMESI.tsv` ile taze
      üretim **birebir** — değilse `exit`. (Bugün elle yapılıyor; `W2`'de bir
      dalga kaçtı.)
- [ ] Kapının **kırmızıya dönebildiği** mutasyonla kanıtlandı (`§2.7 #9`:
      *"iki farklı girdide iki farklı çıktı"*)
- [ ] Çalışma süresi `npm run guards`'ı kabul edilemez yavaşlatmıyor — ölçülsün
      (üretici `route-scope.sh`'i de çağırıyor, yani iki kez ayrıştırma riski var)

## ⛔ DİKKAT — `Z29` tuzağı

Kapılar `A1`/`FILTRESIZ` gibi **sıfırlanması HEDEF** olan sayılara bakmıyor, ama
`G6 gocen=N` **büyüyor** ve `G3`/`W1` evrenleri **küçülüyor** (`W8`'de `ROLES`
kovası `72`'ye inecek). Bağlarken sor: *"bu kontrolün reddettiği durum, projenin
HEDEFİ olabilir mi?"* — ve *"girdisi kontrol ettiği şeyden mi türüyor?"*


---

## ✅ KAPANDI (2026-08-25) — `W4`'ten ÖNCE (ürün sahibi sıra kararı)

Gerekçe `T-285`'in **birebir aynısı**: *"bilinen bir kör nokta, tek dalga bile
beklemez"* — ve `W4` **29 rotalık en büyük kısmi dalga** olacak, yani tam da
uydurmanın çıktığı **karma-controller** sınıfında.

**Yapılan:** `run-all.sh` üreticiyi çağırıyor · **`G7` TSV drift kapısı** ·
`SKIPPED` bir geçiş değil (`exit 2`).

**Üç dal da mutasyonla kanıtlandı:**

| mutasyon | sonuç |
|---|---|
| uydurma geri düşüşü **geri kondu** | `G7` drift → `npm run guards` **`EXIT=1`**, çıktı uydurmayı birebir gösterdi (`ADMIN,FINANCE` ↔ `-`) |
| TSV **kaldırıldı** | `exit 2` **SETUP HATASI** — sessiz geçiş yok |
| TSV'de **tek satır bozuldu** | `exit 2` — *"artefakt bayat"* |
