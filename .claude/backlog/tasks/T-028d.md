---
id: T-028d
title: dashboard/settlement scope kopyalarını ortak servise taşı + F6 latent bug fix
epic: E-001
sprint: S-001
status: done
assignee: backend-engineer
created: 2026-07-28
updated: 2026-07-28
---

## Sonuç
- `dashboard.service.resolveCplScope` ve `settlement-summary`'deki ad-hoc scope blokları
  **kaldırıldı**; ikisi de `AccessScopeService` kullanıyor (`grep resolveCplScope` → canlı kod yok).
- **Rol semantiği tek yerde:** her iki serviste `if (role === ...)` scope kararı kalmadı;
  UNRESTRICTED/pair/category-only tanımı yalnız `AccessScopeService`'te.
- **F6 düzeltildi:** `cplId=null` ("tüm CPL'ler") artık UNRESTRICTED — önceden `.filter(id=>!!id)`
  ile sessizce "hiçbiri" oluyordu. 2 regresyon testi eklendi.
- **Bilinçli davranış genişlemesi:** CATEGORY_MANAGER artık dashboard/settlement'ta da
  kategori-scoped (önce tenant-wide görüyordu). "Rol semantiği tek yerde"nin doğal sonucu,
  BRD yönünde doğru. PLANNER davranışı değişmedi (T-028c).
- 432 unit (5 geçersiz rol-loop testi kaldırıldı, 2 F6 testi eklendi) + 131 e2e.

## Team Lead ek düzeltmesi — e2e bütçe sızıntısı (3. tekrar)
Agent "7 e2e hatası pre-existing" dedi; doğruladım: **kod temiz, sebep DB state birikimi**.
T-029'dan sonra onaylı planlar bütçeyi doğru tutuyor ama BRD gereği API'den silinemiyor →
her e2e koşumu zarfı kalıcı tüketiyordu (190 artık plan, `NKA-Q1` 500.000 → 8.073 kalmış).
→ `cleanupTestPlans` helper'ı eklendi (test-only SQL, `cleanupTestTransactions` deseni).
**Kanıt: 3 ardışık koşum 131/131, artık plan 0, zarf available=500.000 sabit.**

## Açıklama
T-028'in 4. parçası (düşük risk). Tasarım: `docs/analysis/0004-rbac-brd-alignment.md` §1 (F6), §4.

Scope mantığı şu an **iki yerde ayrı ayrı** yazılı: `dashboard.service.resolveCplScope` ve
`settlement-summary.service`. T-028b ortak `AccessScopeService`'i getirince bunlar üçüncü/dördüncü
kopya olmadan ona taşınmalı (5. çift-sayım hatasından çıkan ders: aynı mantığı elle tekrarlama).

## 🐞 F6 — latent bug (taşırken düzeltilecek)
`dashboard.service.ts:304` → `scopes.map(s => s.cplId).filter(id => !!id)`.
`user-scope.entity.ts:22`'ye göre `cplId = null` **"tüm CPL'ler"** demek; filtre onu sessizce
**"hiçbiri"ye** çeviriyor → NULL-scope'lu kullanıcı dashboard'da boş veri görüyor.

## Acceptance Criteria
- [ ] dashboard + settlement-summary `AccessScopeService`'i kullanıyor; yerel kopyalar kaldırıldı
- [ ] **F6 düzeltildi:** `cplId=null` → UNRESTRICTED (sessizce "hiçbiri" değil)
- [ ] Mevcut dashboard/settlement unit+e2e testleri yeşil kalıyor (mock'lar AccessScopeService'e taşınır)
- [ ] Regresyon testi: NULL-scope'lu kullanıcı tüm CPL'leri görür

## İlgili
- Tasarım: `docs/analysis/0004-rbac-brd-alignment.md` · Bağımlı: [[T-028b]] (AccessScopeService)
