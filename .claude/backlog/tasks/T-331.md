---
id: T-331
title: POST /plans/:id/review approve KOŞULSUZ 400 — channelCode kilitli satırdan okunuyor
epic:
sprint:
status: review
assignee: debugger
created: 2026-08-29
updated: 2026-08-29
touches:
  - collmind.backend/src/modules/modes/planning-first/plan/approval-workflow.service.ts
migration_seq:
---

## Bulgu — `W1` senaryo turunda çıktı, **canlı uç kusuru**

`POST /plans/:id/review` `decision=approve` → **koşulsuz `400`**.

### Mekanizma `[ÖLÇÜLDÜ]` — Team Lead bağımsız teyidi
```
approval-workflow.service.ts:512   const channelCode = plan.channel?.code || '';
  plan NEREDEN:  planRepo.findByIdForUpdate(...)          (:433 / :203 / :779)
  o metot:       manager.findOne(Plan, { where, lock: pessimistic_write })
                 ⛔ relations YOK
  ⇒ plan.channel HER ZAMAN undefined  ⇒  channelCode = ''
  ⇒ envelope araması '' ile başarısız  ⇒  400
```

⛔ **Ve repo metodu DOĞRU — çağıran hatalı.** Kanıt: `plan.repository.spec.ts:302`
*"findByIdForUpdate locks via the given manager (pessimistic_write), **no relations**"*
— ilişkisizlik **kasıtlı ve pinli** (TypeORM'un kilit+relations kısıtı).

### Kardeş yol AYNI ŞEYİ DOĞRU YAPIYOR — `§7` sınıfı
```
plan.service.ts:904   const initial = await this.findById(id, tenantId, actor);
                      const channelCode = initial.channel?.code || '';
                      ⇒ KİLİT ÖNCESİ, relations'lı okumadan
```
📌 *"Aynı yetenek iki kez yazıldı — biri doğru, biri yanlış."*
POZ.KONTROL: aynı repo dosyasında **7** metot `relations:` kullanıyor ⇒ desen mevcut.

## Reprodüksiyon — **ZATEN VAR, kusur ÖNCE GÖRÜLDÜ**
`W1` turunun yazdığı **üç test bugün KIRMIZI** ve doğru sözleşmeyi pinliyor:
```
test/plan-review-decision.e2e-spec.ts        R3
test/plan-escalate-to-finance.e2e-spec.ts    E2 · E3
```
⇒ **Testi ürüne uydurma** — ürün düzelince üçü de yeşile döner. `qa-engineer` bunları
**bilerek kırmızı bıraktı** (`DUR` listesi gereği) ve bu **doğru davranıştı.**

## İş
`channelCode`'u **kilitten önce**, `relations`'lı bir okumadan al — kardeş yolun deseni.
⚠️ **Kilidi zayıflatma:** `findByIdForUpdate`'e `relations` **ekleme** (o pinli ve
TypeORM kısıtı var). Okuma **ayrı** yapılır, kilit **aynen** kalır.

## ⛔ `§7.1` — SAY
`plan.channel?.code` / `plan.channel?.name` deseni **başka nerede** kilitli bir satırdan
okunuyor? `approval-workflow.service.ts:164` ve `:263` de aynı ailede olabilir —
**HER BİRİNİ ölç** (`plan` o satırlarda nereden geliyor?). *"Kardeş yol etkilenmiyor"*
iddiası **ölçülmeden yazılamaz**.
⚠️ POZ.KONTROL: taraman `:512`'yi buluyor mu?

## Acceptance
- [ ] `POST /plans/:id/review` `approve` **çalışıyor**; üç kırmızı test **yeşil**
- [ ] `findByIdForUpdate` **değişmedi** (`git diff` ile kanıt)
- [ ] Kardeş `channel?.code` okumaları **sayıldı ve sınıflandırıldı**
- [ ] Tam e2e yeşil · `T-047 PASS`
