---
id: T-293
title: CANLI KUSUR — LTA formu kaydediliyor, spend motoru onu ASLA görmüyor
epic:
sprint:
status: done
assignee: architect
created: 2026-08-26
updated: 2026-08-30
touches:
  - collmind.backend/src/database/migrations/1817000000000-BindLtaAgreementsToAgreements.ts
  - collmind.backend/src/database/entities/lta-agreement.entity.ts
  - collmind.backend/src/modules/shared/lta/lta-agreement.service.ts
  - collmind.backend/src/modules/shared/lta/lta.module.ts
  - collmind.backend/src/modules/shared/lta/dto/create-lta-agreement.dto.ts
  - collmind.backend/src/common/kpi/roi-denominator.ts
  - collmind.backend/src/database/seeds/kpi.seed.ts
  - collmind.backend/src/modules/master-data/kpi/kpi.service.ts
  - collmind.backend/test/lta-lifecycle-bond-and-base-chain.e2e-spec.ts
  - collmind.backend/test/t269-lta-agreement-permission-grant.e2e-spec.ts
  - collmind.backend/test/helpers/seed-e2e.ts
---

## ⛔ Ölçülmüş durum

```
KULLANICI YOLU     LTAAgreementForm → POST /agreements → agreements(agreementType:LTA)
MOTOR YOLU         SpendCalculationService → ltaAgreementService.getLTAForPlanContext()
                   → lta_agreements + lta_rates
BAĞLANTI           YOK — agreements yaratımı lta_agreements'i BESLEMİYOR
                   (poz.kontrol: aynı grep shared/lta'da DÖRT dosya buluyor)
TABLOLAR           lta_agreements 0 · lta_rates 0 · lta_plan_overrides 0
```

⇒ **Kullanıcı LTA formunu doldurup kaydettiğinde, spend-calc tarafında hiçbir LTA
indirimi asla hesaplanmıyor** — bir hata dalı değil, **yapısal ve her plan için**.

`kpi.seed.ts`'teki `PLANNED_LTA_ON/OFF` ve `BASE_LTA_ON/OFF` KPI'ları bu yüzden
**hiçbir CPL için** bir LTA indirimi üretmiyor.

## Sınıfı — ve neden `İlke 3`'ten AĞIR

`İlke 3` *"verisi düzenlenemeyen kural fiilen koddur"* der. Burada durum bir adım öte:

> **Kullanıcı veri GİRİYOR, motor onu ASLA görmüyor.**

`budget_allocations` deseninin **büyüğü**: orada en azından bir tüketici vardı
(dashboard). Burada yazma **ve** okuma tarafı frontend'den **tamamen kopuk**, motor
tarafında ise **canlı ve zorunlu**.

## ⚠️ Ve iki hipotez ÇÜRÜDÜ

| hipotez | ölçüm |
|---|---|
| *"`lta_agreements` gereksiz bir kopya, kaldırılabilir"* | **ÇÜRÜDÜ** — `SpendCalculationService`'in gerçek bağımlılığı |
| *"iki model aynı ticari gerçekliğin iki temsili"* | **ÇÜRÜDÜ** — biri boş; *"iki meşru temsil"* argümanı kurulamıyor |

`agreements` tablosu `lta_rates`'in kapasitesini **karşılamıyor**: kanal×kategori bazlı
çoklu oran satırları · `minimum_volume_commitment` · `maximum_discount_cap` ·
`payment_terms`. `agreements` tek bir `mechanic_value`/`cap_total_amount` taşıyor.

⇒ Çift-model **gereksiz kopya değil, EKSİK BİRLEŞME**.

## ⛔ Ürün sahibi kararı gerekiyor — iki yol, ikisi de tasarım kararı

| yol | bedeli |
|---|---|
| LTA yaratımı `lta_agreements`'e **devredilir** | mevcut plan onay/audit akışı `agreements` üzerinde kurulu — **kopma** yaratır |
| `agreements` LTA yaratımı `lta_agreements`+`lta_rates`'i **otomatik besler** | iki model yaşamaya devam eder, senkron sorumluluğu doğar |

## Acceptance Criteria

- [ ] Ürün sahibi kararı alınır (yukarıdaki iki yol)
- [ ] Repro-pin: bugünkü kopukluk **önce görülür** (LTA anlaşması yarat → spend-calc
      çıktısında LTA indirimi **yok**), sonra düzeltilir (`T-273`)
- [ ] Düzeltme sonrası pin: aynı akış **indirim üretiyor**
- [ ] `K5` (LTA rol hizalaması) bu karardan **sonra** ele alınır — `Z37 §1`'in askısı
      bu task'a bağlı


---

## ⛔ HÜKÜM (ürün sahibi, 2026-08-26 — `Z38 §3`): İKİ SEÇENEK DE REDDEDİLDİ

> **Soru YANLIŞ KURULMUŞTU. Doğru cevap devir değil: BAĞ + EKSİK YÜZEY.**

| seçenek | neden kapalı |
|---|---|
| devir | onay/audit akışını **koparır** — kayıtlı karar ailesinin **ihlali** |
| otomatik besleme | **YARIM** — başlık satırını üretir, **oranları üretemez**; oran verisinin hâlâ **hiçbir giriş yüzeyi yok** |

**Kök neden:** eksik birleşme **+ EKSİK YÖNETİM YÜZEYİ** — mekanik-alanları sınıfının
LTA hâli (`EK_E` ailesi).

### (a) Mimari çerçeve
```
agreements   = YAŞAM DÖNGÜSÜNÜN kanonik yeri (onay · audit · SoD · defter bağı)
lta_rates    = ORAN ŞARTLARININ kanonik yeri (kanal×kategori kademe)
bağ          AÇIK — agreements-LTA kaydı EBEVEYN, oran kademesi ona BAĞLI doğar
oran girişi  KENDİ YÜZEYİNİ alır
```

### (b) İnşası `Faz 2` GİRİŞ KOŞULU — bugünün kod işi DEĞİL
Motor `Faz 2`'nin motoru, deploy yok, **ölü KPI bugün kimseyi yanıltmıyor**.
→ `Faz-2` giriş listesi + **`EK_E` şema-uyum turu** (oran yüzeyi satırı).

### (c) ✅ TEK BUGÜNLÜK İŞ — YAPILDI
`EK_E`'ye dürüstlük satırı yazıldı (`Z38 §3(c)` yetkisiyle).

⚠️ **Ve yazarken `EK_E`'nin taksonomisinde bir varsayım bulundu:** `⚠️` bölümünün
başlığı *"kullanıcıya **görünür**"* — `T-293` **görünmüyor**. Üçüncü şekil:
**sessiz kırıklık = YANILTMA**, ve üçünün **en pahalısı**. İşaret kararı ürün sahibinde.

### Ve `Z37` askısı buna BAĞLANDI
LTA dörtlüsünün kaderi **meşruiyet sorusundan TASARIM sorusuna** döndü: bağ
mekanizmasının parçası olur · oran-yüzeyinin API'si olur · ya ölür. **Üçü de bu
kararın türevi.** `Z25`: sağlayıcı = bu task'ın mimari kararı, tetikleyici = `Faz-2`
giriş koşulları derlemesi.

---

## ⛔ KİLİT METNİ — `SHARED_WRITE`'tan TAŞINDI (`Z39`, 2026-08-26, `B3 W5` kapanışı)

`SHARED_WRITE` yeteneği `Z39`'da (sıfır-rota, `H3` emsali) `capabilities.ts`'ten
**düştü**. Hücre canlıyken taşıdığı KİLİT notu — `853d580:src/common/authorization/capabilities.ts:696-713`'ün (⚠️ SHA'lı atıf: o satırlar bugün bu metni **taşımıyor ve bir daha taşımayacak** — hücre düştü)
2026-08-26 (code-reviewer B2) hâli — burada **kaybolmadan** duruyor, çünkü LTA
dörtlüsü bu task çözülmeden zaten o sabite göçmeyecekti:

> ⛔ **VE BİR KİLİT — ÖLÇÜLDÜ 2026-08-26 (code-reviewer B2):**
> ```
> @RequireCapability(SHARED_WRITE) taşıyan rota   SIFIR
>   (poz.kontrol: aynı grep SHARED_READ icin 23 satir donuyor)
> bu sabiti tasiyan roller   {ADMIN, PLANNER, CATEGORY_MANAGER, FINANCE}
> kalan DORT rotanin @Roles union'i  {ADMIN}
>   ⚠️ DEGER DUZELTILDI (2026-08-26, code-reviewer): once
>   "bes rota / {ADMIN, PLANNER}" yaziyordu. `budget/reserve`
>   o union'daki PLANNER'in TEK KAYNAGIYDI; uc silinince union
>   {ADMIN}'e dustu. Ilk duzeltme SAYIYI (5→4) duzeltti ama
>   DEGERI birakti — bayat-yorum sinifinin ayni turda tekrari.
> ```
> Yani union, koruduğu HİÇBİR rotanın kümesinden GENİŞTİ. Bu sabite yeni bir
> rota bağlanırsa (kaza-dalgasında LTA dörtlüsü — `budget/reserve` artık YOK,
> `K6c/d`'de SİLİNDİ, `T-289`) `{ADMIN}` → DÖRT ROL **sessiz genişleme** olurdu
> (fark artık DAHA BÜYÜK), ve bunu gören guard YOK — `G6`'nın evreni yalnız
> GÖÇMÜŞ rotalar. **⇒ Bu sabite yeni rota bağlanmadan önce union daraltılırdı.**

⚠️ **`SHARED_WRITE` artık YOK** — kilit bugün bir sabite değil, bu task'ın
KARARINA bağlı: LTA dörtlüsü çözüldüğünde doğru hücre bu task'ın kararıyla
(bağ mekanizması / oran-yüzeyi API'si / ölüm) **ve cümlesiyle** doğar. Eski
`{ADMIN,PLANNER,CATEGORY_MANAGER,FINANCE}` union'ı bir başlangıç noktası
DEĞİLDİR — yeni hücre bu kararın ekseninden türetilir, eski union'dan miras
alınmaz.


---

## ✅ KAPANDI — `W2 DALGA-B` (2026-08-30), migration `1817000000000`

`Z38 §3(a)` çerçevesi **birebir** uygulandı: **devir DEĞİL, otomatik besleme
DEĞİL — BAĞ.**

```
agreements(agreement_type='LTA')   = YAŞAM DÖNGÜSÜ (onay · audit · SoD · defter)
       ▲ FK  ON DELETE RESTRICT · UNIQUE
lta_agreements.agreement_id  NOT NULL          ← "BAĞLI DOĞAR"
lta_rates                          = ORAN ŞARTLARI (kanal×kategori kademe)
```

### `§7` — "bu yeteneğin mevcut implementasyonu var mı?"

Arandı (`rg -i`, **case-duyarsız varsayılan**, `Z64 §5-2`):
`agreement_id` · `ltaAgreementId` · `LTAAgreementService` · `lta` (agreement
modülü içinde). **Sonuç: BAĞIN implementasyonu YOK** —
`agreement_id`/`ltaAgreementId` yalnız `lta_rates` + `lta_plan_overrides`'ta;
`actuals-first/agreement/` içinde `lta` yalnız **süre kuralı** olarak geçiyor
(`agreement.service.ts:107,451`). Poz. kontrol: aynı grep `shared/lta`'da
dört dosya buluyor.

⚠️ **AMA bir DUPLİKASYON bulundu (yeni kod değil, mevcut):** LTA taban
formülü **iki kez** yazılmış —
`LTACalculationService.calculateBaseLTASpend` (yalnız iki controller
rotasından ulaşılıyor) ve `SpendCalculationService.calculateAllSpendsForSKU`
(plan recalc'ın **canlı** yolu). İkisi aynı formülü kendi kopyasıyla
hesaplıyor. Team Lead'e bildirildi.

### Ne kuruldu

1. **Migration `1817000000000`** — kolon + FK(`RESTRICT`) + `UNIQUE`.
   Dört dal ampirik (bkz. `MIGRATION_SEQUENCE.md`).
2. **`CreateLTAAgreementDto.agreementId` ZORUNLU** (`@IsUUID`).
3. **`LTAAgreementService.createAgreement` — ÜÇ AÇIK KONTROL** (§2.5, hiçbiri
   sessiz düzeltme değil):
   ebeveyn yok → `404` · ebeveyn `agreement_type != LTA` → `400` ·
   `cplId` ebeveyninkiyle uyuşmuyor → `400` (sessizce ebeveyninkiyle
   DEĞİŞTİRİLMEZ) · ebeveyn zaten bir başlık taşıyor → `409`.
4. **`updateAgreement` KİMLİK ALANLARINI değiştirmeyi AÇIKÇA REDDEDER** —
   `UpdateDto = PartialType(Create)` ⇒ `agreementId` **ve `cplId`** şema
   düzeyinde kabul edilir, `Object.assign` ikisini de **uygulamaz**.
   ⚠️ İlk yazımda yalnız `agreementId` kapatılmıştı; `cplId` **sessizce yok
   sayılıyordu** (review `S2`) — yani yaratımdaki `parent.cplId === dto.cplId`
   invaryantının **PATCH kaçış deliği**. İkisi de artık `400`.
   Reprodüksiyon (mutasyon: `cplId` dalı kaldırıldı → değişen bölge basıldı →
   e2e koştu): `PATCH {cplId: başka}` → **`200`** (sessiz yok sayma). Guard
   geri konunca `400`. Geri yükleme `shasum -a 256 -c` → OK.
   Poz. kontrol pinde: kimlik-DIŞI bir alan (`notes`) aynı uçtan hâlâ `200`
   alıyor ⇒ reddedilen şey PATCH'in kendisi değil.

5. **`BASE_TO` şerhi** (review `B2`) — pin `BASE_TO = BASE_GSV − BASE_LTA_ON`
   assertion'ını taşıyor, ve `Z65 §1` bunu **SAPMA** ilan etti (gerçek
   `TO = GSV − (on+off)`; bugünkü formül aslında `NIV`). Şerhsiz bırakılsaydı
   test sapmayı bir **sözleşme** olarak pinler ve [[T-334]]'ü *"regresyon"*
   diye gösterirdi — `§7.1` `T-084` emsali birebir (*"bir hatayı belgelemek
   onu koruma altına alır"*). Dosya başına ve assertion'ın üstüne `Z65 §1` +
   `T-334` atıflı **beklenen-değişim** şerhi yazıldı.

### REPRODÜKSİYON — kusur ÖNCE GÖRÜLDÜ, ve kırmızının SEBEBİ ölçüldü

`test/lta-lifecycle-bond-and-base-chain.e2e-spec.ts` düzeltmeden ÖNCE
koşturuldu: **4 failed / 4 total**, ve sebep:
```
POST /lta-agreements { agreementId: … }
  → 400 {"message":["property agreementId should not exist"]}
     (ValidationPipe forbidNonWhitelisted) ⇒ BAĞ ALANI YOK
POST /lta-agreements { agreementId YOK }
  → 201  ⇒ ÖKSÜZ oran-şartları başlığı DOĞUYOR
```
İkinci satır kusurun **kendisidir**: motorun uyguladığı indirimin onaylı /
denetlenebilir bir anlaşmaya izi sürülemiyordu.

⚠️ Kırmızı koşum `T-047` satır-sayısı invaryantını da ateşledi
(`lta_agreements 0→1`) — çünkü assertion düşünce `push` satırına
ulaşılmıyordu. Temizlik **id listesinden ÖN EKE** çevrildi; sızan satır
elle silindi, sonraki koşumlarda `T-047 PASS`.

### FIXTURE — kalıcı, ve FARKI OKUYAN assertion'lı

`lta_agreements 0 · lta_rates 0 · lta_plan_overrides 0` ⇒ zincir **hiç
koşmuyordu** (`T-273` ailesi, iki tabloda birden). Pin kalıcı bir fixture
kurar: **on-invoice %7 / off-invoice %2** — repodaki diğer LTA fixture'ının
`5/3`'ünden de, birbirinden de FARKLI. Ve fark **okunuyor**
(`Z64 §5-1`: *"fark taşımak GEREKLİ, okuyan assertion olmadan YETERSİZ"*):
```
BASE_LTA_ON ≠ BASE_LTA_OFF            (iki oran karışsaydı düşerdi)
BASE_LTA_ON ≠ baseGsv × 5%            (bağ yanlış başlığa düşseydi düşerdi)
BASE_TO     = BASE_GSV − BASE_LTA_ON  (on-invoice mekanik TABANI)
```
Zincir **uçtan uca gerçek üretim yolundan** koşuyor:
`POST /agreements`(LTA) → `POST /lta-agreements`(bağlı) → `activate` →
`POST /plans` → `POST /plans/:id/fus` → `PATCH …/volume` → recalc → KPI.

### ⛔ AÇIK KALAN — Team Lead kararı gerekiyor

**Ebeveynin DURUMU motoru kapılıyor mu?** Bugün motor yalnız
`lta_agreements.status = 'active'`e bakıyor; ebeveyn `agreements.status`
`DRAFT`/`REJECTED` olsa bile oran uygulanır. Bu **bu turda değiştirilmedi**
(mevcut, gözden geçirilmiş yaşam döngüsü davranışı; bağ onu değiştirmiyor) —
ama bağ kurulduğu için artık **görünür** bir tasarım sorusu.

**`S6` — *"tek nokta"* YARIMDI, tamamlandı:** `formulaText` sabitten
geliyordu ama `kpiDescription` aynı payda adını **iki dosyada elle**
taşıyordu (`kpi.seed.ts` · `kpi.service.ts`) ⇒ payda değiştiği gün
`formula_text` değişir, `kpi_description` **eski paydayı anlatmaya devam
ederdi** (`F8` ailesi, hem de onu kapatmak için açılan turda). Açıklama da
`GP_ROI_PCT_DESCRIPTION` olarak tek noktadan türetiliyor; üretilen dizge
canlı `main.kpis` satırıyla **birebir** (ölçüldü: `MATCHES_DB: true`) ⇒
davranış değişmedi.

**Oran giriş YÜZEYİ** (`Z38 §3(a)` *"oran girişi KENDİ YÜZEYİNİ alır"*):
backend tarafı **VAR ve canlı** (`POST /lta-agreements`, `@Roles(ADMIN)`);
**frontend yüzeyi YOK** — `EK_E` `🔒` kalemi, bu turun (backend) kapsamı dışı.
