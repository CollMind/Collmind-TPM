---
id: T-028e
title: Agreement approve/reject'te CM kategori-scope'u yok (tasarım matrisiyle çelişki)
epic: E-001
sprint: S-001
status: done
assignee: backend-engineer
created: 2026-07-28
updated: 2026-07-28
---

## Sonuç
- `resolveEffectiveCategoryId`: `categoryId` → yoksa `fuId→GU.categoryId` → yoksa `null` + `logger.warn`.
- **Gizli bug bulundu:** `findById` ham `categoryId` kolonunu kullanıyordu (133/133 boş) →
  CM scope'u agreement tarafında zaten **fiilen bozuktu**, sadece eksik değildi.
- Enforcement: `approve`/`reject` kapsam dışı CM → **403**; `findById` → **404 OUT_OF_SCOPE**;
  `findAll` JOIN'li `COALESCE(agreement.categoryId, gu.categoryId)` filtresi — **N+1 yok, tek sorgu**.
- `AccessScopeService.applyToQueryBuilder`'a opsiyonel `categoryColumnExpr` (diğer çağıranlar etkilenmedi).
- **Fail-closed zarif:** kategori çözülemezse `null`; `null` bir CM pair'iyle asla eşleşmez →
  özel durum kodu gerekmedi. Planner'ın `categoryId:null` ("her kategori") pair'leri etkilenmedi.
- Perf: EXPLAIN ANALYZE **3.09 ms** (<500ms bütçesi).
- **445 unit + 149 e2e — hem flag KAPALI hem AÇIK yeşil.** G1-G6 testleri (200/404/403/liste).

## Not — golden-path fixture onaycısı değişti
`createAndApproveAgreement` ve C3/C7 adımları CM yerine **FINANCE_MANAGER** kullanıyor artık.
Gerekçe: bu fixture'lar rastgele FU/kategorili agreement üretiyor; CM doğru şekilde kategori-scoped
olduğu için 403 alması **doğru davranış**. Fixture kurulumu kısıtsız rolle yapılır; CM kısıtı
G1-G6 ile ayrıca doğrulanıyor. Test zayıflatma değil, sorumluluk ayrımı.
- `agreements.category_id` backfill'i **bilinçli yapılmadı** (FU yeniden kategorilenirse bayatlar).

## Açıklama
T-028c code-review'ında bulundu. Tasarım matrisi (`docs/analysis/0004-rbac-brd-alignment.md` §5)
`agreement | CATEGORY_MANAGER | R(c) · A(c)` diyor — yani CM agreement'ı da **kategori-scoped**
onaylamalı. Ama kodda:

- `agreement.service.ts:474` (`approve`) ve `:554` (`reject`) → `findById(id, tenantId)` **actor'suz**
- Sonuç: CM **herhangi bir kategorideki** agreement'ı onaylayabiliyor/reddedebiliyor

Plan tarafında bu kısıt T-028b'de uygulandı; agreement tarafı kapsam dışında kalmış
(kod içi yorum da "CM'e özgü akış yok" diyerek bilinçli bırakmış). Route PLANNER'a kapalı
olduğu için T-028c'nin AC'sini teknik olarak ihlal etmiyor, ama **tasarım matrisiyle çelişiyor**
ve BRD "Category Manager atanmış kategoriyi onaylar" kuralının agreement ayağı eksik kalıyor.

## ✅ Ürün kararı (2026-07-28) — agreement kategorisi FU→GU'dan TÜRETİLİR
`agreement.categoryId` opsiyonel ve **133/133 agreement'ta BOŞ**; ama `fuId` 133/133 dolu ve
**FU → GU → `GU.categoryId` zinciri 133/133 çalışıyor** (DB doğrulaması).
Karar: kategori **türetilir** (kolon backfill'i YOK — veri kopyası bayatlar; gevşetme YOK — fail-open olur).
- `agreement.categoryId` doluysa o kullanılır (öncelikli).
- Boşsa `fuId → forecasting_units.gu_id → generic_units.category_id` ile türetilir.
- İkisi de yoksa: fail-closed (CM göremez) + anlaşılır log.

## Acceptance Criteria
- [ ] `agreement.service.approve/reject` actor alır; CM için kategori kesişimi yoksa **403**
- [ ] Kapsam dışı agreement okuma → **404** (`OUT_OF_SCOPE`) — plan tarafıyla tutarlı
- [ ] Agreement'ta `categoryId` yoksa davranış netleştirilir (yalnız cpl boyutu mu, CM hepsini mi görür)
- [ ] E2E: CM kendi kategorisindeki agreement'ı onaylar (200); başka kategoridekini onaylayamaz (403)
- [ ] Unit + e2e yeşil (her iki flag durumunda)

## İlgili
- Tespit: T-028c code-review · Tasarım: `docs/analysis/0004-rbac-brd-alignment.md` §5
- Kardeş uygulama: [[T-028b]] (plan tarafı)
