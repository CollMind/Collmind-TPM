---
id: T-395
title: "Pin: S0'da görülen ve pinde olmayan imza KIRMIZI değil — override unutulursa pin fixture'ları SESSİZ YEŞİL · ⛔ 1838 ÖNCESİ"
epic:
sprint:
status: done
assignee: data-engineer
priority: P1
created: 2026-09-13
updated: 2026-09-14
closed_by: "backend 6b11add · meta 7a8fb35 · TL gerçek harness 45/45 · reviewer 🔴 yok (kalanlar T-398/T-401/T-402) · Z111 §38–§39"
due: "1838000000000 harness koşumundan ÖNCE"
touches:
  - collmind.backend/scripts/migration-verify.sh
  - collmind.backend/scripts/verification/synthetic-migrations/pin-*
---

## Nasıl doğdu
Pin dar düzeltmesi review'u (`Y-1`, 2026-09-13) · Team Lead gerçek harness doğrulaması · hüküm `Z111 §31`.

## Kusur
```
[ÖLÇÜLDÜ: tl-verify-pinfix.sh, gerçek harness]
pin-narrow-default-removed  MIGRATION_VERIFY_PIN_EXTRA_FILE YOK  → rc=0 "✓ maske türleri pinle eşleşti (4 imza)"
pin-vanish-table-drop       MIGRATION_VERIFY_PIN_EXTRA_FILE YOK  → rc=0 "✓ … eşleşti"
(override İLE: sırasıyla rc=1 "pini daralt" · rc=2 "körlük şüphesi")
```
Kök neden (reviewer, kod okuması): K8 yalnız **H** imzalarını pinle karşılaştırıyor (`comm`); **S0** imza kümesi pinle hiç
karşılaştırılmıyor — yalnız "kaybolan imza S0'da var mıydı" sorusunda okunuyor.

## Hüküm (`Z111 §31`)
*"Pinde olmayan imza → KIRMIZI"* kuralı imzanın **nerede görüldüğünden bağımsız**: S0'da görülen ve pinde olmayan imza ya **bayat
pin** ya **unutulmuş override** — ikisi de KIRMIZI. ⛔ ÖLÇEMEDİM DEĞİL (gerçek kusuru gömer).

## Acceptance Criteria
- [ ] S0 imza kümesi − pin (üretim + varsa ek dosya) ≠ ∅ → KIRMIZI, sebep satırı imzaları ve kolonları adıyla basar
- [ ] Bilinen-kırmızı, **GERÇEK harness koşumunda**: iki pin fixture'ı override **olmadan** → rc=1 · override **ile** → mevcut sonuçlar DEĞİŞMEZ
- [ ] Gerçek HEAD → "✓ pin eşleşti" değişmez
- [ ] Tam regresyon (harness brief §9.13.8 listesi) aynı sonuç
- [ ] Hata yolu olan fonksiyon değer döndürmez; `$( )` içinde `unmeasured`/`red` yok (DISIPLIN F12)

## ⛔ Tarih bağı
**`1838000000000` harness koşumundan ÖNCE** kapanır — 1838 pinin **ikinci gerçek müşterisi**. 1835 bu task'a bağlı DEĞİL
(override yok, S0 = gerçek DB — `Z111 §31.1`).

## Done tanımı
CLAUDE.md §4.2 — backend `npm run guards` exit 0 · meta `bash scripts/run-all.sh` exit 0 · code-reviewer onayı.

## Kapanış (data-engineer, 2026-09-13)
`collmind.backend/scripts/migration-verify.sh` K8'e S0-imza kontrolü eklendi: `NEW_SIGS_S0_FILE`
= `comm -23 OBSERVED_SIGS_S0_FILE PIN_SIGS_FILE` — S0'da (migration UYGULANMADAN ÖNCE) görülüp
pinde (üretim + varsa ek dosya) OLMAYAN her imza artık KIRMIZI, nerede görüldüğünden BAĞIMSIZ
(Z111 §31). GERÇEK harness'ta kanıtlandı: iki pin fixture'ı override'sız → rc=1 (önceden rc=0);
override'lı → sonuçlar DEĞİŞMEDİ (vanish rc=2 "körlük şüphesi", narrow rc=1 "pini daralt");
gerçek HEAD 1835 → rc=0 "✓ pin eşleşti (4 imza)". Mutasyon (kopyala→mutasyon→geri
yükle+shasum) GERÇEK harness'ta doğrulandı: S0-comm satırı boşaltılınca narrow-override'sız
tekrar rc=0'a düştü. `npm run guards` ve `npm run build` exit 0. Taban değişmedi (HEAD 1835,
n=90, table=51, `_mv%`=0). code-reviewer onayı BEKLİYOR — commit/push YAPILMADI.
⛔ NE ÖLÇÜLMEDİ: harness brief §9.13.8'in tam regresyon listesi (30 set + nullcollapse) bu
turda KOŞULMADI — yalnız real-1835 + iki pin fixture'ı (4 vaka) çalıştırıldı; paralel şerit
(T-397, `scripts/guards/declared-migrations.sh`) meta `run-all.sh`'ı bu turda kırmızı
gösterebilir, sebebi bu task'ın dosyaları DEĞİL.

## ⭐ EK — `Z111 §38` (2026-09-14): pin H'yi izler, S0-kontrolü ÜÇ DALA
Birleşme review'u 🔴-1: bu task'ın S0-kırmızısı + D-4 (c) birlikte tür kaldıran meşru migration'a yeşil yol bırakmıyor. Brief:
`docs/process/HARNESS_PG_ENUM_KORLUGU_BRIEF.md` §9.13.11–§9.13.12. ⚠️ Yukarıdaki AC2'nin "override olmadan rc=1" beklentisi bu hükümle
DEĞİŞTİ (bilinçli): narrow override'sız → rc=0 + zorunlu "KALDIRILDI" satırı · vanish override'sız → rc=2.
- [x] Üç dal (§9.13.12 İŞ 1) · dal 2 ulaşılamazlığı ölçüldü, H-kontrolü mesajı iki sebebi adlandırır (N58)
- [x] D-4 (c) mesajları "aynı commit" (N57) · §9.13.12 KANIT tablosu gerçek harness'ta

## Kapanış — EK (data-engineer, 2026-09-14)
`migration-verify.sh` K8'in S0-bloğu üç dala ayrıldı (`scripts/migration-verify.sh` ~2435-2521):
temiz kaldırma (S0'da var/H'de yok/pinde yok/tablo SABİT) → YEŞİL + koşulsuz ZORUNLU satır
"imza X bu migration tarafından KALDIRILDI — pin daraltılmış ✓"; dal 2 (S0'da var/H'de VAR/pinde
yok) için ölü dal YAZILMADI — bu duruma H−pin kontrolü (yukarıda) zaten `red()`/exit 1 ile
ulaşır; o mesaj artık "YENİ ifade türü YA DA BAYAT PİN / UNUTULMUŞ override" diye iki sebebi
adlandırıyor (N58); dal 3 (tablo DÜŞTÜ) ÖLÇEMEDİM "körlük şüphesi" DEĞİŞMEDİ (§31). D-4 (c)
mesajları (":pini daralt" iki yer) "AYRI commit'te" → "migration'la AYNI commit'te" (N57).
pin-narrow/pin-vanish fixture'larının başlıkları ve `pin-extra.txt` yorumları üç-dal davranışına
göre güncellendi.

GERÇEK harness'ta (`scratchpad/t395b-lane/verify.sh`, taban HEAD 1835): 33/33 vaka TUTTU —
narrow-override-yok ÖNCE(pre-kopya) rc=1 → SONRA rc=0+"KALDIRILDI" (grep: 1 satır) · vanish-
override-yok ÖNCE rc=1 → SONRA rc=2 "körlük şüphesi" · dal-2 ulaşılamazlık (üretim pininden
`uuid_generate_v4` geçici çıkarılıp REAL 1835'e karşı koşuldu, kopyala→mutasyon→geri
yükle+shasum) rc=1, mesaj iki sebebi adlandırıyor · MUT tablo-sayısı-guard kaldırılınca vanish-
override-yok 2→0'a düşüyor (guard'ın kritikliği kanıtlandı) · MUT yeşil-satır kaldırılınca rc
DEĞİŞMEDİ (0) ama "KALDIRILDI" kanıtı log'dan KAYBOLDU (satırın gerçekten gerekli olduğu
kanıtlandı) · override'lı narrow/vanish DEĞİŞMEDİ · tam regresyon (30 set + nullcollapse) + REAL
HEAD 1835 hepsi TUTTU. Harness ve pin artefaktı sha'sı başta==sonda, taban HEAD 1835/n=90/
synth=0 olarak döndü, `.t395b-*` kalıntısı 0.

Backend `npm run guards` rc=0 · `npm run build` rc=0. Meta `bash scripts/guards/declared-
migrations.sh --self-test` (gerçek, düzenlenmiş harness'a karşı, env override YOK) rc=0 ·
`--check` rc=0 ("taranan .ts: 90", "beyanlı: yok") · `bash scripts/run-all.sh` rc=0 ·
`bash scripts/push-order-self-test.sh` rc=0 (7 senaryo). code-reviewer onayı BEKLİYOR —
commit/push YAPILMADI.

⛔ NE ÖLÇÜLMEDİ: N58'in "dal 2'ye BEKLENMEDİK biçimde ulaşılırsa" kolu (S0_UNEXPECTED_MSG) canlı
bir fixture'la TETİKLENMEDİ — yalnız kod okumasıyla + genel invaryant argümanıyla (H−pin kontrolü
her zaman önce koşar) doğrulandı; bilerek (bu kolun tetiklenmesi bir kod/ölçüm tutarsızlığı
anlamına gelir, normal bir davranış DEĞİL). Y-1 (S0'da pinsiz imzanın override'sız KIRMIZI
vermesi gereken durumu, §31.1) ve F04 retro-tarama (T-396) bu turun DIŞINDA — brief'te zaten
ayrı task'lara bağlı.
