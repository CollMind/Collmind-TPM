---
id: T-064
title: Finansal invariant guard'ları — Faz 1 (rapor modu) + Faz 2 (bloklama)
epic: E-001
sprint: S-001
status: review        # Faz 2 bitti; `done` için tek eksik: code-reviewer onayı
assignee: backend-engineer
created: 2026-08-03
updated: 2026-08-03
touches:
  - collmind.backend/scripts/guards/lib.sh
  - collmind.backend/scripts/guards/run-all.sh
  - collmind.backend/scripts/guards/migration-schema.sh
  - collmind.backend/scripts/guards/ledger-direction.sh
  - collmind.backend/scripts/guards/financial-ordering.sh
  - collmind.backend/scripts/guards/schema-isolation.sh
  - collmind.backend/scripts/guards/allowlist.txt
  - collmind.backend/package.json
  - collmind.backend/src/database/migrations/1777000000000-LedgerReversalSupport.ts
  - collmind.backend/src/database/migrations/1779000000000-CreateUserScopes.ts
  - docs/verification/GUARD_BASELINE_REPORT.md
  - docs/contracts/SYSTEM_INVARIANTS.md
  - .claude/commands/qa.md
  - .claude/agents/code-reviewer.md
  - .claude/backlog/BACKLOG.md
  - CLAUDE.md
migration_seq:      # migration yok
---

## Açıklama
`SYSTEM_INVARIANTS.md` §10 guard backlog 2–5: dört invariant (INV-M-002, INV-L-007,
INV-N-001, INV-M-003) bugün hiçbir mekanizmayla korunmuyor. Faz 1 onları tespit edilebilir
hâle getirir — kod davranışı DEĞİŞMEZ, yalnızca bash+grep kontrol script'leri eklenir.

Faz 1 rapor modudur: `GUARD_MODE=report` (varsayılan) bulguyu basar ve exit 0 döner.
`GUARD_MODE=block` bulgu varsa exit 1. Faz 1'de hiçbir yere `block` yazılmaz.

## Acceptance Criteria
- [x] Dört guard + `run-all.sh` + `allowlist.txt` eklendi, tümü bash+grep, yeni npm bağımlılığı yok
- [x] `package.json`'a yalnızca `"guards"` script'i eklendi
- [x] `npm run guards` → exit 0, bulgular basılıyor
- [x] `GUARD_MODE=block npm run guards` → exit 1
- [x] Guard 1 bilinen doğru pozitifi buldu (`1777000000000-LedgerReversalSupport.ts:28,:45`)
- [x] Guard 4 DB erişilemezse SKIPPED + exit 0
- [x] Allowlist gerekçe zorunluluğu çalışıyor (gerekçesiz satır susturmuyor)
- [x] `docs/verification/GUARD_BASELINE_REPORT.md` — triyaj edilmiş baseline
- [x] Hiçbir mevcut kaynak dosya değiştirilmedi, bulunan ihlaller düzeltilmedi

## Done tanımı (hepsi işaretlenmeden `done` yazılmaz)
- [x] Testler yeşil (unit + ilgili e2e) — bu faz test eklemiyor; guard'lar fixture ile
      doğrulandı. Faz 2 sonrası regresyon: 627 test / 45 suite yeşil
- [ ] **code-reviewer onayı — TEK AÇIK MADDE.** 1. tur yapıldı (opus): 2 blocker + 6 madde.
      Hepsi kapatıldı (aşağıda), ama **düzeltmeler henüz review edilmedi** — 2. tur gerekiyor.
- [x] Üretim çağrı yolu var — **Faz 2'de kapandı.** `npm run guards` üç yerden çağrılıyor:
      `.claude/commands/qa.md` (backend test adımları), `.claude/agents/code-reviewer.md`
      (kırmızı guard = 🔴 Blocker), `BACKLOG.md` + `CLAUDE.md` §4.2 Done checklist'i.
      CI yok (CLAUDE.md §5), bu üçü pipeline'ın yerine geçen çağrı yoludur.
- [x] Bağlayıcı koşullar guard'a bağlandı — **Faz 2'de "tavsiye"den "kapı"ya yükseldi.**
      `GUARD_MODE` varsayılanı `block`; bulgu → exit 1, gerekçesiz allowlist satırı → exit 2
- [x] `touches:` gerçekte dokunulan dosyalarla güncel
- [x] Migration varsa catalogue guard'ları şema-nitelendirilmiş — Faz 1'de migration yoktu;
      Faz 2 mevcut 5 ihlali düzeltti (LedgerReversalSupport 2, CreateUserScopes 3)

## Baseline sonucu
7 bulgu: GERÇEK 6 · YANLIŞ_POZİTİF 1 · EMİN_DEĞİLİM 0.
Kanıtlanmış sapma: `LedgerReversalSupport1777000000000` `main.migrations`'ta kayıtlı ama FK'sı
ve `UQ_ledger_entries_reversal_per_tenant` index'i yalnızca `public` şemasında.

## Faz 2 — tamamlandı (commit `0d63e9b` backend, `bb9017f` meta)
Yapılanlar: pencere → template literal blok sınırı · `::regclass`/`to_regclass`
guard'ın kendisinde tanınıyor · 5 catalogue sorgusu şema-nitelendirildi · allowlist
dolduruldu (tek satır, dar anahtar: `schema-isolation|db:collmind_tpm`, T-067'ye bağlı) · `GUARD_MODE` varsayılanı
`block` · `/qa` + `code-reviewer` + Done checklist'ine bağlandı · SYSTEM_INVARIANTS v0.2.

Ölçüm: Faz 1'de 7 bulgu (6 gerçek + 1 yanlış pozitif) → Faz 2'de 6 (hepsi gerçek).
Blok bazlı okuma yeni ihlal ortaya çıkarmadı; maskeleme mekanizması fixture ile kanıtlandı
ama bu kod tabanında kimseyi gizlemiyordu.

Faz 2 kapsamı DIŞINDA bırakılanlar:
- **T-066** — `finance-reporting.service.ts:492` dinamik `sortField` (ayrı task)
- **T-067** — şema ayırma; allowlist'teki tek susturmanın karşılığı
- `db:reset` ile `INV-L-005` / `INV-M-001`'in gerçekten HOLDS'a geçmesi — T-057
  commit'lendikten sonra. Fix kenar DB'de (`collmind_tpm_guardtest`, 54 migration boştan)
  kanıtlandı, çalışılan DB'ye uygulanmadı.

## Code review 1. tur — bulgular ve kapanışları
İki blocker gerçekti ve ikisi de guard'ın **kendi kör noktasıydı**:

1. **Backtick paritesi → sessiz yanlış negatif.** `//` yorumundaki bir backtick literal
   içi/dışı ayrımını kaydırıp guard'ı susturuyordu. Fixture: düzeltme öncesi 0 bulgu,
   sonrası 1. Yorum backtick'leri artık temizleniyor; parite hâlâ tekse veya kaçırılmış
   backtick varsa dosya **atlanmıyor, bulgu basılıyor**. (`1795...ts:73,74,103,110` bugün
   tam da bu deseni taşıyor — şans eseri çift.)
2. **`financial-ordering` kapsamı iddiadan dardı.** 273 dosyanın 132'si taranıyordu;
   `finance-reporting`, `spend-calculation`, `kpi-engine` tamamen dışarıdaydı. `INV-N-001`'in
   "0 findings across the codebase" kanıtı kapsamdan büyüktü. Kapsam 176'ya çıkarıldı
   (yine 0) ve guard'ın dinamik `orderBy` argümanını göremediği açıkça yazıldı → **T-066**.

Ayrıca kapatılanlar: `SKIPPED` artık yeşil sayılmıyor (kaynak kod guard'ı çalışamazsa exit 1;
DB guard'ı DB'siz ortamda `ÖLÇÜLMEDİ` diye raporlanıyor) · allowlist ile susturulan bulgu
özet satırında görünüyor (`0 bulgu (1 susturuldu)`) · `ENV` joker yerine dar anahtar
`db:collmind_tpm` · `ENV` yalnız `schema-isolation`'da geçerli · 4 alanlı allowlist satırı
artık reddediliyor · boşta kalan `T-065`/`T-066` referansları çözüldü.

**Kendi doğrulamamda çıkan hata:** `bash -n a.sh b.sh` yalnızca **ilk** dosyayı kontrol eder,
kalanları argüman sayar. Faz 2 boyunca "syntax OK" çıktısı aslında tek dosyayı doğruluyordu ve
`lib.sh`'teki bir sözdizimi hatasını (awk programı içindeki Türkçe kesme işareti bash tek
tırnağını kapatıyordu) gizledi. Doğrulama artık dosya başına döngüyle yapılıyor.

## İlgili
- `docs/contracts/SYSTEM_INVARIANTS.md` §10 (v0.2.1)
- `docs/verification/GUARD_BASELINE_REPORT.md`
- [[T-066]] · [[T-067]]
