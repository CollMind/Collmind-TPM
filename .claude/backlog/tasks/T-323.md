---
id: T-323
title: Zarf formuna budgetOwnerId alanı — OPSİYONEL (Z59 §3a)
epic:
sprint:
status: review
assignee: frontend-engineer
created: 2026-08-28
updated: 2026-08-28
touches:
  - collmind.frontend/src/components/budget/
  - collmind.frontend/src/types/budget.types.ts
migration_seq:
---

## Hüküm — `Z59 §3`

`budgetOwnerId` bugün **üründe hiçbir yazıcısı olmayan** bir alan (ölçüldü: backend
`src` 1 kullanım ve o bir OKUMA · frontend `src` 2 kullanım ve ikisi de **tip beyanı**
· seed 0 · test 0 · canlı DB **4/4 NULL**).

⛔ **OPSİYONEL KALIR — ZORUNLU OLMAZ.**
> Zorunlu kılmak, bugün hiçbir formun taşımadığı bir alanı bir gecede **her
> zarf-yaratma çağrısının kırılma noktası** yapar — `T-306` dersinin **tersi**:
> **yol açmadan zorunluluk koymak.**

## İş
Zarf oluşturma/düzenleme formuna **opsiyonel** "bütçe sahibi" alanı (kullanıcı seçici).
DTO alanı backend'de **zaten var** (`CreateBudgetEnvelopeDto.budgetOwnerId?`) ve
`...createDto` ile yazılıyor — **backend'e dokunma gerekmiyor**, ölçüldü.

## ⛔ DUR listesi
- Alanı **zorunlu** yapma
- Backend'e dokunma (`budget.service.ts`, DTO) — yol **zaten açık**
- Varsayılan bir kullanıcı **otomatik seçme** — boş bırakmak **meşru-tanımlı** durum
  (`Z59 §3c`), ve cevabı `WARNING` fallback'idir

## Acceptance
- [ ] Alan formda, opsiyonel, boş bırakılabiliyor
- [ ] Boş bırakılınca istek `budgetOwnerId` **göndermiyor** (boş string DEĞİL)
- [ ] `npm run type-check` 0 · `npm test` yeşil · lint kırmızı değil

---

## ⛔ REVIEW BULGUSU — seçicinin veri kaynağı, formu açan rolün ERİŞEMEDİĞİ bir uç
*(Team Lead, 2026-08-28 — ajan bu kesişimi ölçmemişti)*

```
GET /users            → USER_MANAGE            = ADMIN                        (ölçüldü)
POST /budget/envelopes → SHARED_ENVELOPE_WRITE = ADMIN · FINANCE · PLANNER    (ölçüldü)
```

⇒ **Zarf yaratabilen ÜÇ rolden İKİSİ (`FINANCE`, `PLANNER`) kullanıcı listesini
ÇEKEMEZ** (`403`). Alternatif bir liste ucu **yok** (`user.controller` taraması:
`GET /` → `USER_MANAGE`, `GET /me` → `@SelfScoped`, `GET /:id` → `USER_MANAGE`).

**Kilitlenme YOK** — `usersData?.data || []` güvenli. Ama sonuç daha sinsiydi:

> **"Yetkin yok" ile "hiç kullanıcı yok" AYNI GÖRÜNÜYORDU** — boş, açıklamasız
> bir seçici.

📌 `§4.2`'nin *"üretim çağrı yolu var mı"* sorusunun **veri bağımlılığına** uygulanmış
hâli: bir form alanı doğdu, ve **onu besleyen yol iki rol için kapalıydı.**

### Yapılan (kural gerektirmeyen kısım)
`isError` ayrıldı + `retry: false`; hata hâlinde **görünür açıklama** basılıyor
(*"bu yetki yöneticiye aittir — boş bırakıldığında bildirim finans ekibine gider"*).
⇒ `Z59 §2`'nin **görünür-fallback** ilkesinin UI tarafı. `type-check` 0 ·
`vitest` 536/536.

### ⛔ MASADA KALAN SORU — hüküm gerektirir
**Zarf yaratabilen bir rol, bütçe sahibi ATAYABİLMELİ Mİ?**

| şık | |
|---|---|
| `(a)` **Hayır** — bugünkü hâl doğru; owner ataması bir **yönetim** işidir, `FINANCE`/`PLANNER` boş bırakır ve fallback devreye girer | ⇒ **kod değişikliği YOK**, yalnız bu kayıt |
| `(b)` **Evet** — dar bir uç (`GET /users/selectable`) + zarf-yazarlarının taşıdığı bir yetkiye bağlanır | ⇒ yeni rota + yeni hücre ⇒ **`Z42` kalemi**, `route-cell-map` etkilenir |
| `(c)` `USER_MANAGE`'i genişlet | ⛔ **REDDEDİLİR** — bir UI kolaylığı için **yetki genişletmek**; `Z42`'nin bütün disiplinine aykırı |

**Team Lead görüşü: `(a)`** — `Z59 §3c` zaten *"owner'sız zarf **meşru-tanımlı** bir
durumdur"* diyor. `FINANCE`'in owner atayamaması bir kusur değil, o hükmün **doğal
sonucu**; ve `FINANCE` zaten **fallback alıcısı**, yani bildirim ona ulaşıyor.
