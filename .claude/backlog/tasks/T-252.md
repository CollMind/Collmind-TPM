---
id: T-252
title: "`ADIM 3` `B0` — rota yetki kapsama RATCHET'i (kapı değil, terfi eder)"
epic: E-001
sprint: S-001
status: review
assignee: qa-engineer
created: 2026-08-21
updated: 2026-08-21
touches:
  - collmind.backend/scripts/guards/
  - collmind.backend/scripts/guards/run-all.sh
---

## Amaç

`ADIM 3` `Faz B`'nin **ilk adımı**. Plan: `docs/process/ADIM3_FAZB_PLAN.md`.

`K-2.6.6` — *"tanımlanmamış uç herkese açık"*. Bugün **`61`** uç öyle, ve bu bir
**izolasyon kusuru**. ⚠️ `RLS` onu **kapatmaz**: `RLS` satır seviyesi, bu **rota**
seviyesi.

## ⛔ KAPI DEĞİL, RATCHET (ürün sahibi kararı, 2026-08-21)

```
"sıfır mı" soran bir kapı   →  bugün 61'de KALICI KIRMIZI
                            →  §2.7 #9: sinyal sabitse, sinyal DEĞİLDİR
                            →  T-113'ün ölçülmüş sonucu: kapı bir hafta içinde
                               DEVRE DIŞI bırakılır
```

```
B0 (BU TASK)   baseline 61'in LİSTESİ · yalnız ARTIŞ kırmızı
B2             baseline DÜŞÜRÜLÜR, her uç bağlandıkça, AYRI COMMIT
B4             baseline 0  →  guard KAPIYA TERFİ EDER
```

> `mode-split`'in dersi: **yeni guard'lar kapı doğmaz, kapıya terfi eder.**

⚡ **Ve bu `K4`'ü cevaplıyor: kademeli** — ratchet tek-dalgayı **imkânsız** kılıyor.
Karar bir tercih değil, bir **sonuç**. Mekanizma kararı zorluyor.

## ÜÇ ŞART (ürün sahibi)

### 1 · Baseline **LİSTE**, sayı DEĞİL

```
rota + dosya      ✅
"61"              ⛔
```

> `CLAUDE.md`: *"dokümanda sayı yazma — niteliksel ayırt edici yaz."* Ve bir sayı-baseline
> *"biri düştü, biri girdi"* gerilemesini **görmez** (`mode-split`'in ölçülmüş dersi).

### 2 · ÜÇ KOVA AYRI

```
filtresiz          61   ← ratchet'in konusu
@Public             3   health · auth/login · auth/refresh
alan-guard'lı       2   ReversalGuard · SettlementGuard
```

⛔ **`2` alan-guard'lı uç *"filtresiz"* kovasına DÜŞMEMELİ.** İkisi de
`{ADMIN, CATEGORY_MANAGER}`'ı **guard'ın içinde** zorluyor:

```
reversal.controller.ts:28    @UseGuards(JwtAuthGuard, ReversalGuard)   ← CONTROLLER seviyesi
settlement.controller.ts:77  @UseGuards(SettlementGuard)               ← rota seviyesi
reversal.guard.ts:19-21      REVERSAL_ALLOWED_ROLES
settlement.guard.ts:19-21    SETTLEMENT_ALLOWED_ROLES
```

⚠️ **Neden kritik:** `B4`'te default-deny bunları **keser**, ve kırılma `403` olduğu
için *"default-deny çalışıyor"* diye okunur. Ayrı kova, o günün yanlış teşhisini
engelliyor.

### 3 · İKİ GİRDİ, İKİ ÇIKTI (`§2.7 #9`)

```
YENİ filtresiz rota      →  KIRMIZI
baseline'daki bir rota   →  YEŞİL
```

Temiz hâlde yeşil olması, kirlide kırmızı olduğunun kanıtı **değildir**.

## ⚠️ `T-250`'NİN DERSİ — kaynak boşsa SETUP HATASI

Rota kümesi boş türetilirse ratchet **sessizce yeşil** verir (`yeni \ baseline = ∅`
çünkü `yeni = ∅`). Bu **setup hatası** olmalı, yeşil değil.

📌 `guard.sh`'ın `0 · Araç sağlığı` bloğunun **dördüncü** uygulaması.

⚠️ Ve `T-250`'nin ikinci dersi: parser **çok kanallı** olacaksa (rota dekoratörü ·
`@Roles` · `@Public` · `@UseGuards`) **her kanal için ayrı pozitif kontrol** gerekir —
biri bozulsa diğerleri eleman üretmeye devam eder ve körlüğü gizler.

## Bugünkü ölçüm (Team Lead, 2026-08-21) — DOĞRULA, kopyalama

```
238 rota   (34 controller dosyası, src/**/*.controller.ts)
172 @Roles'lu
 66 filtresiz  =  3 @Public + 2 alan-guard'lı + 61 gerçek boşluk
```

⚠️ **`app.controller.ts` `src/modules` DIŞINDA** — kapsamı `src/**` yap, yoksa health
ucu sayılmaz. (`0072`'nin `237` tabanı `src/modules` kapsamıyla birebir tuttu; fark
tam olarak bu dosya + `T-242a`'nın yeni rotası.)

## Acceptance Criteria

- [ ] Ratchet yazıldı, `run-all.sh`'e bağlı, `npm run guards` kapsamında
- [ ] **Bağlandığı DOĞRULANDI** — runner alt guard'ın `RC != 0`'ını **fatal** sayıyor
      mu? (bilinen vaka: yalnız `RC=2`'ye karşı sınanmış, `RC=1` yutuluyordu)
- [ ] Baseline **liste** hâlinde, versiyonlu bir dosyada
- [ ] Üç kova ayrı raporlanıyor
- [ ] İki-girdi-iki-çıktı kanıtı, satırlar basılı
- [ ] Boş-kaynak → setup hatası (yeşil DEĞİL), ayrıca sınandı
- [ ] Bugünkü repoda **exit 0** (baseline = bugünkü hâl)

## DUR

- Sayım Team Lead'in `238/172/66`'sıyla **uyuşmazsa** → **DUR ve farkı raporla**
  (hangi rota, hangi kova). İki ölçüm çelişirse üçüncüsü yapılır ya da iddia geri
  çekilir.
- Üçüncü bir koruma kanalı bulursan (`@UseGuards` ile başka bir rol guard'ı) →
  **DUR**, kapsamı kendi başına genişletme.
- `docs/` altına **yazma**.

## İlgili

- `docs/process/ADIM3_FAZB_PLAN.md` — plan · [[T-250]] — ratchet/guard deseni
- [[T-246]] · [[T-234]] — baseline bakım borcu ailesi; bu task o borcu **baştan**
  çözüyor (baseline düşürmek `B2`'nin kabul kriteri)


## ✅ KAPANDI (2026-08-21) — ratchet indi, bugün `exit 0`

```
scripts/guards/route-scope.sh            guard/ratchet
scripts/guards/route-scope.awk           parser (girinti + parantez derinliği, SABİT PENCERE YOK)
scripts/guards/route-scope-baseline.txt  61 satır LİSTE
scripts/guards/route-scope-self-test.sh  self-test, run-all.sh'e bağlı
```

**Baseline anahtarı satır numarası İÇERMİYOR** (`<dosya>|<YÖNTEM>|<yol>`) — dosyanın
başka bir yerine eklenen satır altındaki tüm rotaları kaydırıp **sahte "yeni rota"**
üretmesin diye. `mode-split`'in `F`-satırı dersi.

### Üç kova

```
FILTRESIZ    61   ← ratchet'in konusu
PUBLIC        3   health · auth/login · auth/refresh
ALAN_GUARD    2   ReversalGuard (controller sev.) · SettlementGuard (rota sev.)
(bilgi) ROLES 172 · TOPLAM 238
```

📌 **`59` rota `JwtAuthGuard,RolesGuard` taşıyor ama `@Roles` metadata'sı YOK** —
`roles.guard.ts:16-18` metadata'sızken `true` döndüğü için bunlar `FILTRESIZ`'e girdi.
`61`'in içindeler. **Guard'ın varlığı koruma değil.**

### Dört (+1) kanal, her biri AYRI mutasyonla

| # | kanal | mutasyon sonucu |
|---|---|---|
| `S` | rota dekoratörü (omurga) | tüm `@Get` rotaları sessizleşti |
| `2` | `@Roles` | yalnız `roled` kırıldı |
| `3` | `@Public` | yalnız `pub` kırıldı |
| `4a` | `@UseGuards` (**rota**) | yalnız `route-guard` — controller sev. **etkilenmedi** |
| `4b` | `@UseGuards` (**controller**) | yalnız `inherited` — rota sev. **etkilenmedi** |

`4a`/`4b` ayrımı gerçek bir tuzağı kapatıyor: `reversal.controller.ts` guard'ı
**controller seviyesinde** taşıyor, ve rota-seviyesi tarama onu *"filtresiz"* sanardı.

### `T-250`'nin üç dersi

```
boş dizin                        → exit 2
dosya var, sıfır rota            → exit 2  (parser bozuk olabilir)
bilinmeyen @UseGuards argümanı   → exit 2  (sessizce sınıflandırmıyor)
```

### İyileşme davranışı — `B2`'nin girdisi

Baseline'daki bir rota `@Roles` kazanınca **`RC=0` kalır** + *"İYİLEŞTİ, baseline'ı
AYRI commit'te güncelle"* bilgi satırı. Silinirse `GONE` diye ayırt edilir.
`money-float --ratchet` deseninin aynısı.

⚠️ **`B2`'nin kabul kriteri bu satırı takip etmektir** — `T-246`/`T-234` ailesinin
(*"baseline azaldıkça güncellenmiyor"*) yapısal engeli.

### ⚠️ Team Lead'in bir doğrulaması YANLIŞ POZİTİFTİ

*"Sınıf seviyesi `@Roles` var mı"* kontrolüm iki dosya buldu — ikisi de `T-249`'un
**açıklama yorumlarıydı**. `CLAUDE.md`'de kayıtlı tuzağın **ters yönü**
(*"yorumdaki `@Roles(` kazandı"*). Ajanın iddiası doğruydu.

### Kapılar

```
guards RC=0 (route-scope 0 bulgu) · self-test RC=0 · unit 1152/1152 · tsc 0 · lint 0
```

E2e koşulmadı — bu tur hiçbir `.ts` **kaynak** dosyasına dokunmadı.


---

# ⚡ AÇILDI YENİDEN — `RolesGuard` KONTROLÜ EKLENİYOR (ürün sahibi, 2026-08-21)

## Bulgu — `T-267` turunda, ve bu guard'ın KENDİ kör noktası

```
@Roles metadata'sı  +  RolesGuard zincirde YOK   →   metadata HİÇ OKUNMAZ
```

`roles.guard.ts:16-18`'in okuduğu metadata, guard zincirinde `RolesGuard` **yoksa hiç
okunmaz**. `T-267` iki dosyada bu durumu buldu (`settlement.controller` ·
`auth.controller` `logout`).

> ⛔ **Yani `@Roles` eklemek KOZMETİK kalabilir: ratchet YEŞİL verir, RBAC
> UYGULANMAZ.**

📌 `T-250`'nin **üçüncü kanal** vakasının aynısı: **koruma mekanizması kendi
eksikliğini gizliyor.**

## Neden AYRI TASK DEĞİL (ürün sahibi)

> *"`T-250`'de aynı karar verildi: `dataSource.getRepository` kapsama eklendi, ayrı
> task açılmadı."*
>
> ⚠️ *"Ve ayrı task açmak bir **gecikme** üretir: `route-scope` bugün `3`'te, ve o kör
> nokta **yarın bir uç eklendiğinde ısırır**."*

## Eklemenin şekli

```
bugünkü kontrol   @Roles dekoratörü VAR mı
EKSİK             RolesGuard ZİNCİRDE mi
```

⛔ **`@Roles` taşıyıp `RolesGuard` taşımayan controller → `exit 2`, bir KOVA DEĞİL.**

> **Çünkü bu bir sınıflandırma sorusu değil, bir KURULUM HATASI: `@Roles` yazılmış ama
> okunmuyor.**

⚠️ **Pozitif kontrol:** bir controller'dan `RolesGuard` kaldırılsın → **kırmızı**.

## Bugünkü durum — ölçüldü (2026-08-21, Team Lead)

```
@Roles var, RolesGuard YOK  →  0 controller     (T-267'nin düzeltmesinden SONRA)
```

Yani guard bugün **bulgu vermemeli**; kontrol **gelecek için**.

## Acceptance Criteria

- [ ] `@Roles` taşıyan her controller'ın `RolesGuard` zincirinde olduğu kontrol edilir
- [ ] İhlal → **`exit 2`** (setup hatası), kova değil
- [ ] ⚠️ **Pozitif kontrol + mutasyon:** bir fixture'dan `RolesGuard` kaldır → **kırmızı**;
      satır **basılır**, geri alma `shasum -a 256 -c`
- [ ] Bugünkü repoda **`exit 0`** (ihlal yok — ölçüldü)
- [ ] ⚠️ **Rota seviyesi `@UseGuards` de sayılır** — `auth.controller`'ın `logout`'u
      tam böyleydi (sınıf değil, **rota** seviyesi)
- [ ] Guard'ın yorumuna: *"`@Roles` DEKORATÖRÜ yetmez — GUARD ZİNCİRİ de gerekir"*
