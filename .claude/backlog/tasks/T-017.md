---
id: T-017
title: SpendCalc parite tamamlama (getAnalysis/baseTo/tactic-spend hardcode)
epic: E-001
sprint: S-001
status: done
assignee: backend-engineer
created: 2026-06-24
updated: 2026-06-24
---

## Akış: architect (tasarım) → backend (impl) → code-review → backend (fix)

## Sonuç
- baseTo/plannedTo NIV fix (off-invoice TO'yu düşürmez; migration 1781 ile hizalı).
- Ölü `calculateFuTacticSpend` (includes('PCT')) silindi.
- `approval-workflow.calculateSpendBreakdown` SpendCalc'a devredildi (mechanic config-driven on/off; canlı bütçe rezervasyonu). `includes('PCT')` 2. kopya kalktı.
- getAnalysis zaten stored KPI okuyor (ekstra iş gerekmedi).
- code-review fix: B-1 tenant scope (2 findOne), S-2 SpendingType.BOTH (category ile, çift-sayım yok), S-3 planMechanicValues relation + validation hizalama, S-5 hata sarmalama.
- 360 unit + 51 e2e; tsc temiz.
- Takip: mechanic seed sınıflandırma → [[T-018]]; on/off ayrı envelope → [[T-019]].

## Açıklama
T-008'den devreden + T-012 architect'in ayırdığı SpendCalc doğruluk işleri. Threshold
config'ten (T-012) ayrı tutuldu — farklı kritik servis (spend-calculation, KPI besler,
<500ms), farklı test yüzeyi, tactic on/off-invoice tipini master-data'dan okuma kararı gerektirir.

## Kapsam
- `plan.service.ts calculateFuTacticSpend` (~865) `tacticCode.includes('PCT')` hardcode → SpendCalc / tactic master-data tipinden.
- `approval-workflow.service.ts` (~638) aynı `includes('PCT')` ikinci kopya.
- `spend-calculation.service.ts calculateCompleteSKUFinancialMetrics.baseTo` (~676) → off-invoice düşmemeli (T-008 NIV hizalaması; recalc'ta çağrılmıyor ama controller'dan erişilirse yanlış).
- `plan.service.ts getAnalysis onOffSplit` (~965/1172) tam SpendCalc entegrasyonu (saklı KPI + tactic breakdown tutarlı).

## Acceptance Criteria
- [ ] Tactic on/off-invoice tipi master-data/SpendCalc'tan; `includes('PCT')` hardcode yok (2 kopya)
- [ ] baseTo NIV-uyumlu (off-invoice turnover'ı düşürmez)
- [ ] getAnalysis onOffSplit saklı KPI/SpendCalc ile tutarlı; gizli recompute yok
- [ ] Unit + e2e yeşil; <500ms korunur

## İlgili
- Tespit: [[T-008]] parite analizi · [[T-012]] architect ayrımı
- BRD: dinamik formül kuralı
