---
id: T-219
title: "e2e bootstrap çöküyor: `Entity metadata for ApprovalRequest#approvalPolicy was not found` — `S5`'ten mi?"
epic: E-001
sprint: S-001
status: todo
assignee: qa-engineer
priority: P1
created: 2026-08-14
updated: 2026-08-14
touches:
  - collmind.backend/test/
  - collmind.backend/src/database/entities/approval-request.entity.ts
migration_seq:
---

## Bulgu — ve iddiası DOĞRULANMADI

`data-engineer` `T-218` turunda bildirdi: iki e2e dosyası (`role-journey.e2e-spec.ts` ve
**alakasız** `kpi-optimistic-locking.e2e-spec.ts`) app bootstrap'ında patlıyor:

```
TypeORMError: Entity metadata for ApprovalRequest#approvalPolicy was not found
```

Ajan bunu *"pre-existing, benimle ilgisiz"* diye niteledi. **Bu iddia ölçülmedi.**

⚠️ `approvalPolicy` ilişkisi `B` dalgasının **`S5`**'inden geliyor olabilir
(`approval_policies` tablosu + `approval-request.entity.ts`'e eklenen FK). Öyleyse
**`B` dalgası bir e2e kırılması bıraktı** — ve o, `Done`'un dışında kalan bir bulgu.

## Yöntem — `git stash` DEĞİL

```
git worktree add <dizin> <B dalgası ÖNCESİ commit>
o dizinde e2e'yi koştur → aynı hata var mı
```

`CLAUDE.md`: taban ölçümü için `git stash` **kullanılmaz** (kapsamı örtük, untracked
dosyalarla etkileşimi sürprizli). `git worktree` izole ve geri alma gerektirmez.

⚠️ Ve **ortamın tazeliğini** doğrula: `start:dev` ayaktayken kaynak düzenlenirse rotalar
bozulabilir ve hata kod kusuru gibi görünür (`CLAUDE.md`, T-113 vakası).

## Done tanımı
- [ ] *"Pre-existing"* iddiası **ölçüldü** — `B` dalgası öncesi commit'te aynı hata var mı
- [ ] `S5`'ten geliyorsa: `T-211`'e bir bulgu olarak işlenir (dalga `done` ama bu **dışarıda
      kaldı**), ve düzeltmesi bir taska bağlanır
- [ ] İki e2e dosyası koşabiliyor
