---
id: T-284
title: Dalga-M mekanizmasının TÜKETİCİSİ — ilk rota göçü (W1)
epic:
sprint:
status: todo
assignee: backend-engineer
created: 2026-08-25
updated: 2026-08-25
touches:
  - collmind.backend/src/modules/notification/notification.controller.ts
  - collmind.backend/src/modules/admin/admin-audit.controller.ts
  - collmind.backend/scripts/guards/route-scope.sh
migration_seq:
---

## Açıklama

`B3 Dalga-M` yetenek mekanizmasını **kurdu ama kullanmadı** — `0` rota göçtü.
Ölçüldü (2026-08-25, `code-reviewer` `B1`): `@RequireCapability` üretim atfı **`0`**,
`CapabilityGuard` üretim atfı **`0`** (tek geçiş bir yorumda), `route-scope.awk`
9. sütun **223/223 sıfır**. POZ.KONTROL: `RolesGuard` **31** controller.

⛔ **Bu yüzden `Dalga-M` `done` DEĞİL, `blocked-unreachable`** — `CLAUDE.md §4.2`:
*"Üretim çağrı yolu var mı? … Yol yoksa status `done` DEĞİL."* Gerekçesi bu projede
**sekiz vakalık** bir aile (`T-033`…`T-062`): mekanizma var, ona giden yol yok.

**Bu task o yolu açar.** `W1` pilotu (`notification` + `admin`, `3` rota) mekanizmanın
ilk tüketicisidir.

## ⛔ ÖN KOŞUL — `Dalga-M` review'ının `S3`'ü

`route-scope.sh`'in dört kovası (`FILTRESIZ`/`PUBLIC`/`SELF`/`ALAN_GUARD`) **9. sütunu
BİLMİYOR**. İlk göçen rota `@Roles`'ünü kaybedip `@RequireCapability` aldığında
**`FILTRESIZ` → `A1`'e düşer** ve ratchet gereksiz kırmızı verir.

`Z26`/`Z28` emsali bağlayıcı: `@SelfScoped` turunda dekoratörle **aynı turda**
sınıflandırıcıya `SELF` kovası eklenmişti. Aynısı burada da gerekir:
**rota göçmeden ÖNCE `route-scope.sh`'e `CAPABILITY` kovası eklenir.**

## Acceptance Criteria

- [ ] `route-scope.sh`'e `CAPABILITY` kovası eklendi; göçen rota `FILTRESIZ`'e **düşmüyor**
- [ ] `W1`'in `3` rotası göçtü: `@Roles` **kaldırıldı**, `@RequireCapability` eklendi,
      controller'ın `@UseGuards`'ına `CapabilityGuard` **eklendi**
- [ ] `single-mechanism.sh` **exit 0** — hem *"iki mekanizma"* hem *"guard takılı değil"*
      (fail-open) kuralı temiz
- [ ] Pin çifti: göç öncesi/sonrası **AYNI davranış** — izinli `200` · izinsiz `403`
- [ ] Kalan-`@Roles` **LİSTESİ** düştü (sayı değil — `Z29`)
- [ ] `§5` metrikleri **mutlak sayı + SINIF** olarak raporlandı (oran DEĞİL, `3` rota
      küçük örneklem)

## Done tanımı
- [ ] Testler yeşil (unit + e2e)
- [ ] `npm run guards` exit 0
- [ ] `bash scripts/guards/lint-ratchet.sh --ratchet` exit 0 — **rapor etmeden ÖNCE**
- [ ] `code-reviewer` onayı
- [ ] **Üretim çağrı yolu var** — bu task'ın varlık sebebi
