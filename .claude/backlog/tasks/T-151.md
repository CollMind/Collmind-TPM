---
id: T-151
title: "`ledger_entries`: BRD'nin `CHECK (amount >= 0)` kısıtı yok, `status` kolonu yok"
epic: E-001
sprint: S-001
status: todo
assignee: data-engineer
priority: P1
created: 2026-08-10
updated: 2026-08-10
touches:
  - collmind.backend/src/database/entities/ledger-entry.entity.ts
  - collmind.backend/src/database/migrations/
migration_seq:
---

## Bulgu ([[T-143]] turu 5 / `docs/analysis/0023 §2.2` ve `§2.3`)

BRD `§3.6` ledger şeması:

```sql
amount NUMERIC(18,2) NOT NULL CHECK (amount >= 0),
status VARCHAR(20)   NOT NULL DEFAULT 'POSTED',
```

ve normatif cümle: *"amount: Transaction amount (**always positive**; sign indicated by
direction)."*

**Ölçüm** (şema-nitelendirilmiş): `main.ledger_entries` üzerinde `pg_constraint contype='c'`
→ **BOŞ, hiç CHECK yok.** `status` kolonu → **yok.**

## Neden P1

**Negatif bir `amount`, `DEBIT` yönüyle birleştiğinde `INV-L-007`'nin `Σ DEBIT − Σ CREDIT`
hesabını sessizce bozar** — ve dokuz `INV-L-*` invariantının **hiçbiri** bu kısıtı içermiyor.

⚠️ ADR 0007 **E7** zaten ölçmüştü: bugün **0 negatif** satır var, yön `entry_direction`'da.
Yani kural bugün **ihlal edilmiyor ama korunmuyor da** — reversal/CREDIT yolları negatif
üretmeye başladığında koruma hazır olmayacak.

**`status` ayrı bir soru:** BRD'nin örnek toplam sorgusu `AND status = 'POSTED'` filtreliyor,
ve §4.3 ledger'da *"staged (pending approval)"* bir durum öngörüyor. Bizde staging batch
tarafında. **Model farkı mı, eksik mi — ölçülmedi.**

## Yapılacak
- [ ] `CHECK (amount >= 0)` migration — numara `MIGRATION_SEQUENCE.md`'den, `data-engineer`
- [ ] ⚠️ **Önce mevcut veriyi ölç**: negatif satır var mı (varsa kısıt eklenemez, önce karar)
- [ ] `status`: model farkı mı eksik mi — `§3.4 Approval Engine` ve §4.3 birlikte okunsun
      ([[T-143]] turu 6) → **karar verilmeden kolon eklenmesin**
- [ ] `INV-L-007`'ye kısıt referansı eklensin (invariant guard'a bağlanmalı)
- [ ] ⚠️ Entity metadata'sı hizalansın — `migration:generate` temiz çıkmalı

## Done tanımı
- [ ] `CHECK` DB'de, entity'de ve `migration:generate` temiz
- [ ] `status` kararı gerekçeli
- [ ] `npm run guards` 0 · `ratchet` 0 (**36/54/10**) · e2e yeşil
- [ ] `code-reviewer` onayı

---

## ⚠️ ŞART EKLENDİ — `CHECK` tek başına eklenirse düzeltme yolu kırılır ([[T-143]] turu 20 / `0038 §3`)

`§6.4` Scenario 2 (Invoice Correction) **negatif tutar** kullanıyor:

```typescript
{ invoice_no: 'CN-INV-001', original_invoice_no: 'INV-001',
  amount: -2,000, correction_reason: 'Pricing error' }
```

**`§3.6` ise `CHECK (amount >= 0)` diyor** ve *"always positive; sign indicated by
direction"*. **BRD kendi içinde çelişiyor** (dördüncü vaka).

### Uzlaştıran tasarım — ve `§3.6` onu zaten veriyor

Düzeltme **negatif tutar** değil, **`entry_direction: 'CREDIT'` + pozitif tutar**.
`§3.6`: *"CREDIT (−spend, **reversal**)"*.

> **`CHECK (amount >= 0)` + `CREDIT` yönü** birlikte hem korumayı hem düzeltmeyi sağlar.

- [ ] ⚠️ Kısıt eklenmeden **önce**: credit note / düzeltme yolunun negatif tutar
      kullanmadığı, `CREDIT` yönüyle yazdığı **doğrulansın** — yoksa düzeltmeler yazılamaz
- [ ] Mevcut veride negatif satır var mı (ADR 0007 E7: bugün **0**, ama yeniden ölç)
