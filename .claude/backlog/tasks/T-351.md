---
id: T-351
title: "Altı yazarsız *_spend kolonu — T-270 kuralı: ya yazar kazanır ya ölür"
epic: E-001
sprint: S-001
status: todo
assignee: architect
priority: P1
created: 2026-08-31
updated: 2026-08-31
touches:
  - collmind.backend/src/database/entities/plan.entity.ts
  - collmind.backend/src/modules/shared/finance-reporting/finance-reporting.service.ts
  - collmind.backend/src/modules/shared/lta/lta-calculation.service.ts
migration_seq:
---

## Hüküm `Z79 §8` (`W8`) — `8a`: `BL` dalgasının İÇİNDE, **ama önce KARAR TABLOSU**

```
finance-reporting.service.ts:584-585   spendOf(planSku.plannedLtaOn/OffInvoiceSpend)  ← OKUYOR
lta-calculation                        değeri DTO DÖNÜŞÜNE koyuyor, plan_skus'a YAZMIYOR
plan.repository.ts:630-637 Pick<>      incrementalVolume·plannedTurnover·tacticSpend·
                                       plannedGp·gpRoi·ragStatus·calculatedKpis
                                       ⇒ ALTI KOLONUN HİÇBİRİ YOK
```

⇒ **Finans raporu, hiç kimsenin yazmadığı iki kolonu okuyor ⇒ bugün DAİMA `0`.**
`T-270` ailesi (*"canlı finansal ekranda yanlış rakam"*).

⛔ **Ve `BL` bunu DÜZELTMEZ — GÖRÜNÜR KILAR:** bugün her şey `0` olduğu için `0` doğru
görünüyor; `plan_skus` dolduğunda o iki kolon **dolu satırların yanında `0`** kalır.

## Karar tablosu — her kolon `T-270` kuralından geçer

| kolon | okunuyor mu | karar |
|---|---|---|
| `planned_lta_on_invoice_spend` | ✅ `finance-reporting:584` | ⛔ **YAZAR KAZANMAK ZORUNDA** |
| `planned_lta_off_invoice_spend` | ✅ `finance-reporting:585` | ⛔ **YAZAR KAZANMAK ZORUNDA** |
| `base_lta_on_invoice_spend` | ölç | ölçümle |
| `base_lta_off_invoice_spend` | ölç | ölçümle |
| `promo_on_invoice_spend` | ölç | ölçümle |
| `promo_off_invoice_spend` | ölç | ölçümle |

## ⛔ Bu task SALT KARAR üretir — kod `BL` dalgasında
Çıktı: her kolon için **yazar mı ölür mü**, gerekçesiyle. Ölüm seçilirse migration
gerekir ⇒ `data-engineer`'a ayrı task + `MIGRATION_SEQUENCE`'ten numara.

---

## ⚠️ EK (2026-09-05, `Z98 §6`) — SORU DEĞİŞTİ: **KOD EKSİĞİ Mİ, GİRDİ EKSİĞİ Mİ?**

Bu task *"altı yazarsız `*_spend` kolonu — ya yazar kazanır ya ölür"* diye açıldı.
`sales_actuals.fu_id` **aynı sınıfın bir üyesiydi** ve ölçüldüğünde kök sebep **kolon
değil, KAYNAK SÖZLEŞMESİ** çıktı:

```
fu_id YAZARI      0
raw_row anahtarları  category · cpl_code · net_amount · channel_code ·
                     gross_amount · discount_amount     ⇒ FU BİLGİSİ YOK
⇒ backend "doldurmayı öğrenemez" — KAYNAKTA DOLDURULACAK ŞEY YOK
```

> ### **YAZARSIZLIK BİR SEMPTOMDUR. SEBEBİ İKİ FARKLI YERDE OLABİLİR —**
> ### **VE İKİSİ FARKLI DÜZELTME İSTER.**

```
KOD eksiği    → yazar eklenir (ya da kolon ölür)          ← bu task'ın VARSAYIMI
GİRDİ eksiği  → SÖZLEŞME değişir (dosya formatı/DTO)      ← fu_id'de ÇIKAN
```

⛔ **Bu task'a başlayan tur, altı kolonun HER BİRİ için önce bu soruyu sorar** ve cevabı
**ölçümle** verir: kolonun beslendiği kaynağın (import satırı · DTO · hesaplama girdisi)
o veriyi **taşıyıp taşımadığını** göster. Varsayma.

📌 `fu_id` bu dalgada (`Z98`) kapandı — **ayrı task açılmadı**, çünkü sebep sözleşmeydi ve
sözleşme aynı dalgada değişiyor.

---

## ⚠️ EK 2 (2026-09-05, `Z98 §7/§8`) — AİLEYE **İKİ YENİ ÜYE**, farklı alt-sınıflardan

### `(1)` `agreements.gu_id` — **kolon var, veri yok**
```
5 anlaşma · 0'ında dolu   ⇒ opsiyonel kapsama boyutu, BUGÜN kullanılmıyor
```
⛔ `Z98 §6` sorusu buna uygulanır: **kod eksiği mi, girdi eksiği mi?**
→ Anlaşma **formunda** GU alanı var mı, kullanıcı doldurabiliyor mu? **Ölç.**
(`AgreementForm.tsx` `guId` taşıyor — yani alan **var** görünüyor; o hâlde soru
*"neden hiç doldurulmamış"*a döner: ürün kararı mı, keşfedilmezlik mi?)

### `(2)` `skus.gu_id` — **türetilebilir kolon, KORUYUCUSU YOK**
```
sku.gu_id == sku.fu.gu_id    170/170 tutuyor · 0 çelişiyor      [ÖLÇÜLDÜ]
CHECK / trigger              YOK
sku.service.ts:30-45         GU'yu ve FU'yu AYRI AYRI doğruluyor, UYUŞTUKLARINI SORMUYOR
```
⇒ **`INV-B-009`** (türetilebilir alan saklanıyor) **+** koruyucu mekanizma yokluğu.
Bugünkü tutarlılık bir **seed tesadüfü**; ilk `GU-A` + `FU-B(gu=GU-C)` kaydında **sapar**.

**Üç seçenek — ürün sahibi kararı (bu task'a başlayan tur SORAR, seçmez):**
```
(a) kolon DÜŞER, sku.gu_id her yerde sku.fu.gu_id'den türer   ← INV-B-009 çizgisi
    ⚠️ ama sku.fu_id NULLABLE — FU'suz SKU'da GU nereden gelir? ÖLÇ (bugün 170/170 dolu)
(b) kolon KALIR + CHECK/trigger ile senkron ZORUNLU kılınır
(c) kolon KALIR, servis doğrulaması uyuşmayı SORAR (en ucuz, ama kapı DEĞİL)
```
