---
id: T-308
title: v_budget_summary security_invoker YOK — her RLS politikasını ATLAR (Alan A)
epic:
sprint:
status: blocked
assignee: data-engineer
created: 2026-08-27
updated: 2026-08-27
touches:
  - collmind.backend/src/database/migrations/
  - collmind.backend/scripts/guards/
migration_seq: (MIGRATION_SEQUENCE.md'den tahsis edilecek)
---

## ⛔ BULGU — `ADIM 5` `RLS` ölçüm turu, envanter kalemi 4

```
ÖLÇÜLDÜ (Team Lead, bağımsız):
  main.v_budget_summary · reloptions = NULL · owner = app_migrate
  ⇒ security_invoker YOK ⇒ alttaki tablolar SAHİBİN hakkıyla okunur
  ve app_runtime'ın view üzerinde SELECT hakkı VAR
```

**Probe (throwaway DB, aynı sahiplik şekli):**
```
security_invoker'sız view  →  İKİ TENANT'IN SATIRI       ⛔ SIZDIRIYOR
security_invoker=true      →  yalnız bağlamdaki tenant    ✅
```

## ⛔ SINIFIN EN SİNSİ ÜYESİ

`budget_envelopes` / `budget_transactions` / `ledger_entries` üzerine politika **yazılsa
bile** view **hepsini atlar** — ve `npm run guards` **yeşil kalır**.

⚠️ Ve view **Alan A** (para) yüzeyinde, **canlı**: `budget.repository.ts:488,502`.

## ⛔ SIRA: POLİTİKALARDAN **ÖNCE**

```
1  ALTER VIEW main.v_budget_summary SET (security_invoker = true)
2  ... sonra politikalar
```
Sıra tersine dönerse **kapılar yeşilken sızıntı canlı** olur.

## Acceptance Criteria

- [ ] Migration: `security_invoker = true` — `RLS` zincirinin **BİRİNCİ** adımı
- [ ] ⛔ **KAPI:** `pg_class.reloptions` denetleyen bir guard — *yeni bir view aynı
      şekilde doğmasın* (`schema-isolation.sh`'ın DB-erişim desenini yeniden kullan)
- [ ] Kapı **`ADIM 3` mühür yasasına** yazılır: **üç meşru çıktı** — ve **ikinci kiracı
      fixture'ı bulunamazsa `exit 2` (setup hatası)**, `0` değil
- [ ] Repro pini: view üzerinden **iki tenant'ın satırı** görülür → düzeltilir → **tek tenant**

## Done tanımı
- [ ] Testler yeşil · `npm run guards` exit 0 · `code-reviewer` onayı


---

## ⛔ AÇIK KALEM (review `B5`, 2026-08-27) — DAVRANIŞSAL PİN **YAZILMADI**

`Z45 §1` **üç şart** koydu. **Şart 3** (*iki-tenant probe'un KALICILAŞMASI*) ve bu
dosyanın **`AC-4`**'ü (repro pini) **KARŞILANMADI**:

```
grep -rn "security_invoker" test src → yalnız guard + self-test; test/ altında SIFIR
POZ.KONTROL: grep -rln "v_budget_summary" test src → 6 test dosyası ⇒ tarama kör değil
```

Bugün var olan tek savunma **yapısal** kapı (`pg_class.reloptions`).
⛔ **`Z44 §8`'in kendi hükmü:** *"yapısal bozulmayı guard verir, **davranışsal**
bozulmayı e2e."* ⇒ **Davranışsal yarı BOŞ.**

**Erteleme MEŞRU** (bugün hiçbir tabloda `RLS` yok ⇒ probe throwaway bir `RLS`
kurulumu ister) — ⛔ **ama SESSİZCE ertelenemez.** Bu yüzden statü `blocked`.

**Açılma koşulu:** `RLS` politikaları indiği an, ya da daha önce bir throwaway
kurulumla. Pin: `invoker'sız → iki tenant · invoker'lı → tek`.

## ⛔ VE İKİ KAPI-BOŞLUĞU (review `S1`/`S2`) — self-test'in kendi körlüğü

| # | mutasyonla ölçüldü | sonuç |
|---|---|---|
| `S1` | guard SQL'ine `AND c.relname='v_budget_summary'` eklendi (**evren tek view'a daraltıldı**) | self-test **5/5 tutuyor, exit 0** ⛔ *(poz. kontrol: dedektör tersine çevrilince self-test **exit 1**, iki case düştü ⇒ kör değil, **yalnız SQL'e kör**)* |
| `S2` | `ALLOWLIST="/dev/null"` (**allowlist bağı tamamen kesildi**) | self-test **5/5 tutuyor, exit 0** ⛔ — `CASE D` guard'ı **hiç çağırmıyor**, `filter_allowlist`'i doğrudan çağırıyor |

⇒ `T-308`'in **en kritik AC'si** (**türetilmiş evren**) hiçbir kapı tarafından tutulmuyor.
**Çözüm:** DB'li bir case (throwaway invoker'sız view → guard onu **adıyla** bulmalı) +
guard'a `VIEW_GUARD_ALLOWLIST` env override'ı ki `CASE D` **gerçek guard'ı** çağırsın.

⚠️ `S3`: boş envanter → `exit 0` (**fail-open**). *"Hiç view yok"* ile *"yanlış
DB/şema"* ayrılmıyor. Öneri: önce `SELECT 1 FROM pg_namespace WHERE nspname='main'`,
dönmezse **`exit 2`**.


---

## ✅ MIGRATION CANLI DOĞRULANDI (2026-08-27)

```
pg_class.reloptions (main.v_budget_summary) → {security_invoker=true}
view-security-invoker guard                 → exit 0
self-test                                   → 5/5 (A boş · B temiz · C ihlal · D susturma · E DB-yok→2)
```
⚠️ `status: blocked` **KORUNUYOR** — davranışsal pin (`Z45 §1` şart 3) hâlâ **yazılmadı**,
ve `S1`/`S2` kapı-boşlukları açık. Yapısal yarı indi, **davranışsal yarı borç**.
