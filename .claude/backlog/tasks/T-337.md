---
id: T-337
title: CANLI spend yolunda sessiz sıfır — T-027 kararının YENİDEN değerlendirilmesi
epic:
sprint:
status: in-progress
assignee: backend-engineer
created: 2026-08-30
updated: 2026-08-31
touches:
  - collmind.backend/src/modules/shared/spend-calculation/sku-spend-inputs.ts          # YENİ — tek resolver
  - collmind.backend/src/modules/shared/spend-calculation/sku-spend-inputs.spec.ts     # YENİ — A/B pini
  - collmind.backend/src/modules/shared/spend-calculation/dto/calculation-context.dto.ts
  - collmind.backend/src/modules/shared/spend-calculation/dto/spend-breakdown.dto.ts
  - collmind.backend/src/modules/shared/spend-calculation/spend-calculation.service.ts
  - collmind.backend/src/modules/shared/spend-calculation/spend-calculation.service.spec.ts
  - collmind.backend/src/modules/modes/planning-first/plan/plan.service.ts
  - collmind.backend/src/common/plan-submission/submission-checks.ts
  - collmind.backend/src/common/plan-submission/submission-checks.spec.ts
  - collmind.backend/test/role-journey.e2e-spec.ts                                     # F12 yorum düzeltmesi
  - collmind.backend/scripts/guards/money-float-baseline.txt                           # 107 -> 102, AYRI COMMIT
---

## ⛔ Bu bir DÜZ DÜZELTME DEĞİL — bir YENİDEN KARARDIR
`?? 0` / `|| 0` düşüşleri `T-027`'nin **bilinçli** kararıdır. Bu task o kararı
**`Z65 §6` yön-deseni ışığında** yeniden değerlendirir; kararı **değiştirmeden** kod
değiştirmek yasaktır.

## Ölçülmüş kalemler (`W2 DALGA-B` + review, ikisi de **doğrulandı**)
```
spend-calculation.service.ts:918-921   baseVolume · plannedVolume · unitPrice · cogs   || 0
  kapsayan metot   calculateAllSpendsForFU:838
  ~~ÜRETİM ÇAĞIRANI  approval-workflow.service.ts:1011~~   ⛔ **YANLIŞ — `K1` ÖLÇTÜ**

plan.service.ts:2368-2371              ?? 0
  ⇒ TOTAL_PLANNED_SPEND olarak KPI motoruna `null` DEĞİL, DÜŞÜK BİR SAYI enjekte ediliyor (:2445)
```

> ⛔ **`F12` — ÖNCÜL DÜZELTİLDİ (2026-08-31, `K1` ölçümü · TL doğruladı).**
> `approval-workflow.service.ts` **811 satır** ve `SpendCalculationService`'i
> **ENJEKTE BİLE ETMİYOR**. `src/` genelindeki **tek** üretim enjeksiyonu
> `plan.service.ts:157`.
> ⇒ **`calculateAllSpendsForFU`'nun bugün SIFIR üretim çağıranı var.**
>
> ⚠️ **Ve kod ÜÇ YERDE bunun tersini İDDİA EDİYOR** (`spend-calc:786` ·
> `plan.service:2464-2465` · `role-journey.e2e:705/857`) — `T-084` **yanlış-yorum
> koruması**: bir okuyucu o satırları görür ve çağrı yolunu **var sanar**.
>
> ⛔ **Ve satır numaraları da BAYAT:** gerçek yerler `spend-calculation:1001-1004`
> ve `plan.service:2532-2535` / `:2603`.

⚠️ **Ayrım önemli** (review ölçtü): `BASE_VOL`/`BPTT`/`COGS` motora `*OrNull` gidiyor ⇒
`GP_ROI_PCT` **`null`**'a düşüyor — **iyimser değil, GÜVENLİ**. İyimserleşen şey
**`TOTAL_PLANNED_SPEND` / `plan.totalSpend`** ve ona bağlı **BÜTÇE EŞİĞİ**.

⛔ Ve `:196/:198/:506/:507` (`finalOnInvoicePct || 0`) **bu sınıfa GİRMEZ** — `ltaContext`
`null` iken `0` **çözülmüş bir değerdir** (LTA yok ⇒ harcama gerçekten `0`), ve
`lta_rates` yüzdeleri şemada `NOT NULL`. *(`DALGA-B`'nin ilk raporu bu kalemi fazla geniş
yazmıştı; review daralttı.)*

## Acceptance Criteria
- [ ] `T-027` kararı okundu ve **hangi gerekçeyle** alındığı yazıldı
- [ ] Bütçe eşiği üzerindeki etkisi **ölçüldü** (fixture ile — `plans=0` körlüğü kırılarak)
- [ ] Karar: koru / daralt / `§2.5`'e çevir — **gerekçesiyle** ve ürün sahibi onayıyla


---

# `DALGA 2a` KAPANIŞ — `Z77` UYGULANDI, **BİR KALEM ÜRÜN SAHİBİNDE**

> **Tarih:** 2026-08-31 · **Ajan:** backend-engineer · izole `git worktree`
> **Taban:** `650755a` · **Girdi:** `docs/research/K1_SESSIZ_SIFIR_OLCUM_TABLOSU.md`

## Yapıldı
1. **Tek resolver** `resolveSkuSpendInputs` — `SKUContext` **markalı** tip,
   nesne literaliyle inşa **DERLENMEZ** (mutasyonla kanıtlandı: `TS2741`).
2. **İki eksen bağımsız** (`SpendBreakdown.base` / `.planned` / `.incremental`
   ayrı ayrı `null` olabilir). `BASE_VOL` yokluğu artık `INCR_SPEND`'i
   şişirmiyor; `PLAN_VOL` yokluğu **taban zincirini öldürmüyor**.
3. `fuTotalGp += plannedGp ?? 0` **öldü** — açık `if/else`, kısmi toplam sayılıyor.
4. Submit: **bloklamayan uyarı**, alan ADIYLA. Rezervasyon: `RESERVATION_INPUT_INCOMPLETE`.
5. Üç yanlış-canlılık iddiası **`F12` iziyle düzeltildi** (silinmedi).
6. `toNullableNumber` silindi → `toFiniteDecimal` (money-float `107 -> 102`).

## ⛔ AÇIK — ÜRÜN SAHİBİ KARARI GEREKİYOR (`S1'`)
```
Grid'de BOŞ bırakılmış bir SKU satırı NEDİR?
  (a) "henüz girilmedi"  ⇒ NOT_EVALUABLE  ⇒ rezervasyon reddedilir  [bugünkü kod]
  (b) "planlanmadı"      ⇒ harcama gerçekten 0 ⇒ rezervasyon geçer
```
`Z77 §1` `PLAN_VOL`'ü `NOT_EVALUABLE` tarafına koydu, ama `(a)`/`(b)` ayrımını
**yazmıyordu**. Ölçüm: `addFu` bir FU'nun **HER** SKU'sunu `planned_volume = NULL`
ile yaratıyor (`FU-TUP-BOYA` = 52 satır) ⇒ `(a)` okuması altında, bir SKU'ya
hacim girip gönderen **sıradan** bir plan da reddediliyor.
⇒ **19 e2e testi kırmızı**, hepsi TEK sebepten (mutasyonla attribute edildi).

## Kapılar
`tsc 0` · `unit 1341/1341` · `guards exit 0` · `money-float --ratchet 0` ·
`lint-ratchet --ratchet 0` · `improved:` yok · hedefli e2e: **19 kırmızı (yukarıdaki
tek sebep)**, kapı devre dışı bırakılınca **129/129 yeşil**.

---

## ⛔ HÜKÜM `Q20` (ürün sahibi, 2026-08-31 · `Z78 §1`) — BLOKAJ KALKTI

```
DOKUNULMAMIŞ satır (tüm girdi-alanları NULL — VARSAYILAN DOĞUM HÂLİ)
  = "planlanmadı" → spend-katkısı YOK (0 DEĞİL, KATILMIYOR) → rezervasyonu BLOKLAMAZ
KISMİ satır (en az bir girdi dolu, gerekli biri eksik: planVol var / BPTT yok)
  = NOT_EVALUABLE → rezervasyon REDDİ, ALAN ADIYLA — `Z77` AYNEN yaşıyor
PLAN düzeyi: dolu-satır-sayısı 0 → submit-uyarısı "boş plan" (BLOKLAMAZ, `Q16` ailesi)
```

> **`Z77`'nin koruduğu tehlike *girilmiş-ama-eksik* verinin sessiz-`0`'a düşmesiydi;**
> **NULL-satır ise *girilmemiş* — farklı olgu.**

`(b)` reddi **ayakta**: dokunulmuş-eksik satır **asla `0` sayılmaz**. Ayrımı **satırın
olgusu** yapar; plan düzeyi **yalnız uyarı katmanı**.

**Evreni ÖLÇÜM seçti** (Team Lead, 2026-08-31):
```
main.plan_skus              base_volume · planned_volume  → nullable, DEFAULT YOK
main.plan_mechanic_values   plan_sku_id kolonu YOK  ⇒ SKU-düzeyi GİRİLEN mekanik ŞEMADA YOK
plan.service.ts:558         addSku(...) ⛔ hacim GEÇMİYOR ⇒ her satır iki-NULL DOĞAR
```
⇒ girdi-alanı evreni **iki kolon**; `BPTT`/`COGS` satırın değil **SKU ana-verisinin** alanı.
⚠️ `Z74`'ün SKU-override tarafı şemaya geldiği gün evren **büyür** ⇒ **adlandırılmış tek
sabit** olarak tutulur.

**Pin üçlüsü:** `1-dolu+N-boş → submit olur, rezervasyon YALNIZ dolu satırı taşır (TUTAR
ölçülür)` · `kısmi → red, alan adıyla` · `0-dolu → uyarı, bloklamaz`.

**Beklenen değişim:** 19 e2e yeşile döner — attribution kanıtlıydı (kapı kapalıyken `129/129`).

**Statü:** `blocked-product-decision` → `in-progress` (`2a` kapanış turu).
