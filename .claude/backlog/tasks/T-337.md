---
id: T-337
title: CANLI spend yolunda sessiz sıfır — T-027 kararının YENİDEN değerlendirilmesi
epic:
sprint:
status: todo
assignee: architect
created: 2026-08-30
updated: 2026-08-30
touches:
  - collmind.backend/src/modules/modes/planning-first/plan/plan.service.ts
  - collmind.backend/src/modules/shared/spend-calculation/spend-calculation.service.ts
---

## ⛔ Bu bir DÜZ DÜZELTME DEĞİL — bir YENİDEN KARARDIR
`?? 0` / `|| 0` düşüşleri `T-027`'nin **bilinçli** kararıdır. Bu task o kararı
**`Z65 §6` yön-deseni ışığında** yeniden değerlendirir; kararı **değiştirmeden** kod
değiştirmek yasaktır.

## Ölçülmüş kalemler (`W2 DALGA-B` + review, ikisi de **doğrulandı**)
```
spend-calculation.service.ts:918-921   baseVolume · plannedVolume · unitPrice · cogs   || 0
  kapsayan metot   calculateAllSpendsForFU:838
  ~~ÜRETİM ÇAĞIRANI  approval-workflow.service.ts:1011~~   ⛔ **YANLIŞ — `K1` ÖLÇTÜ**

plan.service.ts:2368-2371              ?? 0
  ⇒ TOTAL_PLANNED_SPEND olarak KPI motoruna `null` DEĞİL, DÜŞÜK BİR SAYI enjekte ediliyor (:2445)
```

> ⛔ **`F12` — ÖNCÜL DÜZELTİLDİ (2026-08-31, `K1` ölçümü · TL doğruladı).**
> `approval-workflow.service.ts` **811 satır** ve `SpendCalculationService`'i
> **ENJEKTE BİLE ETMİYOR**. `src/` genelindeki **tek** üretim enjeksiyonu
> `plan.service.ts:157`.
> ⇒ **`calculateAllSpendsForFU`'nun bugün SIFIR üretim çağıranı var.**
>
> ⚠️ **Ve kod ÜÇ YERDE bunun tersini İDDİA EDİYOR** (`spend-calc:786` ·
> `plan.service:2464-2465` · `role-journey.e2e:705/857`) — `T-084` **yanlış-yorum
> koruması**: bir okuyucu o satırları görür ve çağrı yolunu **var sanar**.
>
> ⛔ **Ve satır numaraları da BAYAT:** gerçek yerler `spend-calculation:1001-1004`
> ve `plan.service:2532-2535` / `:2603`.

⚠️ **Ayrım önemli** (review ölçtü): `BASE_VOL`/`BPTT`/`COGS` motora `*OrNull` gidiyor ⇒
`GP_ROI_PCT` **`null`**'a düşüyor — **iyimser değil, GÜVENLİ**. İyimserleşen şey
**`TOTAL_PLANNED_SPEND` / `plan.totalSpend`** ve ona bağlı **BÜTÇE EŞİĞİ**.

⛔ Ve `:196/:198/:506/:507` (`finalOnInvoicePct || 0`) **bu sınıfa GİRMEZ** — `ltaContext`
`null` iken `0` **çözülmüş bir değerdir** (LTA yok ⇒ harcama gerçekten `0`), ve
`lta_rates` yüzdeleri şemada `NOT NULL`. *(`DALGA-B`'nin ilk raporu bu kalemi fazla geniş
yazmıştı; review daralttı.)*

## Acceptance Criteria
- [ ] `T-027` kararı okundu ve **hangi gerekçeyle** alındığı yazıldı
- [ ] Bütçe eşiği üzerindeki etkisi **ölçüldü** (fixture ile — `plans=0` körlüğü kırılarak)
- [ ] Karar: koru / daralt / `§2.5`'e çevir — **gerekçesiyle** ve ürün sahibi onayıyla
