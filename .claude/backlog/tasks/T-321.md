---
id: T-321
title: K-2.2.7a %100 BLOCKED kademesinin HİÇBİR uygulaması yok — hüküm bekler
epic:
sprint:
status: review
assignee:
created: 2026-08-28
updated: 2026-08-28
touches:
  - docs/verification/SYSTEM_INVARIANTS.md
migration_seq:
---

> ⛔ **BU BİR KOD TASK'I DEĞİL.** Hüküm bekler. Bu dalgada yalnız
> `SYSTEM_INVARIANTS` satırı yazılır — **uygulanmamışlık KAYITLI olsun.**

## Nasıl doğdu — bir ÇÜRÜME (`Z57 §3b`)

Bildirim brief'inde *"renk merdiveni davranış için kullanılıyor, `%90` gelirse
çelişki doğar"* (`DUR-2`) yazılmıştı — `L2_01`'in belgelediği sapmaya dayanarak.
**Ölçüm çürüttü:**

```
toStatus / isExceeded çağrı yeri      6
bunlardan DAVRANIŞ üreten             0   ← ALTISI DA UtilizationStatus (RENK)
%100 BLOCKED yolu (tüm src)           0
```

⇒ `DUR-2` **kalktı** (çelişki yok), ama çürüme **daha büyük bir boşluk açtı**:

> **`K-2.2.7a`'nın üç kademesinden `%80` uyarı dışında HİÇBİRİ uygulanmamış.**
> `%90 FINANCE_REVIEW` → `T-316`/`T-318` kapatıyor. **`%100 BLOCKED` → AÇIK.**

📌 `DISIPLIN`: *"çürüten ölçüm, doğrulayan ölçümden değerlidir."*

## Neden bu dalganın işi DEĞİL

**Bildirim değil, KONTROL.** Bir dalganın kapsamı, ölçüm yolda başka bir şey
bulduğu için genişletilmez (`DISIPLIN`: *"kapsam genişletme yetkisi yoktur"*).

## Bekleyen hüküm — `Faz-1` kapanış denetimine GİRDİ

```
SORU   BLOCKED-yolu Faz-1'de mi, Faz-2'de mi?
YER    kapanış denetiminin BEŞ-ÖLÇÜT tartışmasının parçası
```

⚠️ **Ürün sahibinin ÖN-EĞİLİMİ — hüküm DEĞİL, denetim günü verilecek:**
`%100 BLOCKED` **çekirdek-döngü koruması**; `Faz-1`-artığı olarak **adresli kalır**
ama **inşası kapanışı BLOKLAMAZ.**

## Bu dalgada yapılacak TEK şey
- [ ] `SYSTEM_INVARIANTS.md`'ye satır: `K-2.2.7a %100 BLOCKED` **uygulanmamış**,
      ölçüm (`bloklama yolu 0`) ve **açılma koşulu** (`Faz-1` kapanış denetimi hükmü)
