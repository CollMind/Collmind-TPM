---
id: T-205
title: "`submittedById` bir yolda **boşaltılıyor** — `K-2.5.16` ihlali, ve dar self-approval kontrolünü sıfır maliyetle deliyor"
epic: E-001
sprint: S-001
status: todo
assignee: backend-engineer
priority: P1
created: 2026-08-12
updated: 2026-08-12
touches:
  - collmind.backend/src/modules/modes/planning-first/plan/plan.service.ts
migration_seq:
---

## Bulgu (`docs/analysis/0069 §C1` · `docs/brd-v2/_ISSUE_B_DALGASI.md §2`)

Kendi gönderdiğini onaylama kontrolü **tek bir alana** dayanıyor:

```
plan.service.ts:1401   approve()  if (plan.submittedById === userId) throw
plan.service.ts:1691   reject()   if (plan.submittedById === userId) throw
approval.service.ts:115           if (request.requestedById === approverId) throw
```

Ve bir yol o alanı **boşaltıyor** — `plan.service.ts:1874` → `submittedById: null`.

```
submittedById = NULL → NULL ≠ onaylayan → kontrol geçer
```

## ⛔ Kural bu turda yazıldı: `K-2.5.16`

`docs/brd-v2/03_IS_KURALLARI/L2_03_onay_yetki_uyum.md §2.5.4a`:

- **`K-2.5.16`** — köken alanları ile yaşam döngüsü alanları **aynı kolonda yaşayamaz**;
  köken alanı güncellenebilir ama **asla boşaltılamaz**
- **`K-2.5.16a`** — yeniden gönderimde alan **yeni gönderenle güncellenir** (önceki denetim
  izinde kalır)
- **`K-2.5.16b`** — boşaltılan kimlik alanı bypass'ı **sıfır maliyetle** yeniden açar

> Yani bu task artık *"bir kusur var"* değil, **yazılı bir kuralın ölçülmüş ihlali**.

⚠️ Ve [[T-207]] (`S13`, `last_modified_by`) inse bile bu delik kapanmaz — kural her iki
senaryoda da aynı: **boşaltma yasak.** İkisi `INV-T-002`'nin kapsam tanımının **veri
ayakları**, aynı dalgada gitmeli.

---

## Kalem 1 · Boşaltan yolu **oku** — sorgu değil, kod okuma

- [ ] `plan.service.ts:1874` **hangi akıştan** çağrılıyor? (reddedilen planın yeniden
      düzenlenmesi mi, `TASLAK`'a dönüş mü, başka bir şey mi)
- [ ] Boşaltmayı kaldırmak **o akışta neyi değiştiriyor**? Alan artık eski göndereni
      taşıyacak — hangi okuma yolları bunu görüyor?

> ⚠️ **Beklenen sürpriz:** bir arayüz *"gönderen"* etiketini **`TASLAK` durumda** gösteriyorsa,
> artık **eski gönderen görünecek**. `K-2.5.16`'ya göre bu **doğru davranış** — *"henüz
> gönderilmedi"* bilgisi `durum` alanında yaşar, kimlik alanında değil. Ama **görünür bir
> değişiklik**, ve bilerek yapılmalı: frontend'de `submittedBy` okuyan yerler taranmalı.

**Çıktı:** akışın adı + etkilenen okuma yolları, task'a yazılı.

## Kalem 2 · Kuralı uygula + regresyon notu

- [ ] `submittedById: null` yazımı kaldırılsın (`K-2.5.16`)
- [ ] Yeniden gönderimde **yeni gönderenle güncelleme** doğrulansın (`K-2.5.16a`) —
      bugün öyle mi, yoksa ilk gönderen mi sabit kalıyor?
- [ ] **Regresyon notu:** `TASLAK` durumda gönderen alanının dolu görünmesi bir davranış
      değişikliğidir; nerede göründüğü ve neden doğru olduğu yazılsın
- [ ] Delik bir **teste** pinlensin — fixture `submittedById = NULL` iken onayın
      **reddedildiğini** göstermeli (bugün: sessizce izin veriyor)

⚠️ `CLAUDE.md`: fixture iki tarafta **farklı** değer taşımalı, yoksa ayrımı ölçmez.

## İlgili
- [[T-207]] — `S13`: `plans.last_modified_by` (aynı dalga)
- `K-2.5.16` / `K-2.5.16a` / `K-2.5.16b` · `INV-T-002` (`SYSTEM_INVARIANTS:422`)
- `docs/analysis/0069 §C1`

## Done tanımı
- [ ] Kalem 1 çıktısı yazılı (akış + etkilenen okuma yolları)
- [ ] `null` yazımı kalktı, `K-2.5.16a` davranışı doğrulandı
- [ ] Regresyon notu yazıldı (`TASLAK`'ta gönderen görünürlüğü)
- [ ] Ayrımı ölçen test yeşil
- [ ] `code-reviewer` onayı
