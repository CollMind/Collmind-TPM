---
id: T-242b
title: Rol DEĞİŞTİRME yolu — arayüzsüz (ERTELENİR, hiçbir şeyi bloklamıyor)
epic: E-001
sprint: S-001
status: todo
assignee: architect
created: 2026-08-20
updated: 2026-08-20
touches:
  - collmind.frontend/src/schemas/user.schema.ts
  - collmind.frontend/src/components/forms/UserForm.tsx
---

## Açıklama

[[T-242a]]'dan **bölündü** (ürün sahibi, 2026-08-20).

**Ölçülmüş boşluk:** rol değiştiren bir arayüz yolu **yok**.

```
updateUserSchema (user.schema.ts:95-102)
   fullName · firstName · lastName · phoneNumber · department · jobTitle
   ⛔ `role` YOK
```

`PATCH /users/:id` rol alanını **kabul ediyor** (`EA-001` logu + `T-241`'in `R2`
kapısı orada), ama **hiçbir ekran onu göndermiyor**.

📌 Sonuç: `R2`'nin `409`'u bugün arayüzden **tetiklenemiyor** — `T-243` ergonomik
savunmayı hazırladı ama **davranışsal olarak sınanamadı** ([[T-243]]'te kilit olarak
kayıtlı).

## ⛔ ERTELENİR — ve gerekçesi ölçüldü

Bu task **eskiden `T-235 ADIM 3`'ü bloklıyordu**. `ADIM 3` bölündükten sonra
(`FAZ1_PLAN §5`) bağlı olduğu yarı **ertelenen** yarı:

```
yetenek modeli + default-deny       FAZ 1        ← T-242b'nin ilgisi YOK
UNRESTRICTED kod dalı temizliği     ERTELENİR    ← T-242b buna bağlıydı
```

> **Artık hiçbir şeyi bloklamıyor.** Ve `K-2.6.8a` veri tarafında **zaten karşılandı**
> (`T-235 ADIM 1` — joker satırlar yazıldı), yani terfi senaryosunun bugünkü zararı yok.

⚠️ **Ama ertelenmiş, çözülmüş değil:** `UNRESTRICTED` kod dalı bir gün kalkarsa
(`İlke 4`), terfi eden bir kullanıcı **eski dar kapsamıyla** `ADMIN` olur ve kusur
**sessizdir**. O gün bu task **ön koşul** hâline gelir.

## Acceptance Criteria

- [ ] Rol değiştiren bir arayüz yolu (`updateUserSchema` + `UserForm`)
- [ ] Rol değişiminde kapsamın **yeniden kurulması** — `R2`'nin `409`'u bir çıkmaz
      sokak olmamalı
- [ ] `R2`'nin `409`'u arayüzden **tetiklenip anlaşılır gösterildiği** doğrulanır —
      [[T-243]]'ün **yazılamayan** test kaleminin sağlayıcısı **bu task**

## Done tanımı

- [ ] Testler yeşil · `type-check` 0
- [ ] **Üretim çağrı yolu var**
- [ ] `code-reviewer` onayı
- [ ] `touches:` güncel
