---
id: T-047
title: E2E fixture adlandırma sızıntısı — E2E- öneksiz kayıtlar sınırsız birikiyor
epic: E-001
sprint: S-001
status: done
assignee: qa-engineer
created: 2026-08-01
updated: 2026-08-01
---

## Açıklama
T-039 doğrulaması sırasında Team Lead buldu. DB'de **201 artık agreement + 196 artık plan**
birikmiş (4 güne yayılmış: 29-30-31 Tem, 1 Ağu):

| Kayıt | Adet | Kaynak |
|---|---|---|
| `STA-2026-003` … `STA-2026-203` (DRAFT, cap 1000) | 201 | optimistic-locking / T-041 / T-043 / T-039 e2e'leri |
| plan `CAS-WINNER` | 108 | yarış testleri |
| plan `bump-version-before-delete` | 88 | delete CAS testleri |

**Kök neden:** bu fixture'lar `E2E-` öneki taşımıyor; `cleanupTestPlans`/`cleanupTestAgreements`
yalnız `E2E-%` sildiği için onlara **hiç dokunmuyor**. [[T-034b]]'de `"renamed before submit"`
ile aynı sınıf — orada yakalanıp düzeltilmişti, yeni testlerde **tekrarladı**.

## 🔴 Asıl ders: Team Lead'in doğrulama invaryantı KÖRDÜ
Bu oturumda her task'ta "reset'siz 3 ardışık koşum + `ENV-2026-NKA-Q1` reserved sabit" invaryantını
kullandım. Bu invaryant **yalnız rezervasyon sızıntısını** yakalar; **satır birikimini yakalamaz**.
Bu kayıtlar DRAFT ve net rezervi 0 olduğu için zarf hiç değişmedi ve sızıntı 4 gün görünmedi.

**Invaryant güçlendirilmeli:** zarf + **satır sayısı** birlikte kontrol edilmeli.

## Acceptance Criteria
- [x] Mevcut 201 agreement + 196 plan artığı temizlensin (test-only SQL)
- [x] Tüm e2e fixture'ları `E2E-` önekli olsun (`optimistic-locking`, `role-journey`,
      `kpi-optimistic-locking`, `recalc-perf-regression` dahil — hepsini tara)
- [x] Testin ortasında yeniden adlandırma yapılıyorsa önek **korunsun** (T-034b dersi)
- [x] **Kalıcı invaryant:** suite sonunda agreement/plan satır sayısı başlangıçtakiyle aynı olmalı
      — yalnız zarf değil. Bu, sızıntının bir daha 4 gün görünmez kalmasını engeller.
- [x] Kanıt: reset'siz **3 ardışık** koşum sonunda hem zarf hem **satır sayıları** sabit

## Sonuç (2026-08-01, qa-engineer)

**Kök neden doğrulandı — tek dosya:** sızıntının tamamı `optimistic-locking.e2e-spec.ts`
içindeydi. Diğer tüm e2e dosyaları (`role-journey`, `kpi-optimistic-locking`,
`recalc-perf-regression`, `settlement*`, `reversal`, `sales-actuals`, `dashboard`, `auth`)
zaten `E2E-` önekini koruyor; taraması yapıldı, ek sorun bulunmadı.

5 PATCH-rename noktası `E2E-` önekini kaybediyordu (`CAS-WINNER`, `CAS-WINNER-AGREEMENT`,
`bump-version-before-delete`, `bump-before-delete` × 2 — biri plan, biri agreement). Ayrıca
`bump-before-delete` DELETE testlerinde **soft-delete** (deleted_at set, satır tabloda kalıyor)
olduğu için sızıntı hem aktif hem soft-deleted kayıtlarda birikmiş (94 soft-deleted satır
canlı DB'de bulundu, Team Lead'in ilk raporunda görünmemişti çünkü onun sorgusu muhtemelen
aktif kayıtları sayıyordu).

**Temizlik:** 203 agreement (STA-2026-0NN, 3 haneli) + 292 plan satırı (CAS-WINNER=109,
bump-version-before-delete=89, bump-before-delete=94, hepsi ilişkili plan_fus/plan_skus/audit
dahil) test-only SQL ile silindi. Seed'in 3 agreement'ı (`LTA-2026-0001`, `STA-2026-0001`,
`STA-2026-0002`) dokunulmadan duruyor.

**Kalıcı invaryant:** `test/global-setup.js` + `test/global-teardown.js` (Jest globalSetup/
globalTeardown, `test/jest-e2e.json`'a bağlandı). Suite başında/sonunda `main.agreements` /
`main.plans` / `main.plan_fus` / `main.plan_skus` satır sayısını (Wella Turkey tenant'ı,
soft-deleted DAHİL) sayıp karşılaştırıyor; fark varsa globalTeardown throw ediyor → Jest
process'i non-zero exit code ile bitiriyor (CI'da KIRMIZI). Gerekçe ve tablo seçimi
`global-setup.js`'in başındaki yorumda detaylı.

**Doğrulama:**
- `pgrep -fl jest` temiz (koşum öncesi/sonrası)
- `npx tsc --noEmit` → temiz
- `npm test` → **540/540** (baseline ile birebir)
- `npm run test:e2e` reset'siz **3 ardışık koşum**, hepsi 199/199 yeşil, invaryant PASS:
  | Koşum | agreements | plans | plan_fus | plan_skus | ENV-2026-NKA-Q1 reserved |
  |---|---|---|---|---|---|
  | 1 (başlangıç→bitiş) | 3→3 | 0→0 | 0→0 | 0→0 | 75000 |
  | 2 | 3→3 | 0→0 | 0→0 | 0→0 | 75000 |
  | 3 | 3→3 | 0→0 | 0→0 | 0→0 | 75000 |
- `SCOPE_ENFORCEMENT_ENABLED=true npm run test:e2e` → 199/199, invaryant PASS
- **Mutasyon kanıtı:** `optimistic-locking.e2e-spec.ts`'te bir rename'i kasıtlı olarak
  `E2E-` öneksiz bıraktım (`MUTATION-PROOF-NO-PREFIX-CAS-WINNER`), `npx tsc --noEmit` temiz
  kaldığını doğruladım (derleme bozulmadı — testin "0 total" ile hiç koşmama riski yok),
  sonra `npx jest --config ./test/jest-e2e.json --runInBand` çalıştırdım: 199/199 test yine
  YEŞİL geçti ama globalTeardown `plans: 0 -> 1` farkını yakalayıp throw etti, **gerçek Jest
  exit code 1** (doğrulandı, `echo $?`). Mutasyonu geri aldım, testi tekrar `git diff`'le
  karşılaştırıp orijinal (düzeltilmiş) haline döndüğünü teyit ettim, mutasyonun ürettiği 2
  artık plan satırını test-only SQL ile temizledim. Son durum: agreements=3, plans=0,
  `npm run test:e2e` tekrar 199/199 + invaryant PASS.

**Değişen dosyalar:**
- `test/optimistic-locking.e2e-spec.ts` — 5 rename noktası `E2E-OPTLOCK-` önekiyle
- `test/global-setup.js`, `test/global-teardown.js`, `test/helpers/e2e-row-count.js` (yeni)
- `test/jest-e2e.json` — globalSetup/globalTeardown bağlandı
- `.gitignore` — geçici snapshot dosyası

## İlgili
- Aynı sınıf: [[T-036]], [[T-037]], [[T-028d]], [[T-034b]] · Tespit: [[T-039]] doğrulaması
