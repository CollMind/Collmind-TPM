---
id: T-330
title: T-321 e2e kapsaması — BLOCKED kapısı + iki-eksen, gerçek DB üzerinden
epic:
sprint:
status: review
assignee: qa-engineer
created: 2026-08-29
updated: 2026-08-29
touches:
  - collmind.backend/test/
migration_seq:
---

## Neden ayrı bir task

`T-321` kapıyı indirdi ve **unit seviyesinde mutasyonla kanıtladı** (beş mutasyon,
ters-mutasyon dahil, `shasum` doğrulamalı). **Ama e2e kapsaması yok** — ölçüldü:
`budget-tier-notification.e2e-spec.ts` ve `budget-reserve-canonical-path.e2e-spec.ts`'de
`BLOCKED`/`assertNotBlocked` **sıfır**.

📌 `CLAUDE.md §3`: *"bir ajan kendi yazdığı kodun testini yazmaz — o `qa-engineer`'ın
işidir."* `T-321` ajanının yazdığı **çağrı-yeri bağlantı** testleri bu kuralın dışında
(kodun *davranışını* değil, **bağlandığını** sınıyor); **kapının kendi davranışının
uçtan uca kanıtı** buraya ait.

## ⛔ NEDEN UNIT YETMİYOR — ve bu bir ölçüm, sezgi değil

```
unit    mock'lu v_budget_summary · mock'lu BudgetPolicyService
e2e     GERÇEK view · GERÇEK politika satırı · GERÇEK transaction
```

`projectedPct = ((allocated − available) + amount) / allocated * 100` hesabı
**`v_budget_summary`'nin döndürdüğü tiplere** bağlı. Bu projede tam bu sınıfta bir
vaka var: `Number(summary.availableAmount)` bir **no-op** sanılmıştı, oysa
`@ViewColumn({transformer})` yüzünden değer **zaten `number`**di — ve `null` geçerse
`Number(null)` → **`0`** (`Z47` review). **Mock o farkı göstermez.**

## İş — iki eksen, GERÇEK yolda
```
1  plan/taahhüt   gerçek zarf %100'ü aşacak bir RESERVE → ConflictException
                  (BUDGET_BLOCK_THRESHOLD_EXCEEDED) ve budget_transactions'a
                  SATIR YAZILMADIĞI SQL ile doğrulanır
2  hakediş        aynı zarf %100 üstündeyken bir LEDGER yazımı (settlement /
                  on-invoice) → GEÇER. K-2.2.7c: "borç doğmuştur, süreç durmaz"
                  ⛔ Bu yarı OLMADAN pin YARIMDIR — ve K-2.2.7c ihlali
                    SESSİZCE geri gelebilir
3  POZ.KONTROL    blockPct tenant konfigürasyonundan DEĞİŞTİRİLİR, pin YİNE tutar
                  (sabit 100 YAZILMAZ — Z56 §4 ilişki-pini)
```

## ⚠️ Fixture uyarısı — `DISIPLIN`
`lta_plan_overrides=0` ve `plans=0` vakalarının dersi: **bir yol bugün koşmuyorsa
"doğru" görünür.** `%100` aşımını **gerçekten** üreten bir fixture kur; yoksa pin
*"verinin yokluğu örter"* sınıfına düşer.

## Acceptance
- [ ] İki eksen de **gerçek HTTP + SQL kanıtı** ile
- [ ] Pozitif kontrol (`blockPct` değiştir) **fiilen koşturuldu**
- [ ] Mutasyon: gate kaldırılınca **plan tarafı** kırmızı, **hakediş tarafı yeşil kalır**
- [ ] Tam e2e yeşil · `T-047 PASS` · taban `811` üstüne eklenir
