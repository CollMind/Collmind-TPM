---
id: T-261
title: ⚠️ ORTAM — `collmind-tpm-backend` container'ı port 3000'de döngüde (`7 restart`, `HTTP 000`)
epic: E-001
sprint: S-001
status: todo
assignee: architect
created: 2026-08-21
updated: 2026-08-21
touches:
  - (ortam — repo dosyası değil)
---

## Bulgu — `T-258` turunda ölçüldü

```
docker ps    collmind-tpm-backend   Up · 0.0.0.0:3000->3000/tcp
docker inspect                      7 restart · running
curl localhost:3000/auth/login      HTTP 000  (bağlantı yok)
```

Alt-ajanın raporu: container `ENOTFOUND postgres` ile çöküyor, ve konfigürasyonu bu
reponun ortamıyla **uyuşmuyor** — şema `public`, port `5433`.

⚠️ **`CLAUDE.md`'nin doğrulanmış ortam notlarıyla çelişiyor:**

```
CLAUDE.md    DB portu 5434 · şema main · container collmind-tpm-postgres
container    port 5433     · şema public · host "postgres" (çözülemiyor)
```

## Neden kayda geçiyor — ve bu bir SORU değil, bir ADRES

> **Ürün sahibi:** *"Sertaç'ın ortamı, ama kayda geçmeli — bir sonraki tur *"e2e neden
> davranışsal doğrulanamadı"* diye sormasın."*

Bu turda iki ölçüm bundan etkilendi:
- `T-258`'in Team Lead doğrulaması **koddan** yapıldı (davranışsal değil)
- Alt-ajan container'ı geçici durdurup taze `start:dev` başlatmak zorunda kaldı

📌 **Ve bir sonraki tur için:** `localhost:3000`'e giden **her** davranışsal ölçüm bu
container'ın durumuna bağlı. E2e **etkilenmiyor** (kendi app'ini kuruyor) — ayrım
önemli.

## ⚠️ Ne zaman başladığı ÖLÇÜLMEDİ

Hipotez (**doğrulanmadı**): `T-241`'in `.env` değişikliği ya da `T-249`/`T-258`'in yeni
`GRANT`'lerinden sonra. **Ölçülmeden yazılmaz** — `docker inspect` başlangıç zamanı ve
log geçmişi bakılabilir.

## ⚡ KAYNAK ÖLÇÜLDÜ (2026-08-21) — TEŞHİS DEĞİŞTİ: bu BİZİM artefaktımız DEĞİL

```
docker inspect collmind-tpm-backend
  imaj             tpm-backend
  oluşturuldu      2026-04-09          ← DÖRT AYDAN ESKİ
  compose projesi  tpm
  compose dosyası  /Users/sertact/Documents/CollMind/Code/TPM/docker-compose.yml
```

⛔ **BAŞKA BİR REPODAN GELİYOR.** `/Code/TPM/` ayrı bir dizin (`collmind-backend` ·
`collmind-frontend` · `collmind.team` içeriyor) — bu repo değil.

### Dört alan da yanlış, ve DÖRDÜ DE o reponun doğrusu

| alan | container | **bu** repo |
|---|---|---|
| `DB_HOST` | `postgres` | `localhost` |
| `DB_PORT` | `5433` | **`5434`** |
| `DB_DATABASE` | `tpm_database` | `collmind_tpm` |
| `DB_SCHEMA` | **`public`** | **`main`** |
| `DB_USERNAME` | **`postgres`** | **`app_runtime`** |

📌 **`DB_SCHEMA=public` bir ipucu:** `CLAUDE.md §1`'in uyarısı — *"aynı PostgreSQL
instance'ı hem `main` (CTPM) hem `public` (TTM) şemasını barındırıyor."*

### ⛔ VE EN KRİTİĞİ — `DB_USERNAME=postgres`

Container **`K-2.6.13`'ten ÖNCEKİ dünyada yaşıyor**: ayrıcalıklı rol, ayrıcalıksız
değil.

> **Ürün sahibi:** *"Konuşlandırmayı ayağa kaldıran kişi onu **kaynak sanarsa**,
> `K-2.6.13`'ün bugün kaldırdığı ayrıcalıklı-rol bağını **geri getirir**."*

⚠️ **Ve tehlikeyi büyüten şey ADI:** `collmind-tpm-backend`. Bu reponun container'ı
gibi görünüyor, **değil**. Bir `docker ps` çıktısında ayırt edilemez.

## ⚠️ [[T-232]] İLE AYNI AİLE — ölü artefakt, YETKİLİ görünen

```
T-232   bitbucket-pipelines.yml   ölü ama YANILTICI — konuşlandırma ayağa kalkarsa
                                  K-2.6.13'ü GERİ ALIR
T-261   /Code/TPM'in container'ı  ölü ama YANILTICI — aynı bağı geri getirir
```

**İkisi de bir konuşlandırma turunda *"kaynak"* sanılabilir**, ve ikisi de
`K-2.6.13`'ün kaldırdığı ayrıcalıklı-rol bağını taşıyor.

## ⛔ AÇIK SORU — ürün sahibine

**`T-232` + `T-261` `ADIM 5`'ten ÖNCE mi, deploy hazırlığında mı?**

| şık | gerekçe |
|---|---|
| **`ADIM 5`'ten önce** | `ADIM 5` (RLS) rol modeline dayanıyor; ayrıcalıklı-rol taşıyan iki artefakt ortada dururken RLS'in kabul kriteri yanıltıcı olabilir |
| **deploy hazırlığında** | ikisi de bugün **çalışmıyor**; `İlke 1` — bugün zarar vermiyorlar, ve deploy turu onları zaten okuyacak |

⚠️ **Ölçülmesi gereken:** `ADIM 5`'in kabul kriteri (*"her politika için reddetmesi
gereken bir satırı isteyen test"*) bu artefaktlardan **etkileniyor mu**? Etkilenmiyorsa
`(b)` yeterli.

## Acceptance Criteria

- [x] ✅ **ÖLÇÜLDÜ:** `/Code/TPM/docker-compose.yml`'den geliyor — **başka bir repo**
- [ ] Artefakt ise **kaldırılsın**; kurulum parçası ise **konfigürasyonu düzeltilsin**
      (`5434` · `main` · doğru host)
- [ ] ⚠️ Port `3000` çakışması çözülsün — `start:dev` ile container **aynı portu**
      istiyor
- [ ] `CLAUDE.md`'nin ortam notlarıyla tutarlılık **doğrulansın**

## İlgili
- [[T-258]] — bulunduğu tur · `CLAUDE.md §1` ortam notları
