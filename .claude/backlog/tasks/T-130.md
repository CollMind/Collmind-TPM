---
id: T-130
title: "on/off-invoice import'ta HTTP seviyesinde e2e yok — T-126'nın vaadi hiçbir yerde iddia edilmiyor"
epic:
sprint:
status: todo
assignee: qa-engineer
priority: P2
created: 2026-08-10
updated: 2026-08-10
touches:
  - collmind.backend/test/
migration_seq:
---

## Bulgu ([[T-126]] review)

[[T-126]]'nın asıl vaadi: **"hata kullanıcıya satır numarasıyla ulaşıyor."**

`customer` için bu HTTP seviyesinde iddia ediliyor
(`test/customer-import-xlsx.e2e-spec.ts`, `error_type: 'INVALID_DATE'` assertion'ı).
`on-invoice`/`off-invoice` için **hiçbir yerde** iddia edilmiyor.

`POST /agreement-transactions/upload`'ın `invalidRows` üretimi
(`agreement-transaction.controller.ts:336-362`) **test edilmemiş tek dikiş**.

## ⚠️ Ve dikişin iki yakası ayrı ayrı pinli, ARASI değil

Parser spec'i alan adlarını (`'invoice_date'`, `'amount'`, `'fiscal_period'`, `'quantity'`,
`'discount_type'`) **elle** yazıyor; validation spec'i de **elle** yazıyor. Parser'da bir alan
adı değişse **iki spec de yeşil kalır**.

Bu §2.7'nin *"mock ile assertion aynı yanlışı paylaşır"* ailesi — ve o aile bu oturumda bir
kör test üretmişti ([[T-116]], `ledger.service.test.tsx`).

Bozulma bugün iyi huylu (çift mesaj), o yüzden P2.

## Yapılacak

- [ ] `POST /agreement-transactions/upload` ve on-invoice karşılığı için gerçek HTTP e2e:
      bozuk bir satır + sağlam satırlar → **doğru `rowNumber`** ile `invalidRows`, sağlamlar
      iniyor, dosya reddedilmiyor
- [ ] ⚠️ Alan adları **tek yerden** gelsin (parser ile spec aynı sabiti kullansın) ya da e2e
      dikişi uçtan uca geçsin — iki elle yazılmış liste bırakma
- [ ] T-047 satır sayısı invaryantı: bu e2e'nin yarattığı satırlar **teardown garantili**
      temizlensin (yarıda ölse bile — [[T-113]]'ün dersi)

## Done tanımı
- [ ] Dikiş uçtan uca pinli; alan adı değişikliği testi kırmızıya çeviriyor (mutasyonla ölçüldü)
- [ ] `npm run test:e2e` **3× reset'siz** yeşil, T-047 PASS
- [ ] `code-reviewer` onayı
