---
id: T-167
title: "D-11 ikiye bölünüyor: RLS tasarım sorusu **cevaplı**, fazlama sorusu ölçülmedi"
epic: E-001
sprint: S-001
status: todo
assignee: architect
priority: P1
created: 2026-08-10
updated: 2026-08-10
touches:
  - docs/contracts/SYSTEM_INVARIANTS.md
  - collmind.backend/src/database/migrations/
migration_seq:
---

## Bulgu ([[T-143]] turu 22 / `docs/analysis/0040`)

`Section_07 §7.5 Data Security & Isolation`:

```
- Every table has `tenant_id` column
- Row-Level Security (RLS) enforced at database level
- No cross-tenant data visibility (even for admins)
```

ve somut politika örneği:

```sql
ALTER TABLE plans ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON plans
  USING (tenant_id = current_setting('app.current_tenant')::UUID);
```

⚠️ Ve uygulama filtresi **yerine değil, EK olarak**: *"Every query **must** filter by
tenant_id — **Required!**"* → **derinlemesine savunma.**

**Ölçüm** (şema-nitelendirilmiş): **0/43 tabloda RLS, 0 politika.** `INV-T-003` VIOLATED.

## D-11 ikiye bölünüyor

`SYSTEM_INVARIANTS §9`: `| D-11 | RLS requirement | INV-T-003 | Second-customer gate |`

| yarı | durum |
|---|---|
| **Tasarım:** *"RLS gerekli mi?"* | ✅ **CEVAPLI — evet, DB seviyesinde.** Kaynak tartışmaya yer bırakmıyor |
| **Fazlama:** *"ne zaman — ilk müşteride mi, ikincide mi?"* | ✅ **CEVAPLI — Phase 1.** `§7.7`: *"✅ Multi-tenant isolation (RLS)"* Phase 1 listesinde; ertelenenler listesinde **yok** ([[T-143]] turu 23) |

> `INV-T-003`'ün *"blocked on D-11"* blokajı **tasarım yönüyle kalkıyor**: geriye bir karar
> değil, **yazılmamış bir koruma** kalıyor.

⚠️ *"Even for admins"* ifadesi tipik olandan **daha güçlü**: admin rotalarımız bugün
uygulama filtresine güveniyor, yani bir admin yolundaki hata tenant sınırını **aşabilir**.

## Yapılacak
- [x] `§7.7` okundu — **RLS Phase 1**; ertelenenler SSO · MFA · IP whitelisting · ABAC
- [ ] `SYSTEM_INVARIANTS` D-11 satırı **ikiye ayrılsın**: tasarım (kapalı) / fazlama (açık)
- [ ] `INV-T-003`'ün remediation'ı *"blocked on D-11"*'den *"unimplemented"*'a çevrilsin
- [ ] Uygulanırsa: `app.current_tenant` ayarını **her istekte** kuran katman + 43 tabloya
      politika; migration `data-engineer`, numara `MIGRATION_SEQUENCE.md`'den
- [ ] ⚠️ **Guard:** `INV-T-003` bugün `Guard: NONE`. Politika-varlık kontrolü (her tabloda
      RLS etkin mi) bir guard script'i olmalı — yoksa bir migration onu sessizce düşürebilir

## Done tanımı
- [ ] D-11 ikiye ayrılmış, tasarım yarısı kapalı
- [ ] Fazlama kararı gerekçeli
- [ ] Uygulanırsa guard'a bağlı; uygulanmazsa `SYSTEM_INVARIANTS`'ta *"bilinçli erteleme"*
      olarak kayıtlı

---

## ⛔ D-11 KAPANDI — her iki yarısıyla ([[T-143]] turu 23 / `0041 §1`)

| yarı | kaynak | sonuç |
|---|---|---|
| Tasarım | `§7.5` *"enforced at database level"* + örnek politika | ✅ **evet** |
| Fazlama | **`§7.7`: ✅ Phase 1** | ✅ **şimdi** |

> **`INV-T-003`'ün VIOLATED statüsü bilinçli bir faz kararı DEĞİL — karşılanmamış bir
> Phase 1 gereksinimidir.**

**Bu task P1 olarak kalır.** D-11 bir *"açık karar"* olmaktan çıkıp **iş**e dönüşür.

- [ ] `SYSTEM_INVARIANTS §9`'dan **D-11 satırı kaldırılsın** (karar değil); `INV-T-003`'ün
      remediation'ı *"blocked on D-11"* → *"unimplemented Phase 1 requirement (§7.7)"*
