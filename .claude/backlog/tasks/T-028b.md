---
id: T-028b
title: AccessScopeService + CM kategori-scoped onay
epic: E-001
sprint: S-001
status: done
assignee: backend-engineer
created: 2026-07-28
updated: 2026-07-28
---

## Sonuç
- **`shared/access-scope/`** (18 unit test): pair semantiği **kanıtlı** — `(CPL1,CatA)+(CPL2,CatB)`
  atanmışsa `(CPL2,CatA)` **reddediliyor** (R-1 düzleştirme tuzağı kapalı). Fail-closed (R-2:
  boş scope → `1=0`), NULL="hepsi", tenantId zorunlu, 5sn TTL cache (`Scope.REQUEST` değil).
  Rol semantiği tek yerde: ADMIN/FM/READONLY UNRESTRICTED · CM category-only · PLANNER pair (T-028c'de bağlanacak).
- **Seed:** planner(11) / planner2(17) / category.manager(2) / category.manager2(1) / manager(2) satır.
  `manager@wella.com` migration 1791 ile zaten CATEGORY_MANAGER — ikinci CM kullanıcısı, hack değil.
- **CM enforcement:** findById/findAll/pending/queue/budget-check/analysis/history + approve/reject
  kategori-scoped; kapsam dışı → **404** (`code:'OUT_OF_SCOPE'`), yetkisiz karar → **403**.
  **F3/F4 fix:** `approval-workflow` `// TODO: role-based filtering` kapandı.
- **F7 fix + gizli bug:** self-approval kontrolü eklendi; ayrıca `submit()` `submittedById`'yi
  **hiç kaydetmiyormuş** → guard sessizce devre dışı kalacaktı, o da düzeltildi.
- 436 unit + 131 e2e (N1-N5, N11, N12, N14 + pozitif CM approve).

## Takip
- `user_scopes.category_id/cpl_id` FK yok (destructive reseed'de stale satır riski) → [[T-021]].
- PLANNER scope → [[T-028c]] · dashboard/settlement F6 refactor → [[T-028d]].

## Açıklama
T-028'in 2. parçası. Tasarım: `docs/analysis/0004-rbac-brd-alignment.md` §3, §4, §6.

BRD'nin asıl onaycısı **Category Manager plan modülünde tamamen görünmez** (F1): kuyruk 403,
tek plan 403, approve 403. Ayrıca approval queue hiç filtrelenmiyor (F3 — kodda `// TODO`).

## Acceptance Criteria
- [ ] `shared/access-scope/access-scope.service.ts` — tek scope çıkış noktası.
      **R-1: pair semantiği zorunlu** (`cplIds[] × categoryIds[]` düzleştirmesi fazladan yetki verir).
      **R-2: `pairs.length===0 → 1=0`** (fail-closed). NULL = "hepsi". tenantId zorunlu. Guard değil servis.
- [ ] `user-scope.seed.ts` (§6 tablosu: planner/planner2/CM/CM2) + seed pipeline
- [ ] CM plan akışına dahil: read/queue/approve/reject/review **kategori-scoped**; kapsam dışı GET → **404**,
      approve → 403; create/update/FU/SKU → **403** (BRD "CM plan düzenleyemez")
- [ ] **F3/F4 fix:** approval-queue + pending-approvals scope+rol filtreli (TODO kapanır)
- [ ] **F7 fix:** legacy `plan.service.approve/reject`'e self-approval kontrolü
- [ ] E2E negatifleri: N1-N5, N11, N12, N14 (§9)
- [ ] Unit + e2e yeşil

## İlgili
- Tasarım: `docs/analysis/0004-rbac-brd-alignment.md` · Önceki: [[T-028a]] · Sonraki: [[T-028c]], [[T-028d]]
