---
id: T-266
title: "KAPSAM RATCHET'i — `B2`'den ÖNCE kurulmalı (`Z19b`)"
epic: E-001
sprint: S-001
status: blocked
assignee: qa-engineer
created: 2026-08-21
updated: 2026-08-21
touches:
  - collmind.backend/scripts/guards/
---

## ⛔ NEDEN `B2`'DEN ÖNCE — ve sonradan kurulamaz

`Z19b`: *"**'Adresle'** fiilinin yumuşaklığı — adres bir **metin notuysa**, `59→0`
olduğunda ratchet **yeşillenir** ve kapsamsız uçlar **korunmuş sayılır**."*

> **Ürün sahibi:** *"Sonradan kurmak da **işe yaramaz**: baseline **bugünkü `❌`
> listesini** almalı. `B2` ilerledikten sonra alınan bir baseline, **düşen uçları
> kapsamlı sanabilir**."*

⚠️ Yani bu bir sıra tercihi değil — **baseline'ın doğruluğu** buna bağlı.

## Şekli (ürün sahibi)

```
baseline   bugün kapsam UYGULAMAYAN uçların LİSTESİ (sayı DEĞİL)
yön        tek yön AŞAĞI — T-253/T-254 kapanışları listeyi ERİTİR
kapı       ARTIŞ kırmızı
```

## ⛔ GUARD *"KAPSAM VAR MI"* DİYE ÖLÇMEZ — ÖLÇEMEZ

```
AccessScopeService atfı YETERLİ DEĞİL   ← T-079: enjeksiyon kullanım değildir
T-253'ün ölçümü:  dashboard.service 5 atıf taşıyordu,
                  ama getDashboardSummary onu HİÇ KULLANMIYORDU
```

**Guard *"listeye ekleme yapılmadığını"* ölçer.** Bu bilinçli bir sınırdır ve guard'ın
yorumuna **yazılır**.

## ⚠️ AMA *"ARTIŞ KIRMIZI"*NIN BİR MEKANİZMASI OLMALI

`CLAUDE.md`: *"doğrulama bir KAPIDIR — durdurmuyorsa doğrulama değildir."*

**Sorun:** guard kapsamsızlığı **tespit edemiyorsa**, yeni bir kapsamsız uç eklendiğinde
onu nasıl görecek? Liste **elle** kurulduğu için kendiliğinden büyümez.

### 📌 Team Lead önerisi — `T-252`'nin ÜÇ KOVA deseni

```
A  kapsam GEREKLİ, uygulanMIYOR   ← BASELINE, tek yön aşağı
B  kapsam GEREKLİ, uygulanIYOR    ← büyüyebilir (iyileşme)
C  kapsam GEREKSİZ                ← gerekçeli (master-data: bir marka CPL'e ait değil)
─  SINIFLANDIRILMAMIŞ             ← ⛔ SETUP HATASI
```

**Mekanizma:** guard rota envanterini (`route-scope`'un ürettiği) `A ∪ B ∪ C` ile
karşılaştırır. Bir rota **hiçbirinde yoksa** → **`exit 2`**, yani yeni her rota
**sınıflandırılmak zorunda**.

⚠️ Böylece guard kapsamsızlığı **ölçmeden** de kapı olur: *"sınıflandırılmamış rota
var"* **durdurur**.

📌 `T-250`'nin *"boş kaynak → setup hatası"* ve `T-252`'nin *"üç kova"* desenlerinin
birleşimi — **üçüncü uygulaması**.

## Baseline'ın KAYNAĞI — `B1`

`docs/process/ADIM3_B1_TASLAK.md`'nin kapsam sütunu. Bugün bilinen `❌`'ler:

```
budget.controller       5 uç   ÖLÇÜLDÜ: budget modülünde AccessScope → 0 atıf
                               (poz.kontrol: dashboard.service → 5)
T-253'ün dört ucu              /users/dashboard-summary · plan-performance
                               agreement-transactions ×2
```

⚠️ **Liste TAM DEĞİL** — `B1` yalnız `59` filtresiz ucu inceledi. `@Roles`'lu ama
kapsamsız uçlar **ayrıca taranmalı** (`T-253`'ün `dashboard-summary`'si tam böyleydi:
`@Roles`'lu **beş rol**, kapsam yok).

## Listeden ÇIKARMA — kanıt ister

> **Ürün sahibi:** *"Bir uç listeden çıkarılırken kanıt istenir: `T-253`'ün deseni —
> **iki farklı kapsamlı kullanıcı, farklı yanıt**."*

```
planner (11 CPL) ile planner2 (17 CPL) → FARKLI sonuç
⚠️ POZİTİF KONTROL: boş sonuç FARK DEĞİLDİR (T-253'ün "veri yok" tuzağı)
```

## ⚠️ `C` KOVASI DİKKAT İSTER — `T-250`'nin dersi

`T-250`'de `lta_plan_overrides` vakası vardı: **bir kova, var olmaması gereken bir şeyi
MEŞRULAŞTIRABİLİR.**

```
✅ "bir marka CPL'e ait DEĞİL — kapsam EKSENİ yok"        ← veri sınıfından
⛔ "bu uçta kapsam gerekmiyor gibi görünüyor"              ← kolaylıktan
```

**`C`'nin her satırı `B1`'in VERİ SINIFINDAN türer.** Türetilemiyorsa satır `C`'ye
girmez — `A`'ya girer ya da `DUR`.

## ⚠️ İKİ RATCHET FARKLI EVRENLERDE — ölçülmüş sonuç

```
@Roles ratchet'i    59 uç      (route-scope-baseline.txt)
kapsam ratchet'i    235 rotanın TAMAMI
```

**Sebep ölçüldü:** `T-253`'ün `/users/dashboard-summary`'si **`@Roles`'lu beş rol**
taşıyordu **ve kapsamsızdı**. Yani kapsam sorusu `@Roles` sorusunun **üst kümesi**.

📌 Bu, `Z19b`'nin *"iki sütun"* kararının **ölçülmüş** gerekçesi — tahmin değil.

## ⛔ BEKLENTİ — ÖLÇÜMDEN ÖNCE YAZILDI (ürün sahibi, `T-243` deseni)

```
bilinen: 7 modül kapsam UYGULUYOR   (plan · approval-workflow · settlement-summary
                                     agreement · user · dashboard · finance-reporting)
bilinen: budget → 0 atıf
```

| tarama sonucu | ne demek | ne yapılır |
|---|---|---|
| `A` küçük (`< ~30`) | çoğu rota `C` (master-data, kapsam ekseni yok) | ratchet **kapı** olur, normal akış |
| **`A` DEVASA** (`235`'in çoğunluğu) | ratchet bir **BORÇ DEFTERİ** olur, **kapı değil** | ⛔ **`A` ÖNCELİK SINIFINA AYRILIR** |

### `A` devasa çıkarsa — öncelik sınıfı

> **Ürün sahibi:** *"`Alan A` verisi (bütçe · defter · hakediş) **önce**."*

```
A1  Alan A verisi        bütçe · defter · hakediş · anlaşma   ← ratchet BUNU kapatır
A2  diğerleri            borç defteri, ayrı kalem
```

⚠️ **Ve bu bir kaçış değil, `T-252`'nin dersinin aynısı:** *"sinyal sabitse, sinyal
değildir."* `235` satırlık bir baseline **hiçbir zaman kapanmaz**, ve kapanmayan bir
ratchet bir hafta içinde **devre dışı bırakılır** (`T-113`'ün ölçülmüş sonucu).

## Acceptance Criteria

- [ ] ⚠️ **ÖNCE: `A`/`B`/`C` listeleri kurulur** — ve `C`'nin her satırı **gerekçeli**
- [ ] Baseline `A` = bugünkü `❌` listesi, **`B2`'den ÖNCE** alınır
- [ ] Guard `A ∪ B ∪ C` ile rota envanterini karşılaştırır; **sınıflandırılmamış rota
      → `exit 2`**
- [ ] `A`'ya ekleme → **kırmızı**; `A`'dan çıkarma → yeşil + *"İYİLEŞTİ"* mesajı
      (`T-252` deseni, baseline **kendini yazmaz**)
- [ ] **İki girdi, iki çıktı** — ve `T-250`'nin dersi: **her kanal için ayrı** pozitif
      kontrol
- [ ] Guard'ın yorumuna **sınırı yazılır**: *"bu guard kapsamın UYGULANDIĞINI ölçmez,
      LİSTENİN BÜYÜMEDİĞİNİ ölçer"*
- [ ] `run-all.sh`'e bağlanır, ve **`RC != 0`'ın fatal olduğu doğrulanır**

## ⛔ DUR

- `A`/`B`/`C` sınıflandırması bir **ürün kararı** içeriyorsa (bir uç kapsam gerektirir
  mi) → **DUR ve sor**. Guard yazarı bu kararı vermez.
- `@Roles`'lu-ama-kapsamsız tarama **beklenenden çok** uç bulursa (`10+`) → **DUR**

## İlgili
- `Z19b` · [[T-252]] (ratchet deseni) · [[T-250]] (setup hatası deseni)
- [[T-253]] · [[T-254]] — listeyi eritecek kapanışlar


---

# ⛔ ÖLÇÜM YAPILDI — `DUR` TETİKLENDİ, guard YAZILMADI (2026-08-21)

Ajan `235` rotanın tamamını sınıflandırdı, **yazılı `DUR` koşuluna çarptı** ve
**durdu**. Repo'da hiçbir dosya değişmedi.

```
C  kapsam gerekmiyor, gerekçeli     103
B  gerekli VE uygulanıyor            38
A  gerekli, UYGULANMIYOR             94    ← 235'in %40'ı
                                    ────
                                     235   ✅ toplam doğrulandı
```

## ⚡ BEKLENTİ TABLOSUNUN `DEVASA` DALINA DÜŞTÜK

Tablo **ölçümden önce** yazılmıştı:

> *"`A` DEVASA → ratchet bir **BORÇ DEFTERİ** olur, kapı değil → `A` öncelik sınıfına
> ayrılır."*

📌 **Ve tablo işini yaptı:** ajan `94`'ü görüp guard yazmaya **devam etmedi**. Yazsaydı
`94` satırlık bir baseline doğardı — ve `T-113`'ün ölçülmüş sonucu: **kapanmayan bir
ratchet bir hafta içinde devre dışı bırakılır.**

## `A`'nın dağılımı — controller bazında

```
customer                  17  (HEPSİ)      agreement-transaction   12/14
budget-allocation         11  (HEPSİ)      budget                   8  (HEPSİ)
finance-reporting          8/9             spend-calculation        8  (HEPSİ)
ledger                     6  (HEPSİ)      lta-agreement            6/10
on-invoice                 6/8             sales-actuals            5  (HEPSİ)
approval                   3/4             settlement               1/2
reversal                   1               agreement                1/11
user                       1/13
```

## ⚠️ `B1`'İN BİLMEDİĞİ ~`75` YENİ BOŞLUK — dördü kayda değer

**1 · `GET /agreements/pending-approvals` kapsamsız.** `@Roles(ADMIN, CATEGORY_MANAGER,
FINANCE, READONLY)`, ve `findPendingApprovals(tenantId)` **hiçbir kapsam parametresi
almıyor** → bir `CATEGORY_MANAGER` **tüm tenant'ın** bekleyen anlaşmalarını görüyor.
`T-253`'ün sınıfı, farklı uç.

**2 · `shared/budget` modülünün TAMAMI (`19` rota) — YAZMA UÇLARI DÂHİL.**
Team Lead doğruladı: modülde kapsam atfı **`0`** (poz.kontrol: `plan.service` → `11`).
`reserve` · `commit` · `split` · `adjust` **kapsamsız**. `B1` yalnız `5` `GET`'i
incelemişti.

**3 · `agreement-transaction` (`12/14`)** — `create` bile
`agreementService.findById(id, tenantId)`'i **actor'süz** çağırıyor → kapsam kontrolü
**hiç tetiklenmiyor**.

**4 · ⚠️ `kpi grid` İKİYE AYRILDI — `B1` TASLAĞI YANLIŞTI.** Team Lead doğruladı:

```
getGridKpisForPlan   actor?: PlanActor alıyor, planService'e delege     →  B
findGridKpis(tenantId)  yalnız tenantId, tenant-geneli katalog          →  C
```

`B1` ikisini birlikte *"plan kapsamlı"* saymıştı — **kod doğrulamadı**. `T-255`'in
dersi doğruydu (veri sınıfı bazında bak), **ama uygulaması eksikti**: iki ucun **aynı
veri sınıfında olduğu varsayıldı, ölçülmedi.**

## ⚡ AJANIN ÖNERİSİ — `İlke 4` yakalaması

`A1`/`A2` bölmesi için **tekerlek yeniden icat edilmesin**:

```
scripts/guards/money-float-domain-a.txt   ← ZATEN ÜRÜN-ONAYLI Alan A listesi (ADR 0007)
  shared/budget · shared/spend-calculation · actuals-first/ledger
  actuals-first/agreement · actuals-first/agreement-transaction
  actuals-first/settlement · actuals-first/on-invoice · planning-first/plan
```

`plan` zaten `B`; geri kalanı `A1` adayıyla **doğrudan örtüşüyor**.

## ⛔ KARAR BEKLEYEN ÜÇ SORU

1. **`A1`/`A2` bölme ölçütü** — `money-float-domain-a.txt` tabanlı mı?
2. **`customer` YAZMA uçları** (`7`) — okuma mantığının genişletilmesiyle `A`'ya kondu;
   customer'ın **yazmada da** CPL eksenli olması gerektiği **ayrıca onaylanmalı**
3. **`READONLY` kapsama tabi mi?** `budget-at-risk`/`variance-analysis`/
   `cash-flow-projection` yalnız `{ADMIN, FINANCE, READONLY}`'e açık. `ADMIN`/`FINANCE`
   `AccessScopeService`'te **koşulsuz UNRESTRICTED`; `READONLY` `T-235`'ten beri kod
   dalında **değil** — bugün joker satır taşıyor. Statüsü netleşmeli (`Z18 §3`'ün
   *"`FINANCE` kayıtsız"* kilidiyle **aynı aile**)
