---
id: T-244
title: Kapsam verme DENETİM KAYDI — yanlış aktör (A1) + hiç kayıt yok (A7), BİRLİKTE
epic: E-001
sprint: S-001
assignee: backend-engineer
created: 2026-08-20
updated: 2026-08-20
touches:
  - collmind.backend/src/modules/user/user.service.ts
  - collmind.backend/src/modules/user/user.controller.ts
  - collmind.backend/src/modules/user/user.module.ts
  - collmind.backend/src/modules/user/user.service.spec.ts
  - collmind.backend/src/database/entities/user-scope.entity.ts
  - collmind.backend/test/helpers/seed-e2e.ts
  - collmind.backend/test/helpers/e2e-row-count.js
status: done
---

## Açıklama

`T-241` ikinci tur review'unun `A1` + `A7` bulguları. **Birlikte gidiyorlar** — ve
gerekçesi ürün sahibinden:

> ⚠️ **`A1` yanlış aktör yazıyorsa, `A7`'nin eksikliği onu GİZLİYOR** — yani ikisi
> ayrı giderse `A1`'in etkisi **ölçülemez**.

### `A1` — `createdBy` YANLIŞ aktörü kaydediyor (**bir KUSUR, ertelenmez**)

`user.service.ts:115` → `createdBy: savedUser.id`

Kapsam satırının *"bu erişimi **kim verdi**"* alanı **yeni kullanıcının kendisini**
gösteriyor. Gerçek aktör, kullanıcıyı yaratan `ADMIN`.

> Bir **yetki verme** kaydında bu, denetimin cevaplaması gereken **tek soruyu**
> kaybediyor.

**Ve desen zaten var, bu yolda kullanılmıyor** (ölçüldü):
```
user.controller.ts:48   POST /users     → @CurrentUser() ALMIYOR
user.controller.ts:85   PATCH me        → alıyor
user.controller.ts:168  PATCH :id       → alıyor
user-scope.seed.ts:114  gerçek createdByUserId yazıyor
```

### `A7` — `POST /users` için denetim izi **YOK**

```
grep -rn "audit" -i src/modules/user/   →  0 eşleşme
```

Kullanıcı yaratma **ve kapsam verme** hiçbir yere loglanmıyor. `CLAUDE.md §2.3`:
*"immutable; silinemez/güncellenemez; onay/red dahil **her işlem** loglanır."*

⚠️ **Ve `T-242`'nin AC'si yalnız GÜNCELLEMEyi kapsıyor** — yaratma tarafı için aynı
boşluk var ve kimse saymıyordu.

## ⚡ KAPSAM DARALTILDI + ÇAPA (ürün sahibi, `Z15`, 2026-08-20)

> **`T-244` bağımsız bir format DEĞİL — denetim sözlüğünün ERKEN AÇILAN İLK SAYFASI.**

```
İlke 4, iki seviyede:
  seviye 1  T-242a önce giderse  → ikinci kayıt biçimi, birleştirilmesi HİÇ yapılmaz
  seviye 2  T-244 kendi başına   → ADIM 6'nın sözlüğü geldiğinde ÜÇÜNCÜ uyumsuzluk
            bir biçim tanımlarsa
```

**Biçim burada değil, `docs/process/DENETIM_SOZLUGU.md` `Madde 1`'de yazılı.**
Bu task onu **uygular**, yeniden tanımlamaz.

⚠️ **Ve `ADIM 2`'nin dört-aile ölçümü sözlüğün TABANIDIR** — yeniden sayılmaz.

## Acceptance Criteria

- [ ] `POST /users` `@CurrentUser()` alır; `createdBy` **gerçek aktörü** yazar
- [ ] ⚡ **Ve bu bir testle pinlenir** — `createdBy !== savedUser.id` olduğu, yani
      *"kullanıcı kendi erişimini verdi"* durumunun **oluşamayacağı**
- [ ] ⚡ **KAPSAM DARALDI (`Z16`)** — yalnız **kapsam verme** denetim kaydına girer.
      **Kullanıcı yaratma OLAYI kapsam DIŞI**, adresi: sözlük `Madde 2` (⛔ AÇIK).
      ⚠️ Yani `A7` **yarım** kapanıyor ve bu kayıtlı — `done` yazarken *"A7 kapandı"*
      denmez, *"A7'nin kapsam yarısı kapandı"* denir.
- [ ] ⚡ **Yaratma anı `SCOPE_UPDATE`, eski küme = `∅`** (`Z16`) — üçüncü tür AÇILMAZ.
      Ayrım ekseni *"hedef küme boşalıyor mu"*, *"ilk verme mi"* değil.
- [ ] ~~Kullanıcı yaratma +~~ kapsam verme **denetim kaydına** girer. ⚠️ Önce **ölç**:
      `K-2.11.2`'nin olay grupları bunu kapsıyor mu, yoksa kapsam genişlemesi mi
      gerekiyor? (`ADIM 2` ölçüm 2: *"kanonik sözlük YOK, dört ayrı aile"*)
- [ ] Kayıt `DENETIM_SOZLUGU.md` `Madde 1`'in alanlarını taşır: **eski küme · yeni
      küme · kim · ne zaman · niyet · gerekçe**(`REVOKE_ALL`'da zorunlu)
- [ ] ⚠️ **İki olay türü**: `SCOPE_UPDATE` · `SCOPE_REVOKE_ALL`. Ayrım bilinçli —
      `K-2.6.8a` gereği boş kapsam **erişim yok** demek, yani `REVOKE_ALL` bir
      güncelleme değil bir **erişim kaldırma**. Tek türe konsaydı denetim
      *"kullanıcı neden hiçbir şey göremiyor"* sorusunu **cevaplayamazdı**
- [ ] ⚠️ **`kim` = AKTÖR**, etkilenen değil — `A1`'in kusuru tam buydu
- [ ] ⚠️ [[T-242a]] aynı kaydı yazar — **iki yol, tek kayıt biçimi**. Bu task biçimi
      **sözlükten uygular**; `T-242a` onu **yeniden tanımlamaz**

## ⚠️ Sınır — `A1` ile kayıt biçimi KARIŞTIRILMAZ (`Z16`, ürün sahibi)

```
A1  createdBy: savedUser.id → gerçek aktör    ⚡ BU TURDA  (bir KUSUR)
A7  kapsam kaydı SCOPE_UPDATE                 ⚡ BU TURDA  (biçim uygulaması)
—   kullanıcı yaratma OLAYI                   ⛔ kapsam dışı → sözlük Madde 2
```

`A1`'in yaratma olayının kaydedilmemesiyle **ilgisi yok**: `createdBy` bir denetim
kaydı alanı değil, `user_scopes` satırının **kendi kolonu**. Yanlış aktör yazması,
olay kaydedilse de kaydedilmese de bir kusurdur.

## ⚙️ Mekanizma VAR — yeni servis yazılmaz (`§7` turu, ölçüldü 2026-08-20)

```
AdminAuditService.logAdminAction()   src/common/services/admin-audit.service.ts
  → altı serviste çağrılıyor (channel · mechanic · agreement · reversal ·
    sales-actuals · settlement-close)
  → transaction-aware overload VAR: options.manager (T-014)
grep -rni 'audit' src/modules/user/   →  0
```

**Yani `A7` bir mekanizma eksikliği değil, KABLOLAMA eksikliği.**

Taşıyıcı sözlüğün altı alanının hepsini karşılıyor:

| sözlük alanı | `admin_audit_logs` |
|---|---|
| eski küme | `before_values` (jsonb) |
| yeni küme | `after_values` (jsonb) |
| kim (**aktör**) | `admin_id` + `admin_email` |
| ne zaman | `created_at` |
| olay türü / niyet | `action_type` (varchar 50) |
| gerekçe | `justification` |
| hedef | `entity_type` + `entity_id` (`K-2.11.4`'ün *"hangi kayıt"*ı) |

## ✅ KAPANIŞ (2026-08-20)

⚠️ **`A7` YARIM kapandı — "A7 kapandı" YAZILMAZ.**

```
A1  createdBy = gerçek aktör                    ✅ kapandı
A7  kapsam verme denetim kaydı (SCOPE_UPDATE)   ✅ kapandı
A7  kullanıcı yaratma OLAYI                     ⛔ AÇIK → sözlük Madde 2
```

`§2.3`'ün *"her işlem loglanır"* ihlali **sürüyor ve biliniyor** (`Z16 §3`).

### İki review turu

**Tur 1** (`code-reviewer`): BLOCKER yok · 4 MAJOR · 5 MINOR · 2 NIT.
En değerli bulgu: **iki mutasyon pinleri delip geçti** —

```
M2 flush'ı transaction İÇİNE taşı  → 42 passed   (sıra ölçülmüyordu)
M4 yabancı manager geçir           → 42 passed   (atomiklik ölçülmüyordu)
```

Yani *"aynı transaction'da"* ve *"commit'ten sonra"* **yorumda** yazılıydı, **testte
yoktu**. `§2.7 #6`'nın vakası: yeşil olmak, ayırt ettiği anlamına gelmiyor.

**İki karar ürün sahibine gitti** (`code-reviewer` `§2.4`'ü uyguladı — varsaymadı):
`m2` (`niyet` alanı) ve `m1` (`entity_type`). İkisi de `Z17`'de.

**Tur 2** (Team Lead doğrulaması): `J4`'ün raporu ile kodu **uyuşmuyordu** —
rapor *"kimlik kontrolü"* diyordu, kod `toEqual(objectContaining(...))` yazıyordu.

```
AYRI fn referanslı klon  →  toEqual ❌      ← bugün koruyan şey bu
AYNI fn referanslı klon  →  toEqual ✅  toBe ❌   ← boşluk burada
```

Üretim tarafındaki mutasyon (`{ manager: { ...manager } }`) **`TS2740`** verdi ve
`Tests: 0 total` — **başarısız bir deney**, kanıt değil. Boşluk gerçek ama bugün
`tsc` kapatıyor.

`toBe`'ye sıkıştırıldı, çünkü **koruma tip kapısına devredilmez**: bir sözleşmenin
geçerliliği çağıranın bugünkü şekline bağlı olamaz. Ve satın aldığı ölçüldü — mock'tan
klon geçirilince `A7` **kırmızıya dönüyor**, eski assertion'da dönmüyordu.

### Kapılar (kapanış anı)

```
tsc 0 · lint 0 · unit 1143/1143 (66 suite) · guards 0 bulgu
money-float --ratchet 0 · lint-ratchet temiz
e2e 320/320 (22 suite) · T-047/T-060 invaryantı PASS (35→35)
```

### Doğurduğu iş

- [[T-247]] — `sales-actuals` `{manager}` ile yazıp hiç flush etmiyor (`m5` yan bulgusu)
- sözlük `Madde 2` — kullanıcı yaşam döngüsü, `ADIM 6`

## Done tanımı

- [ ] Testler yeşil (unit + ilgili e2e)
- [ ] `npm run guards` exit 0 · `money-float --ratchet` exit 0
- [ ] **Üretim çağrı yolu var** — `POST /users` canlı
- [ ] `code-reviewer` onayı
- [ ] `touches:` güncel
