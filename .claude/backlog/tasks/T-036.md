---
id: T-036
title: E2E bütçe zarfı tükenmesi — agreement tarafı (settlement-budget-release spec)
epic: E-001
sprint: S-001
status: done
assignee: qa-engineer
created: 2026-07-29
updated: 2026-07-29
---

## Sonuç
İki ayrı, gerçek test-altyapısı hatası bulundu (**üretim kodu sağlam — `git status` ile teyit
edildi, yalnız 4 test dosyası değişti**):

1. **`cleanupTestTransactions` hiçbir zaman çalışmamış.** `ledger_entries.source_id`
   **agreement_id** tutuyor (`ledger.service.ts:74` `sourceId: agreementId`; entity yorumu
   "Agreement.id or Plan.id"), ama sorgu `source_id IN (SELECT id FROM agreement_transactions …)`
   filtreliyordu → **asla eşleşmeyen sorgu**. DEBIT ledger satırları hiç silinmiyordu,
   her koşum `consumed`'ı kalıcı şişiriyordu. Hata vermediği için görünmemişti.
2. **`role-journey` C7-C9** bir agreement'ı APPROVED bırakıp hiç close/cancel etmiyordu
   → her koşum `reserved` +20.000.

Düzeltme: `cleanupTestTransactions` idempotency-key + `reverses_entry_id` FK ile doğru satırları
buluyor; yeni `cleanupTestAgreements` (`cleanupTestPlans` deseni) yalnız `E2E-` önekli kayıtları
**tamamen siliyor** — kompanzasyon RELEASE satırı YOK (ledger append-only; sahte iş kaydı
gerçek bir bütçe hatasını gizlerdi).

## Kanıt (Team Lead bağımsız koşturdu — reset YOK)
| Koşum | Flag | Sonuç |
|---|---|---|
| 1 | kapalı | 153/153 (7 suite) |
| 2 | `SCOPE_ENFORCEMENT_ENABLED=true` | 153/153 |
| 3 | kapalı | 153/153 |

Sonrası: `E2E` artık kayıt **0**, tüm zarflar `reserved=0`, `available=allocated`.

## Bulunan ayrı sorunlar → [[T-037]]
- `test:e2e` script'inde `--runInBand` YOK; settlement ↔ reversal aynı mutable seed fixture'ını
  paylaşıp paralel worker'larda yarışıyor (flaky 400/409). Bu oturumun tüm kanıtları
  `-- --runInBand` ile alındığı için görünmemişti.
- `seed-e2e.ts:86` diriltme hack'i: kapatılan seed agreement'ı `APPROVED`'a geri yazılıyor ama
  rezervasyon geri kurulmuyor → **onaylı agreement bütçeden düşmüyor** (BRD ihlali, [[T-029]] sınıfı).
  Pre-existing; leak-tespit invaryantlarını zehirliyor.

## İlgili
- Plan tarafı muadili: [[T-028d]] (`cleanupTestPlans`) · Devamı: [[T-037]]
