---
id: T-335
title: Ebeveyn agreements.status motorda okunmuyor — bağ yapısal, semantik değil
epic:
sprint:
status: todo
assignee: backend-engineer
created: 2026-08-30
updated: 2026-08-31
touches:
  - collmind.backend/src/modules/shared/lta/lta-agreement.repository.ts
  - collmind.backend/test/helpers/seed-e2e.ts
---

## Açıklama (review `S3`, `W2 DALGA-B`)
`lta-agreement.repository.ts:59-89 findActiveForCPL` **yalnız** `lta.status='active'`
filtreliyor; `agreements` tablosuna **JOIN YOK**. `AgreementStatus` yedi değer taşıyor
(`DRAFT · PENDING · APPROVED · ACTIVE · CLOSED · REJECTED · CANCELLED`).

⛔ **`DRAFT` ya da `REJECTED` bir yaşam döngüsü kaydının oran kademesi, `lta_agreements.status='active'`
olduğu sürece MOTORA İNİYOR.**

Bu `Z38 §3(a)`'nın bağa yüklediği anlamla çelişiyor (*"`agreements` = onay · audit · SoD ·
defter bağının kanonik yeri"*): **bağ kuruldu ama onay durumunu okuyan hiçbir yol yok.**
*"Mekanizma var, ona giden yol yok"* sınıfının **okuma tarafındaki** şekli.

⚠️ **Bugün bu kabul `seed-e2e.ts:740`'ta bir TEST YORUMUNDA yaşıyor** — bir kayıt kalemi
olmalı, bir test yorumu değil. *(Bu task o kaydın kendisidir.)*

## Acceptance Criteria
- [ ] Hangi `AgreementStatus` değerlerinin oranı motora indirdiği **karara bağlandı** (ürün sahibi)
- [ ] Kural bir **guard**'a bağlandı (test/DB constraint) — bağlanamıyorsa "tavsiye"ye düşürülüp öyle işaretlendi
- [ ] `seed-e2e.ts:740` yorumu bu task'a atıf verir
- [ ] Fixture **ayırt edici**: `DRAFT` ebeveyn + `active` lta ⇒ beklenen davranış **okunuyor**

---

## ⛔ HÜKÜM `Q21` (ürün sahibi, 2026-08-31 · `Z78 §2`) — İNİYOR

**Kabul edilen statü kümesi `{APPROVED, ACTIVE}` — ONAYLI, ve KAYNAĞIYLA:**
```
docs/brd/01_Main_BRD/Section_04…:603
  if (lta && lta.status !== 'ACTIVE' && lta.status !== 'APPROVED')
```
⇒ **icat değil, ölçülmüş.** `L2` ve ADR'ler sessiz; sessizlik *"kural yok"* değil,
*"kaynak BRD'dir"* demek.

**`CLOSED` bilerek DIŞARIDA** — açık soru **koşul satırıyla**:
> `CLOSED` anlaşmanın **geçmiş-dönem okumaları** ayrı sorudur (**okuma-yolu**, planlama-yazımı
> değil) — **ilk gerçek `CLOSED` vakası tetikler.**

⛔ **BEŞİNCİ KOPYA YAZILMAZ.** Küme kod tabanında zaten dört kez var; gerekçe ürün sahibince
onaylandı: *"dördü aynı **değeri** taşıyor, aynı **soruyu** sormuyor"* ⇒ **birleştirme yok.**
Ama **çapraz-referans yorumu düşülür**: biri değişirse diğer üçünün **bilerek** değişmediği
görünür olsun. Kaynak (`Section_04:603`) **bir kez** yazılır, diğer üçü atıf verir.

**Statü:** `2c` inişi ile birlikte kapanır.
