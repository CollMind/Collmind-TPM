---
id: T-343
title: ragAmberThreshold ÖLÜR · ragGreenThreshold AD-BORCU alır (targetRoiThreshold sınıfı)
epic:
sprint:
status: review
assignee: backend-engineer
created: 2026-08-31
updated: 2026-08-31
touches:
  - collmind.backend/src/common/kpi/target-roi.ts                 # YENİ — kanonik eksen
  - collmind.backend/src/common/kpi/target-roi.spec.ts            # YENİ
  - collmind.backend/src/database/entities/kpi.entity.ts
  - collmind.backend/src/database/migrations/1820000000000-TargetRoiThresholdRenameAndDropRagAmber.ts
  - collmind.backend/src/modules/master-data/kpi/                 # repository · service · dto
  - collmind.backend/src/database/seeds/kpi.seed.ts
  - collmind.backend/src/modules/shared/kpi-engine/kpi-engine.service.ts
  - collmind.backend/src/modules/modes/planning-first/plan/approval-workflow.service.ts
  - collmind.backend/src/modules/shared/finance-reporting/           # service · module · dto
  - collmind.frontend/src/api/endpoints/kpi.endpoints.ts
  - collmind.frontend/src/api/endpoints/finance-reporting.endpoints.ts
  - collmind.frontend/src/components/features/admin/KpiManagementPage.tsx
  - collmind.frontend/src/components/finance/widgets/BudgetAtRiskWidget.tsx
  - collmind.frontend/src/utils/export.ts
migration_seq: 1820000000000
---

## Hüküm (`Z70 §2`, ürün sahibi 2026-08-31)

**`İlke 3` sorusu soruldu ve cevap `HAYIR`:**
> `İlke 3` testi: *"bu kural, kullanıcının **düzenlemek isteyeceği** bir kural mı?"*
> Kadran-RAG'ın tanımı **eşik değil, İŞARET tabanlıdır** (`iTO`/`iGP` **sıfır çizgileri**).
> ⛔ **`"sıfırdan büyük"` konfigüre edilecek bir DEĞER değil, KAVRAMIN KENDİSİDİR.**

⛔ **Ve tehlike adlandırıldı:** `AMBER`'ı eşikle oynatmak *(ör. `iGP < −5000` olursa Amber
sayalım)* **kadranın anlamını GERİ-EŞİĞE çevirmek** olur — **dün öldürdüğümüz tek-eksen
dünyasının GERİ DÖNÜŞ KAPISI.**

## İş

### `1` · `ragAmberThreshold` **ÖLÜR**
Tüketicisiz — **ölçüldü** (`T-342 A0b`): yalnız `kpi-engine.service.ts determineRagStatus`'a
giriyordu, kadran onu **girdisiz** bıraktı.
```
kolon  +  endpoint alanları  +  tip beyanları
```
📌 **Emsal `E2`/`tier_roles`** — ama **bu kez CANLIYKEN ölüyor**, ölü doğmuş değil.
⇒ Üç durum ayrımı ve `down()` yolu **daha da önemli**.

### `2` · `ragGreenThreshold` **YAŞAR ama AD-BORCU ALIR**
Tüketicisi **RAG değil** — `plan.service.ts:2892` **Target ROI**
(`ABOVE_/ON_/BELOW_TARGET`), RAG'dan **bağımsız**.
> ⛔ **`rag` önekiyle yaşaması, `ragAmber`'ın ÖLDÜĞÜ bir dünyada yarın birini
> *"RAG konfigüre edilebilir"* yanılgısına götürür.**

⇒ Yeniden adlandırma (**`targetRoiThreshold` sınıfı**) **`AD-BORCU` paketine** —
`Q12` (`weighted_avg`) ile aynı pakete, **eşleme-sonrası tek dokunuş**.
⛔ **BU TASK'TA YENİDEN ADLANDIRMA YAPILMAZ** — yalnız ad-borcu **kaydedilir**.

### `3` · SPEKÜLATİF KONFİGÜRASYON İNŞA EDİLMEZ (`İlke 1`)
> İleride bir tenant *"kadran yetmez, eşik isterim"* derse — **o gün süzgeçten geçer,
> OLAY-TETİKLİ.** Bugün spekülatif konfigürasyon **inşa edilmez**.

## Acceptance Criteria
- [ ] `ragAmberThreshold` **tüketicisizliği YENİDEN ölçüldü** (poz. kontrollü) — kaldırmadan **önce**
- [ ] Kolon + endpoint alanı + tip beyanı **birlikte** düştü; FE/BE tip kapısı yeşil
- [ ] Migration üç durum ayrımı (`1805`+ deseni), dalların **en az ikisi ampirik**;
      `down()` kolonu **geri getiriyor** (canlıyken ölen bir kolon ⇒ geri dönüş yolu gerçek)
- [ ] `ragGreenThreshold` **AD-BORCU** olarak kayıtlı; **yeniden adlandırılmadı**
- [ ] ⛔ Kadran sınırlarının (`0`) **kodda sabit** olması `§2.3` bağlamında **gerekçesiyle** yazılı
      *(`Z69 §4b`: dayandığı-alan değişimi — kural yaşar, neyin konfigüre edildiği değişir)*

## Kaynak
`Z70 §2` · `T-342` `A0b` ölçümü · `Z69 §4b` · `Z66 §2`


---

## ⛔ KAPSAM REVİZE EDİLDİ (`Z71 §3`) — ve `Q13` ile AYNI İŞ ÇIKTI

`§2`'nin *"BU TASK'TA YENİDEN ADLANDIRMA YAPILMAZ"* satırı **geçersizdir**:
`Z71 §3` ad-borcunu **bu dalganın parçası** yaptı, çünkü **below-target
uyarısının okuduğu kalem TAM O**. Yapılan iş üç kalem:

```
1  rag_amber_threshold      ÖLDÜ           (kolon + DTO + tip + admin UI)
2  rag_green_threshold  →   target_roi_threshold   RENAME (veri TAŞINDI)
3  below-target uyarı + Finance kovası     İNŞA EDİLDİ
```

## KAPANIŞ ÖLÇÜMLERİ

### `AC` · `ragAmberThreshold` tüketicisizliği — KALDIRMADAN ÖNCE yeniden ölçüldü
```
davranışsal okuyucu   kpi-engine.service.ts determineRagStatus   → TEK
pozitif kontrol       aynı desen ragGreenThreshold'u 10 dosyada buluyor
                      (yani tarama kör değil)
kataloğa bakış        main.kpis 32 satır · rag_amber_threshold dolu: 1
                      (GP_ROI_PCT = 10.0000)
```
⇒ Ve `determineRagStatus`'ün kendisi de bu turda **kaldırıldı** (`Z71 §1`,
*"kadran TEK OTORİTE"*): eşik-RAG yolunun taşıyıcı dışında **sıfır** tüketicisi
olduğu ölçüldü (FE'nin dört `RAGCell` çağrısının dördü de `['GP_ROI_PCT']`).

### `AC` · Migration üç durum ayrımı — **ÜÇ DAL AMPİRİK, BİRİ NEGATİF TESTLE**
| dal | nasıl ölçüldü | sonuç |
|---|---|---|
| RENAME + DROP (beklenen) | `migration:run` | `target_roi_threshold=20.0000` **veri taşındı** |
| NO-OP (tekrar koşum) | `up()` ikinci kez doğrudan çağrıldı | patlamadı |
| İPTAL (küme değişmiş) | `GP_MARGIN_PCT.rag_amber_threshold=7` kuruldu | **exit 1**, satır ADIYLA raporlandı |
| `down()` | `migration:revert` ×2 | kolon + **değer** geri geldi (`20.0000` / `10.0000`) |

⛔ İptal dalı bir **pozitif kontroldür**: kapı gerçekten kapı mı? Ve `RENAME`
adımının **transaction'la geri sarıldığı** da ölçüldü (throw sonrası iki eski
kolon da yerindeydi).

### `AC` · `down()` byte-birebir — ve TEK FARK YAZILI
`pg_dump --schema-only -n main` öncesi/sonrası diff: **iki kalem**, ikisi de
anlamsal değil — pg_dump `\restrict` nonce'u ve `rag_amber_threshold`'un
**ordinal pozisyonu** (PostgreSQL yeniden eklenen kolonu her zaman sona koyar;
emsal `1814`'te de kayıtlı).

### `AC` · Kadran sınırlarının kodda sabit olması — gerekçe YAZILI
`rag-quadrant.ts` docstring'inde `Z69 §4b` atfıyla: **dayandığı-alan değişimi**
(kural yaşar, neyin konfigüre edildiği değişir) + `İlke 3` cevabı **HAYIR** +
geri-eşik kapısının adı konmuş hâli. Konfigüre edilebilirlik **ertelendi,
reddedilmedi**; tetikleyici olay-tetikli.


---

## REVIEW TURU — `E1` · `B1` · `S1`–`S7` (2026-08-31)

### 🔴 `E1` — tam e2e kırmızısı KAPANDI
`test/kpi-optimistic-locking.e2e-spec.ts:192` eski adı (`ragGreenThreshold`)
gönderiyordu ⇒ DTO beyaz listesi `400` döndürüyordu, test `409 STALE_VERSION`
bekliyordu. Yeni ada çevrildi + **dersi dosyaya yazıldı**:
> **Bir yeniden adlandırmanın tüketici evreni TESTLERİ de içerir** — ve bu satır
> `§7.1` taramamda görünmedi çünkü testin KONUSU optimistic locking; eşik alanı
> yalnızca bir **TAŞIYICIYDI**. Bir alanı *kendi konusu için değil, taşıyıcı
> olarak* kullanan kod, "bu alanın tüketicisi" diye aranırken bulunmaz.

Yeniden tarama: eski ada **canlı referans 0** (21 eşleşmenin hepsi yorum/docstring
+ tarihsel `1818` SQL'i, o da `1820`'den ÖNCE koşuyor). Poz. kontrol: yeni ad
**19 dosyada** canlı.

### 🔴 `B1` — `decimal` DİZGE gelir; `toFixed` PATLIYORDU
**Reprodüksiyon önce koşuldu, çökme GÖRÜLDÜ** (probe exit 42):
```
main.kpis.target_roi_threshold  pg_typeof=numeric · sürücü typeof "string" · "20.0000"
Number.isNaN("20.0000")  → false   ⇒ §2.5 kapım GERÇEK GİRDİDE HİÇ ÇALIŞMIYORMUŞ
10.5 < "20.0000"         → true    ⇒ karşılaştırma TESADÜFEN doğru
threshold.toFixed(1)     → ⛔ TypeError: threshold.toFixed is not a function
```
📌 **`§7.1`'in tam vakası:** kaldırdığım `determineRagStatus` **tam bu yüzden**
`Number(greenThreshold)` yazıyordu; korunan `plan.service` hâlâ `Number(...)`
yapıyor. **Yeni yol o dönüşümü taşımadı.**

**Düzeltme:** normalizasyon `evaluateTargetRoi`'nin İÇİNE alındı (`toFiniteNumber`:
dizge→sayı, sonlu değilse `NOT_EVALUABLE`; **`0`'a çökmez**) · `approval-workflow`
`as` cast'i **kaldırıldı** → `kind` daraltması · `finance-reporting` aynı dosyadaki
**iki farklı muamele** birleştirildi (DTO da karşılaştırma da tek değerlendirmeden).

**Mutasyon kanıtı** (normalizasyonu geri al → dizge sızsın): **7 test öldü** ve
loglarda `toFixed is not a function` **2 kez** çıktı — 60 test koşarken, yani
derleme hatası değil davranışsal. `shasum -c` ile geri yüklendi.

### `S1`–`S7`
| # | durum | kanıt |
|---|---|---|
| `S1` | ✅ `1820.up()`'a **varlık assert'i** eklendi (çift taraflı) | kolon `NULL`'landı → `up()` **exit 1**, mesaj *"var olmayan bir değeri geri yazmak bir ÖLÇÜM değil bir UYDURMA olurdu"* |
| `S2` | ✅ iddia düzeltildi (filtre DEĞİL) | `IN` `NULL`'ı dışlar ⇒ alan bu uçta **yapısal `null`**; DTO + servis yorumu bunu yazıyor, ve alanın `getPlanPerformance`'ta **canlı** olduğu belirtiliyor. ⛔ *"Dışlanmış planlar risk raporuna girmeli mi"* → `totalAtRisk` anlamını değiştirir = **`S9`, kapsam dışı** ⇒ ürün sahibine |
| `S3` | ✅ `export.ts` Plan Performance sitesi düzeltildi | `LTA_ONLY` ⇒ `ragStatus=null ∧ gpRoi=null` ve **bu rapora GİRER** (RAG filtresi yok) ⇒ bu turun doğurduğu sınıf satırı **yeni ulaşılabilir** kılmıştı. `?? 0` değil `'—'` |
| `S4` | ✅ `GrandTotals` tooltip **bağlandı** | `title` üretiliyordu, hiçbir öğeye verilmiyordu ⇒ ölü alan + yarım iniş |
| `S5` | ✅ kova için **5 unit test** | `belowTargetRoiPlans` dolar · `totalAtRisk`'e girer · `riskLevel='BELOW_TARGET'` · dizge eşikle de dolar · eşiksizken boş · `AMBER` iki kez sayılmaz. **Mutasyon: 2 test öldü** |
| `S6` | ✅ `approval-queue` ayrımı taşıyor | DTO alanı + `approval-workflow:1009` eşlemesi |
| `S7` | ✅ `1819` iptal dalı **ampirik** | kolon `integer` olarak elle kuruldu → **exit 1**, bulunan+beklenen tip mesajda adlandırıldı |

**Nit'ler:** `ragCoverage.test.ts` yorumu *"beş tüketici"* → **dört** (`PlanningGrid.tsx`
ölü; `PlanDetailPage` `PlanningGridEnhanced`'i o **takma adla** import ediyor) ·
`riskLevel` ham dizge (kullanıcı `BELOW_TARGET` görür) `risk-report.dto.ts`'e kayıt
düşüldü, sunum metnine çevirme kapsam dışı.
