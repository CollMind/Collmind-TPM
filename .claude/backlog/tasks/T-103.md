---
id: T-103
title: "GET /on-invoice/entries iç hata mesajını 4xx gövdesine koyuyor — aynı dosya, aynı sınıf"
epic:
sprint:
status: todo
assignee: backend-engineer
priority: P2
created: 2026-08-07
updated: 2026-08-07
touches:
  - collmind.backend/src/modules/modes/actuals-first/on-invoice/on-invoice.service.ts
  - collmind.backend/src/modules/modes/actuals-first/on-invoice/on-invoice.service.spec.ts
migration_seq:
---

## Bulgu (`code-reviewer`, T-098 ikinci review, S6)

`on-invoice.service.ts:655` civarı — `GET /on-invoice/entries` yolunda:

```ts
throw new BadRequestException(`...: ${error.message}`);
```

İki kusur bir arada:

1. **İç mesaj HTTP gövdesine giriyor.** Nest, açıkça fırlatılan bir `HttpException`'ın
   mesajını yankılar — filter gerekmez. Yani bu, [[T-098]]'in kapattığı sınıfın **dönen**
   ucu.
2. **İç hata 4xx olarak sınıflandırılıyor.** İstemci hatası olmayan bir şey "senin isteğin
   bozuk" diye raporlanıyor.

## Neden ayrı task, neden P2

**Aynı dosya, aynı kusur sınıfı** — §7.1'in "kusurlar dosya bazlı kümelenir" kuralı bunun
kapsama alınmasını beklerdi. T-098'de kapsam dışı bırakıldı çünkü **farklı bir rota** ve
kendi semantiği var (orada satır bazlı ERROR yok, doğrudan istek başarısız oluyor).

⚠️ Ama kayda geçmesinin sebebi ayrı: `decimal.transformer.ts` yorumunda bu uç **adıyla
sayılıyor** ve düzeltilmeden bırakılıyor. §7.1: *bir hatayı belgelemek onu koruma altına
alır.* Bu task o riski kapatır.

**Ondalık değer için bugün zararsız:** [[T-098]]/1 mesajı **kaynağında** arındırdı, dolayısıyla
`InvalidDecimalError` ham değeri buraya taşıyamaz. Tek-nokta kararının değeri tam olarak bu.
Ama başka hataların mesajları hâlâ geçiyor.

## Bu turda verilen karara bağlı

Ürün sahibi kararı (2026-08-07): **varsayılan redaksiyon, istisna beyanla.**
`src/common/errors/user-facing.ts` — `asUserFacing()` / `isUserFacing()`.

Bu task o kararı bu uca uygular: mesaj varsayılan olarak geçmez; geçmesi gerekiyorsa
fırlatan taraf **beyan eder**.

## ⚠️ Yalnız bu satır değil — kalıbın tamamı sayılmalı

`code-reviewer` her iki ucu da saydı. Yeniden fırlatarak mesajı gövdeye koyan noktalar:

```
on-invoice.service.ts:655 · on-invoice-file-parser.service.ts:70,128,138
off-invoice-file-parser.service.ts:71,129,139 · customer/services/file-parser.service.ts:67,137,147
customer.service.ts:244
```

Kalıcılaştıran / yanıtta dönen noktalar:

```
agreement-transaction.service.ts:296 · customer.service.ts:365
approval-workflow.service.ts:1001 (iç mesajı InternalServerErrorException'a gömüyor)
agreement.service.ts:280 (error.stack + JSON.stringify(dto) → debug_agreement_error.log; src/'deki TEK fs yazımı)
```

**Bu task en az `on-invoice.service.ts:655`'i kapatmalı; diğerleri ölçülüp ya kapsama alınmalı
ya gerekçesiyle bırakılmalı.** Sayının kendisi bayatlar — kalıbı ara, listeyi değil.

`agreement.service.ts:280` ayrıca kendi başına bir soru: üretim kodu diske debug log yazıyor.

## Acceptance Criteria

- [ ] `GET /on-invoice/entries` iç hata mesajını gövdeye koymuyor
- [ ] İç hata **4xx olarak sınıflandırılmıyor** (500 veya uygun bir kod)
- [ ] `asUserFacing` ile beyan edilmiş bir mesaj **geçiyor** — ayrım testle sabitlenmiş
- [ ] Yukarıdaki diğer uçlar ölçüldü; kapsama alınmayanların gerekçesi yazılı
- [ ] Mutasyon: her düzeltmeyi tek tek geri al → **yalnız** o noktanın testi kırmızı

## Done tanımı
- [ ] Testler yeşil (unit + e2e), 3× reset'siz
- [ ] `npm run guards` exit 0 · `money-float.sh --ratchet` exit 0, korunanlar **36/54/10**
- [ ] `code-reviewer` onayı
- [ ] Üretim çağrı yolu var (`GET /on-invoice/entries` canlı)
- [ ] `touches:` güncel
