---
id: T-079
title: "POST /plans/:id/fus ölçek kapısı yok — 999 bugün 201 dönüyor (açık kapı)"
epic:
sprint:
status: todo
assignee: backend-engineer
priority: P1
created: 2026-08-05
updated: 2026-08-05
touches:
  - collmind.backend/src/modules/modes/planning-first/plan/plan.service.ts
  - collmind.backend/src/modules/modes/planning-first/plan/dto/add-fu.dto.ts
  - collmind.backend/test/plan-scale-validation.e2e-spec.ts
migration_seq:
---

## Açıklama

**Bu bir eksik değil, açık kapı.** F2/C3 `PATCH /plans/:id/fus/:fuId/tactics` üzerine ölçek
kapısı kurdu. Aynı `plan_fus.tactics` JSONB kolonuna yazan **ikinci** endpoint'e kurulmadı.

Bugünkü davranış (ölçüldü, 2026-08-05):

```
POST  /plans/:id/fus                    { fuId, planVersion, tactics: { CPP_ON_PCT: 999 } }  → 201
PATCH /plans/:id/fus/:fuId/tactics      { tactics: { CPP_ON_PCT: 999 }, version }            → 400
```

Kanıt: `add-fu.dto.ts:24` `tactics?: Record<string, number>` alıyor; `plan.service.ts:505`
bunu doğrudan `planRepo.addFu(..., dto.tactics)` ile yazıyor. Ne ölçek doğrulaması ne
`UNKNOWN_MECHANIC_CODE` kontrolü var.

Aynı kolona biri kapılı biri açık iki yazma yolu — bu reponun yedi kez kaydettiği ayrışma
sınıfının kendisi. Kapılı yolun varlığı, açık yolu daha tehlikeli yapar: "ölçek doğrulanıyor"
diye okunur.

Bulgu kaynağı: `code-reviewer`, F2/C3 birinci tur (blocker 1). C3'e sıkıştırılmadı çünkü
ikinci bir endpoint'in davranışını değiştirmek ayrı ölçüm ve ayrı e2e hak ediyor — bu oturumda
ölçülmemiş kapsamın **beş kez** düşük çıktığı görüldü.

## ⚠️ SIRA: bu task [[T-080]]'DEN ÖNCE merge edilmeli

Karşılıklı bağ — T-080'de de yazılıdır. Tek yönlü yazılırsa biri diğerini görmeden ilerler.

Bugün `POST /fus` ile diske düşen bozuk bir anahtar **yalnızca** PATCH'in tüm JSONB'yi
değiştirmesi sayesinde silinebiliyor — yani kurtarma yolu, T-080'in düzelttiği hatanın kendisi.
T-080 merge semantiğine geçince o kaçış kapanır.

Doğru sıra bunu bedelsiz kılar: **bu task bozuk anahtarın diske düşmesini önlerse kurtarmaya
gerek kalmaz.** Ters sırada gidilirse (T-080 önce), bu task bitene kadar `POST /fus` ile düşen
bir anahtar silinemez hâle gelir. Küçük risk, ama gereksiz — ve bugün diskte bozuk anahtar yok
(`main.plan_fus` 0 satır, ölçüldü 2026-08-05), yani sırayı doğru kurmanın maliyeti sıfır.

## Ölçüm önce (kod yazmadan)

- [ ] `POST /plans/:id/fus` bugün gerçekten `tactics` ile çağrılıyor mu? Frontend'de çağrı yeri
      var mı, gövdesinde `tactics` gönderiliyor mu? (Kod adına değil, gönderilen gövdeye bak.)
- [ ] `addFu` yolunda mekanikler yüklü mü? Değilse — `updateFuTactic`'te olduğu gibi bir yükleme
      gerekir. `addFu` sıcak yol değil, ek sorgu maliyeti önemsiz; yine de ölç.
- [x] ~~`addFu` sonrası recalc'te kilitlenme zinciri var mı?~~ **VAR — ölçüldü, hipotez değil**
      (`code-reviewer`, C3 ikinci tur, S2). `plan.service.ts:508` `addFu` kendi bağlantısında
      commit ediyor; `:527` recalc ayrı transaction'da `buildMechanicValues` →
      `unknownMechanicCodeError` fırlatıp yalnızca kendi transaction'ını geri alıyor. Yani
      `updateFuTactic`'te C3'ün kapattığı zincirin **aynısı** bu endpoint'te duruyor: bozuk
      anahtar diske düşüyor, sonraki her recalc ve submit aynı 400'e çarpıyor. Yeniden
      türetme — bu satır kanıttır.

## Acceptance Criteria

- [ ] `POST /plans/:id/fus`, `updateFuTactic` ile **aynı** kapıdan geçiyor: `checkEnteredScale`
      (üç dallı) + `unknownMechanicCodeError`. Yeni bir doğrulama yazılmaz — mevcut tek
      üreticiler çağrılır.
- [ ] e2e: `POST` ile `CPP_ON_PCT: 999` → 400 `INVALID_SCALE`; `NO_SUCH_MECHANIC` → 400
      `UNKNOWN_MECHANIC_CODE`; `PRICE_SUP: 0.0125` → 201 (muafiyet burada da korunur).
- [ ] e2e: 400 sonrası `plan_fus` satırı **oluşmamış** olmalı (kısmi yazma yok).
- [ ] `docs/verification/NUMERIC_CONTRACT_COLUMNS.md` C3 bölümündeki "yazma yolu kısmen kapandı"
      ifadesi güncellenir.

## Done tanımı
- [ ] Testler yeşil (unit + e2e), 3× reset'siz
- [ ] `npm run guards` exit 0
- [ ] `money-float.sh --ratchet` exit 0 — `plan.service.ts` **36**'da kalmalı
- [ ] `code-reviewer` onayı
- [ ] Üretim çağrı yolu var (endpoint canlı)
- [ ] `touches:` güncel
