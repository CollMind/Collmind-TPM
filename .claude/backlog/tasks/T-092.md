---
id: T-092
title: "format ve lint, değişen dosyalarla sınırlansın — repo genelini kirletiyorlar"
epic:
sprint:
status: review
assignee: backend-engineer
priority: P3
created: 2026-08-05
updated: 2026-08-05
touches:
  - collmind.backend/package.json
  - collmind.frontend/package.json
migration_seq:
---

## Problem — ölçüldü, iki kez yaşandı

**`npm run format`** (backend): tek bir dosyayı biçimlendirmek için koşuldu, **11 dosyayı**
yeniden yazdı — migration'lar, `mechanic-input.ts`, `optimistic-locking.e2e-spec.ts` dahil.
Hepsi committed dosyalar, hiçbiri o turun işi değildi.

**`npm run lint`** (backend): `--fix` ile koşuyor. Ölçüm amacıyla bir kez çalıştırıldı ve
`optimistic-locking.e2e-spec.ts` dahil dokuz dosyayı yeniden biçimlendirdi. O sırada
çalışma ağacındaki iş `git stash`'teydi ve `stash pop` **reddedildi** — kurtarma gerekti.

Sonuç her iki vakada da aynı: ilgisiz format değişiklikleri diff'e karışıyor, ve onları
ayıklamak elle yapılıyor.

## ⚠️ Neden kural değil, mekanizma

CLAUDE.md'ye "kendi dosyaların dışını geri al" diye bir satır yazmak **insan disiplinine**
dayanır. Bu oturumun tek tekrar eden bulgusu, **disiplinin mekanizmaya yenildiği** —
E15/T-086'da da aynı sonuca varıldı (JSDoc kuralı → guard'a bağlanmalı).

Script sınırlandırılırsa sorun ortadan kalkar; kural yazılırsa sorun her turda yeniden
hatırlanmayı bekler.

## Öneri

`git diff --name-only` (+ `--cached` ve untracked) beslemeli bir hedef listesi:

```
format:  değişen .ts dosyaları → prettier --write
lint:    değişen .ts dosyaları → eslint --fix
```

Tam repo taraması ayrı bir script'te kalsın (`format:all`, `lint:all`) — CI onu kullanır.

## Acceptance Criteria

- [x] `npm run format` yalnız değişen dosyalara dokunuyor
- [x] `npm run lint` yalnız değişen dosyalara dokunuyor
- [x] `format:all` / `lint:all` duruyor · **ve yeni `lint:check`** — tam repo, `--fix` yok
- [x] Temiz ağaçta `format` **0** dosya (eskiden 11) · `lint` **0** dosya (eskiden 9)
- [x] Frontend: kendi `scripts/changed-files.sh`'i · `lint` **kasıtlı olarak değiştirilmedi**
- [x] ⚠️ **Denetleyiciler daraltılmadı.** `guards` zaten lint/format çağırmıyordu (ölçüldü);
      `lint:check` tam repoda 108 hata buluyor ve **0 dosya kirletiyor**; `tsc` değişmedi.

## Done tanımı
- [ ] `npm run guards` exit 0 · `money-float.sh --ratchet` exit 0
- [ ] `code-reviewer` onayı
- [ ] `touches:` güncel


---

## ✅ KAPANDI — 2026-08-07, backend `29efe29`, frontend `910fe71`

### Ayrım: düzelticiler daraltıldı, denetleyiciler daraltılmadı

Ölçüm bunu somutlaştırdı: **frontend `lint`'te `--fix` yok** — o hiç kirletmiyordu, yani bir
denetleyici. Daraltılmadı. Backend `lint` ise `--fix` taşıyor, hem düzeltici hem denetleyiciydi;
ikiye ayrıldı:

| Script | Kapsam | Ağaç |
|---|---|---|
| `format`, `lint` | değişen dosyalar | yazar |
| `format:all`, `lint:all` | tam repo | yazar (kasıtlı süpürme) |
| **`lint:check`** (yeni) | **tam repo** | **yazmaz** — 108 hata, 0 dosya |
| `guards`, `tsc` | tam repo | yazmaz (değişmedi) |

`guards`'ın zaten lint/format çağırmadığı **ölçüldü**, varsayılmadı — yani task bir guard'ı kör
etme riskini hiç taşımıyordu.

### ⚠️ İlk kanıt denemesi yanlıştı — ve tam bu task'ın konusu olan tuzağa düştü

Temiz ağaç elde etmek için `git stash -u` kullandım. O **`package.json`'ı da stash'ledi**, yani
`npm run format` **eski** script'i koştu ve 6 dosya kirletti. Kurulum, ölçülmek isteneni ortadan
kaldırdı — CLAUDE.md §2.7 vaka 4, ve **bir tooling kusurunu düzeltirken aynı tooling tuzağına
düşerek**.

Doğru ölçüm: değişiklikleri commit et, sonra gerçekten temiz ağaçta koş.

### `lint` artık exit 0 — ve bu bir kayıp değil

108 pre-existing hata kimsenin değiştirmediği dosyalarda; düzeltici oraya artık uğramıyor.
Sayı `lint:check`'te görünür kalıyor. Eskiden `lint` her koşumda exit 1 veriyordu ve bu yüzden
**hiçbir Done checklist'inde gerçek bir kapı değildi.**
