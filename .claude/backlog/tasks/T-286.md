---
id: T-286
title: Bölme sonrası AD-TABANLI sarkan atıflar (≥31) — CLAUDE.md → DISIPLIN.md
epic:
sprint:
status: todo
assignee: claude
created: 2026-08-25
updated: 2026-08-25
touches:
  - .claude/backlog/tasks/
  - docs/
migration_seq:
---

## Açıklama

`CLAUDE.md` bölmesi (2026-08-25) `63` kuralı `docs/DISIPLIN.md`'ye taşıdı.
Kayıpsızlık üç ölçütle kanıtlandı — **ama ölçüt 2'nin EVRENİ eksikti**
(`code-reviewer` bulgusu).

`Ölçüt 2` iki atıf biçimi tanımladı: `#çapa` (ileri) ve `§N` (geri). **Bu repoda
baskın biçim ikisi de değil** — `CLAUDE.md`: *"kural metni"*, yani **ad-tabanlı**.

**Ölçüm** (`.claude/` + `docs/`, bölünen iki dosya ve plan belgeleri hariç):

```
CLAUDE.md atfı taşıyan satır          475
bunlardan AD-TABANLI (§N yok)         116
  → yalnız DISIPLIN.md'de çözülüyor    31   ✗ SARKAN
  → yalnız CLAUDE.md'de çözülüyor       3   ✓
  → ikisinde birden (n-gram gürültüsü) 50
```

**POZ.KONTROL:** sınıflandırıcı **her iki yönde de** sonuç üretti (`3` çekirdek /
`31` disiplin), tek cevaba saplanmıyor. `31` **muhafazakâr taban**.

### Örnekler
- `.claude/backlog/tasks/T-212.md:29` — *"Doğrulama, çıkışı akışı durduran bir kapıdır"* → `docs/DISIPLIN.md`
- `.claude/backlog/tasks/T-110.md:42` — *"Bir testin fixture'ı…"*
- `.claude/backlog/tasks/T-231.md:39` — *"bir enum DEĞERİ JWT/API/URL üzerinden…"*
- `docs/brd-v2/04_KARAR_KAYDI.md:2276` — *"`@deprecated` bir niyet beyanıdır"*
- `docs/analysis/0072-...md:334` — *"bir hipotezi DOĞRULAYAN ölçüm…"*

### Ve ÜÇ çıplak `§` dosya sınırını geçti
Eski dosyada `` `§` `` *"bu dosya"* demekti:

| yer | hedef |
|---|---|
| `docs/DISIPLIN.md` *"`§`'nin flaky maddesi"* | **`CLAUDE.md`** — dosya dışı |
| `CLAUDE.md` *"`§`'nin negatif sonuç…"* | **`docs/DISIPLIN.md`** |
| `CLAUDE.md` *"`§`'nin bir kusur başka bir kusur…"* | **`docs/DISIPLIN.md`** |

Kalan `7` çıplak `§` hedefini **aynı dosyada** buluyor (kontrol edildi).

## ⛔ Neden bölme turunda DÜZELTİLMEDİ

`30+` dosyaya dokunmak **salt-taşıma sözleşmesini ihlal ederdi** — ve o sözleşme
üç ölçütün kanıt gücünün taşıyıcısı. `TODO` ile değil **TASK** ile kaydedildi.

📌 Sınıf: **`KAPSAM MASKELEMESİ` — *"desen çalışır, EVREN eksiktir"***, ve o kural
tam da bu turda taşınan kuralların **kendisi**. Ayrıca `§4.2`: *"kabul listesi
EKLEDİĞİNİ değil BOZABİLECEĞİNİ de saymalı"* — liste `63` indeks satırını saydı,
`31` bozulan atfı **saymadı**.

## Acceptance Criteria
- [ ] `31`'in her biri ya `DISIPLIN.md` atfına çevrildi ya *"çözülüyor"* diye ölçüldü
- [ ] Üç çıplak `§` dosya-nitelendirildi (`CLAUDE.md §…` / `DISIPLIN.md §…`)
- [ ] Tarama **tekrarlanabilir** bir script'e indi (elle sayım DEĞİL) ve
      **pozitif kontrollü** (uydurma bir kural adı *"hiçbir yerde"* demeli)
- [ ] `50` *"ikisinde birden"* satırı örneklenerek gürültü mü gerçek mi ayrıldı

## İlişkili
Aynı tura alınabilir: **araç-satırı envanteri** (`CLAUDE.md`'de adı geçen araç
`4`, gerçek araçlaşma `10+`) + *"desen anlatıp araç adlandırmayan kural"*
taraması (`Z34`'ün kuyruktaki genellemesi). Ürün sahibi: *"iki iş birleşsin."*
