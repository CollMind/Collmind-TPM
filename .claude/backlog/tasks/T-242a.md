---
id: T-242a
title: Kapsam GÜNCELLEME yolu — FAZ 1 (çıkış ölçütü madde 2: filtreyi BESLEYEN yol)
epic: E-001
sprint: S-001
status: review
assignee: architect
created: 2026-08-18
updated: 2026-08-20
touches:
  - collmind.backend/src/modules/user/
  - collmind.backend/src/common/services/admin-audit.service.ts
  - collmind.backend/src/modules/shared/access-scope/  # clearCache çağrısı (m1)
  - collmind.backend/scripts/db-roles/02-runtime-grants.sql
  - collmind.backend/test/user-scope-update.e2e-spec.ts
  - collmind.backend/test/user-scope-creation.e2e-spec.ts
  - collmind.backend/test/db-role-negative-yetki.e2e-spec.ts
  - docs/verification/DB_ROL_IZIN_ENVANTERI.md
---

## ⚡ BÖLÜNDÜ (ürün sahibi, 2026-08-20) — ve bu yarı `FAZ 1`

```
T-242a   kapsam GÜNCELLEME       FAZ 1      ← çıkış ölçütü madde 2:
         (BU TASK)                            "kapsam filtresi AÇIK ve
                                               BESLEYEN YOLLARI var"
T-242b   rol DEĞİŞTİRME          ERTELENİR  ← yalnız ADIM 3'ün ertelenen
                                              yarısına bağlıydı
```

📌 `ADIM 3` bölündüğü için (`FAZ1_PLAN §5`): *yetenek modeli + default-deny* `Faz 1`,
*`UNRESTRICTED` kod dalı temizliği* **ertelenir**. `T-242b` ikincisine bağlıydı —
yani **artık hiçbir şeyi bloklamıyor**.

⚠️ **Bu task ise bloklamaya devam ediyor:** bayrak açılmadan çıkış ölçütünün `2.`
maddesi karşılanmaz, ve bayrak **beslenen bir filtre** ister — bir kez seed'lenmiş
değil, **yönetilebilir** bir kapsam.

## Açıklama

[[T-241]]'den **ayrıldı** (ürün sahibi, 2026-08-18). Sorulan soru:

> *"Bir `PLANNER`'ın kapsamı değişirse — bu `T-241`'de mi, ayrı mı?"*

**Karar: ayrı.**

```
YARATMA      bayrağı AÇAR — kapsamsız kullanıcı doğmasını engeller   → T-241
GÜNCELLEME   bir YÖNETİM YETENEĞİ — bayrağın ön koşulu DEĞİL         → BU TASK
```

> **Ürün sahibi gerekçesi:** *"Yaratma bayrağı açıyor, güncelleme bir yönetim
> yeteneği."*

📌 Ayrımın pratik sonucu: `T-241` **`P1` ve bloklayıcı**; bu task **değil**. Bayrak
açıldıktan sonra kapsamı değiştirmek gerekirse bugün tek yol **doğrudan DB** — çalışır,
ama bir ürün yeteneği değil.

## Neden yine de gerekli

`K-2.6.9` kapsamı **tenant'ın ayarlayacağı** bir şey sayıyor (`T-235`'te kaydedildi:
*"dar kapsam gerekirse tenant onu sonra ayarlar — o zaten `K-2.6.9`'un konusu"*).
Ayarlama yolu yoksa o cümle **bir sonraki boşluk** olur — ve bu projenin imza kusuru
tam olarak bu sınıf (*"mekanizma var, yol yok"*).

## ⚡ `P2` — AMA `T-235 ADIM 3`'Ü BLOKLUYOR (ürün sahibi, 2026-08-18)

```
bugün        önemsiz — UNRESTRICTED_ROLES kod dalı ADMIN'i zaten geçiriyor
ADIM 3 sonrası   o dal KALKAR → terfi eden kullanıcı ESKİ DAR KAPSAMIYLA
                 ADMIN olur
```

> **Ve o gün kusur SESSİZ olur:** kullanıcı *"neden her şeyi göremiyorum"* der, ve
> sebebi hiçbir yerde görünmez — yönü fail-closed.

Yani bu task **öncelik olarak `P2`**, ama `T-235 ADIM 3`'ün **ön koşulu**. İkisi
karıştırılmamalı: düşük öncelik ≠ bloklamıyor.

⚠️ **Ve bir kullanıcı rol değiştirirse ne olur?** `PLANNER` → `ADMIN` terfi ederse
kapsamı ne olmalı — açık kapsamı **kalır mı**, joker satıra **dönüşür mü**? Bugün
`UNRESTRICTED_ROLES` kod dalı `ADMIN`'i zaten geçiriyor, ama `T-235 ADIM 3` o dalı
kaldıracak. **O gün bu soru zorunlu hâle gelir.**

## ⚡ KAPSAM GENİŞLEDİ (2026-08-20, `T-243` turunun ölçümü)

`T-243`'ü koşarken ölçüldü: **rol değiştiren bir ARAYÜZ YOLU DA YOK.**

```
updateUserSchema (user.schema.ts:95-102)
   fullName · firstName · lastName · phoneNumber · department · jobTitle
   ⛔ `role` YOK
```

Yani `PATCH /users/:id` **rol** alanını kabul ediyor (ve `EA-001` logu + `T-241`'in
`R2` kapısı orada), ama **hiçbir ekran onu göndermiyor**.

📌 Sonuç: `R2`'nin `409`'u bugün arayüzden **tetiklenemiyor** — `T-243` ergonomik
savunmayı hazırladı (genel hata yolu), ama **davranışsal olarak sınanamadı, çünkü
tetikleyecek bir yol yok.**

⚠️ Yani bu task **iki** boşluğu birden taşıyor:

```
kapsam GÜNCELLEME    yol yok  (bu task'ın asıl konusu)
rol DEĞİŞTİRME       yol yok  (bu turda ölçüldü — aynı ekranın eksiği)
```

Ve ikisi **aynı ekranda** buluşuyor: rol değişimi kapsamı **etkilediği** için
(`R2` kapısı) ayrı ayrı tasarlanamazlar.

## ⚠️ BU TASK'IN KENDİ TARİHÇESİ BİR DERS — ters işaretin bedeli

```
2026-08-18  P2 açıldı, "bugün önemsiz" diye kaydedildi
            → çünkü YALNIZ fail-closed yön sayılmıştı (PLANNER → ADMIN)
2026-08-20  ikinci tur review: sayılmayan yön (joker → kapsamlı rol)
            FAIL-OPEN ve CANLI çıktı                       → R2
2026-08-20  T-243 turu: rol DEĞİŞTİREN arayüz yolu da YOK  → ikinci boşluk
bugün       İKİ halkayı birden blokluyor: T-235 ADIM 3 + bayrak
```

> **Ürün sahibi:** *"`P2` etiketi ters işaretin sonucuydu — ve işaret düzelince
> öncelik de değişir."*

📌 `CLAUDE.md`'nin *"bir düzeltmenin iki ekseni vardır: hedefi ve yönü. Hedef hatası
görünür; yön hatası görünmez"* maddesinin **bir task kaydında** yaşanmış hâli. Kayıt
doğru alana dokunuyordu, doğru mekanizmayı anıyordu — yalnız **işareti tersti**, ve
o yüzden iki gün boyunca `P2` olarak bekledi.

⚠️ **Ve bedeli ölçülebilir:** o iki günde `R2`'nin açtığı delik **canlıydı**
(`CATEGORY_MANAGER`, bayraktan bağımsız).

## Acceptance Criteria

- [x] ✅ **KARAR 1 — TAM DEĞİŞTİRME** (declarative replace), `Z15`.
      *"Kapsam bir KÜME DURUMUDUR, olay akışı değil."* Ekle/çıkar üç maliyet getirir:
      sıra bağımlılığı · kısmi-başarı ara durumları · ⚠️ **ve asıl önemlisi `R1`/`R2`
      guard'larının ARA DURUMLARI denetleme zorunluluğu** — `CATEGORY_MANAGER` joker
      sızıntısı tam böyle bir ara-durum vakasıydı.
      Replace'te: kontroller **tek noktada hedef durum üzerinde** · istek
      **idempotent** · kayıt doğal olarak *"eski küme → yeni küme"*.
      📌 Ekle/çıkar **ergonomisi UI'nin işi**: ekran diff'i hesaplar, tek replace
      gönderir. `T-241`'in atomik-yaratma deseniyle **simetrik**.
- [x] ✅ **KARAR 2 — BOŞALTMA izinli ama SESSİZ OLAMAZ**, `Z15`.
      Risk kural değil **kaza**: UI hatası boş dizi yollar, kullanıcı **sessizce
      kilitlenir**, ve **hiçbir test yakalamaz** — sessiz-yıkım sınıfı.
      ⚠️ Çözüm **onay diyaloğu DEĞİL** (o UI'da yaşar, **API'yi korumaz**):
      ```
      aynı uçta   intent: UPDATE | REVOKE_ALL
      doğrulama   boş küme ∧ intent ≠ REVOKE_ALL  →  RET
      ```
      **Tek uç, tek kod yolu** (`İlke 4`) — boşaltma ayrı mekanizma değil, aynı ucun
      **bilinçli-niyet doğrulaması**.
- [x] ✅ **KARAR 3 — kayıt biçimi `DENETIM_SOZLUGU.md` `Madde 1`'den gelir**, `Z15`.
      [[T-244]] onu **açar**; bu task **uygular**, yeniden tanımlamaz.
      ⚠️ **SIRA: `T-244` ÖNCE.**
- [x] ✅ Kapsam **boşaltma** mümkün, ve **açık niyet** istiyor — `KARAR 2`'nin
      `intent: REVOKE_ALL` alanı + `REVOKE_ALL`'da **zorunlu gerekçe**. Ayrı olay
      türü (`SCOPE_REVOKE_ALL`) ve `m2` ile **high-risk alarm** üretiyor.
- [x] ✅ `K-2.6.10` korunuyor — `code-reviewer` ölçtü (`§2.5` sorusu **temiz**): uç
      yalnız veriyi kuruyor, yetki kararı `AccessScopeService`'te. ⚠️ Ve `m1` ile
      **cache invalidasyonu** o servise bağlandı.
- [x] ✅ **Ölçüldü, varsayılmadı** ([[T-244]] turu → `Z16`): `K-2.11.2` olay
      gruplarını zorunlu kılıyor, kanonik biçim `DENETIM_SOZLUGU.md` `Madde 1`'de.
      Bağ **iki yönlü** (`L2` → sözlük, sözlük → `K-2.11.2`).

## ✅ KAPANIŞ (2026-08-20)

`PATCH /users/:id/scope` **canlı** — `@Roles(ADMIN)`, replace semantiği, `intent`
zorunlu, denetim kaydı sözlük `Madde 1`'den.

### `ADIM 0` bir CANLI KUSUR buldu — ve kod yazılmadan önce ölçüldü

```
PATCH /users/:id  {scope:[...]}
  status 200 · body.scope YANKILANIYOR · user_scopes DEĞİŞMEMİŞ
```

`UpdateUserDto extends PartialType(CreateUserDto)` `scope`'u miras alıyordu,
`update()` ise `create()`'in yaptığı ayıklamayı yapmıyordu → `Object.assign` ile
entity'ye konup TypeORM tarafından **sessizce düşürülüyordu**. `OmitType` ile
çıkarıldı → artık `400`.

📌 Şıkkı **seçmedik, ölçtük** — brief üç şıkkı da sonucuyla yazmıştı.

### `code-reviewer`: 2 BLOCKER · 4 MAJOR · 5 MINOR · 2 NIT — hepsi kapandı

| # | bulgu | sonuç |
|---|---|---|
| `B1` | `UNIQUE` NULL'ları **ayırt etmiyor** — *"create zaten korur"* iddiası yanlış | migration **`1810`** (`NULLS NOT DISTINCT`) + üç yanlış gerekçe düzeltildi + `POST /users`'ın sessiz davranış değişikliği **pinlendi** |
| `B2` | denetim kaydının **sıfır test kapsamı** | 9 yeni unit test; **iki takas mutasyonu da kırmızı** |
| `M1` | `GRANT UPDATE` tablo düzeyinde | **kolon düzeyi**: `created_by,updated_by,is_active,updated_at` |
| `M2` | reaktivasyonda `createdBy` bayat | `createdBy = actorId` — reaktivasyon **yeni bir verme** |
| `M3` | idempotence testi ayırt etmiyor | `id`/`created_at`/`updated_at` de okunuyor |
| `M4` | izin envanteri bayat | `docs/verification/` güncellendi (`S3 tur 22`) |
| `m1` | `clearCache` **0 üretim çağıranı** | commit sonrası çağrılıyor |
| `m2` | `SCOPE_REVOKE_ALL` high-risk değil | listeye eklendi |

### İki ölçüm-tasarımı yakalaması (kaydediliyor, çünkü tekrarlanabilir)

**1 · `M1`'in kolon listesi İKİ FARKLI AKTÖRLE ölçüldü.** Tek aktörle ölçülseydi
TypeORM'un diff-tabanlı `UPDATE`'i `created_by`'ı `SET`'ten atlardı ve liste
**eksik** çıkardı — yani `GRANT` fazla dar olur, reaktivasyon üretimde patlardı.
`§2.7`'nin *"fixture ayırt etmek istediği iki tarafta FARKLI değer taşımalı"*
kuralının **izin ölçümüne** uygulanmış hâli.

**2 · Paylaşılan fixture sabiti sızıntısı** (`§2.7 #4`): bir testin mutasyonu bir
sonraki testin *"önceki"* durumuna sızıyordu — `makeExistingActiveRow()`
factory'sine çevrildi.

### ⚠️ Kaydedilen TETİKLEYİCİ — `logAdminAction`'ın çağrı sözleşmesi kırılgan

```
12 pozisyonel argüman, ve hiçbir test onları ayırt etmiyordu
T-244  M2/M4 körlüğü      birinci vaka
T-242a B2 körlüğü         İKİNCİ vaka — aynı serviste
```

Kısa vadeli çözüm uygulandı: testler argümanları **ada göre**, `toBe` ile pinliyor.

> **Uzun vadeli kalem (ürün sahibi, 2026-08-20): `logAdminAction` pozisyonel değil
> NESNE parametresi almalı.** ⛔ **Bu turda YAPILMADI.** Tetikleyici: **üçüncü vaka
> çıkarsa** [[T-247]]'ye komşu bir task açılır. *"İki vaka bir desendir"* — üçüncüsü
> sözleşmenin kendisinin değişmesini gerektirir.

### Kapılar

```
tsc 0 · lint 0 · unit 1152/1152 (66 suite) · e2e 334/334 (23 suite)
T-047/T-060 invaryantı PASS (35→35) · guards 0 bulgu · money-float --ratchet 0
```

## Done tanımı

- [x] ✅ `1152/1152` unit · `334/334` e2e · `T-047` invaryantı PASS (`35→35`)
- [x] ✅ `guards` 0 bulgu · `money-float --ratchet` 0 · `lint-ratchet` temiz · `tsc` 0 · `lint` 0
- [x] ✅ `PATCH /users/:id/scope` · `@Roles(ADMIN)` · e2e kapsamı var
- [x] ✅ Bir tur: 2 BLOCKER · 4 MAJOR · 5 MINOR · 2 NIT — **hepsi kapandı**, ikisi ürün sahibi kararına gitti (`§2.4`)
- [x] ✅ Sekiz yola genişletildi (`docs/verification/` dâhil — meta repo)
