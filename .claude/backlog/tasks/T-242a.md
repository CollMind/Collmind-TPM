---
id: T-242a
title: Kapsam GÜNCELLEME yolu — FAZ 1 (çıkış ölçütü madde 2: filtreyi BESLEYEN yol)
epic: E-001
sprint: S-001
status: todo
assignee: architect
created: 2026-08-18
updated: 2026-08-20
touches:
  - collmind.backend/src/modules/user/
---

## ⚡ BÖLÜNDÜ (ürün sahibi, 2026-08-20) — ve bu yarı `FAZ 1`

```
T-242a   kapsam GÜNCELLEME       FAZ 1      ← çıkış ölçütü madde 2:
         (BU TASK)                            "kapsam filtresi AÇIK ve
                                               BESLEYEN YOLLARI var"
T-242b   rol DEĞİŞTİRME          ERTELENİR  ← yalnız ADIM 3'ün ertelenen
                                              yarısına bağlıydı
```

📌 `ADIM 3` bölündüğü için (`FAZ1_PLAN §5`): *yetenek modeli + default-deny* `Faz 1`,
*`UNRESTRICTED` kod dalı temizliği* **ertelenir**. `T-242b` ikincisine bağlıydı —
yani **artık hiçbir şeyi bloklamıyor**.

⚠️ **Bu task ise bloklamaya devam ediyor:** bayrak açılmadan çıkış ölçütünün `2.`
maddesi karşılanmaz, ve bayrak **beslenen bir filtre** ister — bir kez seed'lenmiş
değil, **yönetilebilir** bir kapsam.

## Açıklama

[[T-241]]'den **ayrıldı** (ürün sahibi, 2026-08-18). Sorulan soru:

> *"Bir `PLANNER`'ın kapsamı değişirse — bu `T-241`'de mi, ayrı mı?"*

**Karar: ayrı.**

```
YARATMA      bayrağı AÇAR — kapsamsız kullanıcı doğmasını engeller   → T-241
GÜNCELLEME   bir YÖNETİM YETENEĞİ — bayrağın ön koşulu DEĞİL         → BU TASK
```

> **Ürün sahibi gerekçesi:** *"Yaratma bayrağı açıyor, güncelleme bir yönetim
> yeteneği."*

📌 Ayrımın pratik sonucu: `T-241` **`P1` ve bloklayıcı**; bu task **değil**. Bayrak
açıldıktan sonra kapsamı değiştirmek gerekirse bugün tek yol **doğrudan DB** — çalışır,
ama bir ürün yeteneği değil.

## Neden yine de gerekli

`K-2.6.9` kapsamı **tenant'ın ayarlayacağı** bir şey sayıyor (`T-235`'te kaydedildi:
*"dar kapsam gerekirse tenant onu sonra ayarlar — o zaten `K-2.6.9`'un konusu"*).
Ayarlama yolu yoksa o cümle **bir sonraki boşluk** olur — ve bu projenin imza kusuru
tam olarak bu sınıf (*"mekanizma var, yol yok"*).

## ⚡ `P2` — AMA `T-235 ADIM 3`'Ü BLOKLUYOR (ürün sahibi, 2026-08-18)

```
bugün        önemsiz — UNRESTRICTED_ROLES kod dalı ADMIN'i zaten geçiriyor
ADIM 3 sonrası   o dal KALKAR → terfi eden kullanıcı ESKİ DAR KAPSAMIYLA
                 ADMIN olur
```

> **Ve o gün kusur SESSİZ olur:** kullanıcı *"neden her şeyi göremiyorum"* der, ve
> sebebi hiçbir yerde görünmez — yönü fail-closed.

Yani bu task **öncelik olarak `P2`**, ama `T-235 ADIM 3`'ün **ön koşulu**. İkisi
karıştırılmamalı: düşük öncelik ≠ bloklamıyor.

⚠️ **Ve bir kullanıcı rol değiştirirse ne olur?** `PLANNER` → `ADMIN` terfi ederse
kapsamı ne olmalı — açık kapsamı **kalır mı**, joker satıra **dönüşür mü**? Bugün
`UNRESTRICTED_ROLES` kod dalı `ADMIN`'i zaten geçiriyor, ama `T-235 ADIM 3` o dalı
kaldıracak. **O gün bu soru zorunlu hâle gelir.**

## ⚡ KAPSAM GENİŞLEDİ (2026-08-20, `T-243` turunun ölçümü)

`T-243`'ü koşarken ölçüldü: **rol değiştiren bir ARAYÜZ YOLU DA YOK.**

```
updateUserSchema (user.schema.ts:95-102)
   fullName · firstName · lastName · phoneNumber · department · jobTitle
   ⛔ `role` YOK
```

Yani `PATCH /users/:id` **rol** alanını kabul ediyor (ve `EA-001` logu + `T-241`'in
`R2` kapısı orada), ama **hiçbir ekran onu göndermiyor**.

📌 Sonuç: `R2`'nin `409`'u bugün arayüzden **tetiklenemiyor** — `T-243` ergonomik
savunmayı hazırladı (genel hata yolu), ama **davranışsal olarak sınanamadı, çünkü
tetikleyecek bir yol yok.**

⚠️ Yani bu task **iki** boşluğu birden taşıyor:

```
kapsam GÜNCELLEME    yol yok  (bu task'ın asıl konusu)
rol DEĞİŞTİRME       yol yok  (bu turda ölçüldü — aynı ekranın eksiği)
```

Ve ikisi **aynı ekranda** buluşuyor: rol değişimi kapsamı **etkilediği** için
(`R2` kapısı) ayrı ayrı tasarlanamazlar.

## ⚠️ BU TASK'IN KENDİ TARİHÇESİ BİR DERS — ters işaretin bedeli

```
2026-08-18  P2 açıldı, "bugün önemsiz" diye kaydedildi
            → çünkü YALNIZ fail-closed yön sayılmıştı (PLANNER → ADMIN)
2026-08-20  ikinci tur review: sayılmayan yön (joker → kapsamlı rol)
            FAIL-OPEN ve CANLI çıktı                       → R2
2026-08-20  T-243 turu: rol DEĞİŞTİREN arayüz yolu da YOK  → ikinci boşluk
bugün       İKİ halkayı birden blokluyor: T-235 ADIM 3 + bayrak
```

> **Ürün sahibi:** *"`P2` etiketi ters işaretin sonucuydu — ve işaret düzelince
> öncelik de değişir."*

📌 `CLAUDE.md`'nin *"bir düzeltmenin iki ekseni vardır: hedefi ve yönü. Hedef hatası
görünür; yön hatası görünmez"* maddesinin **bir task kaydında** yaşanmış hâli. Kayıt
doğru alana dokunuyordu, doğru mekanizmayı anıyordu — yalnız **işareti tersti**, ve
o yüzden iki gün boyunca `P2` olarak bekledi.

⚠️ **Ve bedeli ölçülebilir:** o iki günde `R2`'nin açtığı delik **canlıydı**
(`CATEGORY_MANAGER`, bayraktan bağımsız).

## Acceptance Criteria

- [ ] Güncelleme ucunun şekli karara bağlanır (tam değiştirme mi, ekle/çıkar mı?)
      ⚠️ `R-1` *pair semantiği*: `cplIds[] × categoryIds[]` **düzleştirme YASAK** —
      uç bu tuzağa düşmemeli (`access-scope.service.ts` başlığı)
- [ ] **Rol değişimi** ile etkileşimi tanımlanır (yukarıdaki soru)
- [ ] Kapsam **boşaltma** mümkün mü? `K-2.6.8a`: boş kapsam = **erişim yok** — yani
      meşru ama **yıkıcı**; açık onay istemeli mi?
- [ ] `K-2.6.10` korunur: uç veriyi kurar, yetki kararı `AccessScopeService`'te kalır
- [ ] Denetim izi: kapsam değişikliği `K-2.11.2`'nin olay gruplarına giriyor mu —
      **ölçülmeli**, varsayılmamalı

## Done tanımı

- [ ] Testler yeşil (unit + ilgili e2e)
- [ ] `npm run guards` exit 0 · `money-float --ratchet` exit 0
- [ ] **Üretim çağrı yolu var** — HTTP rotası + RBAC + e2e
- [ ] `code-reviewer` onayı
- [ ] `touches:` güncel
