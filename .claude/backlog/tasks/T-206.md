---
id: T-206
title: "`sales_actuals`'ta SKU/hacim yokluğunun gerekçesi ölçülsün — veri kaynağı sınırı mı, domain kararı mı?"
epic: E-001
sprint: S-001
status: done
assignee: architect
priority: P2
created: 2026-08-12
updated: 2026-08-13
touches:
  - docs/brd-v2/03_IS_KURALLARI/
  - collmind.backend/src/database/entities/sales-actual.entity.ts
migration_seq:
---

## Bulgu (`docs/analysis/0069 §F16` · `_ISSUE_B_DALGASI.md §4`)

`sales_actuals` grain'i **CPL × Kategori × Kanal × Dönem**; `sku`/`quantity`/`volume`
kolonu **yok**. Entity gerekçeyi yazıyor:

> *"FU/SKU ve hacim boyutu YOKTUR — **Wella actuals CSV'sinde** `fu_code`/`volume`
> kolonları bulunmuyor."*

## ⛔ Soru: bu bir VERİ KAYNAĞI sınırı mı, bir DOMAIN kararı mı?

| okuma | sonucu |
|---|---|
| **veri kaynağı sınırı** (ERP veremiyordu) | `A2` ayakta; `B` dalgasına kolon eklenir, kaynak gelince dolar |
| **pilot profili kararı** (*"o müşterinin CSV'sinde yoktu"*) | karar **tenant profiline** iner (`İlke 5`); ürün kuralı bu değil |
| **gerçek domain kararı** (*"actuals tutar agregasıdır"*) | kaynak değişse de **model değişmez**; `A2`'nin tabanı yeniden karara gider |

## ✅ ÖLÇÜLDÜ VE KAPANDI (2026-08-13) — sınıf **`pilot profili`**

> Tam kayıt: `docs/analysis/0070-b-dalgasi-olcum-turu-ttm-kanitiyla.md §B1`

**`Done` şartı sessiz taşımayla değil, ÖLÇÜMLE kapandı.** `CollMind/TTM` klonlandı
(`19c6376`) ve iddia doğrudan doğrulandı:

```
actuals.service.ts:991   if (!row.fu_code?.trim()) throw new Error('fu_code is required')
actuals.service.ts:1045  volume === 0 && gross > 0 → throw
apps/web/public/templates/actuals_template.csv
  cpl_code,fu_code,gross_amount,net_amount,discount_amount,volume     ← KANONİK
TTM'de: 22 actuals CSV · 16'sı fu_code+volume · 4'ü CTPM biçimi (eski test verisi)
```

**Sınıf: `pilot profili`.** *"Kaynak sınırı"* elendi — kardeş ürün aynı veriyi **zorunlu
tutuyor**. *"Domain kararı"* elendi — ürün ilkesi olsaydı TTM'in kanonik şablonu onu ihlal
ederdi.

→ `A2` / `K-2.1.8a` **ayakta ve uygulanabilir**; karar `İlke 5` gereği **tenant profiline**
iner. `B` dalgasının `F16` kalemi yeniden yazıldı.

---

## Ölçümün gövdesi (2026-08-13)

```
gerekçe metni    sales-actual.entity.ts:7-11
ne zaman         dd7eaaf · 2026-07-27 · T-020 (dosyayı YARATAN commit)
hangi bağlam     docs/analysis/0002 §Kritik bulgu
```

> *"**Wella CSV başlığı:** `cpl_code,category,channel_code,gross_amount,net_amount,discount_amount`
> → `fu_code` YOK, `volume` YOK. **TTM `validateRow` bunları zorunlu tutuyor**; yani TTM
> validator'ı bu veriyi tümden reddederdi."*

Fixture doğruluyor: `seeds/data/actuals_2026-01.csv` başlığı **birebir** o altı kolon.

| kanıt | ne diyor |
|---|---|
| gerekçe **tek bir müşterinin CSV başlığı** | ürün geneli değil |
| **TTM validator'ı bu kolonları ZORUNLU tutuyordu** | kardeş üründe veri **vardı** |
| ERP'nin veremediğine dair **tek cümle yok** | *"kaynak sınırı"* iddiası **yazılı değil** |

✅ **O kalan doğrulama YAPILDI** (yukarıya bak): atıf `:989, 1046-1053` idi, gerçek satırlar
`:991` ve `:1045` — **kayma var ama iddia doğru.** Belge iddiası artık ölçülmüş bir iddia.

**Ve iki kural bu ayrıma bağlı:**

1. **`K-2.1.8a`** (`A2`) dağıtımı **geçmiş SKU hacim payına** bağlıyor → bugünkü modelle
   **beslenemez**
2. **`K-2.13.14h3`** (`A4`, net taban) ile aynı entity'nin ikinci sınırı kesişiyor:
   *"`discountAmount` asla bütçeye/ledger'a yazılmaz — on-invoice zaten yazıyor, tekrar
   kullanılırsa **çift sayım**"* (T-003/T-017'nin kökü)

## Yapılacak
- [x] `T-020`'nin tasarım dokümanındaki *"Kritik bulgu"* bölümü okundu — gerekçe orada
- [x] Sonuç `L2`'ye not olarak girdi — `K-2.1.8a` altına (atıfla)
- [x] `A2` **uygulanabilir**: taban değişikliği gerekmiyor, gereken şey kaynak genişlemesi

## ⚡ [[T-209]] ile **tek oturumda** koşulur

Gerekçe `K-2.1.19a`'da yazılı — aynı sorunun iki yüzü:

```
T-206   neden SKU kırılımı ve hacim yok?
T-209   discount_amount hangi olayı taşıyor?
ortak   gerçekleşen satış tablosunun SÖZLEŞMESİ nedir?
```

Ve **yöntemleri aynı:** `git blame` + commit bağlamı + alanı besleyen içe aktarma yolunun
**kaynak kolonu**.

> **Ortak sonuç senaryosu:** ikisi de *"dönemsel satış özeti olarak tasarlandı"* çıkarsa,
> `K-2.1.19` ön kararı **doğrulanır** ve iki task **tek kararla** kapanır.

✅ **`B` dalgası onayının ön koşulu KALMADI** — bu task kapandı.

## Done tanımı
- [x] Gerekçenin türü **ölçülmüş** ve yazılı — `pilot profili`
- [x] TTM'in `validateRow` iddiası **doğrulandı** (ölçülmedi değil)
- [x] `K-2.1.8a`'ya atıflı not eklendi
- [x] `A2`'nin uygulanabilirliği karara bağlandı — **ayakta**
- [ ] ⏸️ `K-2.1.19a`'nın *"iki yüz"* iddiası — [[T-209]] kapanmadan doğrulanamaz;
      **bu task'ı bloklamıyor**, orada izleniyor
