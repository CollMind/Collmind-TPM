---
id: T-089
title: "Birleşik indirim tavanı PERCENT mekaniklerde HİÇ ÇALIŞMIYOR — akümülatör string birleştiriyor"
epic:
sprint:
status: review
assignee: debugger
priority: P1
created: 2026-08-05
updated: 2026-08-05
touches:
  - collmind.backend/src/modules/shared/spend-calculation/spend-validation.service.ts
  - collmind.backend/src/modules/shared/spend-calculation/spend-validation.service.spec.ts
migration_seq:
---

## Sınıf: var olan ama hiç çalışmamış koruma

`postingDate` bulgusuyla ("on-invoice özelliği bugüne kadar tek bir ledger satırı bile
yazamamış") **aynı sınıf.** Kod var, test yeşil, koruma hiç ateşlememiş.

**Fark edilme yolu yok:** hata vermiyor · yanlış sonuç göstermiyor · sadece hiç çalışmıyor.

## Kök neden

`spend-validation.service.ts` — `validateCombinations` içindeki akümülatör döngüsü:

```ts
let totalOnInvoiceDiscount = 0;                 // number
...
const entered = readEnteredValue(pmv, mechanic); // ← STRING (transformer'sız decimal kolon)
if (mechanic.mechanicType === 'PERCENT') {
  totalOnInvoiceDiscount += entered;             // ← TOPLAMA DEĞİL, BİRLEŞTİRME
}
```

`0 + "10.0000"` → `"010.0000"`. Akümülatör ilk PERCENT mekanikte **string'e dönüyor.**

**Ölçüldü (node, ampirik):**

```
1 mekanik (%10)   → "010.0000"
2 mekanik (+%5)   → "010.00005.0000"
combinedDiscount  → "010.00005.00000"
combined > 30     → false          ← Number("010.00005.00000") = NaN, NaN > n hep false
combined.toFixed(1) → TypeError: not a function
```

## Etkilenen dört koruma — hepsi sessizce devre dışı

| Koruma | Bugün |
|---|---|
|  `MAX_ON_INVOICE_DISCOUNT` (50) | hiç ateşlemiyor |
| `MAX_OFF_INVOICE_DISCOUNT` (30) | hiç ateşlemiyor |
| **`MAX_COMBINED_DISCOUNT` (60)** — sınıf sabiti, her koşumda çalışır | hiç ateşlemiyor |
| mekanik-başı `maxCombinedDiscountPercentage` | hiç ateşlemiyor |

Bir planner **%10 + %5 + %90** girse birleşik indirim kontrolü **sessizce geçer.**

⚠️ Yalnız **PERCENT** dalı kirletiyor. Diğer dal (`:280`, `:294`) `(pmv.calculatedSpend /
totalPlannedGsv) * 100` hesaplıyor — bölme/çarpma coerce ettiği için **sayı** üretiyor ve
temiz. Yani kusur mekanik tipine bağlı, ve seed'deki üç PERCENT mekaniği
(`CPP_ON_PCT`, `CPP_OFF_PCT`, `MEC-DISCOUNT`) tam olarak bu dala düşüyor.

## ⚠️ GİZLİ BOMBA: düzeltme dört `.toFixed` çağrısını İLK KEZ çalıştıracak

Dört `.toFixed` çağrısı — her biri yalnız kendi kontrolü ateşlediğinde koşuyor. Kontroller
hiç ateşlemediği için bu satırlar **bugüne kadar hiç çalışmadı.**

Karşılaştırma sayısallaşınca kontroller çalışmaya başlayacak ve `.toFixed` **ilk kez**
koşacak. Akümülatör hâlâ string olsaydı orada `TypeError` → **500** olurdu.

**Aynı task'ta çözülmeli.** Düzeltmenin doğru sırası: önce akümülatörü sayı yap, sonra
karşılaştırmaların çalıştığını doğrula, sonra `.toFixed`'lerin çalıştığını doğrula. Üçü ayrı
test istiyor — özellikle `.toFixed` yolları, çünkü onlar bu düzeltmeye kadar ölü koddu.

## Plan seviyesine de yayılıyor

`validateBeforeSubmission`'ın rollup'ı bu değerleri FU'lar arasında topluyor: `reduce((sum, cv) => sum + cv.X, 0) / length`.
Ölçüldü:

| FU'daki PERCENT mekanik sayısı | Plan seviyesi sonuç |
|---|---|
| 1 | **10** (tesadüfen doğru — `Number("0010.0000")` = 10) |
| 2+ | **NaN** |

Yani tek mekanikli planlarda doğru görünüyor, çok mekanikli planlarda `NaN` dönüyor. Bu,
kusurun neden bugüne kadar fark edilmediğinin bir parçası olabilir.

## ⚠️ DÜZELTME NOKTASI: dönüşüm okuma yerinde DEĞİL, akümülatöre girerken

`src/common/numeric/`'in yardımcıları (`rateFromNumericString`) kullanılacak — hem ratchet
tabanı artmaz (E15: dedektör orada ateşlemiyor) hem F1'in yatırımı kullanılır.

**Ama yardımcı tek başına yetmez.** Asıl kusur akümülatörün **başlangıcı ve tipi**, tek tek
okumaların değil. Yanlış yerde dönüştürülürse semantik korunur ama **tip hâlâ karışır** —
örneğin her `entered` okumasında dönüştürüp akümülatörü olduğu gibi bırakmak sorunu çözmez
görünürken çözer, ama başka bir string kaynağı eklendiğinde sessizce geri döner.

Dönüşüm, **değerin akümülatöre girdiği noktada** ve tipi orada sabitlenerek yapılmalı.

## ⚠️ BU DOSYANIN HİÇ SPEC'İ YOK — ve bu tesadüf değil

Ölçüldü: `spend-validation.service.ts` için **hiçbir spec dosyası yok**, ve `validate-inputs`
rotasına dokunan **hiçbir test yok**. Dosyada 10 ratchet bulgusu ve **üç kusur sınıfı** var
(bu task + [[T-085]]'in iki kusuru), sıfır test.

**Test edilmeyen dosya, kusur biriktiren dosyadır.** Ve bu dosya canlı bir rotada.

## Acceptance Criteria

- [x] `totalOnInvoiceDiscount` / `totalOffInvoiceDiscount` / `combinedDiscount` **her zaman
      sayı** — mekanik tipinden bağımsız
- [x] Dört tavanın **her biri ayrı testle** ateşliyor — biri diğerini kanıtlamıyor
- [x] Dört `.toFixed` yolunun hepsi **koşuluyor ve patlamıyor** — mutasyonda tam bu dördü
      `TypeError: toFixed is not a function` ile kırıldı, yani gerçekten ilk kez çalıştılar
- [x] Plan seviyesi rollup (`validateBeforeSubmission`) çok mekanikli FU'da **NaN dönmüyor** —
      ölçüldü: 10+5 → **15**. Ö2 sorusunun cevabı: rollup'ta bağımsız string kaynağı yoktu,
      FU seviyesi düzelince rollup da düzeldi (varsayılmadı, testle doğrulandı)
- [x] Ratchet: `spend-validation.service.ts` tabanı **10'da kaldı** — `src/common/numeric/`
      yardımcıları kullanılarak. Artış gerekiyorsa **DUR ve bildir.**
- [x] **`spend-validation.service.spec.ts` oluşturuldu** — dosyanın ilk spec'i, 7 test.
      Paylaşılan fixture'lar (`createHarness`/`makeMechanic`/`buildPmv`/`buildPlanFu`)
      [[T-085]] ve [[T-088]] tarafından yeniden kullanılabilir şekilde kuruldu.
- [x] **Mutasyon kanıtı:** dönüşümü geri al → **7/7 test kırmızı**. Dördü
      `TypeError: toFixed`, ikisi birleştirme (`Expected 40, Received "040.0000"`), biri
      rollup NaN. Mutasyon dosya içeriğinden doğrulandı (§2.7 #7).

## Done tanımı
- [ ] Testler yeşil (unit + e2e), 3× reset'siz
- [ ] `npm run guards` exit 0 · `money-float.sh --ratchet` exit 0, `plan.service.ts` **36**
- [ ] `code-reviewer` onayı
- [ ] Üretim çağrı yolu var (`GET /spend-calculation/validate-inputs/:planFuId`)
- [ ] `touches:` güncel


---

## ⚠️ SATIR NUMARASI KULLANMA — bu task'ın kendi dersi

İlk yazımında bu task metod adlarını **yanlış** verdi (`validateCombinationRules` → gerçekte
`validateCombinations`; `validateForSubmission` → gerçekte `validateBeforeSubmission`) ve satır
numaraları düzeltmenin eklediği yorum bloğuyla kaydı.

`qa-engineer` ikisini de yakaladı ve davranışı satır numarasına değil **git-stash ile canlı
doğrulamaya** dayandırdı — doğru refleks.

Ders: task metninde **metod adı** yaz, satır numarası yazacaksan "civarı" de. Satır numarası
kendi düzeltmen tarafından geçersiz kılınır.
