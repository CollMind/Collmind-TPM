---
id: T-060
title: T-047 invaryantı kör: approval_requests 9.116 öksüz satır biriktirmiş
epic: E-001
sprint: S-001
status: done
assignee: qa-engineer
created: 2026-08-02
updated: 2026-08-02
---

## Açıklama
**Ölçüm (Team Lead):** `approval_requests` = **9.116 satır**, `plans` = **0**. Yani her e2e koşumu kalıcı satır bırakıyor ve [[T-047]] invaryantı bunu **görmüyor** — invaryant yalnız agreements/plans/plan_fus/plan_skus sayıyor.

**Neden önemli:** bu invaryant, oturumun tüm doğrulama zeminini oluşturuyor (3 ardışık reset'siz koşum protokolü). Kör olduğu her tablo, sızıntının fark edilmeden birikebildiği bir yüzey. [[T-047]]'nin kendisi de tam bu sebeple açılmıştı — aynı hata bir seviye ötede tekrarlamış.

**Yapılacak:**
- İnvaryantı ilgili tüm tablolara genişlet (approval_requests dahil; hangi tabloların kapsanması gerektiğini **ölçerek** belirle, ezbere liste yapma)
- 9.116 öksüz satırı temizle
- Teardown'ın neden bırakmadığını kök nedeniyle bul (soft-delete mi, FK cascade eksikliği mi — [[T-051]] dersi: ölç, tahmin etme)
- **Mutasyon kanıtı:** kasıtlı bir satır sızdır → invaryant KIRMIZI olmalı

**Ön koşul niteliği:** [[T-056]]'nın 7 adımı bu zemin üzerinde doğrulanacak; önce zemin onarılmalı.

## İlgili
- Tasarım: `docs/analysis/0009-submit-yolu-yakinsamasi-tasarimi.md` · Karar: `docs/decisions/0005-*`


## Sonuç (2026-08-02) — DONE

**Kapsam ölçülerek belirlendi** (ezbere liste değil): tek koşumun öncesi/sonrası **39 tabloda**
karşılaştırıldı; yalnız 3 tablo sıfırdan farklı delta verdi.

| Tablo | Koşum başına delta | Birikmiş öksüz |
|---|---|---|
| `approval_requests` | +38 | **9.154** (3.673 PLAN + 5.481 AGREEMENT, %100 öksüz) |
| `admin_audit_logs` | +6 | **3.167** |
| `users` | +1 | **289/298** |

**Kök nedenler (kanıtlı):**
- `approval_requests`: `entity_id` **FK'siz, polimorfik**; `cleanupTestPlans`/`cleanupTestAgreements`
  bu tabloyu hiç kapsamıyordu
- `admin_audit_logs`: `reversal.service.ts:170-176` her reverse'te FK'siz audit satırı yazıyor
- `users`: role-journey N9 `POST /users` ile kullanıcı yaratıyor, `DELETE /users` ucu yok

**Ajanın kendi mutasyonu ikinci bir gerçek sızıntı buldu:** `cleanupTestTransactions`,
`cleanupTestAgreements`'tan **önce** çalışıp `agreement_transactions` satırını erken siliyor,
sonraki audit temizliği o id'yi bulamıyordu. Mutasyondan bağımsız, yaşayan hata — düzeltildi.

**Temizlik (SELECT-önce-DELETE):** `approval_requests` 9.154→0, `admin_audit_logs` 3.179→12
(kalan yalnız meşru SalesActualBatch), `users` 298→9 (yalnız gerçek seed hesapları).
Korunanlar doğrulandı: `agreements=3`, `budget_tx=4`, NKA-Q1 ve NKA-Q2 75.000.

## Team Lead bağımsız doğrulama
- **İlk mutasyon denemem GEÇERSİZDİ** — regex'im gerçek kodu değil bir **yorum satırını** kapattı;
  invaryantın geçmesi beklenen sonuçtu, hiçbir şey kanıtlamıyordu. Ayrıca `EXIT=$?` boru hattında
  `tail`'in kodunu okuyordu. (Bu oturumda ikinci kez yaptığım hata sınıfı — kayıt için.)
- **İkinci deneme geçerli:** `seed-e2e.ts:293`'teki gerçek `DELETE FROM main.approval_requests`
  devre dışı → `tsc` temiz, **JEST EXIT=1**, invaryant doğru tabloyu doğru sayıyla raporladı
  (`approvalRequests: 0 -> 21`)
- **🔴 Önemli gözlem:** mutasyon koşumunda **testler 227/227 YEŞİL ama exit code 1** — invaryant
  `globalTeardown`'dan fırlıyor. "Tests: passed" satırına bakmak **tek başına yetmez**;
  doğrulamalarda exit code okunmalı.
- Temiz zeminde: **JEST EXIT=0**, 227/227, invaryant PASS, sayılar sabit
  (`approval_requests=0 admin_audit_logs=12 users=9`)
- 3 ardışık reset'siz koşum (flag kapalı/açık/kapalı): üçünde de 227/227 + PASS + NKA-Q1 75.000

## Ders
[[T-047]]'yi "sızıntı fark edilmeden birikiyor" diye açmıştım; kapsamını 4 tabloyla sınırlı
tutunca **aynı hata bir seviye ötede tekrarladı** ve ~9.000 satır birikti. İnvaryantın kapsamı
artık ölçümle belirleniyor ve gerekçesi `e2e-row-count.js` içinde belgeli.
