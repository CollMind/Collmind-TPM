---
id: T-307
title: CANLI CROSS-TENANT — bir kiracının ADMIN'i başka kiracıyı okuyabilir/silebilir
epic:
sprint:
status: done
assignee: backend-engineer
created: 2026-08-27
updated: 2026-08-27
touches:
  - collmind.backend/src/modules/tenant/tenant.controller.ts
  - collmind.backend/src/modules/tenant/tenant.service.ts
  - collmind.backend/src/modules/tenant/tenant.service.spec.ts
  - collmind.backend/test/tenant-cross-tenant-isolation.e2e-spec.ts
  - collmind.backend/test/tenant-capability-boundary.e2e-spec.ts
  - collmind.backend/scripts/guards/lint-ratchet-baseline.txt
  - collmind.frontend/src/routes/index.tsx
  - collmind.frontend/src/components/layout/Sidebar.tsx
  - collmind.frontend/src/components/common/EnumBadge.tsx
  - collmind.frontend/src/types/index.ts
  - collmind.frontend/tests/mocks/handlers.ts
  - "collmind.frontend: 14 dosya SİLİNDİ (tenants yüzeyi — bkz. Z46 §1a)"
  - docs/process/B3A_EK3_ROTA_HUCRE_ESLEMESI.tsv
---

## ⛔ BULGU — `ADIM 5` `RLS` ölçüm turu, envanter kalemi 6

```
GET    /tenants        → tenantService.findAll()       ← TÜM kiracıları döner
GET    /tenants/:id    → findOne(id)                   ← çağıranın tenant'ıyla KARŞILAŞTIRMA YOK
PATCH  /tenants/:id    → update(id, dto)               ← "
DELETE /tenants/:id    → remove(id)                    ← "
+ /activate · /suspend · /stats                        (sekiz rota)
```

**Ölçüldü (Team Lead, bağımsız):**
```
tenant.service.ts + tenant.controller.ts → "tenantId|TenantId|CurrentUser" = 0
POZ. KONTROL: aynı grep customer.service.ts'te = 37   ⇒ araç kör değil
sekizinin yeteneği: TENANT_READ | TENANT_WRITE → ROLE_CAPABILITIES'te {ADMIN}
```

⛔ **`ADMIN` bu üründe KİRACI-İÇİ bir roldür.** ⇒ Bir kiracının `ADMIN`'i, **başka bir
kiracıyı okuyabilir, değiştirebilir ve SİLEBİLİR.**

## ⛔ NEDEN BUGÜNE KADAR GÖRÜNMEDİ

```
main.tenants = 1
```

> ***"Verinin yokluğu örter"*in DERS KİTABI VAKASI.** Kusur bir hata dalında değil,
> **varsayılan hâlde** — ve ikinci kiracı geldiği gün **kendiliğinden** canlanır,
> bir düzeltme turu olmadan.

## ⛔ VE BU BİR `RLS` SORUSU DEĞİL — `RLS` BUNU ÇÖZEMEZ

`tenants` tablosu **`tenant_id` taşımıyor** (ölçüldü: `main`'de `tenant_id` taşımayan
dört tablodan biri) ⇒ **kiracı→satır politikası YAZILAMAZ**. `Z44`/`K3`'ün *"kapsam
uygulamada kalır"* cümlesinin **karşılanmadığı tek yer**.

⇒ Çözüm **uygulama katmanında**: ya `ADMIN` kendi tenant'ı dışına çıkamaz, ya da bu
aile **platform-operatörü** yüzeyine taşınır (yeni bir yetenek/rol kararı — ürün
sahibi).

## Acceptance Criteria

- [ ] ⛔ **REPRO PİNİ ÖNCE** (`T-273` disiplini · `Z44 §7`): **iki-tenant** fixture,
      `T1`'in `ADMIN`'i `T2`'yi **GÖRÜYOR** — kusur **görülür**, sonra düzeltilir
- [ ] Ürün sahibi hükmü: **daraltma** mı (`ADMIN` kendi tenant'ıyla sınırlı) yoksa
      **platform-operatörü yüzeyi** mi
- [ ] Davranış pini **iki-girdi-iki-çıktı**: `T1-ADMIN → T1` `200` · `T1-ADMIN → T2` `403`
      ⛔ **Boş sonuç FARK DEĞİLDİR** — `T2` gerçek satır taşımalı
- [ ] `Z44 §2` kaydı güncellenir: `K3`'ün *"kapsam uygulamada kalır"* cümlesi bu aileyi
      **adıyla** kapsar

## Done tanımı
- [ ] Testler yeşil · `npm run guards` exit 0 · `code-reviewer` onayı


---

## ⛔ AÇIK KALEM (review `S7`) — `create` daraltılmadı, `findAll` daraltıldı

`create()` **bilinçli** kapsam dışı (`Z45 §2` madde 2 → ürün sahibine `DUR`). Ama
`findAll` daraltıldığı için **ölçülebilir bir tutarsızlık** doğdu:

```
useCreateTenant → onSuccess: invalidateQueries(tenantKeys.lists())
GET /tenants    → artık YALNIZ çağıranın kendi kiracısını döner
⇒ YARATILAN KİRACI LİSTEDE HİÇ GÖRÜNMEZ, ve HATA DA YOK — SESSİZ KAYBOLMA
```

⚠️ Aynı tur içinde bir uç daraltılıp kardeşi daraltılmayınca **UI yalan söylüyor**.
⇒ `create`/`delete` hükmü verilene kadar **açık kalem**.

## ⛔ VE `create`/`delete` TÜKETİCİ ÖLÇÜMÜ: **SIFIR DEĞİL**

```
collmind.frontend  TenantForm.tsx → useCreateTenant   CANLI
                   TenantList.tsx → useDeleteTenant   CANLI
```
⇒ `T-289` emsalinin **kaldırma zinciri şartı SAĞLANMADI**. Hüküm ürün sahibinde:
**admin yetkisinde mi kalsın, operatör-yoluna mı devredilsin?**

*(Bu turda yapılan: yalnız **canlı cross-tenant sızıntı** kapatıldı — `remove` artık
`assertSelfTenant`'tan geçiyor, yani `T1` admin'i `T2`'yi **silemiyor**. *"Kim
silebilir"* sorusuna **cevap verilmedi**.)*


---

## ✅ DOĞRULANDI (2026-08-27, Docker geri geldi)

```
REPRO PİNİ  assertSelfTenant DEVRE DIŞI → ALTI uçta birden KIRMIZI
            (GET · PATCH · DELETE · activate · suspend · stats)
            ⇒ T1-ADMIN, T2'ye GERÇEKTEN ERİŞİYORDU — kusur GÖRÜLDÜ (T-273 şartı)
e2e         801/801 · 49 suite · T-047 PASS
            tenant-cross-tenant-isolation.e2e-spec.ts  PASS
guards      exit 0
```

## ⛔ `Z46 §1` — MADDE-2 HÜKMÜ: yaşam döngüsü **OPERATÖR-YOLUNA**

> **`ADMIN` kiracı-İÇİ bir rol; kiracı yaratmak/silmek PLATFORM-seviyesi iş.**
> Kiracı-içi bir yetkinin kiracı-üstü nesneye dokunması **`T-307`'nin ta kendisiydi**;
> `create`/`delete`'i `ADMIN`'de tutmak **aynı sınıfın *"ama biz kullanıyoruz"*
> muafiyetli hâli** olurdu.
>
> Canlı tüketici **kaldırmayı durdurmaz, ŞEKLİNİ değiştirir**: bu ekran fiilen bir
> **operatör-konsolu**, ama **kiracı-admin kimliğiyle** — ***yanlış katmanda doğru iş.***

**Kalan iş (`T-307-m2` dalgası):**
- [x] `create`/`delete`/`findAll` uçları **+** `TenantForm`/`TenantList` **ÖLÜR** (iki-repo-tek-kapanış)
- [x] `GET`/`PATCH` **kendi-kayıt** (tenant ayarları) **KALIR**
- [x] ⛔ **Sessiz-kaybolma REPRO-PİNE girsin ÖNCE:** *"yarat → listede yok → hata yok"* **bir kez görülsün**
- [x] Geçiş kaydı: *"tenant yaşam-döngüsü = script + seed · sahibi **operatör** · adresi **`K1`**"*

## ✅ `T-307-m2` TAMAMLANDI (2026-08-27, backend-engineer)

```
ORTAM     ghost `tpm` compose projesi (collmind-tpm-frontend + -backend)
          çalışıyordu → DURDURULDU (rapor edildi)
REPRO PİNİ (c) tek kullanımlık e2e ile GÖRÜLDÜ: POST /tenants → 201, GET
          /tenants → 200, yaratılan id LİSTEDE YOK — sessiz kaybolma
          doğrulandı. Satır DB'den temizlendi, pin dosyası SİLİNDİ
          (kalıcı test değil, bir defalık kanıt).
BACKEND   POST /tenants · DELETE /tenants/:id · GET /tenants (findAll)
          KALDIRILDI. GET/PATCH/:id/activate/:id/suspend/:id/stats KALDI
          (zaten assertSelfTenant ile kilitli). tenant.service.spec.ts +
          iki e2e dosyası (tenant-capability-boundary,
          tenant-cross-tenant-isolation) ölen rotalara ait bloklardan
          arındırıldı.
FRONTEND  ⚠️ KAPSAM GENİŞLETİLDİ (kod incelemesi gerekli): brief yalnız
          `TenantForm.tsx`/`TenantList.tsx` ölümünü adlandırıyordu, ama
          `TenantForm` create+update'i BİRLİKTE taşıyordu ve
          `TenantDetailPage`/`TenantCreatePage`/`TenantsPage`/
          `TenantDetail`/`TenantStats` hiçbiri isimlendirilmemişti. Bu
          dosyaların TÜMÜ (+ `tenants.service.ts`, `tenants.endpoints.ts`,
          `tenant.schema.ts`, `tenant.types.ts`, iki test dosyası) ölü
          kod / erişilemez orphan route bırakmamak için AYNI DALGADA
          silindi — gerekçe: TenantList ölünce `/tenants/:id`'ye giden
          TEK navigasyon yolu da ölüyor, dolayısıyla kalan ekranlar
          hiçbir zincirden erişilemez hâle geliyordu. Backend
          GET/PATCH/activate/suspend/stats CANLI ve teste bağlı kalıyor —
          şu an frontend tüketicisi YOK (`🔒` — ileride bir ayarlar
          ekranı için altyapı, `Faz-3` kararı beklenmeden).
          Nav linki + route + breadcrumb (route objesi) aynı diffte öldü.
NAV       Sidebar 'Tenants' linki + `Building2` ikonu kaldırıldı.
ROTA SAYISI  beklenti: 210 → 207 (−3: POST/DELETE/GET-liste) — ölçüm
          `route-cell-map` G7 ile BİREBİR eşleşti (`docs/process/
          B3A_EK3_ROTA_HUCRE_ESLEMESI.tsv` yeniden üretildi ve
          commit'lendi).
GUARDS    `npm run guards` exit 0 · money-float --ratchet temiz ·
          lint-ratchet --ratchet temiz (1 `improved` satırı
          [tenant.service.ts any: 2→1] AYNI TURDA baseline'a işlendi) ·
          route-cell-map temiz.
TESTLER   backend unit 1151/1151 · e2e 784/784 (49 suite) · T-047 PASS ·
          frontend type-check 0 · frontend test 536/536 · frontend build
          başarılı.
AÇIK NOKTA  Frontend kapsam genişletmesi ürün sahibi/code-reviewer
          onayı bekliyor — brief'in isimlendirmediği 7 ek dosyanın ölümü.
```


---

## ⚠️ `(c)` REPRO PİNİ — ARTEFAKT SAKLANMADI, ve ARTIK ÜRETİLEMEZ

Sessiz kaybolma **görüldü** (`POST → 201` · `GET → 200` · **id listede yok, hata da
yok**) — ama pin **geçiciydi**, dosyası **silindi**, satır DB'den **temizlendi** ve
**ham çıktı SAKLANMADI**.

⛔ **Bu, review `S2` gereği AÇIKÇA yazılıyor:** iddia bir daha **hiç doğrulanamaz**,
çünkü `create` **öldü** ⇒ davranış **üretilemez**. Kalıcı pin yazılamaması **meşru**;
artefaktın saklanmaması **bir eksikliktir** ve kanıtlanmış gibi okunmamalıdır.

> `DISIPLIN`: *"kusur var demek, kusur yok demek kadar bir iddiadır."*

📌 **Ders:** bir kusuru **öldüren** turda repro artefaktı, **öldürmeyen** turdakinden
**DAHA** gereklidir — çünkü ikinci bir şans yoktur.

## 🔒 DOĞAN ALARM → `T-313`

`Z46 §1(b)` beş ucu **meşru yüzey** diye korudu; frontend'in tamamı öldüğü için bugün
**tüketicisi sıfır**. `EK_E` `🔒` sınıfı — ve **bir parantez değil, bir TASK** olarak
kayıtlı: **`T-313`**.
