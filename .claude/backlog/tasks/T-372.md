---
id: T-372
title: app-runtime-grants kaynak A KÖR — dördüncü (manager.getRepository) ve BEŞİNCİ (ilişki yüklemesi) kanal
epic:
sprint:
status: todo
assignee: backend-engineer
created: 2026-09-04
updated: 2026-09-04
touches:
  - collmind.backend/scripts/guards/app-runtime-grants.sh
  - collmind.backend/scripts/guards/app-runtime-grants-source.awk
  - collmind.backend/scripts/guards/app-runtime-grants-self-test.sh
---

## Nasıl bulundu

`T-362` kaynak C'yi (canlı DB) ekledi. `B \ A` yönü (*"beyan var, kod ihtiyaç duymuyor"* =
fazla yetki) eklenmeden önce **sayıldı**: **2 satır**. Ürün sahibi şartı: *"kod erişmiyor bir
NEGATİFtir — string evreni de taranır"*. Tarandı, ve **ikisi de fazla yetki DEĞİL**.

## Ölçüm `[ÖLÇÜLDÜ, 2026-09-04]`

```
v_budget_summary
  forFeature / InjectRepository  →  0   (grep, src/modules)
  gerçek erişim                  →  budget-tier-notification.service.ts:129
                                    `manager.getRepository(BudgetSummaryView)`
  ⇒ DÖRDÜNCÜ KANAL

lta_plan_overrides
  forFeature                     →  lta.module.ts:16 — BİLEREK konmadı (T-250 DUR #2)
  gerçek erişim                  →  lta-agreement.repository.ts:128
                                    `.leftJoinAndSelect('lta.planOverrides', 'planOverrides')`
  ⇒ BEŞİNCİ KANAL: İLİŞKİ YÜKLEMESİ
```

**Dördüncü kanalın bugünkü evreni** (`manager|m|txManager|entityManager.getRepository(X)`):
`BudgetEnvelope 6 · BudgetTransaction 4 · UserScope 3 · SalesActualBatch 3 · PlanSku 3 ·
ApprovalRequest 3 · PlanApprovalHistory 2 · Plan 2 · Notification 2 · BudgetSummaryView 2 ·
User 1 · SalesActual 1 · PlanFu 1 · BaselineVolumeImportBatchRow 1 ·
BaselineVolumeImportBatch 1 · BaselineVolume 1 · AdminAuditLog 1`
⇒ **17 sınıf**; yalnız `BudgetSummaryView`'ün diğer kanallarda karşılığı **yok**.

## ⭐ Ve bu bir *"kanıt eskidi"* vakası

Guard'ın başlığı diyordu ki: *"dördüncü kanal EKLENMEDİ — 14 sınıf ölçüldü, hepsi zaten
forFeature/InjectRepository birleşiminde MEVCUTTU (`comm -23` → 0 fark). **Bugün eklemek
kaynak A'yı DEĞİŞTİRMEZ**."*

O ölçüm **doğruydu** ve **eskidi**. `BudgetSummaryView` o gün başka bir kanalda vardı ya da
o çağrı henüz yoktu; bugün **tek kanalı** dördüncü kanal.

📌 Ve **SQL beyanı koddan türetilen `A` kümesinden DAHA DOĞRUYDU**: iki GRANT da ölçülmüş bir
`500` yüzünden eklenmişti (`lta-agreement.repository.ts:120-127` yorumu bunu yazıyor).

## Yapılacak
- [ ] Kaynak A **dördüncü kanalı** kapsasın (`manager.getRepository(X)` ve türevleri)
- [ ] Kaynak A **beşinci kanalı** kapsasın — ilişki yüklemesi
      (`leftJoinAndSelect('<alias>.<relation>')` · `find({ relations: […] })` ·
      `@OneToMany`/`@ManyToOne` hedef tabloları). ⛔ **Bu ZOR:** relation adı → hedef entity →
      tablo adı zinciri statik olarak çözülmeli. Tek makul şekil yoksa **DUR ve raporla**.
- [ ] Her yeni kanal için **bilinen-kırmızı** fixture (kanal bağımsızlığı, case 13-15 deseni)
- [ ] Kanal genişledikten SONRA `B \ A` (**over-granted**) dördüncü yön olarak eklenir
      — `not-live` · `not-applied` · `undeclared-live` yanına
- [ ] Genişlemiş A ile `B \ A` **yeniden sayılır**; sıfır değilse **DUR** (kırmızı doğan kapı ölür)

## Done tanımı
- [ ] `docker ps --filter "label=com.docker.compose.project=tpm"` boş
- [ ] `npm run guards` exit 0 · self-test bilinen-yeşil **ve** bilinen-kırmızı
- [ ] `code-reviewer` onayı
