---
id: T-251
title: 🔴 `mechanic_spend_breakdown.plan_sku_id` KATALOG sku id'siyle dolduruluyor — FK ihlali, /spend-calculation yazma tarafı komple kırık
epic: E-001
sprint: S-001
status: todo
assignee: debugger
created: 2026-08-20
updated: 2026-08-20
touches:
  - collmind.backend/src/modules/shared/spend-calculation/spend-distribution.service.ts
---

## Bulgu

[[T-249]] turunun yan bulgusu. **`GRANT`'ten tamamen bağımsız** bir uygulama kusuru.

```
kolon      mechanic_spend_breakdown.plan_sku_id
FK         REFERENCES main.plan_skus(id)          ← katalogdan doğrulandı
yazılan    planSku.skuId                          ← KATALOG skus.id
```

**Dört dağıtım stratejisinin DÖRDÜ de aynı** (`spend-distribution.service.ts`):

```
:462  skuId: planSku.skuId
:492  skuId: planSku.skuId
:530  skuId: planSku.skuId
:547  skuId: planSku.skuId
       ↓
:618  planSkuId: dist.skuId      → plan_sku_id kolonuna
```

## ⚠️ Ve OKUMA tarafı tersini varsayıyor — iki uç çelişiyor

```
:303  skuId: b.planSkuId          ← breakdown'dan geri okurken planSkuId olduğunu varsayıyor
:207  where: { planSkuId: skuId } ← sorgularken de öyle
```

Yani **aynı alan yazılırken `skus.id`, okunurken `plan_skus.id` sanılıyor.** Üç bağımsız
tanık kolonun `plan_skus.id` taşıması gerektiğini söylüyor: **kolon adı** ·
**FK hedefi** · **okuma yolu**. Yanlış olan **yazma**.

## Etkisi

Entered değeri `> 0` olan her `distributeMechanicSpend` çağrısı
`FK_54dbfe9df242d1aada3b456eb95` ihlaliyle düşüyor → **500**.

⚠️ **Ve bu, [[T-249]]'un öncülünü kısmen yanlışlıyor:** `T-249` *"GRANT verilirse rota
çalışır"* varsayıyordu. `/spend-calculation/*` **yazma** tarafı için bu **yanlış** —
izin gelse bile FK'de düşüyor. O rotalar **iki kez** kırıktı.

📌 `CLAUDE.md`: *"bir kusur başka bir kusur tarafından örtülüyordu."* Burada `GRANT`
eksikliği FK kusurunu örtüyordu — `permission denied` daha önce ateşliyor.

## Acceptance Criteria

- [ ] ⚠️ **ÖNCE YÖNÜ DOĞRULA** (`CLAUDE.md`: *"bir düzeltmenin iki ekseni vardır:
      hedefi ve yönü"*). Üç tanık `plan_skus.id` diyor — ama **dördüncüyü ara**:
      bu kolonu okuyan/yazan başka yol var mı (rapor · view · export · migration ·
      frontend)? Yön yanlışsa düzeltme sessiz bir veri bozulması üretir
- [ ] Yazma tarafı `planSku.id` yazar
- [ ] ⚠️ **Var olan veri**: `mechanic_spend_breakdown`'da bugün kaç satır var, ve
      kaçının `plan_sku_id`'si `plan_skus`'ta **yok**? (`LEFT JOIN` + `IS NULL`
      **yeterli değil** — `plan_sku_id IS NOT NULL` şartını ayrıca koy.) Öksüz varsa
      temizlik **ayrı karar**, sessizce silme
- [ ] Regresyon testi: entered değeri `> 0` olan bir dağıtım **uçtan uca** çalışır
- [ ] ⚠️ Test **mutasyonla** sınanır: yazma tarafını `planSku.skuId`'ye geri çevir →
      **kırmızı** vermeli. Değiştirilen satır **basılır**

## ⛔ ÖN KOŞUL

`/spend-calculation/*` `GRANT`'i [[T-249]] ile geliyor. **Bu task ondan sonra**
ölçülebilir hâle gelir — öncesinde `permission denied` FK'den **önce** ateşler ve
regresyon testi yanlış sebeple kırmızı verir.

## İlgili

- [[T-249]] — bulunduğu tur, ve `GRANT` ön koşulu
- [[T-252]] — aynı controller'da `@Roles` yok
