---
id: T-071
title: Grid'de gömülü 8 formül dinamik formül kapsamı dışında — `main.kpis`'e taşı veya sunucudan oku
epic: E-001
sprint: S-001
status: todo
assignee: architect
created: 2026-08-04
updated: 2026-08-23  # ESKI: 2026-08-04
---

## Açıklama
`docs/analysis/0012-frontend-calculation-layer.md` **R3**.

Grid'in 12 türetilmiş değerinden **8'inin `main.kpis`'te karşılığı yok** — TypeScript'te gömülüler:
`INCR_GSV`, `BASE_NIV`, `PLAN_NIV`, `INCR_NIV`, `INCR_TO`, `INCR_GP%`, `ROI%`, `VOL_UPLIFT_PCT`.

Karşılığı olan 4 değerde de (`BASE_GSV`, `PLANNED_GSV`, `BASE_TO`, `PLANNED_TO`) senkron **kod
tarafında elle** sağlanıyor; üçünün `show_in_grid=false`.

## Neden önemli
BRD: *"KPI/ROI/Spend/Profit = Admin tanımlı dinamik formül."* Admin `main.kpis` üzerinden bir
formülü değiştirirse grid'in bu 8 değeri **eski formülle hesaplamaya devam eder** ve değişiklik
**sessizce yok sayılır**. Yani dinamik formül ilkesi fiilen yalnız sunucuda geçerli.

**Bugün zararsız** çünkü bu değerler hiçbir yere yazılmıyor ve hiçbir kararı etkilemiyor
(0012 §S2). **Yazılmaya veya karara bağlanmaya başladıkları an sınıf finansal ihlale döner.**

## Karar gerekiyor (bu yüzden `architect`)
- Formüller `main.kpis`'e mi taşınsın (admin tanımlı), yoksa sunucu bu değerleri hesaplayıp
  API yanıtında mı dönsün? İkincisi "frontend sadece render eder" ilkesine daha yakın.
- `show_in_grid` bayrağının fiilen bir tüketicisi var mı? (0012 açık kalan #2 — ölçülmedi)

## Ayrıca çözülmeli — R4 (0012 açık kalan #1)
`BASE_TO` için **üç kaynak uyuşmuyor**:
- DB `formula_text`: `BASE_GSV - BASE_LTA_ON`
- migration 1780 açıklaması: `BASE_GSV - BASE_LTA_ON - BASE_LTA_OFF`
- istemci (`PlanningGridEnhanced.tsx:219-223`): OFF'u **da** düşüyor

Hangisi canonical? `PLANNED_TO`'nun tam metni de okunmadı.

## İlgili
- Rapor: `docs/analysis/0012-frontend-calculation-layer.md` R3, R4 · [[T-069]], [[T-070]]


---

## 2026-08-23 — AÇIK SORU #2 CEVAPLANDI (`T-265` ölçüm turu)

Yukarıda *"`show_in_grid` bayrağının fiilen bir tüketicisi var mı? (**ölçülmedi**)"* yazıyordu.
**Ölçüldü — cevap: üretimde SIFIR.**

```
GET /master-data/kpis/grid        →  200, 8 KPI  (showInGrid kataloğu)
GET /master-data/kpis/grid/:planId →  aynı katalog + plan okuma KAPISI (assertReadScope)

çağıran:  PlanningGrid.tsx:236,241
          ⛔ ama PlanningGrid.tsx'i KİMSE import etmiyor
POZ.KONTROL: PlanningGridEnhanced → PlanDetailPage.tsx:27   ✅ desen çalışıyor

canlı grid (PlanningGridEnhanced.tsx):
  ./column-definitions'tan 59 HARDCODED kolon okuyor
  KPI ucunu HİÇ çağırmıyor  (grep 'kpi' → yalnız calculatedKpis?.['GP_ROI_PCT'] okumaları)
```

### 📌 Yani bu task'ın konusu bir "eksik" değil, bir **terk edilmiş çözüm**

Dinamik-KPI ilkesini frontend'de uygulayan kod **yazılmış** — ve **ölü bir dosyada duruyor**.
Grid'in canlı sürümü onun yerine hardcoded kolon listesine geçmiş.

```
katalogda showInGrid   8 KPI
canlı grid'de          59 hardcoded kolon
```

⚠️ **`T-222`'nin (*"iki grid, biri karanlıkta"*) aynı ailesi** — ve `T-253`'ün dersiyle
birlikte okunmalı: *"bir kopya ölü diye işaretlendiğinde ölçüm durur."* Burada kopya
işaretlenmemiş bile; yalnız **import edilmiyor**.

### ⛔ Ve bağlanırken bir tuzak var — fallback zinciri PORT EDİLMEMELİ

```tsx
PlanningGrid.tsx:239-248
  getGridKpisForPlan(planId)   catch →  getGridKpis()   catch →  return []
       ↑ kapsam KAPISI var              ↑ kapsam YOK
```

Kapsam kapısı `404` verdiğinde kod sessizce **kapsamsız** uca düşüyor — **tasarlanmış bir
bypass**. `CLAUDE.md`: *"davranış taşınır, onu DOĞRU KILAN BAĞLAM taşınmaz."*
Ve `catch { return [] }` `§2.5` ihlali: KPI tanımları sessizce kaybolur, grid statik
kolona düşer.

### ⚠️ Bağlama turunun kabul kriterine bir satır

> *"`59` hardcoded kolonun kaçının KPI kataloğunda karşılığı var?"*

Yeni KPI seti eskisinden **dar** olursa grid kolon **kaybeder** — kullanıcı için regresyon.
Bugünkü fark `8` ↔ `59`.

### Statü

`K-2.4`'ün dinamik-KPI ilkesinin ihlali — **`Faz 3`'ün konusu**, ürün sahibi kararı
(2026-08-23). Bu turda **açılmıyor**, kayda geçiyor.

### İlgili
`T-265` (uçların kaderi) · [[T-222]] · [[T-253]]
