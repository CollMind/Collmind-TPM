---
id: T-243
title: UserForm kapsam seçicisi — T-241 arayüzü kırdı, varsayılan yol 400 dönüyor
epic: E-001
sprint: S-001
status: todo
assignee: frontend-engineer
created: 2026-08-20
updated: 2026-08-20
touches:
  - collmind.frontend/src/types/user.types.ts
  - collmind.frontend/src/components/forms/UserForm.tsx
  - collmind.frontend/src/api/endpoints/users.endpoints.ts
---

## Açıklama

`code-reviewer`'ın `B2` blocker'ı (`dfdbe7f` review'u). Kayıt: karar defteri **`Z12`**
+ `EK_E` iki satır (`Kullanıcı yönetimi` → `⚠️` · `Kapsam atama` → `🔒`).

`T-241` **backend sözleşmesini değiştirdi**; frontend bilmiyor:

```
src/types/user.types.ts:52-65        CreateUserDto'da `scope` alanı YOK
src/components/forms/UserForm.tsx:153  Object.values(UserRole) → PLANNER/CM seçilebilir
src/components/forms/UserForm.tsx:54   VARSAYILAN rol = PLANNER
```

⚠️ **Varsayılan yol kırık:** formu açıp hiçbir şey değiştirmeden kullanıcı yaratmak
**`400`** döner.

📌 Prod etkisi **YOK** (CTPM yalnız lokal koşuyor) — ama **ürün yeteneği bugün kayıp**.

## ⛔ Brief `B1` kararıyla yazıldı — sıra bilinçliydi

`B1` (joker yasağı) **önce** kapandı (`25f6b07`), ve bu brief **ondan sonra** yazıldı.
Gerekçe (ürün sahibi): *"joker yasaklandığına göre `UserForm` kapsam seçicisi
**zorunlu** olacak, opsiyonel değil. Önce yazılsaydı iki kez yazılırdı."*

## Şekil

```
SCOPE_REQUIRED (PLANNER · KATEGORİ MÜDÜRÜ)   kapsam ZORUNLU — joker seçeneği YOK
UNRESTRICTED   (ADMIN · FİNANS · İZLEYİCİ)   kapsam alanı GİZLİ (backend joker yazar)
```

⚠️ **Joker seçeneği arayüzde BULUNMAMALI** — `B1` kararı: bu roller joker alamaz.
Boş bırakılmış bir alan `[{}]` göndermemeli; backend onu `400` ile reddeder
(`user.service.ts`, `emptyPairIndex` kapısı), ama **arayüz o hatayı hiç üretmemeli**.

## Acceptance Criteria

- [ ] `CreateUserDto` (frontend) `scope?: { cplId?: string | null; categoryId?: string | null }[]`
      taşır — backend DTO'suyla **alan adları birebir**
- [ ] Rol seçimi **kapsam alanının görünürlüğünü** belirler (yukarıdaki tablo)
- [ ] `SCOPE_REQUIRED` rollerde form **gönderilemez** kapsam verilmeden — istemci
      tarafı doğrulama
- [ ] ⚡ **Ve backend `400`'ü KALIR — arayüz tek savunma değil.** Bir kabul satırı
      bunu **açıkça** yazar: istemci doğrulaması bir **ergonomi** katmanıdır, bir
      **güvenlik** katmanı değil
- [ ] ⚠️ **`R-1` pair semantiği arayüzde de korunur:** `cplIds[] × categoryIds[]`
      **düzleştirme YASAK** — kullanıcı **çift** seçer, iki bağımsız çoklu-seçim değil
      (`access-scope.service.ts` başlığı; backend bunu zaten uyguluyor)
- [ ] Kapsam **güncelleme** bu task'ta **DEĞİL** ([[T-242]]) — yalnız **yaratma**
- [ ] Test: varsayılan rolle (PLANNER) form gönderimi **artık `400` üretmiyor**
- [ ] `npm run type-check` exit 0 · `npm test` (vitest) yeşil
- [ ] `roleEnumContract` testi yeşil kalır (rol enum'una dokunulmuyor, ama sözleşme
      testi bu turda da koşulur)

## Done tanımı

- [ ] Testler yeşil (vitest)
- [ ] `npm run type-check` exit 0
- [ ] **Üretim çağrı yolu var** — ekran gerçekten çalışıyor (`EK_E` satırı `⚠️` → `✅`
      olabilmeli, ve `Z12`'nin `EK_E` notu o zaman güncellenir)
- [ ] `code-reviewer` onayı
- [ ] `touches:` güncel
