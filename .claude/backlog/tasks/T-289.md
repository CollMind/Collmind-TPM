---
id: T-289
title: POST /budget/reserve — doğrulanmamış agreementId ile POSTED defter satırı
epic:
sprint:
status: todo
assignee: backend-engineer
created: 2026-08-25
updated: 2026-08-26
touches:
  - collmind.backend/src/modules/shared/budget/budget.service.ts
  - collmind.backend/src/modules/shared/budget/budget.controller.ts
  - collmind.frontend/src/components/budget/ReserveBudgetDialog.tsx
  - collmind.frontend/src/routes/index.tsx
migration_seq:
---

## Açıklama

`W4b` ölçüm turunda (`architect`) bulundu, **Team Lead dört yüzeyde bağımsız
doğruladı**. Bir rol sorusundan **büyük**: rotanın rol kümesi, meşruiyeti
çözülmeden atanamaz.

### Zincir — dört halka, dördü de ölçüldü

| # | halka | ölçüm |
|---|---|---|
| 1 | `/budget` ekranı **rol kapısız** | `routes/index.tsx:282` `<ProtectedRoute>` — `requiredRole` **YOK**. POZ.KONTROL: aynı dosyada **40** rotada var |
| 2 | `agreementId` **serbest metin** | `ReserveBudgetDialog.tsx:27` `useState('')` · `:75` `<Input>` · tek doğrulama `.trim()` |
| 3 | API `{ADMIN, PLANNER}` | `POST budget/reserve` |
| 4 | servis **doğrulamıyor** | `budget.service.ts` `reserveBudget`: zarfı doğruluyor (`NotFound` · *"not active"* · idempotency · yetersiz bakiye) ama **`agreementId` için VARLIK/DURUM kontrolü YOK** — doğrudan `sourceId` olarak yazılıyor, `txStatus: POSTED` (*"Immediate posting"*) |
| + | kapsam katmanı **yok** | `BudgetService`'te `AccessScope` atfı **0**. POZ.KONTROL: **7** servis onu kullanıyor |

### Sonuç

Bir `PLANNER`, **var olmayan bir anlaşma kimliği** yazarak **herhangi bir zarfa**
`POSTED` bir **defter satırı** üretebilir. Kapsam katmanı olmadığı için zarf
seçimi de sınırsız.

⚠️ `txStatus: POSTED` — yani satır bir taslak değil, **kesinleşmiş** kayıt.

### Ve `K-2.2.4` ile gerilim

Kural (`L2_01:465`) `Rezerve` kovasının ***"bir anlaşma ONAYLANDIĞINDA"***
dolduğunu söylüyor. Bu uç ise **elle** doldurulan bir yol; plan yolundaki
kanonik motor **ayrı ve başka**
(`approval-workflow.service.ts` `budgetService.reserveTypedForPlan`).

⇒ Yani bu uç, kuralın tarif ettiği tetikleyiciyi **atlayan** ikinci bir yol.

## ⛔ Bu task B3'ün rol kararını BLOKLUYOR

`POST /budget/reserve` `SHARED_WRITE`'ın **eşlenemeyen** rotalarından
(`W4b` `3b`). Ne `SINIF B`'ye (zarf **yapısı**) ne `SINIF C`'ye (plan ızgarası)
oturuyor — yazdığı şey **defterin kendisi**.

**Meşruiyeti çözülmeden rol kümesi atanamaz:** uç meşruysa kimin çağırabileceği
bir soru; meşru değilse soru **ucun kendisi**.

## Acceptance Criteria

- [ ] `agreementId` **varlık + durum + tenant** doğrulaması (ya da uç kaldırılır —
      **ürün sahibi kararı**)
- [ ] `/budget` ekranına `requiredRole` (ya da neden olmadığı **kayda geçer**)
- [ ] `ReserveBudgetDialog` serbest metin yerine **seçim** (var olan anlaşmalardan)
- [ ] `BudgetService`'e kapsam katmanı — ya da **eksikliği kayıtlı** (sağlayıcı `T-266`)
- [ ] `K-2.2.4` ile ilişki **kayda**: bu uç kuralın tetikleyicisini atlıyor mu,
      yoksa meşru bir ikinci yol mu
- [ ] Repro-pin: **kusur ÖNCE görülür** (`T-273` disiplini) — uydurma `agreementId`
      ile `POSTED` satır üretilebildiği **ölçülür**, sonra düzeltilir

## İlişkili
`T-287` (ekran kapısı ↔ rota kümesi ayrışması — `/budget` aynı aileden) ·
`T-266` (kapsam sağlayıcısı) · `B3_KARAR_BEKLER_PAKETI` `W4b`


---

## ⛔ HÜKÜM (ürün sahibi, 2026-08-26) — UÇ MEŞRU DEĞİL, KALDIRILIR

> *"Bu bir rol kararı değil, `K-2.2.4`'ün savunması."*

Zincir dört halkada da ölçüldü. `POST /budget/reserve`, `K-2.2.4`'ün tarif ettiği
tetikleyiciyi (*"bir anlaşma **onaylandığında**"*) **atlayan**, doğrulanmamış kimlikle
**kesinleşmiş** defter satırı üreten **ikinci bir yol** — `budget_allocations`
deseninin **defter-seviyesi** hâli: kanonik motorun yanında yaşayan **paralel yazma
yolu**. `K-2.2.3`'ün gerekçesi (*"fark sessizdir"*) burada birebir.

Manuel rezervasyon gerçek bir ürün ihtiyacıysa **kanıtıyla ve kendi kararıyla gelir**
(`İlke 1`). Bugün o kanıt yok; olan şey **denetimsiz bir musluk**.

### Kaldırma disiplini — sıra DEĞİŞTİRİLMEZ

```
(a) ÖNCE REPRO-PİN      uydurma agreementId ile POSTED satır üretilebildiği GÖRÜLÜR
                        ⛔ T-273 dersi: "kusur var" da bir İDDİADIR ve aynı kapıdan geçer
(b) DEFTER TARAMASI     bu yolla doğmuş satır var mı?
                        VARSA → ADR-0012: fiziksel silme YOK, ele alınışı AYRI karar
                        ⚠️ Bu bir KAPI: satır bulunursa (c)'ye geçilmez, ürün sahibine dönülür
(c) İKİ REPO TEK KAPANIŞ  uç + ReserveBudgetDialog BİRLİKTE ölür (T-277 deseni)
                        /budget ekranının kalanı requiredRole alır ([[T-287]] ailesiyle)
(d) TEK YOL PİNİ        kanonik motorun (reserveTypedForPlan) tek yol kaldığı pinlenir
```

Dalga: **kaza/istisna dalgası** (`docs/process/B3_KAZA_DALGASI_BRIEF.md` `K6`).
Bu uç `SHARED_WRITE`'ta kalır ve `Z36`'nın üç sınıfına **girmez** — kaldırılacağı için.
