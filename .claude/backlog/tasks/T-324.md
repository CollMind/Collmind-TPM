---
id: T-324
title: T-047 invariant evreni HÂLÂ yetki-filtreli — 9 tablo kör; sayım rolü hüküm bekler
epic:
sprint:
status: review
assignee: qa-engineer
created: 2026-08-28
updated: 2026-08-28
touches:
  - collmind.backend/test/helpers/e2e-row-count.js
  - collmind.backend/test/global-setup.js
migration_seq:
---

> ⛔ **Hüküm bekler.** `T-319` evreni **elle listeden türetilmişe** taşıdı — gerçek ve
> değerli bir ilerleme. Ama ürün sahibinin hükmü (`Z60 §3`) *"bu sınıf **üçüncü kez
> doğamaz**"* diyordu ve **o henüz sağlanmadı.**

## Ölçüm

```
main şemasında relkind='r'          48
app_runtime SELECT edebiliyor       39      ← evren BU
app_migrate  / app_operator         48 / 48
KÖR KALAN 9: _t019_backfilled_tx · claim_matches · claims · fiscal_periods
             migrations · roles · tactic_realizations · typeorm_metadata
             user_role_assignments
```

⇒ `claims`/`claim_matches`/`fiscal_periods`/`roles`/`tactic_realizations`/
`user_role_assignments`'a **yeni bir yazıcı gelirse invaryant YİNE kör kalır.**
📌 `claims` ailesi **açık port-adayı** (`CLAUDE.md §1`) — yani bu **yakın** bir risk.

## ⛔ VE KÖRLÜĞÜN SEBEBİ BİR ZORUNLULUK DEĞİL — **GEÇERLİLİĞİNİ YİTİRMİŞ BİR GEREKÇE**

`e2e-row-count.js:72-77` sayım bağlantısını `app_runtime` yapıyor ve şöyle
gerekçelendiriyor:

> *"…çünkü ölçtüğü tablolar **zaten uygulamanın kendisi tarafından sürekli
> okunuyor**"*

Bu, evren **elle seçilmiş uygulama tabloları** iken **doğruydu**. Evren **tüm şemaya
türetildiği an ÖNCÜLÜ KALKTI.**

⇒ **`Z60 §1`'in AYNI GÜN, AYNI DOSYADA ikinci vakası:** *"bir gerekçe, dayandığı
ölçümün **tarihiyle** yaşar — ve o ölçümü **değiştiren tur**, gerekçenin
okuyucusudur."* Ölçümü değiştiren tur `T-319`'du ve gerekçeyi **okumadı**.

## Ve dosya kendi içinde ÇELİŞİYORDU *(düzeltildi 2026-08-28)*

```
manşet    "böylece bu sınıf ÜÇÜNCÜ KEZ doğamaz"
12 satır  "o tablolara yeni bir yazıcı gelirse ... invaryant YİNE kör kalır"
sonra
```
**İki çelişkili iddia, tek blokta — ve manşet olan, teslim edilmeyendi.**
⇒ `Z58 §3` sınıfı (*kısmen doğru bir güvence, tamamen doğru okunur*). Yorum
**yürürlükteki dar iddiayla** değiştirildi; **kod değişmedi.**

## Hüküm bekleyen soru: SAYIM ROLÜ

`DB_MIGRATE_USERNAME`/`PASSWORD` **`.env`'de mevcut**, `app_migrate` **48/48** görüyor.
Yani düzeltme **elde** — ama iki şeye dokunur:

| | |
|---|---|
| `K-2.6.13a/d` gerekçesi | sayım bağlantısının `app_runtime` olması *"suite `app_runtime` altında yeşil"* AC'siyle ilişkilendirilmiş. ⚠️ **Ölçülmeli:** AC **uygulamayı** mı bağlıyor yoksa **ölçüm koşumunu** mu? İkisi ayrıysa rol değişimi AC'yi zayıflatmaz |
| sayım ŞEKLİ | bugün **38 tablo `tenant_id` + `tenants.id`**. `migrations`/`typeorm_metadata`'nın `tenant_id`'si **yok** ⇒ ham sayım gerekir (emsal **var**: `admin_audit_logs` bilerek **ham** sayılıyor) |

### Şıklar
| | | |
|---|---|---|
| `(a)` | sayım `app_migrate` ile · evren **48/48** · tenant'sız tablolar **ham** sayılır | sınıf **gerçekten** kapanır |
| `(b)` | `app_runtime`'a eksik `SELECT`'ler verilir | ⛔ **yetki genişletmek** — bir ölçüm kolaylığı için; `Z42` disiplinine aykırı |
| `(c)` | bugünkü hâl kalır, **dar iddiayla** | kör nokta **kayıtlı** ama açık; `claims` port'u geldiğinde **yeniden doğar** |

**Team Lead görüşü: `(a)`.** Sayım bağlantısı bir **harness**'tır, ürün yolu değil;
`app_migrate` ile **yalnız `count(*)`** yapmak yapı gereği SELECT-only'dir.

## Acceptance
- [ ] Evren **yetki filtresiz** (`relkind='r'` + `nspname='main'`, **48/48**)
- [ ] Tenant kolonu olmayan tablolar için sayım şekli **açık** (ham) ve **gerekçeli**
- [ ] ⛔ Reprodüksiyon: kör tablolardan **birine** artık bırakan bir koşum **kırmızı** verir
- [ ] `K-2.6.13a/d` gerekçesi **ölçülerek** güncellenir (AC neyi bağlıyor?)

---

## ⛔ HÜKÜM = **(a)** *(ürün sahibi, 2026-08-28 — `Z61`)*

> **Sayım bağlantısı bir ÖLÇÜM-HARNESS'IDIR, ürün-yolu değil.**
> `K-2.6.13`'ün ayrımı (*"`app_runtime` = uygulamanın kimliği"*) **harness'ı
> BAĞLAMAZ**, çünkü harness **uygulama değil — uygulamayı ÖLÇEN şeydir.**
>
> **VE ÖLÇEN ŞEYİN EVRENİ, ÖLÇÜLEN ŞEYİN YETKİSİNDEN GENİŞ OLMAK ZORUNDADIR** —
> yoksa **tam bu vaka doğar:** kapı, **uygulamanın göremediği yerde doğan artığı
> göremez.**

`app_migrate` ile `count(*)` **yapı gereği SELECT-only** (yazma yolu yok) ⇒ risk
profili **sıfıra yakın**.

**`(b)` REDDİ — ve cümlesi `DISIPLIN`'e:**
> **Ölçüm kolaylığı için ÜRETİM YETKİSİ genişletilmez.**
> *(Sınıf tekrar gelecek: her yeni kapı *"`app_runtime`'a şu `GRANT`'ı verelim mi"*
> sorusunu doğurabilir. Cevap hep aynı: **hayır — harness KENDİ ROLÜYLE ölçer.**)*

## Uygulama
1. `e2e-row-count.js` bağlantısı **`DB_MIGRATE_*`**'a döner
2. Gerekçe yorumu **YENİ ÖNCÜLLE** yazılır:
   > *"Evren tüm-şema; harness `app_migrate` ile ölçer çünkü
   > **ölçüm-evreni ⊇ uygulama-evreni** olmalı — `Z60`/`T-324`."*
   ⚠️ Eski gerekçe (*"ölçtüğü tablolar zaten uygulama tarafından okunuyor"*)
   **silinmez, iziyle ölür** (`F12` deseni) — o cümle **niçin** geçersizleştiğinin
   kaydıdır.
3. Tenant kolonu olmayan tablolar için sayım şekli **açık ve gerekçeli** (ham sayım;
   emsal **var**: `admin_audit_logs`)

## ⛔ EK ÖLÇÜM — DOKUZ KÖR TABLONUN SINIFLANDIRILMASI *(ürün sahibi şartı)*

> *"`app_runtime` `claims`'i okuyamıyor" bir NOT olarak kalamaz — sınıflandırılmalı.*
> Ve **"muhtemelen" bir hüküm değildir.**

**Dokuz tablonun HER BİRİ için tek soru:**
```
backend'de bu tabloya bir repository/entity VAR MI?
  VAR + GRANT YOK   →  ⛔ GERÇEK KUSUR   (T-306 sınıfı: İLK ÇAĞRIDA PATLAR)
  YOK               →  "port-bekleyen, GRANT portuyla gelir" KAYDI
```
*(On dakikalık `grep`; iki sınıfı ayırır.)*

📌 **Ve kaydın işlevi geleceğe dönük:** `claims` portu geldiğinde `GRANT`'ın **o
dalganın checklist'inde** olması **bu kayıtla garanti olur** — `Z60 §2`'nin dersi:
*yeni-çağıran-doğuran dalga, `GRANT`-gerekçelerini tarar.*

## Acceptance
- [ ] Evren **48/48**, yetki filtresi **yok** — DB'den doğrulandı
- [ ] ⛔ **KÖR-TABLO MUTASYONU:** `claims`'e sentetik artık satır → invaryant **KIRMIZI**
      *(kusuru ÖNCE GÖR; bugün bu yol kırmızı VEREMİYOR — kanıt bu)*
- [ ] Dokuz tablonun sınıflandırması, **her biri için** `repository/entity` ölçümüyle
- [ ] Eski gerekçe **iziyle** ölmüş, yenisi `Z60`/`T-324` atfıyla yazılmış
- [ ] `CLAUDE.md §4.2` Done listesi tam

