---
id: T-216b
title: "`GRİ` sunum durumu — PLAN seviyesi (`PlanList` · `GrandTotals`): taşıyıcıya bağlı"
epic: E-001
sprint: S-001
status: done
assignee: frontend-engineer
priority: P1
created: 2026-08-14
updated: 2026-08-14
touches:
  - collmind.frontend/src/components/features/plans/PlanList.tsx
  - collmind.frontend/src/components/features/plans/GrandTotals.tsx
migration_seq:
---

## ⛔ BLOKE — [[T-218]]'e bağlı

Plan seviyesinde `coverageRatio` **taşınmıyor**: `main.plans`'ta ne `calculated_kpis` ne
`coverage_ratio` var (0 kolon, ölçüldü). Değer `kpi-engine.calculatePlan()`'da hesaplanıyor
ve `plan.service.ts` onu atıyor. [[T-218]] kolonu açıp persist edecek.

## Yapılacak (blokaj kalkınca)
- [ ] `PlanList.tsx:49` — `if (!ragStatus) return null` yerine **gri rozet + kapsama oranı**
      (`K-2.4.22b`: değer gizlenmez)
- [ ] `GrandTotals.tsx` — ⚠️ oradaki `null → AMBER` çöküşü **bu task'ta DEĞİL**, [[T-172]]'de
      (aynı sınıf, aynı dosya, `touches` çakışıyor)

⚠️ `§4.3`: `GrandTotals.tsx` [[T-172]] ile çakışıyor — **sıralı** gitmeli, paralel değil.

## İlgili
[[T-218]] (taşıyıcı) · [[T-216a]] (grid yarısı) · [[T-172]] (`null` → iş yargısı) ·
`INV-N-004` · `K-2.4.22a1`
