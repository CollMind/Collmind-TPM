---
id: T-218
title: "`plans.coverage_ratio` — değer hesaplanıyor ve yolda düşürülüyor"
epic: E-001
sprint: S-001
status: todo
assignee: data-engineer
priority: P1
created: 2026-08-14
updated: 2026-08-14
touches:
  - collmind.backend/src/database/migrations/1804000000000-*.ts
  - collmind.backend/src/database/entities/plan.entity.ts
  - collmind.backend/src/modules/modes/planning-first/plan/plan.service.ts
migration_seq: 1804000000000
---

## Bulgu — İKİ ajan bağımsız olarak aynı yere vardı

```
kpi-engine.calculatePlan()   plan seviyesinde coverageRatio'yu HESAPLIYOR
plan.service.ts:2621-2659    onu ATIYOR — yalnız overallRoi/ragStatus persist ediyor
main.plans                   ne calculated_kpis ne coverage_ratio (0 kolon, ölçüldü)
plan_fus / plan_skus         taşıyor — ama o FU kapsaması, PLAN GENELİ DEĞİL
```

⚠️ **Bulgunun yapısal olduğunun kanıtı:** `frontend-engineer` (PlanList gösteremiyor) ve
`backend-engineer` (plan.service atıyor) **farklı yollardan** aynı blokaja vardı. Bir
uygulama detayı olsaydı yalnız bir taraf görürdü.

## Karar (ürün sahibi, 2026-08-14)

```
plans.coverage_ratio    numeric(9,4) nullable
```

⛔ **`calculated_kpis` JSONB kovası AÇILMAZ** — bugün ihtiyaç olmayan bir şema esnekliği.
Tek alan yeter (`İlke 1`).

⛔ **Rapor katmanında yeniden türetme YASAK:** `recomputeRatioFromChildren`'ın kesişim
mantığını kopyalamak `İlke 4` ihlali olur — ve `T-177`'nin kendi notu *"B1 IS NOT CLOSED
AT THIS LEVEL"* diyor, yani kopya **henüz kapanmamış** bir mantığın kopyası olurdu.

## Yapılacak
- [ ] Migration `1804000000000` (**tahsis edildi**, seçme): tek kolon, `down` basit
- [ ] `plan.entity.ts` alanı
- [ ] `plan.service.ts:2621-2659` — **zaten hesaplanan** değeri persist et, türetme
- [ ] ⚠️ `plan.service.ts:2550`'deki **bayat atıf** düzeltilsin: *"frontend consumption is
      out of scope here — [[T-172]]"* yanlış adres (`T-172` `overallRoi` çöküşünü ele
      alıyor). Doğrusu: grid tüketimi → [[T-216a]], plan seviyesi → [[T-216b]]

## Done
- [ ] `migration:generate` bu kalemi göstermiyor (entity ↔ DB eşitliği)
- [ ] `npm run guards` 0 · `ratchet` 0 · `tsc` 0
- [ ] Değerin **DB'ye gerçekten yazıldığı** ölçüldü — dönüş değeriyle değil, **deltayla**
