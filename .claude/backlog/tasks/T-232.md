---
id: T-232
title: "`bitbucket-pipelines.yml` ÖLÜ ve YANILTICI — `K-2.6.13` sonrası tek-rol modelini miras verir"
epic: E-001
sprint: S-001
status: todo
assignee: architect
priority: P2
created: 2026-08-16
updated: 2026-08-16
touches:
  - collmind.backend/bitbucket-pipelines.yml
  - collmind.frontend/bitbucket-pipelines.yml
migration_seq:
---

## Bulgu

`backend-engineer`, `B4` turunda `bitbucket-pipelines.yml`'in `K-2.6.13` rol ayrımıyla
**senkron olmadığını** buldu ve *"CI bugün koşarsa düşer"* diye bildirdi:

```
bugün:  tek DB_USERNAME=_collmind_staging_user, HEM ana servise HEM db-init job'una
        DB_RUNTIME_* / DB_MIGRATE_*  HİÇ set edilmiyor (yalnız eski DB_PASSWORD secret'ı)
sonuç:  db-role-env.ts'in sessiz-sıfır yasağı → requireDbEnvVar ANINDA fırlatır
```

## ⚠️ Ama ölçüm aciliyeti DÜŞÜRDÜ — pipeline KOŞMUYOR

Team Lead ölçtü (2026-08-16):

```
üç uzak da            github.com/CollMind/...
.github/workflows     0 dosya (üç repoda da)
başka CI konfigi      hiçbiri (gitlab · circle · travis · drone · azure · Jenkins)
gh run list           her iki repoda da BOŞ — hiç workflow koşumu yok
bitbucket-pipelines.yml son dokunuş   2026-02-27   (repo son commit: bugün)
```

**Bitbucket Pipelines yalnız Bitbucket'ta koşar; kod GitHub'da.** Yani CI kırılmıyor —
**CI yok.** `CLAUDE.md §1`'in *"CTPM bugün yalnızca lokal geliştirme ortamında koşuyor,
deploy edilmiş staging/production YOK"* notuyla tutarlı.

## Asıl tehlike başka yönde — ve `P2` bu yüzden, `P3` değil

Dosya **yetkili görünüyor.** Konuşlandırmayı bir gün ayağa kaldıran kişi onu kaynak sanıp:

- tek `DB_USERNAME` modelini **miras alır** → `K-2.6.13`'ün az önce kaldırdığı
  ayrıcalıklı-rol bağını **geri getirir**
- `RUN_MIGRATIONS` dalını ana serviste açabilir (`entrypoint.sh`'ta hâlâ duruyor) →
  migrate kimliği runtime sürecine geri döner, `B4`'ün düzeltmesi **geçersizleşir**

📌 `T-223`/`T-225`'in sınıfı ama tersten: orada *"mekanizma var, yol yok"*du; burada
**yol tarif eden bir belge var, ama yolun kendisi yok** — ve tarif **bayat**.

## Karar gerektiren (ürün sahibi)

| yol | sonuç |
|---|---|
| (a) **sil** | en temiz; konuşlandırma geldiğinde sıfırdan ve doğru yazılır |
| (b) **güncelle** | `DB_RUNTIME_*`/`DB_MIGRATE_*` + secret adları; ama koşmayan bir dosyayı bakımda tutmak |
| (c) **başlığına ⛔ ÖLÜ uyarısı** | ucuz, ama bir sonraki okuyucu uyarıyı okumayabilir |

⚠️ **Ve `entrypoint.sh`'ın `RUN_MIGRATIONS` dalı ayrı bir kalem** — pipeline silinse bile
o kod yolu duruyor ve migrate kimliğini ana sürece geri getirebilir.

## Done tanımı
- [ ] Yol seçildi ve **gerekçesi yazıldı**
- [ ] `entrypoint.sh` `RUN_MIGRATIONS` dalının durumu karara bağlandı
- [ ] Seçim (b) ise: `K-2.6.13a`'nın *"`app_migrate` runtime dizgesinde ASLA"* şartı
      pipeline'da da **ölçülebilir** hâle geldi
