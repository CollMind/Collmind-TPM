---
id: T-126
title: "on-invoice / off-invoice importlarında satır-bazlı hata kanalı yok — tek hücre tüm dosyayı düşürüyor"
epic:
sprint:
status: todo
assignee: backend-engineer
priority: P2
created: 2026-08-10
updated: 2026-08-10
touches:
  - collmind.backend/src/modules/modes/actuals-first/on-invoice/services/on-invoice-file-parser.service.ts
  - collmind.backend/src/modules/modes/actuals-first/agreement-transaction/services/off-invoice-file-parser.service.ts
  - collmind.backend/src/modules/modes/actuals-first/on-invoice/services/on-invoice.service.ts
  - collmind.backend/src/modules/modes/actuals-first/agreement-transaction/services/agreement-transaction.service.ts
migration_seq:
---

## Bulgu ([[T-123]] sırasında ölçüldü)

`customer` importunda satır-bazlı hata kanalı **var** (`FieldParseError` / `parseErrors` →
`{ row, error_type, error_message }`, [[T-121]] (a) ile bağlandı).

Bu iki importer'da **hiç yok.** `getDateValue`/`getNumberValue`/`getFiscalPeriod`/
`getDiscountType` doğrudan `BadRequestException` fırlatıyor; throw `.map()` içinden
`parseExcel`/`parseCSV`'nin dış `catch`'ine çıkıyor ve **tüm dosyayı satır numarasız**
reddediyor.

**Ölçüldü:**
```
A1  fiscal_period = "çöp"   ->  THROW
A2  sağlam satır            ->  o da reddedildi (TÜM DOSYA)
```

## ⚠️ Bu task, T-123'te GERİ ALINAN bir katılığın ön koşuludur

T-123 madde 3, `off-invoice.getFiscalPeriod`'un sessiz `undefined`'ını bir `throw` ile
kapatmıştı — **§2.5 açısından doğru bir kural**. Ürün sahibi kararı (2026-08-10) onu **geri
aldırdı**, çünkü katılığın gideceği bir teslim yolu yoktu:

> *"Katılığı eklerken teslimi düzeltmemek, §2.5'i bir ucundan uygulayıp diğerini açık bırakmak
> olur."*

Geri alma gerekçesi **kodda yazılı** (`off-invoice-file-parser.service.ts`, `getFiscalPeriod`
sonundaki blok) ve bu task'a atıf veriyor.

> **Bu kanal geldiğinde o `throw` geri konmalı** — o zaman bozuk hücre **satırı** reddeder,
> dosyayı değil.

## ⚠️ Ve §2.5'in sınırı burada çizildi (kayda değer)

Ürün sahibi: **sessiz varsayılan ile sessiz fallback aynı şey değildir.**

- **Sessiz varsayılan** (§2.5 yasaklıyor): bilgi **uyduruyor** — `?? 0`, `catch { return 0 }`
- **Sessiz fallback** (buradaki): başka bir **kaynak** kullanıyor —
  `agreement-transaction.service.ts:109-119` sırayla `agreement.periodMonth`, sonra
  `invoiceDate`'ten türetilen dönem

İkincisi sessizdir ve iyileştirilmelidir, ama **uydurma değildir**.

## Yapılacak

- [ ] `customer`'daki `FieldParseError`/`parseErrors` desenini bu iki importer'a **taşı**
      (kopyalama değil — ortak bir yere çıkar; §7)
- [ ] Üçlü ayrım korunsun: **verilmemiş** (`undefined`, meşru) · **verilmiş ama okunamamış**
      (satır hatası) · **dosyanın kendisi bozuk** (dosya hatası, **kalmalı**)
- [ ] `on-invoice.service.ts` ve `agreement-transaction.service.ts` satır hatalarını
      kullanıcıya döndürsün — bu servislerin bugün bir `errors` sözleşmesi var mı, **ölç**
- [ ] T-123'ün geri alınan `throw`'u geri konsun (`off-invoice.getFiscalPeriod`)
- [ ] Ölçüm: bir dosyada bir bozuk hücre → **diğer satırlar iniyor**, hata **doğru `row`
      numarasıyla** raporlanıyor

## İlgili

- [[T-124]] (`amount <= 0` asimetrisi) — o kural da ilk kez gerçekten tetiklenecek; satır
  kanalı geldiğinde mesajı satır bazında görünür olur.
  ⚠️ Ve ölçülmüş bir sıra etkisi var: DTO literal'inde `invoiceDate`, `amount`'tan **önce**
  değerlendiği için ikisi de bozuk bir satırda **tarih mesajı** öne çıkıyor.
- [[T-125]] (yüzde biçimli hücre)

## Done tanımı
- [ ] Bir bozuk hücre dosyayı düşürmüyor; hata satır numarasıyla raporlanıyor
- [ ] T-123'ün `throw`'u geri kondu
- [ ] `npm run guards` 0 · `ratchet` 0 (**36/54/10**) · e2e yeşil
- [ ] `code-reviewer` onayı
