---
id: T-363
title: "Danışman paketi Bölüm 1 — 5-halka akış doğrulaması (bugünkü canlı ölçüm + screenshot)"
epic:
sprint:
status: review
assignee: architect
created: 2026-09-03
updated: 2026-09-03
touches:
  - docs/domain/BOLUM1_AKIS_DOGRULAMASI_TASLAK.md
  - docs/domain/screenshots/
migration_seq:
---

## Mod
**SALT-OKUMA + SCREENSHOT.** Kod/migration/e2e değişikliği YOK. Yazma alanı yalnız `docs/domain/`.

## Kaynak
- `docs/DISIPLIN.md` · `CLAUDE.md`
- `docs/process/FAZ1_KAPANIS_BEYANI.md` §2 — 5-halka mührü (2026-08-28 tarihli, DEVRALINACAK ama
  yeniden ölçülecek — o tarihten bu yana BL-1..BL-4b, T-334, T-335/336/337 vb. çok sayıda commit var)
- `docs/research/DEMO_EXCEL_KPI_TACTIC_REFERANSI.md` §4 — Promo statü akışı + CTPM fark tablosu (6 kalem)

## Paralellik
`T-362` (architect, `scripts/guards/app-runtime-grants.sh`) eşzamanlı koşabilir — dosya kesişimi yok.
Commit bu görev **T-362'nin push'undan sonra** yapılır (ürün sahibi talimatı). `T-325` kilidine göre
tam e2e koşarken screenshot alınmaz.

## İş
5-halka zincirin her halkası için BUGÜNKÜ CANLI akış ölçülür (BRD/beyan değil, ekran+kod):
1. plan: ay+CPL+kategori → ürünler → uygun tactic'ler → adet → indirim → KPI → submit
2. gerçekleşme: hangi yükleme yolları CANLI (off-invoice batch / on-invoice / sales_actuals)
3. eşleştirme: bugün NE VAR (grain, girdi-alanı, otomatik bağ yok — açıkça yazılır)
4. hakediş/settlement/claim: canlı olan / şema-only olan
5. defter: ALLOCATE/RESERVE/CONSUME/RELEASE akışı
+ statü yaşam döngüsü (canlı enum + geçişler) — Excel §4 şemasıyla FARK tablosu

Her halka: hangi ekran · hangi rol · hangi kural gerçekten uygulanıyor · STUB/YOK olan adıyla
(bir stub'ı canlı diye anlatmak YASAK).

## Ekran görüntüleri
Planner / CM / Finance ile login → akış adım adım → PNG →
`docs/domain/screenshots/<halka>-<rol>-<adım>.png`; seed-verisiyle, e2e koşmazken.

## Çıktı
`docs/domain/BOLUM1_AKIS_DOGRULAMASI_TASLAK.md` — halka başına: akış-cümlesi · diyagram-tarifi
(mermaid) · screenshot listesi · `[ÖLÇÜLDÜ]`/`[STUB]`/`[YOK]` işaretleri · danışmana sorulacak
"sektörde böyle mi?" cümlesi.

## Kural
Sayı/statü yazarken ölçüm-kaynağı (dosya:satır / SQL sorgusu / ekran adı) birlikte yazılır;
"muhtemelen" yok.

## Done tanımı
- [x] Beş halkanın hepsi ölçülmüş (ya `[ÖLÇÜLDÜ]` ya gerekçeli `[ÖLÇEMEDİM]`) — iki paralel `architect`
      ölçüm turu (halka 1+statü · halka 2-3-4-5) + Team Lead'in bizzat canlı gezinmesi
- [x] Statü yaşam döngüsü fark tablosu güncel canlı enum'a göre — 3 yeni fark eklendi (Excel §4 tablosu)
- [x] Screenshot seti — **tamamlandı** (2026-09-03, kullanıcı tarafından): 68 PNG + `INDEX.md`,
      `docs/domain/screenshots/` altında. Dört rol (Planner 17, CM 10, Finance 10, Admin 31), salt-okuma
      (hiçbir form submit edilmedi/onay-red butonuna basılmadı). Erken teknik kısıt notu (Browser aracı
      PNG kaydetmiyor) bu turda **aşıldı** — kullanıcı kendi ortamından çekti.
- [x] Taslak dosyası `docs/domain/BOLUM1_AKIS_DOGRULAMASI_TASLAK.md` yazıldı — ekran görüntüleriyle
      genişletildi (§8)

## Ek not 2 — ekran turu dört bulgu daha çıkardı (üçü yeni task, biri mevcut task'a bağlandı)
4. `T-370` — "Finans Paneli" React crash, CM+ADMIN (P0, YENİ)
5. `T-371` — yetkisiz rotada sessiz `/dashboard` yönlendirmesi, CM (`/budget/ledger`) + FINANCE
   (`/plan-approvals`) (P2, YENİ) — `T-364`'ün görsel kanıtı da bu turda eklendi
6. Admin'e özgü `/agreement-approvals` ekranı ("Anlaşma Onayları") — Plan Onayları'ndan ayrı bir kuyruk,
   BOLUM1'de adlandırılmamıştı — taslağa dokümantasyon olarak eklendi (bug değil, envanter eksiği)
7. Admin > Audit Log "yakında eklenecek" stub'ı **mevcut `T-326` ile aynı** (denetim ekranı, API hazır
   sayfa stub) — yeni task açılmadı, çapraz-referans eklendi. Bölge Yönetimi boş — düşük öncelik, seed
   veri notu olarak taslakta kaydedildi, ayrı task açılmadı (TPM'in beş halkasını doğrudan etkilemiyor)

## Ek not — bu turda bizzat bulunan üç yeni kusur/gözlem (taslakta detaylı)
1. Kategori-scope seed verisi 8 kategoriden yalnız 3'ünü bir CM'ye bağlıyor — 5 kategoride açılan plan
   hiçbir CM tarafından görülemez/onaylanamaz (bug değil, seed kapsama boşluğu).
2. Finance "Financial Ledger" ekranındaki "CONSUME" etiketi `ledger_entries.entryDirection===DEBIT`'in
   ön-yüz çevirisi — `budget_transactions.tx_type=CONSUME` hâlâ 0 satır (ölü enum, T-360/ring-ölçümüyle
   tutarlı, ama bu turda ekranda somut olarak da görüldü).
3. Bütçe panelinde "Consumed ₺0" ile "Available" (%89,3) tutarsız görünüyor — RESERVE tutarı ayrı
   gösterilmiyor, kullanıcı "hiçbir şey tüketilmedi" okuyabilir.
