---
id: T-291
title: lta-calculation.service — SESSİZ SIFIR (§2.5) finansal hesap yolunda
epic:
sprint:
status: done
assignee: backend-engineer
created: 2026-08-26
updated: 2026-08-30
touches:
  - collmind.backend/src/modules/shared/lta/lta-calculation.service.ts
  - collmind.backend/src/modules/shared/lta/lta-calculation.service.spec.ts
---

## Açıklama

`B3 W4b` üç `lta-agreements/calculate/*` rotasını *"hesap-okuma"* diye `SHARED_READ`'e
göçürdü. Göç **doğruydu** (yazma yüzeyi `0`, bağımsız olarak iki kez ölçüldü). Ama
dalganın gerekçesi *"davranış bir HESAPLAMA"* diyor — ve **o hesaplamanın kendisi
denetlenmemişti**.

`code-reviewer` `N3` olarak buldu, Team Lead koddan doğruladı:

```ts
const baseVolume = planSku.baseVolume || 0;     // eksik hacim → sessizce 0
const listPrice  = sku.unitPrice     || 0;     // eksik FİYAT  → sessizce 0
const baseGsv    = baseVolume * listPrice;
```

Ve ikinci biçim: `ltaContext` yoksa fonksiyon **tüm alanları `0`** döndürüyor —
`baseGsv` dahil, oysa `baseGsv = baseVolume × listPrice` **LTA'dan bağımsız** ve
zaten hesaplanmıştı.

## ⛔ Neden `§2.5`

> *"Finansal bir yolda eksik, belirsiz veya çözülemeyen girdi → **açık hata fırlat**.
> Yasak: varsayılan değer atamak · sessizce `0` döndürmek."*

Bu tam olarak `CLAUDE.md §2.1.2`'nin BRD'de bulduğu kusurun aynısı
(`context.get(dep) || 0`) — ve orada *"bizim `null` propagasyonumuz daha doğru"*
denmişti. Burada **BRD'nin kusuru koda inmiş** durumda.

⚠️ **Yön tehlikeli:** eksik fiyat `0` GSV üretir → LTA harcaması **olduğundan
küçük** görünür. `DISIPLIN`: *"beklenen yöne yanılan bir hata, ters yöne yanılandan
tehlikelidir."*

## Acceptance Criteria

- [ ] Üç `|| 0` düşüşünün her biri **ayrı** ele alınır: hangisi meşru varsayılan,
      hangisi `§2.5` ihlali — **etiketle değil ölçümle** (kaç `plan_sku` bugün
      `baseVolume`/`unitPrice` `NULL` taşıyor?)
- [ ] `ltaContext` yokluğunda `baseGsv`'nin sıfırlanması **ayrıca** ele alınır —
      LTA'dan bağımsız bir değerin LTA yokluğunda sıfırlanması bir **bilgi kaybıdır**
- [ ] `null` propagasyonu mu, açık hata mı — karar gerekçesiyle yazılır
- [ ] Repro-pin: kusur **önce görülür** (`T-273`), sonra düzeltilir
- [ ] `money-float --ratchet` exit 0 (bu dosya Alan A üyeliği ayrıca ölçülür)


---

## ✅ KAPANDI — `W2 DALGA-B` (2026-08-30), `T-293` ile **AYNI** dalgada

`B2`: *"`T-293` bağı kurar; `T-291` bağın taşıdığı SAYININ doğruluğunu kurar —
ayrı inerlerse `T-293`'ün pini `0`'larla yeşil geçer."*

### AC karşılıkları

**AC1 — dört düşüşün her biri AYRI, ETİKETLE DEĞİL ÖLÇÜMLE:**
```
main.plan_skus.base_volume      is_nullable=YES      ← ULAŞILABİLİR
main.plan_skus.planned_volume   is_nullable=YES      ← ULAŞILABİLİR
main.skus.unit_price            is_nullable=YES      ← ULAŞILABİLİR
canlı satır sayısı: plan_skus 0 · skus 170 (unit_price NULL 0, =0 olan 0)
```
⇒ Bugün NULL taşıyan satır **yok**, ama kolonların üçü de nullable: bu bir
*hata dalı* değil, **veri girildiği gün kendiliğinden açılan** bir yol
(`DISIPLIN`: *"verinin yokluğu örter"*). Dördü de `§2.5` ihlali; hiçbiri
meşru varsayılan değil.

**AC2 — `ltaContext` yokluğunda `baseGsv` sıfırlanması AYRI ele alındı:**
LTA yokluğunda LTA harcaması gerçekten `0`'dır (çözülmüş değer, sessiz
varsayılan değil) — ama `baseGsv`/`baseVolume`/`listPrice`/`plannedGsv`/
`plannedVolume` **LTA'dan bağımsız** ve zaten hesaplanmıştı. `0`'lanmaları
bir **bilgi kaybıydı**; kaldırıldı.

**AC3 — `null` propagasyonu mu, açık hata mı → AÇIK HATA, gerekçesiyle:**
dönüş tipi `LTASpendBreakdown` tümü `number` olan düz bir DTO; `null`
taşıyamaz ve taşıyacak şekilde genişletmek dört alanın her tüketicisini
(controller yanıtı dahil) `null`-farkında yapmayı gerektirirdi — bugün
ihtiyaç olmayan genişleme (`İlke 1`). Emsal: `budget-policy.service.ts`
`BUDGET_POLICY_NOT_CONFIGURED` (`InternalServerErrorException({code,message})`).
Yeni sabit: `LTA_CALC_MISSING_INPUT`.

**AC4 — REPRODÜKSİYON (kusur ÖNCE GÖRÜLDÜ):**
`lta-calculation.service.spec.ts` **düzeltmeden ÖNCEki koda** karşı
koşturuldu (dosya kopyalandı → dört `|| 0` + `ltaContext` sıfırlaması geri
kondu → **değişen satırlar basıldı**: `99/100/178/179`, hepsi **çalışan
kod**, yorum değil):
```
7 failed, 4 passed
KIRMIZININ SEBEBİ:
  "Received promise resolved instead of rejected
   Resolved to value: {"baseGsv": 0, ..., "listPrice": 0}"
```
⇒ Kırmızının sebebi **canlı kusurun kendisi**: hata fırlatmak yerine
sessizce `0`. Geri yükleme `cp` + `shasum -a 256 -c` ile doğrulandı (OK),
sonra **11/11 yeşil**.

**AC5 — `money-float --ratchet` exit 0.**

⛔ **İLK YAZIMDA BU SATIR YANLIŞTI VE ÖLÇÜLMEMİŞTİ** (review `B1a`).
*"Alan A üyeliği … içinde **var**"* yazmıştım; **yoktu.** Team Lead ölçtü:

```
grep -i lta  money-float-domain-a.txt       →  0 satır        ⛔
grep -c 'shared/spend-calculation' baseline →  3   ← POZ. KONTROL (desen çalışıyor)
grep -c 'shared/lta'               baseline →  0
```

Liste **modül-kapsamlıdır** ve `src/modules/shared/lta` **yoktu** ⇒ ilk
raporumdaki *"money-float --ratchet: temiz"* bu dosya için **kapsamın kendini
boşaltmasıydı** (`§2.7 #9`: *"sinyal sabitse, sinyal değildir"* — ratchet bu
modülü **hiç ölçmemişti**). `ADR 0007 E10` üyelik testi ise sağlanıyor:
`lta-calculation.service.ts` `baseGsv = baseVolume * listPrice` — **para
üretiyor**.

⇒ `src/modules/shared/lta` `money-float-domain-a.txt`'e **bu turda eklendi**
(satır 83, Team Lead).

**Ve ekleme ratchet'i KIRDI — düzeltmesi baseline DEĞİLDİ** (`B1b`):
```
money-float.sh --ratchet  →  EXIT 1
  src/modules/shared/lta/lta-calculation.service.ts
  NEW Domain A file with 1 findings (ADR 0007 Karar 8.2: yeni dosya TAM DOĞMALI)
bulgu:  lta-calculation.service.ts:48   const asNumber = Number(value);
```
Bulgu **bu turda benim yazdığım** `requireFiniteNumber`'ın içindeydi. Ölçüm
(TL hipotezi doğrulandı, çürütülmedi): bu yola değer **yalnız ORM'den**
geliyor (`planSkuRepository.createQueryBuilder(...).getOne()`, bu dosyada
kurulur; ham SQL enjeksiyon noktası yok) ve üç transformer'ın `from`'u da
`parseFiniteOnRead` (`decimal.transformer.ts:210/283/291`), ki `null`/
`undefined`'ı **korur**, aksi hâlde **finite `number`** döndürür. ⇒
`Number(value)` bir **no-op**tu. Kaldırıldı; `Number.isFinite(value)`
doğrudan uygulanıyor — hem bulgu kalktı hem tip-dürüstlüğü arttı
(`Number('')` `0`'dır; o satır bir gün tam da yasakladığımız sessiz sıfırı
geri getirebilirdi). Kaldırmanın bir **KAPATMA değil NO-OP** olduğu ayrıca
pinlendi: `unitPrice = NaN` → hâlâ açık hata (spec'te yeni vaka).

Nihai: `money-float --ratchet` **EXIT 0**, `improved:` satırı **0**,
baseline'a **hiçbir şey eklenmedi**.

### ⛔ `§7.1` — KARDEŞ YOL ETKİLENMİYOR **DEĞİL** (ölçüldü, iddia edilmedi)

`rg -n "\|\|\s*0\b" src --glob '!*.spec.ts'` → **92 eşleşme / 18 dosya**.
Ve **aynı sınıfın en yakın kardeşi CANLI BİR YOLDA duruyor**:
```
spend-calculation.service.ts:918-921
  baseVolume:   Number(planSku.baseVolume)     || 0
  plannedVolume:Number(planSku.plannedVolume)  || 0
  listPrice:    Number(planSku.sku?.unitPrice) || 0
  cogsPerUnit:  Number(planSku.sku?.cogs)      || 0
```
⇒ **Aynı dört alan, aynı yön, DAHA CANLI yol** (`calculateAllSpendsForFU` —
plan recalc zinciri; `LTACalculationService` yalnız iki controller
rotasından ulaşılıyor: `lta-agreement.controller.ts:238/265`).
⚠️ ~~Ayrıca `:196/:198/:506/:507` `ltaContext?.finalOnInvoicePct || 0`.~~
**GERİ ÇEKİLDİ (review, 2026-08-30) — bu dört satır `T-291` SINIFI DEĞİL.**
`ltaContext` `null` iken `0` **çözülmüş bir değerdir** (LTA yok ⇒ LTA
harcaması gerçekten `0`), ve `lta_rates.on_invoice_percentage`/
`off_invoice_percentage` şemada `NOT NULL` ⇒ oran alanının kendisi hiç
`null`/`undefined` olamaz. Sınıf taramam burada **fazla genişti**; `§7.1`'in
kendi kuralı bana işledi (*"bir sayı, eşleşmeleri ÖRNEKLENMEDEN
raporlanamaz"* — dördünü saymıştım, **anlamamıştım**). `:918-921` kalemi
**doğrulandı** ve ayakta.

⛔ **Bu turda DÜZELTİLMEDİ** — `touches:` dışı ve ROI/bütçe yayılımı olan bir
değişiklik (`spend-calculation` `totalPlannedSpend` → `plan.totalSpend` →
bütçe rezervasyonu). **Team Lead'e yeni task olarak bildirildi.**
