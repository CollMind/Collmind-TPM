---
id: T-355
title: "mergeConfig setupFiles'ı BİRLEŞTİRİYOR — tests/setup.ts iki kez yükleniyor, server.use() sessizce ölüyor"
epic: E-001
sprint: S-001
status: todo
assignee: debugger
priority: P1
created: 2026-09-02
updated: 2026-09-02
touches:
  - collmind.frontend/vitest.config.ts
  - collmind.frontend/tests/setup.ts
migration_seq:
---

## Bulgu (`T-353` şeridi, yan bulgu 1 — 2026-09-02)

`vitest.config.ts`'i `mergeConfig` ile genişleten bir konfigürasyon, `setupFiles` dizisini
**birleştiriyor** ⇒ `tests/setup.ts` **İKİ KEZ** yükleniyor ⇒ MSW sunucusu iki kez kuruluyor
⇒ **her `server.use()` override'ı SESSİZCE ölüyor.**

**Belirti:** 19 test *"promise resolved instead of rejecting"* / **bayat payload** ile düşer.

## ⭐ NASIL YAKALANDI — `§2.7 #4`'ün CANLI vakası

Ajan bunu **kendi tanı aracını kurarken üretti**, ve **boşta da aynı şekilde düştüğü için**
yakaladı:
> **KANIT KURULUMU, ÖLÇÜLEN DURUMU DEĞİŞTİRMİŞTİ.**

⛔ Yakalanmasaydı `3×` çekişme sonucu **gerçek sanılacaktı** — yani teşhisin tamamı
yanlış temele oturacaktı. `§2.7 #4` (*"fixture yolunu bildirime eklemek boş-durum
davranışını yok eder"*) ailesinin **konfigürasyon** yüzü.

## Neden bu bir TASK

`T-353` bunu **düzeltmedi** (kendi kapsamı değildi) ve tuzak **duruyor**: `vitest.config.ts`'i
`mergeConfig` ile genişletecek **herkes** aynı deliğe düşer — ve düştüğünü **anlamaz**,
çünkü belirti *"testler yanlış davranıyor"*, *"config bozuk"* değil.

## Acceptance
- [ ] `setupFiles` çift-yükleme **üretilebiliyor** (reprodüksiyon şartı) ve **kapatılıyor**
- [ ] `tests/setup.ts` çift yüklemeye karşı **kendini koruyor** ya da `mergeConfig`
      kullanımı **yasaklanıyor** (guard/lint/kontrat testi)
- [ ] ⛔ Koruma **mutasyonla sınandı**: koruma kaldırılınca kırmızı, hem de bir
      **assertion** ile (derleme hatası değil)
