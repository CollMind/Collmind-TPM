---
id: T-328
title: cash-flow-projection endDate'i AY-GÜN TAŞMASI yaşıyor — istenenden GENİŞ pencere döndürüyor
epic:
sprint:
status: todo
assignee: debugger
created: 2026-08-29
updated: 2026-08-29
touches:
  - collmind.backend/src/modules/shared/finance-reporting/finance-reporting.service.ts
migration_seq:
---

## Bulgu — `W0` turunda **kapsam dışı** çıktı, ve bir TEST sorunu DEĞİL

`finance-reporting.service.ts:1084+`:
```ts
const startDate = filters.startDate ? new Date(filters.startDate) : new Date();
const endDate = new Date(startDate);
endDate.setMonth(endDate.getMonth() + months);      // ⛔ GÜN TAŞMASI
```

`setMonth`, hedef ayda o **gün yoksa** bir sonraki aya **taşar** — sessizce.

### Ölçüm `[ÖLÇÜLDÜ]` 2026-08-29
```
start 2026-08-29  months=6  →  end 2027-03-01  monthDiff=7   ⛔
start 2026-08-31  months=6  →  end 2027-03-03  monthDiff=7   ⛔
start 2026-01-31  months=1  →  end 2026-03-03  monthDiff=2   ⛔  (EN KÖTÜ: 2 KAT)
POZ.KONTROL
start 2026-08-28  months=6  →  end 2027-02-28  monthDiff=6   ✅  ← DÜN YEŞİLDİ
start 2026-08-01  months=6  →  end 2027-02-01  monthDiff=6   ✅
start 2026-08-15  months=6  →  end 2027-02-15  monthDiff=6   ✅
```

## ⛔ NEDEN BU BİR *"FLAKY TEST"* DEĞİL

`test/cash-flow-projection-months-contract.e2e-spec.ts:105` **dün geçiyordu, bugün
düşüyor** — ve tetikleyen şey **yük değil, TAKVİM**.

> `DISIPLIN`: *"Flaky bir test, ürünün **aralıklı bozulduğunun** kanıtı olabilir."*
> Burada aralığı **ayın günü** belirliyor: `29`/`30`/`31`'inde başlayan bir
> projeksiyon, hedef ay **daha kısaysa** taşar.

⇒ *"Ortam yavaş"* / *"test kırılgan"* açıklamalarının **takvim** hâli. İkisi de yanlış.

## ÜRÜN ETKİSİ — sessiz ve finansal

Uç, **istenenden GENİŞ** bir projeksiyon penceresi döndürüyor ve **hiçbir yerde
uyarmıyor**. `months=6` isteyen bir kullanıcı `7` aylık pencere alıyor.
📌 Yön **tehlikeli**: pencere **genişliyor** ⇒ nakit akışı **olduğundan iyi**
görünebilir *(daha fazla tahsilat penceresi)*.

## İş
1. Ay ekleme **gün-taşmasız** yapılır — hedef ayın son gününe **clamp**.
   ⚠️ `§7`: bu projede bir tarih yardımcısı **var mı**? ARA, bul, KULLAN. Yoksa
   gerekçesiyle yaz. *(Bu proje aynı yeteneği birden çok kez yazdı — dört `decimal`
   implementasyonu `T-105`'te tek yere indirildi.)*
2. ⛔ **`§7.1` — SAY:** aynı `setMonth`/`setDate` deseni **başka nerede**?
   `grep -rn "setMonth\|setDate\|setFullYear" src` — ve **her birini** ölç.
   *"Kardeş yol etkilenmiyor" iddiası ölçülmeden yazılamaz.*
3. Pin **tarihten bağımsız** olmalı: `startDate` **parametreyle** verilir
   (`29`/`31` dahil), `new Date()`'e bağlı kalmaz.
   ⛔ **Reprodüksiyon:** düzeltmeden önce `2026-01-31 + 1 ay` → `monthDiff=2`
   **GÖRÜLÜR**, sonra `1` olur.

## Acceptance
- [ ] `29`/`30`/`31` başlangıçları için `monthDiff == months` (parametreli pin)
- [ ] Kardeş `setMonth`/`setDate` yolları **sayıldı ve raporlandı**
- [ ] Reprodüksiyon **iki çıktılı**
- [ ] Tam e2e `792/792`, `T-047 PASS`
