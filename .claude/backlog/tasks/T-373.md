---
id: T-373
title: On-invoice zarf eşleşmesi KATEGORİYİ hiç ayırt etmiyor — sessiz yanlış-zarf (finansal yol)
epic:
sprint:
status: todo
assignee: debugger
created: 2026-09-06
updated: 2026-09-06
touches:
  - collmind.backend/src/modules/master-data/sku/sku.service.ts
  - collmind.backend/src/modules/modes/actuals-first/on-invoice/on-invoice.service.ts
  - collmind.backend/src/modules/shared/budget/budget.repository.ts
---

## Bulan
`qa-engineer`, halka-2 QA şeridi (2026-09-06) — `INV-R-001/002` e2e'sini kategori farkıyla
ayırt etmeye çalışırken. **Team Lead yapısal olarak doğruladı.**

## Mekanizma `[ÖLÇÜLDÜ]`
```
sku.service.ts:77   findOne → relations ['genericUnit','forecastingUnit']   genericUnit.category YOK
sku.service.ts:95   başka bir metot 'genericUnit.category' YÜKLÜYOR         ⇒ iki metot TUTARSIZ
on-invoice.service.ts:454   if (sku.genericUnit && sku.genericUnit.category) ⇒ her zaman undefined
                            ⇒ findEnvelopeByDimensions'a category HİÇ geçmiyor
budget.repository.ts:165    if (category) { … }                              ⇒ blok HİÇ çalışmıyor
```
> ### **ZARF, YALNIZ KANALA GÖRE SEÇİLİYOR. AYNI DÖNEM/KANALDA BİRDEN ÇOK KATEGORİ ZARFI**
> ### **VARSA, SESSİZCE YANLIŞ ZARF — FİNANSAL YOLDA `§2.5` İHLALİ.**

## Neden görülmedi
`T-057` split-envelope testi: her dönemde **tek zarf** ⇒ kategori filtresi hiç ayırt edici
olamadı (`§2.7` vaka 6: *kapsam var, ayırt etme gücü yok*). Ve `if (category)` **`else`'siz**
— eksik girdi **hata fırlatmak yerine** filtreyi düşürüyor.

## Düzeltme yönü (debugger ÖLÇER, seçmez)
1. **Reprodüksiyon ÖNCE:** aynı dönem+kanalda iki kategori zarfı kur, on-invoice partisi
   geçir, **yanlış zarfa** düştüğünü GÖR (yönsüz: görülmezse hipotez elenir).
2. `findOne` relation'ı `:95` ile hizalanır — ya da `on-invoice` kategori-yüklü metodu çağırır.
   **`§7`: iki `findOne` neden farklı? Ölç.**
3. ⛔ `budget.repository.ts:165`'teki `if (category)` **`else`siz kalamaz** — kategori
   çözülemediyse **açık hata** (`§2.5`), sessiz "kanal-yalnız" eşleşme DEĞİL.
   ⚠️ Ama bu, bugün kategori taşımayan meşru bir çağıran varsa **onu kırar** — **`§7.1`:
   düzeltmeden önce SAY**, `findEnvelopeByDimensions`'ın tüm çağıranları.
4. Regresyon testi: iki-kategori-zarf fixture'ı (`qa` e2e'si kanalla ayırt ediyor; kategori
   ayrımı ayrıca pinlenir).

## Done tanımı
- [ ] `docker ps --filter "label=com.docker.compose.project=tpm"` boş
- [ ] Reprodüksiyon görüldü → düzeltme → aynı fixture yeşil
- [ ] `findEnvelopeByDimensions` çağıranları sayıldı; kırılan yok (ya da kırılan adıyla)
- [ ] unit + e2e yeşil · `npm run guards` exit 0 · `money-float --ratchet` exit 0
- [ ] `code-reviewer` onayı
