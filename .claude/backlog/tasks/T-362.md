---
id: T-362
title: app-runtime-grants guard'ı KAYNAK ↔ KAYNAK karşılaştırıyor — ORTAM hiç ölçülmüyor
epic:
sprint:
status: todo
assignee: architect
created: 2026-09-03
updated: 2026-09-03
touches:
  - collmind.backend/scripts/guards/app-runtime-grants.sh
---

## Ölçülmüş kusur (2026-09-03, `BL-4` kapanış turu)

```
kaynak A   route'tan erişilebilen entity'lerin TABLO adları        (KOD)
kaynak B   02-runtime-grants.sql'in GRANT verdiği tablo adları     (KOD)
kontrol    A \ B = ∅
psql çağrısı sayısı: 0    ⛔ GUARD DB'YE HİÇ BAKMIYOR
```

**Vaka:**
```
02-runtime-grants.sql:724  GRANT SELECT, INSERT ON ...baseline_volume_import_batch_rows
                           TO app_runtime;                       ← BEYAN VAR
canlı DB                   app_runtime → SIFIR ayrıcalık          ← UYGULANMAMIŞ
guard                      app-runtime-grants: 0 bulgu            ← YEŞİL
SET ROLE app_runtime; SELECT ...  →  permission denied            ← NEDENSELLİK DOĞRULANDI
```

⇒ `POST /master-data/baseline-volumes/upload` bu ortamda **her çağrıda `500`**;
`BL-2`/`BL-3`/`BL-4` uçtan uca **çalışmıyor** — ve `tsc`/unit/guards **yeşil**.

> ### **YENİ KÖRLÜK SINIFI: *"KAYNAK BEYAN EDİYOR, ORTAM UYGULAMADI"***
> İki taraf da **koddur**; hiçbiri **koşan sistem** değildir.

📌 Guard'ın şekli **ürün sahibi tarafından belirlenmişti** ve iki sınırı **yazılıydı**
(kolon düzeyi · SELECT↔INSERT ayrımı). **Bu üçüncü sınır YAZILI DEĞİLDİ.**

⚠️ Ve guard **T-249'u yakalamak için** doğmuştu (*"kod doğru · rota canlı · tablo var ·
izin yok"*) — bugün **tam o sınıfın bir vakasını kaçırdı**.

## Seçenekler (ürün sahibi kararı — ajan seçmez)
- **(a)** Guard'a **üçüncü bir kaynak** eklenir: canlı DB (`information_schema.role_table_grants`).
  DB'ye ulaşılamadığında **`ÖLÇEMEDİM` (exit 2)** — sessiz yeşil DEĞİL.
- **(b)** Ayrı bir guard (`runtime-grants-applied`) — mevcut guard'ın sözleşmesi bozulmaz.
- **(c)** Guard değişmez; sınır **yazılır** ve uygulama disiplini başka bir yere bağlanır.

## Done tanımı
- [ ] Seçilen şeklin **bilinen-kırmızı** kanıtı: bu tablo GRANT'siz iken guard **KIRMIZI** vermeli
- [ ] Bilinen-yeşil kanıtı: GRANT uygulandıktan sonra **YEŞİL**
- [ ] `npm run guards` exit 0 · `code-reviewer` onayı
