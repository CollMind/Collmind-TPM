---
id: T-316
title: budget_policies CANLANIR — davranış merdiveni okunur hâle gelir (+ seed 80/90/100, AYNI COMMIT)
epic:
sprint:
status: todo
assignee: backend-engineer
created: 2026-08-28
updated: 2026-08-28
touches:
  - collmind.backend/src/modules/shared/budget/budget-policy.service.ts
  - collmind.backend/src/modules/shared/budget/budget.module.ts
  - collmind.backend/src/database/seeds/budget-policy.seed.ts
migration_seq:
---

## Hüküm — `Z57 §1` (ürün sahibi, 2026-08-28)

`DUR-1` = **(a)**: `budget_policies` **canlanır**, ayrım **korunarak**.

```
budget_alert_configurations   RENK       (görsel durum)   ← DOKUNULMAZ
budget_policies               DAVRANIŞ   (karar-eşiği)    ← CANLANIR
```

⛔ **`BudgetThresholdService` ile BİRLEŞTİRME YASAK.** Yanına gelir, içine girmez.
Gerekçe (`Z57 §1`, ürün sahibi): renk tablosundan okumak `T-276`'da kapatılan
**katman-karışıklığının eşik hâli** olurdu — *"bugün çakışma yok diye iki katmanı
tek kaynağa bağlamak, yarın **rengi değiştirdim, davranış değişti** sürprizini
üretir."*

📌 `§7` cevabı **yazılı**: bu yeteneğin mevcut implementasyonu **arandı**
(`grep -rl BudgetPolicy src` → runtime dosya **0**; pozitif kontrol aynı grep
şekliyle `BudgetAlertConfiguration` → **2**, `Notification` → **6**). Yani
**mevcut implementasyon YOK**, ve `BudgetThresholdService` **farklı bir olguyu**
okuyor.

## ⛔ ŞART — seed düzeltmesi AYNI COMMIT'te (`Z57 §1a`)

```
canlı DB   warning 50.00 · finance_review 60.00 · mode NOTIFY   ← ÖLÇÜLDÜ
K-2.2.8    görüşlü varsayılan 80 / 90 / 100
```

> **Örtü kaldırılırken altındaki AYNI COMMIT'te** — bugün `50/60` zararsız (okuyucu
> yok); **okuyucu doğduğu an yanlış-davranış üreteci** olur.

⇒ `budget-policy.seed.ts` **aynı commit'te** `80/90` yazar.
⚠️ **Canlı DB'deki mevcut satır da güncellenmeli** — seed dosyasını değiştirmek
canlı satırı değiştirmez. Seed'in idempotency davranışı **ölçülür** (`ON CONFLICT`
mi, `skip if exists` mi?) ve satırın gerçekten `80/90`'a döndüğü **DB'den
doğrulanır**. Dönmüyorsa: bu bir migration kalemidir → **DUR**, Team Lead'e bildir.

## İş

1. `BudgetPolicyService` — `K-2.2.8a/b/c` çözümlemesi:
   - `K-2.2.8a` iki boyut: **kanal** ve **kategori** (`channel_id` / `category_id`)
   - `K-2.2.8b` çakışma DB'de imkânsız (`UNIQUE`, `NULLS NOT DISTINCT` — `1810` deseni)
   - `K-2.2.8c` **en spesifik kayıt kazanır**; eşit spesifiklik **mümkün değil**
2. Dönüş: `{ warningPct, financeReviewPct, financeReviewMode }` + **kaynak** bilgisi
3. ⛔ **`§2.5` SESSİZ SIFIR YASAĞI:** eşik **okunamıyorsa** varsayılan atama **YOK** —
   **açık hata**. ⚠️ `BudgetThresholdService`'in `fallback` deseni **emsal DEĞİLDİR**:
   orada fallback `ThresholdSource`/`ThresholdFallbackReason` ile **görünür**; burada
   `§2.5` bir **finansal karar eşiği** için **fallback bile istemiyor**. Kararsızsan → DUR.

## Acceptance
- [ ] `BudgetPolicyService` var, `K-2.2.8c` en-spesifik çözümlemesi **iki girdi iki çıktı** ile test edildi
- [ ] seed `80/90` + **canlı DB satırı DB'den doğrulandı** (`50/60` kalmadı)
- [ ] `BudgetThresholdService` **DEĞİŞMEDİ** (`git diff` ile kanıt)
- [ ] eşik okunamama yolu → **açık hata** (test)
- [ ] `CLAUDE.md §4.2` Done listesi tam (ölçüm ortamı temizliği dahil)

## ⛔ DUR listesi
- `BudgetThresholdService`'e dokunma
- `budget_alert_configurations`'a **`%90` kademesi ekleme** — `Z57 §1` bunu reddetti
- Migration yazma — bu task'ın `migration_seq`'i **yok**; gerekiyorsa **DUR**
- ⚠️ **Doğrulamanı izole `git worktree`'de yap** (`T-317` paralel koşuyor);
  paylaşılan ağaçta `--fix` / mutasyon / `git checkout` **çalıştırma**
