---
id: T-313
title: 🔒 Tenant ayarları ekranı YOK — beş uç canlı, arayüz sıfır
epic:
sprint:
status: todo
assignee: frontend-engineer
created: 2026-08-27
updated: 2026-08-27
touches:
  - collmind.frontend/src/routes/index.tsx
  - collmind.frontend/src/components/features/
---

## ⛔ `EK_E` `🔒` SINIFI — *yetenek var, arayüzü yok*

`Z46 §1(b)` şöyle hükmetti:
> *"`GET`/`PATCH` **kendi-kayıt** (tenant **ayarları**) **KALIR** — kiracı-içi
> **MEŞRU YÜZEY**."*

`T-307-m2` backend'i **korudu** ama frontend'in **tamamını** öldürdü (haklı olarak:
tüm yüzey `:id`-parametreli **platform-konsolu** şeklindeydi). Sonuç:

```
CANLI UÇ (beşi de assertSelfTenant kilidinde):
  GET /tenants/:id · PATCH /tenants/:id · :id/activate · :id/suspend · :id/stats
FRONTEND TÜKETİCİSİ:  SIFIR   (ölçüldü — T-307-m2 review)
```

⇒ **`Z46 §1(b)`'nin *"meşru yüzey"* gerekçesi, bugün YÜZEYİ OLMAYAN bir hüküm.**

## ⛔ NİÇİN BİR TASK — ve niçin bir PARANTEZ YETMEZ

`T-307.md`'de bu durum **kapanmış bir bloğun içinde bir parantez** olarak duruyordu.
`DISIPLIN`: ***"Bilinen eksiklik TODO ile değil, TASK ile kaydedilir (ZORUNLU)"*** —
ve **kapanmış bir task'ın anlatı parantezi bir task değildir; kimse ona bakmaz.**

> ⛔ **`🔒` BİR KABUL DEĞİL, BİR ALARMDIR** (`CLAUDE.md §2.1`) — ve bugün **alarm
> ötmüyordu**. Bu task alarmı **kurar**.

⚠️ **`blocked-unreachable` DEĞİL:** HTTP rotası **var** ve çalışıyor; eksik olan **UI**.
İkisi farklı sınıf — `§4.2`'nin *"üretim çağrı yolu"* maddesi **karşılanıyor**, `EK_E`'nin
*"arayüz"* sütunu **karşılanmıyor**.

## Acceptance Criteria

- [ ] **Yüzeyin ŞEKLİ karara bağlanır:** *"kendi ayarlarım"* ekranı **`:id` TAŞIMAZ** —
      kiracı **JWT'den** türer. ⛔ `:id`-parametreli bir sayfa, öldürdüğümüz
      **platform-konsolunu** geri getirir
- [ ] `activate`/`suspend`'in yeri ayrıca sorulur: bunlar **ayar** mı, **yaşam-döngüsü**
      mü? *(`Z46 §1` yaşam-döngüsünü **operatör-yoluna** taşıdı — kendi kiracını askıya
      almak hangi sınıfta?)* ⚠️ Ve `T-311`: **`suspend` bugün hiçbir şey yapmıyor**
- [ ] `EK_E` satırı **ancak bir karar kaydıyla** girer (belge **donmuş**)
- [ ] Tel-protokol pini **render'dan bağımsız** *(`DISIPLIN`: kullanımın yokluğu
      sözleşme kırığını örter)*

## Done tanımı
- [ ] Testler yeşil · `code-reviewer` onayı
