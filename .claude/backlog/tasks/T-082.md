---
id: T-082
title: "null taktik değeri: yazma kapısı 400 diyor, okuma tarafı sessizce atlıyor (asimetri)"
epic:
sprint:
status: todo
assignee: backend-engineer
priority: P2
created: 2026-08-05
updated: 2026-08-05
touches:
  - collmind.backend/src/modules/shared/spend-calculation/spend-calculation.service.ts
migration_seq:
---

## Açıklama

**T-078 ailesi** — "değer yok" ile "sıfır" ayrımının aynı sınıfı, ama yeni bir yüzü: aynı girdiye
iki katmanın **farklı** cevap vermesi.

F2/C3 sonrası:

| Katman | `{ "CPP_ON_PCT": null }` girdisine cevabı |
|---|---|
| Yazma kapısı (`checkEnteredScale`) | **400** `INVALID_SCALE` — "value must be a finite number" |
| Okuma tarafı (`spend-calculation.service.ts:719`) | **sessizce atlıyor** — `if (val != null) put(...)`, `else` yok |

Yön doğru (CLAUDE.md §2.5: reddetmek atlamaktan iyidir) ve bugünün UI'ı `null` üretemiyor
(`PlanningGridEnhanced.tsx:1499` `isNaN` koruması). Ama **asimetri kayıt altında değildi** ve
diskte zaten `null` taşıyan bir satır varsa okuma tarafı onu hâlâ sessizce yutuyor.

`:719` C3 diff'inde **değiştirilmedi** — önceden var olan bir §2.5 vakası, C3'ün getirdiği
regresyon değil. Bulgu kaynağı: `code-reviewer`, C3 ikinci tur (S4).

## Ölçüm önce

- [ ] Diskte `tactics` içinde `null` değer taşıyan satır var mı? (`plan_fus`, şema `main`, port
      **5434**. Bugün 0 satır olabilir — o zaman vaka teorik, ama kural yine de yazılmalı.)
- [ ] `:719`'un `else` dalı ne yapmalı: fırlatmak mı, `MechanicInput`'a `null` taşımak mı?
      **T-078 ile aynı karar ailesi** — `rawOf`'un `?? 0` çöküşü ve `readEnteredValue`'nun
      `?? 0`'ı da aynı soruyu soruyor. Ayrı ayrı karar verilirse üç farklı cevap çıkar.
      **T-078 ile birlikte karara bağlanmalı, tek başına değil.**

## Acceptance Criteria
- [ ] Yazma ve okuma katmanları `null` için **aynı** cevabı veriyor, ya da farklılık kod
      yorumunda gerekçelendirilmiş
- [ ] Karar T-078'inkiyle tutarlı (tek karar, üç uygulama noktası)
- [ ] Testler: `null` girdisi için hem yazma hem okuma yolu

## Done tanımı
- [ ] Testler yeşil (unit + e2e), 3× reset'siz
- [ ] `npm run guards` exit 0 · `money-float.sh --ratchet` exit 0
- [ ] `code-reviewer` onayı
- [ ] `touches:` güncel
