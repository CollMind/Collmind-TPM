---
id: T-154
title: "Addendum H2'nin üç maddesi eksik: SERIALIZABLE, retry sözleşmesi, eşzamanlılık kabul testi"
epic: E-001
sprint:
status: todo
assignee: qa-engineer
priority: P2
created: 2026-08-10
updated: 2026-08-10
touches:
  - collmind.backend/test/
  - collmind.backend/src/modules/shared/budget/
migration_seq:
---

## Bağlam ([[T-143]] turu 7 / `docs/analysis/0025 §1`)

**İyi haber önce:** Addendum H2 (**MANDATORY**) pessimistic locking emrediyor ve
**ADR 0005 bu belgeyi hiç görmeden aynı kararı vermiş.** `budget.repository.ts`
`findEnvelopeWithLock` → `setLock('pessimistic_write')` ✅, ADR 0005 K5 para yollarını
bilinçli olarak pessimistic bırakmış ✅.

**Üç boşluk ölçüldü:**

| H2 maddesi | durum |
|---|---|
| `BEGIN TRANSACTION ISOLATION LEVEL SERIALIZABLE` | ❌ **yok** (`SERIALIZABLE`/`isolationLevel` araması boş) |
| Action 2.2 retry + exponential backoff (`LOCK_TIMEOUT`) | ⚠️ `agreement.service.ts`'te bir backoff var; **bütçe yolunda ölçülmedi** |
| **Action 2.3 kabul testi** | ⚠️ eşzamanlılık e2e'leri var ama **H2'nin senaryosu yok** |

## H2'nin kendi kabul testi (BRD'de yazılı)

```
10 eşzamanlı istek × 1.500 TL, 10.000 TL'lik envelope
Beklenen:  Approved: 6 (9.000 TL)  ·  Rejected: 4  ·  Final Available: 1.000 TL  ✅ PASS
```

**Bu, kaynağın verdiği hazır bir kabul ölçütü** — yazılması ucuz, ve `INV-B-002`
(*"spend never exceeds CAP"*) ile `INV-B-005` için doğrudan kanıt üretir.

## Yapılacak
- [ ] H2'nin kabul testini **birebir** yaz (10×1.500 / 10.000) — fixture iki tarafta farklı
      sonuç vermeli (§CLAUDE.md: yeşil olması ayırt ettiği anlamına gelmez)
- [ ] ⚠️ `SERIALIZABLE` gerçekten gerekli mi **ölç** — `FOR UPDATE` READ COMMITTED altında
      bu deseni zaten kapatıyor olabilir; gerekmiyorsa **gerekçesiyle** kaydet, körü körüne
      ekleme
- [ ] Bütçe yolunda retry/backoff var mı — yoksa H2 Action 2.2'ye göre eklensin
- [ ] Exit kodları boruya sokulmadan alınsın (§2.6)

## Done tanımı
- [ ] H2 kabul testi yeşil, **3× ardışık** koşumda kararlı
- [ ] `SERIALIZABLE` kararı gerekçeli
- [ ] `code-reviewer` onayı

---

## 📌 Kaynağın kendi statüsü bu boşluğu **birebir** sayıyor ([[T-143]] turu 30 / `0048 §2`)

`03_Candidate_Log` **CANDIDATE-003** (Source: **Addendum §H2**):

```
Status: 🟠 Partially Implemented (Phase 1) — SQL logic written, load test pending
Deferral:
  ❌ Conceptually defined in BRD v1.0, but not stress-tested
  ❌ Requires real concurrency scenarios (10+ simultaneous users)
  ❌ Database isolation level implications not validated in production-like environment
```

**Bizim bulgumuz:** pessimistic locking ✅ (ADR 0005 yakınsaması) · `SERIALIZABLE` ❌ ·
retry ölçülmedi · **H2 kabul testi yazılmamış**.

> **Aynı üç boşluk.** Task bir sapma değil, **kaynağın da izlediği açık bir madde** —
> *"planlanmış ama yapılmamış doğrulama"*.

Ve `SERIALIZABLE` sorusu da orada: *"database **isolation level** implications not
validated in production-like environment"* → bu task'ın *"gerekli mi, ölç"* maddesi
kaynakla uyumlu.
