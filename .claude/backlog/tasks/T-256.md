---
id: T-256
title: 🔴 `@CurrentUser('id')` argümanı YOK SAYIYOR — `my-requests` 500, ve EA-001 self-approval koruması HİÇ ateşlemiyor
epic: E-001
sprint: S-001
status: todo
assignee: debugger
created: 2026-08-21
updated: 2026-08-21
touches:
  - collmind.backend/src/common/decorators/current-user.decorator.ts
  - collmind.backend/src/modules/shared/approval/
---

## Kök neden — üç satır

```ts
export const CurrentUser = createParamDecorator(
  (data: unknown, ctx: ExecutionContext) => {
    const request = ctx.switchToHttp().getRequest();
    return request.user;          // ← `data` HİÇ KULLANILMIYOR
  },
);
```

`@CurrentUser('id')` yazan her yer **tüm `request.user` OBJESİNİ** alıyor, string
`id`'yi değil.

## Çağrı yerleri — ölçüldü

```
approval.controller.ts:57,72,100,112,123     ← 5, hepsi etkileniyor
admin-audit.controller.ts:29                 ← adminId HİÇ KULLANILMIYOR (ölü param, zararsız)
```

## Sonuçları — ikisi zıt yönde

| uç | ne oluyor | yön |
|---|---|---|
| `GET /approvals/my-requests` | `WHERE requestedById = <obje>` → **`500`** (davranışsal: PLANNER·PLANNER2·ADMIN üçünde de) | fail-closed, **görünür** |
| `POST /approvals/:id/cancel` | `request.requestedById !== userId` — string ≠ obje **her zaman** → gerçek sahip bile **`403`** | fail-closed, **işlevsiz** |
| **`POST /approvals/:id/approve` · `/reject`** | `request.requestedById === approverId` **hiçbir zaman `true` değil** → **self-approval koruması HİÇ ateşlemiyor** | ⛔ **FAIL-OPEN** |
| `GET /approvals/pending` | `userId` repository'de **hiç kullanılmıyor** — JSDoc *"for current user"* diyor, tenant'ın TÜMÜNÜ döndürüyor | ayrı bulgu |

## ⛔ En ağırı: `EA-001` self-approval koruması devre dışı

Kod kendi talebini onaylamayı **engellemek için yazılmış** ve karşılaştırma
**hiçbir zaman tutmuyor**. Yani koruma **var görünüyor ve hiç çalışmıyor**.

📌 `CLAUDE.md`: *"bir doğrulamanın çalıştığı sanılması, girdinin ona hiç ULAŞMAMASINDAN
gelebilir."* Burada girdi ulaşıyor ama **tipi yanlış**, ve karşılaştırma sessizce
`false` veriyor — `§2.5`'in tip tarafındaki hâli.

⚠️ **Ve `Z18`'in `READ_OWN` için verdiği "tam örnek" (`approvals/my-requests`) bugün
HİÇ ÇALIŞMIYOR.** Kod incelemesi *"yüklem var"* derdi ve **doğru** derdi — ama o
yüklem çalışan bir sorguya hiç varmıyor.

## Acceptance Criteria

- [ ] Decorator `data`'yı kullanır: `data ? request.user?.[data] : request.user`
- [ ] ⚠️ **Düzeltme DAVRANIŞI DEĞİŞTİRİYOR** — `approve`/`reject`/`cancel` bugün
      *"her zaman izin ver"* / *"her zaman reddet"* modunda. Düzeltmeden sonra
      **gerçek** kararlar üretecekler. Her üçü için regresyon pini gerekir
- [ ] `my-requests` `200` döner ve **yalnız kendi taleplerini** verir
- [ ] `EA-001` self-approval reddi **davranışsal** pinlenir: kendi talebini onaylamaya
      çalış → **reddedilmeli**. ⚠️ Pozitif kontrol: **başkasının** talebini onayla →
      **kabul edilmeli** (yoksa `403`'ün sebebi ayırt edilemez)
- [ ] `GET /approvals/pending`'in `userId`'yi kullanmaması ayrı ele alınır — JSDoc
      ile davranış çelişiyor, **hangisi doğru** kararı gerekiyor
- [ ] ⚠️ `approvals` tablosu bugün **0 satır** → fixture gerekiyor, yoksa hiçbir pin
      ayırt etmez (`§2.7 #6`)

## İlgili

- [[T-255]] (aynı ölçüm turu) · `Z18` `READ_OWN` · `EA-001`
