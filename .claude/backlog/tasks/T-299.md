---
id: T-299
title: MODES_READ hücresi YEDİ ayrı rol kümesi taşıyor — hücre normalizasyonu
epic:
sprint:
status: todo
assignee: architect
created: 2026-08-26
updated: 2026-08-26
touches:
  - docs/process/B3A_EK3_ROTA_HUCRE_ESLEMESI.tsv
---

## Ölçüldü (`code-reviewer`, `K2` turu — nit `5`)

```
awk -F'\t' '$5=="MODES_READ"{print $4}' | sort | uniq -c
  12 × {A,F,P}   ·   10 × 5/5   ·   5 × {A,F}   ·   3 × {A,F,P,RO}   ·   …
  ⇒ YEDİ ayrı rol kümesi, TEK hücrede
```

## Neden `K2` bunu kapatmadı — ve adın önemi

`K2` `ledger` **ailesini** `6/6` yaptı, **hücreyi** değil. Ajan buna
*"normalizasyon"* dedi ve bu **hücre-normalizasyonu** gibi okunabiliyor.

> **Aile normalizasyonu ≠ hücre normalizasyonu.** Biri kardeş uçları hizalar,
> diğeri bir yetenek hücresinin **tek küme** taşımasını sağlar.

## Bağlantı

`SUMMARY`/`MODES_READ` **ölçüm-kök paketinin** (dokuz kalem) parçası — `MODES_READ`
o paketin `2` numaralı kalemi. Ayrı çözülmez: **tek `architect` turu, tek karar
dönüşü** (ürün sahibi kararı).

## Acceptance Criteria

- [ ] Yedi kümenin her biri için: **kayıt var mı** (`git log -L`, gerekçe yorumu)
- [ ] Kayıtsız olanlar **kaza adayı**; kayıtlı olanlar **istisna**
- [ ] `T-287`'nin dersi uygulanır: **UI'dan ulaşılamaz üyelik**, üyeliğin bir karar
      değil kaza olduğunun **davranışsal** işareti — ve kayıt aramaktan **daha ucuz**
- [ ] Ürün sahibine **tek pakette** gider

---

## ⚠️ ÇAPRAZ NOT (`K6c/d`, 2026-08-26) — küme-cebiri etkisi

`K6c/d` bir rota **sildi** ve o rota, kalıntı `SHARED_WRITE` union'ındaki
**`PLANNER`'ın TEK KAYNAĞIYDI**:

```
ÖNCE   5 rota → union {ADMIN, PLANNER}
SONRA  4 rota → union {ADMIN}
```

⇒ **Bir rotanın ölümü, komşularının union-gerekçesini de öldürdü.** Ve ilk düzeltme
**sayıyı** düzeltip **değeri** bıraktı (`code-reviewer` yakaladı).

📌 **Aynı etki bu task'ta da çıkabilir:** `MODES_READ`'in **yedi** rol kümesi
incelenirken, bir kümenin tek kaynağı olan rotalar **ayrıca işaretlenmeli** — o
rotalar göçerse/ölürse küme **kendiliğinden** değişir, ve ona yaslanan gerekçeler
**sessizce** bayatlar.

⇒ Tarama şartına ek: her küme için *"bu kümedeki en dar rolü **hangi rota** taşıyor —
tek mi?"*
