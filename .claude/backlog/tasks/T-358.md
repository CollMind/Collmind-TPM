---
id: T-358
title: "BL-3 ADIM 1 — import_batch_rows: red/kabul satır tablosu (teşhis + sourceMatchRatio paydası)"
epic: E-001
sprint: S-001
status: done
assignee: data-engineer
priority: P0
created: 2026-09-02
updated: 2026-09-05
touches:
  - collmind.backend/src/database/migrations/1823000000000-*.ts
  - collmind.backend/src/database/entities/
  - collmind.backend/scripts/db-roles/02-runtime-grants.sql
migration_seq: 1823000000000
---

## Hüküm `Z87` (ürün sahibi, 2026-09-02) — `BL-3`'TEN **ÖNCE** VERİLDİ

> **Gerekçe:** hükümsüz `BL-3` metriği **icat eder**, `AD-BORCU` doğar.

```
TAŞIYICI      HTTP cevabı UÇUCU — sayfa kapanınca "hangi satır NEDEN" KAYBOLUR.
              BL-3'ün "%95'e neden ulaşamıyorum" sorusunun cevabı TAM BU SATIRLAR;
              teşhis raporu FİLTRELENİR ⇒ jsonb bunu TAŞIMAZ
DESTEKLEYİCİ  denetim izi: yüklenen-ama-REDDEDİLEN veri bir OLAYDIR (ADR 0012 ruhu);
              ve BL-2'nin ölçütü zaten "SORGULANACAKSA TABLO"ydu
```

## ŞEKİL
```
batch_id FK RESTRICT · row_no · raw jsonb (hücre-ham)
status   ENUM(ACCEPTED, REJECTED)
reason   ENUM(SKU_NOT_FOUND, CPL_NOT_FOUND, INVALID_PERIOD, INVALID_VALUE, DUPLICATE)
resolved_sku_id / resolved_cpl_id  NULLABLE — kabul edilende DOLU
GRANT  SELECT + INSERT · ⛔ UPDATE/DELETE YOK (satır IMMUTABLE)
RLS    tenant_id + politika TANIMLI / ENABLE KAPALI  (Z85 §2 deseni)
```

## ⛔ İKİ ŞART

**1 · `ACCEPTED` SATIRLAR DA BURADA YAŞAR** (`Z87 §2`)
> Yalnız red kaydedilirse **`sourceMatchRatio`'nun PAYDASI kaybolur**, ve *"kabul edilen
> satır **hangi kaynak satırdan** geldi"* izi **KOPAR**.
Köprü: `baseline_volumes` ↔ `batch_id` + `row_no`.
📌 `DISIPLIN`'in *"negatif kanıt tetikleyen fixture olmadan kanıt değildir"* ailesinin
**veri modeli** hâli: bir **oran** tutacaksan **paydayı da saklamalısın**.

**2 · ⛔ ÖZET KOLON YOK**
`INV-B-009` — senkron mekanizmasız kopya-kolon sınıfı. **Batch immutable olsa bile
TEK-KAYNAK ilkesi.** İki metrik de **sorguyla** türer.

## ⚠️ AD KONVANSİYONUNU ÖLÇ
Ürün dilinde `import_batch_rows`; kardeşi `baseline_volume_import_batches`.
Sapıyorsan **gerekçe yaz** (`F8`: aynı kavramın iki temsili).

## Done ek maddeleri
- [ ] `run → revert → run` ampirik · **üç durum ayrımı** (beklenen · no-op · İPTAL)
- [ ] `down()` `pg_dump --schema-only` diff'i **byte-birebir**
- [ ] `new-table-rls` **KADEME 1** yeşil (gerçek fail-closed politika, `ENABLE` YAZILMAZ)
- [ ] `app-runtime-grants` yeşil — `GRANT` **ölçülmüş minimal** (`UPDATE`/`DELETE` YOK)
- [ ] Entity metadata migration ile **birlikte**
- [ ] `plans=0` penceresi migration **öncesi ve sonrası** ölçüldü
