---
id: T-287
title: CANLI KUSUR — iki ekranda rol-kapısı ile rota-kümesi ayrışıyor (403)
epic:
sprint:
status: todo
assignee: frontend-engineer
created: 2026-08-25
updated: 2026-08-25
touches:
  - collmind.frontend/src/routes/index.tsx
  - collmind.frontend/src/components/finance/FinanceDashboard.tsx
  - collmind.frontend/src/components/features/off-invoice/OffInvoiceTransactionsPage.tsx
migration_seq:
---

## Açıklama

`B3` `READ`-hücreleri ölçüm turu (`architect`, 2026-08-25) **iki canlı kusur**
buldu. İkisi de aynı sınıf: **ekran kapısı bir rolü içeri alıyor, o ekranın
çağırdığı rota o rolü taşımıyor.** Team Lead bağımsız doğruladı.

### K2 · `/finance` — `CATEGORY_MANAGER` girer, üç widget `403`

```
ekran kapısı  routes/index.tsx:448-456   ['ADMIN','FINANCE','CATEGORY_MANAGER','READONLY']
FinanceDashboard.tsx                      rol kapısı YOK (dosyada "role" kelimesi 0 kez)
```

| widget | rota | küme | `CATEGORY_MANAGER` |
|---|---|---|---|
| BudgetUtilization · SpendTrend · SpendComposition · MechanicEffectiveness | `{A,CM,F,RO}` | ✅ | görür |
| **BudgetAtRisk** | `finance-reporting/budget-at-risk` | `{A,F,RO}` | ⛔ **403** |
| **VarianceAnalysis** | `finance-reporting/variance-analysis` | `{A,F,RO}` | ⛔ **403** |
| **CashFlowProjection** | `finance-reporting/cash-flow-projection` | `{A,F,RO}` | ⛔ **403** |

Üçünün kümesi `d40ca16`'dan (2026-02-25) beri değişmedi, **hiçbir kayıt yok**.

📌 Ek: `plan-performance` `5/5` (`PLANNER` dahil) ama **`PLANNER` `/finance`
ekranına giremiyor** — tek tüketicisi o ekran. `PLANNER` üyeliği **UI'dan
ulaşılamaz**.

### K3 · `/off-invoice/transactions` — `READONLY` için sayfanın TAMAMI kırık

```
ekran kapısı  routes/index.tsx:482        ['ADMIN','FINANCE','PLANNER','READONLY']
OffInvoiceTransactionsPage.tsx:52,72,83   tek Promise.all
```

| çağrı | rota | küme | `READONLY` |
|---|---|---|---|
| `getCount` | `agreement-transactions/count` | `{A,F,P}` | ⛔ **403** |
| `getTransactions` | `agreement-transactions` | `{A,F,P}` | ⛔ **403** |
| `getSummary` | `agreement-transactions/stats/summary` | `{A,F,P}` | ⛔ **403** |
| `onInvoice.getCount`/`getEntries` | `on-invoice/count`, `/entries` | `{A,F,P,RO}` | ✅ |

⚠️ **`Promise.all` nedeniyle on-invoice yarısı başarılı olsa da sayfa TÜMÜYLE
düşüyor.**

### K5 · `/budget/ledger` — kapısız ekran (aynı sınıf, daha hafif)
`routes/index.tsx:307` `<ProtectedRoute>` **`requiredRole` YOK** ⇒ beş rol de
açabiliyor; çağırdığı `GET /ledger` ise `{A,F,P}` ⇒ `CATEGORY_MANAGER` ve
`READONLY` `403` alıyor.

## ⛔ MEKANİZMA — bir karar değil, BİR COMMIT

`f3b9f82` (2026-03-05, *"add READONLY role"*, `TPM-142`) `READONLY`'yi **ayrım
yapmadan** ekledi:

```
DOKUNDUĞU          agreement.controller +4 · on-invoice +3 · plan +7 ·
                   approval +4 · finance-reporting +8 (SEKİZİNİN SEKİZİNE)
DOKUNMADIĞI        ledger.controller · agreement-transaction.controller  → RO=0
```

⇒ **`READONLY`'nin bugünkü varlığı/yokluğu, büyük ölçüde TEK BİR COMMIT'in hangi
dosyalara dokunduğunun fonksiyonu.**

## ⛔ BU TASK BİR ROL KARARI DEĞİL

Kusur **ayrışmada**: ekran kapısı ile rota kümesi **aynı fikirde değil**. İki
düzeltme yönü var ve **hangisi doğru olduğu ürün sahibi kararıdır**:

```
(i)  rota kümesini ekrana hizala    → rol GENİŞLETMESİ, kayıtlı istisna ister
(ii) ekranı/widget'ı rota kümesine  → UI'da rol kapısı, davranış korunur
```

⇒ **Karar gelmeden düzeltme yapılmaz.** Bu task kusuru **kaydeder ve ölçer**.

## Acceptance Criteria
- [ ] Üç ekranın her biri için düzeltme yönü **kayıtlı** (ürün sahibi kararı)
- [ ] `Promise.all` kırılganlığı ayrıca ele alındı — bir `403` **sayfanın
      tamamını** düşürmemeli (kısmi başarı gösterilebilmeli)
- [ ] Pin: her ekran için **rol × widget** matrisi, `403` beklenen yerde `403`
- [ ] Çapraz-repo: ekran kapısı ↔ rota kümesi **tek kaynaktan** türetilebilir mi
      — yoksa `İlke 4` çifti kalıcı mı? (ayrı soru, kayda geçsin)

## İlişkili
`B3` ölçüm turu raporu · `T-286` (atıf normalizasyonu) · `K4` (üç kardeş
içe-aktarma modülü, üç okuma politikası) · `K6`/`K7` (tüketicisi olmayan rotalar)
