---
id: T-397
title: "declared-migrations guard: SEBEP ZORUNLULUĞU kümesi ELLE yazılı (G5 ihlali) — harness'a yeni değer girince kod tarafı sessiz yeşil"
epic:
sprint:
status: review
assignee: backend-engineer
priority: P1
created: 2026-09-13
updated: 2026-09-14
touches:
  - scripts/guards/declared-migrations.sh
---

## T-397 DAR DÜZELTME — 2026-09-14 (Z111 §38, brief §11)

Birleşme sonrası reviewer bir 🔴 iki 🟡 buldu (bkz. `BEYANLI_MIGRATION_RATCHET_BRIEF.md`
§11). Üçü + 🔵 TEK turda düzeltildi:

- 🔴-2: liste tarafındaki sebep kontrolü artık `reason_required_{rev,eff}_values`
  (KOD tarafıyla AYNI türetilmiş küme) kullanıyor — eskiden `!= "DATA_CONDITIONAL"`
  diye ELLE yazılıydı.
- 🟡-1: `keyword_pattern` artık TÜM alan adlarından (REASON dahil) kuruluyor — eskiden
  yalnız birincil adlardan kurulup `_REASON`-yalnız dosyaları hiç okumuyordu.
- 🟡-2: `derive_unique_line` artık `validate_condition_line` ile eşleşen satırın TEK
  BAŞINA TAMAMLANMIŞ olduğunu (satır sonu `\` yok, `; then` ile bitiyor, `[ … ]`/`!=`
  sayaçları tutarlı) doğruluyor — bölünmüş harness satırı artık ÖLÇEMEDİM.
- 🔵: harness OKUNAMADIĞINDA (izin) sebep artık "0 ya da >1 eşleşme" ile
  KARIŞTIRILMIYOR — ayrı rc (2) ile "OKUNAMADI" deniyor.

Self-test'e dört yeni/düzeltilmiş senaryo eklendi: K-a (🔴-2), 5a(ii) düzeltmesi (🟡-1,
reviewer'ın "ana alan taşımasın" notu), K-c (🟡-2, bölünmüş satır), K-d (🔵, harness
chmod 000). Her biri için ÖNCE (t397b-pre kopyası)/SONRA rc farkı ve bağımsız mutasyon
(kopyala→uygula→satır bas→ölç→geri yükle→shasum -a 256 -c) ile self-test'in yakaladığı
gösterildi.

Kanıt: `--self-test` rc=0 · gerçek `--check` rc=0 ("taranan .ts: 90", "beyanlı migration:
yok") · `bash scripts/run-all.sh` rc=0 · `bash scripts/push-order-self-test.sh` rc=0 (7/7)
· backend `npm run guards` rc=0 · baseline ve `MIGRATION_SEQUENCE.md` sha DEĞİŞMEDİ (yalnız
`scripts/guards/declared-migrations.sh` değişti).

Bu turun dışında bırakılan: 🟡-3 (izin kümesi ↔ zorunluluk kümesi) — brief §11.0'da açıkça
T-task'a bırakılmış.

## Nasıl doğdu
Ratchet review'u `S3` (2026-09-13) · hüküm `Z111 §33` ("G5 ihlali — T-task'ta öncelikli").

## Kusur [reviewer, kod okuması + türetme satırı ölçümü]
- **Tanınan değerler** harness'tan türetiliyor ✓ (`migration-verify.sh`'taki "tanınmıyor" reddi, eşleşme 1/1).
- **Hangi değerin sebep istediği** guard'da ELLE yazılı (kod tarafı ve liste tarafı ayrı yerlerde).
- Harness'a sebep isteyen yeni bir değer eklenirse (ör. `DATA_X`): guard onu **tanır**, kod tarafı sebep **istemez** (sessiz yeşil), liste
  tarafı sebep **ister** — iki taraf zıt davranır.
- Türetme `grep -m1` ile ilk eşleşen satırı alıyor, eşleşme sayısının 1 olduğunu kontrol etmiyor (harness'a o metni içeren bir yorum
  eklenirse yanlış satıra hizalanır — DISIPLIN F12).
- Harness'ın "sahipsiz REASON" ÖLÇEMEDİM'i guard'da yok: `DATA_CONDITIONAL` + `EFFECT_REASON` guard'da yeşil, harness'ta ÖLÇEMEDİM.
- Guard başlığındaki "iki ayrı liste elle yazılmaz" cümlesi bugün DOĞRU DEĞİL.

## Acceptance Criteria
- [ ] Sebep zorunluluğu kümesi de harness'tan (ya da tek bir ortak kaynaktan) TÜRETİLİR — elle liste YOK (G5)
- [ ] Türetme eşleşme sayısı == 1 değilse ÖLÇEMEDİM
- [ ] Sahipsiz REASON guard'da da ÖLÇEMEDİM (harness ile aynı)
- [ ] Bilinen-kırmızı: harness kopyasına sebep isteyen yeni bir değer eklenmiş fixture → guard kod tarafında sebepsiz dosyayı KIRMIZI verir
- [ ] Başlık cümlesi davranışla tutarlı · self-test beklentileri hükme atıf taşır (DISIPLIN F03)

## Done tanımı
CLAUDE.md §4.2 — `bash scripts/run-all.sh` exit 0 · `push-order-self-test.sh` exit 0 · code-reviewer onayı.
