---
id: T-109
title: "grid-cells düzenleme dalı ölü — T-106'nın kendi ürettiği erişilemezlik"
epic:
sprint:
status: review
assignee: frontend-engineer
priority: P1
created: 2026-08-08
updated: 2026-08-08
touches:
  - collmind.frontend/src/components/features/plans/PlanningGridEnhanced.tsx
  - collmind.frontend/src/components/features/plans/grid-cells.tsx
migration_seq:
---

## ⚠️ Bu, KENDİ ÜRETTİĞİMİZ bir erişilemezlik

[[T-106]] `grid-cells.tsx`'e doğru grameri, biçimli düzenleme kutusunu ve satır-içi
hata mesajını koydu. **Hiçbiri render edilmiyor.**

`PlanningGridEnhanced.tsx:1481` (FU) ve `:1605` (SKU) aynı `columns.map` gövdesinde,
EditableCell'e ulaşmadan **kendi** `<input type="number">`'ını döndürüyor:

```tsx
if (isEditing) {
  return (
    <TableCell …>
      <input type="number" defaultValue={currentValue}
        onBlur={(e) => {
          const numValue = parseFloat(e.target.value);
          if (!isNaN(numValue)) { … }      // ← else YOK: okunamayan girdi sessizce atılıyor
        }} />
```

Yani `parseUserNumber` / `formatForEdit` / `describeNumericInputFailure`'ın tek çağıranı
`grid-cells.tsx`'in `isEditing` dalı, ve o dal canlı gridde hiç çalışmıyor.

**Bu, CLAUDE.md §4.2'nin "üretim çağrı yolu var mı?" maddesinin işe yaradığı bir kayıt.**
Madde olmasaydı T-106 `done` yazılırdı: testler yeşildi, `type-check` temizdi, gramer
22.345 girdide backend ile birebirdi — ve kullanıcıya hiçbir şey ulaşmıyordu.

Sınıfın dokuzuncu vakası (T-033, T-036, T-039, T-046d, T-048, T-052, T-053, T-062), ve
**ilk kez bu turda üretildi** — önceki sekizi devraldığımız kusurlardı.

## Kanıt (`code-reviewer`, bağımsız ölçüm)

`tests/e2e/04-grid-cell-kpi.spec.ts:80` bir hücreye tıklayıp `input[type="number"]`'ın
**görünür** olmasını bekliyor ve T-106'dan sonra hâlâ geçiyor. EditableCell yolu canlı
olsaydı `type="number"` → `type="text"` değişikliği o testi kırardı. Kırmadı.

## Yapılacak

- [ ] `PlanningGridEnhanced`'in iki satır-içi editörü **kaldırılsın**, düzenleme EditableCell'e
      devredilsin (`editingCell` state'i EditableCell'in kendi `isEditing`'i ile değiştirilebilir
      ya da EditableCell kontrollü hâle getirilebilir — **tasarım kararı, ölçülerek verilsin**)
- [ ] `editInputRef` / `handleCellEdit`'in 50 ms'lik focus'u yeni yapıya uyarlanmalı
- [ ] `tests/e2e/04-grid-cell-kpi.spec.ts` **kasten** `type="text"` beklemeli — bu testin
      kırılması erişilebilirliğin kanıtıdır
- [ ] Mutasyon: EditableCell'i devre dışı bırak → e2e kırmızı (bugün kırmızıya dönmüyor)
- [ ] `parseFloat` yolundaki `if (!isNaN(...))` **else'siz** dalı gitmiş olmalı (§2.5)
- [ ] **Escape yazma tetiklemiyor** — [[T-112]]'de düzeltildi, devretme sonrası da korunmalı.
      Yan etki olarak düzeltilen bir kusur kayıtsız kalırsa "neden değişti" sorusuna cevap kalmaz.
- [ ] Null hücre düzenleme kutusunu **boş** açıyor (bugün `value ?? 0` ile `0` gösteriyor).
      Ürün sahibi kararı: `0` bir taahhüttür — kullanıcı üzerine yazmadan kaydederse `0`
      girmiş olur; boş kutu hiçbir şey iddia etmez. Ve `?? 0` bu oturumda kaldırdığımız kalıp.

## Adım 2 öncesi ölçüm — "aynı anda tek hücre" garantisi SAĞLANMIYOR

Silmeden önce sorulan soru: satır-içi editörlerin `EditableCell`'de karşılığı olmayan bir
davranışı kaldı mı? Beşinci fark (odak + tek-hücre garantisi) **ölçüldü ve karşılığı yok.**

Fark yapısal:

| | garantiyi ne sağlıyor |
|---|---|
| satır-içi editörler | tek bir parent `editingCell` değeri — iki hücre **yapısal olarak** açılamaz |
| `EditableCell` | her örneğin **kendi** `isEditing`'i (`grid-cells.tsx:48`) — koordinasyon yok |

`EditableCell`'de eski hücreyi kapatan tek şey blur. Ama `handleBlur` her zaman kapatmıyor:
T-106 kararı gereği **okunamayan metinde kutu bilerek açık kalıyor** (`grid-cells.tsx`,
`setInputError(describeNumericInputFailure(parsed))` dalı — `setIsEditing(false)` yok).

O karar ile bileşen-yerel state çarpışıyor. Ölçüm (iki `EditableCell` render edildi,
A'ya `abc` yazılıp blur, sonra B'ye tıklandı):

```
after unreadable blur on A -> open editors: 1
after opening B            -> open editors: 2      ← garanti yok
error text visible: 1
focus is on the NEWLY opened cell: true | stale cell still holds its text: "abc"
```

Yani A açık, hatasını gösteriyor, **odaksız**, ve kendiliğinden hiç kapanmayacak.

### Ve ikinci, daha sessiz bir fark: kontrollü ↔ kontrolsüz

Ölçüm sırasında çıktı: `EditableCell` **kontrollü** (blur'da `editValue` *state*'ini okur),
satır-içi editörler **kontrolsüz** (`defaultValue` + `e.target.value`). Probe'un ilk koşumu
tam da bu yüzden yanlış ölçtü — `fireEvent.blur(input, { target })` state'e dokunmuyor, bileşen
açılış değerini ayrıştırıp kapandı ("0 açık editör" göründü). §2.7 ailesinin bir üyesi, ve
devretme sırasında testleri yazacak kişinin bileceği bir şey.

### Karar gerekiyor (§2.4 — varsayarak ilerlenmeyecek)

- **(a)** Bileşen-yerel state korunur; birden çok hücre aynı anda açık kalabilir, her biri kendi
  hatasını gösterir. Bugünkü bir garanti kaybedilir.
- **(b)** Açık/kapalı kararı tek bir koordinatöre alınır (parent state ya da context);
  `EditableCell` render/ayrıştırma işini tutar, "kim açık" bilgisini paylaşır. Garanti korunur.
- **(c)** Okunamayan metinde de kapatılır — **T-106 kararıyla çelişir** (kullanıcının okunamayan
  girdisini sessizce atmak, T-106'nın reddettiği şeyin ta kendisi). Önerilmiyor.

Ölçüm (a) ile (b) arasında bir tercih **dayatmıyor**; ikisi de tutarlı. Karar ürün sahibinin.

### KARAR: (b) — koordinatör (ürün sahibi, 2026-08-08)

**Gerekçe kararın kendisi:** kaybedilen şey bir UX detayı değil, bugün var olan bir
**invaryant**; ve bir silme işi invaryantı sessizce düşürmemeli. Bu, tüm oturum boyunca
kovaladığımız sınıfın tam tersi olurdu — bir refactor, hiçbir yerde yazılı olmayan bir
garantiyi yan etki olarak kaldırıyor. `budget_alert_configurations`'ın kısmi UNIQUE'i gibi:
**bir kısıtı yan etki olarak gevşetmek, bir kararın yazarsız kalmasının yoludur.**

Ve (a) bugün somut bir kusur üretiyor: *"A açık, hatasını gösteriyor, odaksız, ve
kendiliğinden hiç kapanmayacak."* Bu "birden çok hücre açık olabilir" değil, **terk edilmiş
bir editör** — kullanıcı B'ye geçti, A'yı unuttu, ve A'yı kapatmanın yolu kalmadı (blur zaten
oldu; tekrar tıklayıp Escape gerekiyor).

T-106 kararı doğruydu — okunamayan metinde kutu açık kalsın, kullanıcı düzeltsin — ama o karar
**tek editör varsayımı altında** verildi. Bileşen-yerel state onu terk edilmiş bir hataya
çeviriyor. (b) ikisini uzlaştırır: kutu açık kalır (T-106), ama yeni bir hücre açılınca
koordinatör eskisini kapatır — ve o noktada kullanıcı zaten başka bir şeye geçmiştir.

### Alt karar: koordinatör kapatırken okunamayan metin ne olacak

Sessizce atmak T-106'nın reddettiği şeydir. **Kapat ve toast göster:**

> `Girilen değer okunamadı, kaydedilmedi.`

Kullanıcı ne olduğunu öğrenir, kutu takılı kalmaz.

### Kapsam ve SIRA (zorunlu)

```
1. Koordinatör (parent state / context) — "kim açık" tek yerde
2. Satır-içi editörlerin silinmesi
```

**Koordinatör önce, silme sonra.** Ters sırada garanti bir süre yok olur.

### Test şartı

- **İki hücre aynı anda açık olamaz** — ve bu **mutasyonla kanıtlansın**: koordinatör
  kaldırılınca test **kırmızıya** dönmeli. Bugün var olan ama hiçbir yerde yazılı olmayan
  invaryant, ilk kez teste bağlanmış olur.
- Terk edilen hücrenin toast'ı da ölçülsün (kapandı **ve** sebebi söylendi).
- ⚠️ **Testleri yazan kişi kontrollü/kontrolsüz farkını bilmeli** (aşağıdaki ölçüm notu):
  `fireEvent.blur(input, { target })` `EditableCell`'de state'e dokunmaz. Bu fark **tüm
  devretme testlerinin şeklini** belirliyor — probe'un ilk koşumu tam bu yüzden yanlış ölçtü
  ve **yanlış cevap rahatlatıcı olandı** ("garanti var" göründü).

## Adım 2a — KOORDİNATÖR (tamamlandı, erişilemez; adım 2b bekliyor)

**Tasarım:** yeni bir Context değil, **zaten var olan `editingCell` state'i** genişletildi.
`EditableCell` kontrollü hâle geldi (`isOpen`/`onOpen`/`onCancel`). Gerekçe: iki koordinasyon
mekanizması yan yana yaşarsa "hangisi asıl kaynak" belirsizliği doğar; ve `cellId` kimliği
(`fuId`/`skuId` + `field`) zaten vardı, yeniden icat edilmedi.

Terk edilme kararı saf bir fonksiyona indi: `decideCellAbandonment(closedByCoordinator,
inputError)` — `numberUtils.ts`'te, `decideCellCommit` ile aynı desende. `notify` bileşenin
kendi `useToast()`'undan geliyor: bağlanacak bir prop yok, **dolayısıyla unutulamaz**.

### Mutasyon kanıtı (Team Lead, bağımsız — ajanınkinden farklı soru)

Soru: testler invaryantı ve toast'ı **ayırt ediyor mu**, yoksa tek şeye mi bağlı?

| mutasyon | sonuç |
|---|---|
| yalnız toast yayımı susturuldu | **6'da 1** kırmızı — sadece toast testi |
| bileşen-yerel state'e dönüş | **6'da 5** kırmızı — invaryant yakalanıyor |

Ayırt ediyorlar. Mutasyonlar dosya içeriğinden geri alındı.

## ⚠️ Bu turda üretilen ve kapatılan regresyon: tıklama yüzeyi

`TableCell`'in `onClick`'i "çift çağrı gereksiz" gerekçesiyle kaldırıldı. Gerekçe kendi içinde
doğruydu (ölçüldü: 11 editable kolon, `calculated`/`inherited` ile çakışma yok, `RAG_STATUS`
editable değil) ama **başka bir soruyu** yanıtlıyordu:

> sorulan: "bu handler'a başka bir yol bağımlı mı?"
> sorulmayan: "handler'ı kaldırmak, kalan yolun kapsadığı **ALANI** değiştiriyor mu?"

Ölçüm (gerçek tarayıcı): tıklanabilir alan hücrenin **%26.6**'sına düştü — ölü bölge `td`'nin
kendi padding'i. **E2E süiti görmedi**, çünkü Playwright elemanın **merkezine** tıklıyor.

`onClick` geri kondu (karar (a)). Mutasyon kanıtı:

```
onClick yerinde      : 4/4 köşe açıyor, kutu değeri boş değil
onClick kaldırılmış  : 0/4 köşe
```

Ve bir incelik: **`td` yolu `EditableCell.handleClick`'ten hiç geçmiyor**, o yüzden `editValue`
formatlaması `handleClick`'ten alınıp `isOpen`-geçiş effect'ine taşındı — aksi hâlde `td`'den
açılan kutu bayat/boş gelirdi.

### Ölçüm artefaktı (kaydedilmeye değer)

Probe önce *"Escape editörü kapatmıyor"* dedi. **Yanlıştı:** parent odağı 50 ms'lik bir
`setTimeout` ile kuruyor, tıklamadan hemen sonra gönderilen Escape `<body>`'ye gidiyor.
`input.press('Escape')` ile kapanıyor. HEAD içeriğine dönülüp ölçülerek "regresyon değil",
sonra doğru ölçümle "kusur da değil" ayrımı yapıldı.

T-113'ün bayat süreç vakasının kardeşi ama tersten: orada **ortam**, burada **zamanlama**
olmayan bir kusur üretti. Ve bu sefer yanlış cevap rahatlatıcı değil **alarm vericiydi** — o
yüzden kovalandı.

## ⚠️ Adım 2a'nın statüsü: `blocked-unreachable`, `in-progress` değil

`isOpen={isEditing}` render edildiği noktada **her zaman `false`** — üstteki `if (isEditing)`
dalı önce dönüyor. Yani koordinatör, terk-etme toast'ı ve `onOpen`/`onCancel` sözleşmesinin
bugün **üretim çağrı yolu yok.** §4.2 gereği bu `done` değil, `blocked-unreachable`.
Blokajı 2b (satır-içi editörlerin silinmesi) kaldırır.

## ⚠️ İkinci tur review: kendi düzeltmem StrictMode altında bozuktu

B1/B2 düzeltmesi önce `prevOpenRef` (ref) kullanıyordu. React StrictMode render fonksiyonunu
iki kez çağırıp ilk pass'i atar; **atılan pass'te mutasyona uğramış bir ref geri alınmaz.**
İkinci pass "geçiş tüketildi" görüp formatlamayı atlıyor, ilk pass'in `setEditValue`'su
atılan render'la gidiyordu.

```
ref,   StrictMode      value=""          ← kutu BOŞ açılıyor
ref,   StrictMode YOK  value="1234,56"
state, StrictMode      value="1234,56"   ← düzeltilmiş hâli
```

`src/main.tsx` uygulamayı `<React.StrictMode>` ile sarıyor — yani bozuk satır **kullanıcının
aldığı** satır. `prevOpen` artık `useState`; React'in bu deseni `useState` ile belgelemesinin
sebebi tam olarak budur. `wasOpenRef` ref kalabilir: o effect'ten, commit sonrası yazılıyor.

### Asıl ders: kanıt ortamı üretimi TEMSİL etmiyordu

509 testin hiçbiri StrictMode ile render etmiyordu; üretim ediyor. Bu yüzden kusuru
getirdiğimde vitest, e2e ve type-check **hep birlikte yeşil kaldı**.

§2.7 ailesinin yeni bir yüzü: orada kanıt kurulumu ölçülen durumu *değiştiriyordu*, burada
onu **temsil etmiyor**. Kapatıldı — `editable-cell-coordinator.test.tsx`'in tamamı artık
`<StrictMode>` altında koşuyor.

Mutasyonla kanıt (Team Lead, bağımsız): `useState` → `useRef` geri alındığında **2 test
kırmızı**. Aynı regresyonu ilk kez getirdiğimde suite tümüyle yeşildi.

⚠️ Testlerin başlıklarında "StrictMode" geçiyor — biri "gereksiz sarmalayıcı" diye kaldırırsa
boşluk sessizce geri açılır.

## ⚠️ İki ölçülmemiş iddia daha (düzeltildi)

- `handleCellEdit` yorumu: *"guard bir focus timer'ı daha kurmayı engelliyor"* — **yanlış
  çıkarım.** `setTimeout` setter'ın dışında ve koşulsuz; guard yalnız gereksiz re-render'ı
  kesiyor. Ölçüm ("2 timer") doğruydu, sonuç yanlıştı — §7.1'in "doğru sayı, yanlış çıkarım"
  sınıfı.
- E2E köşe testinin değer assertion'ı: bugün dolu gelen kutu **legacy `defaultValue`'dan**
  geliyor, `EditableCell`'in formatlamasından değil. `EditableCell`'de formatlama tümüyle
  silinse o satır yeşil kalırdı. Yorum düzeltildi ve 2b'de neye dönüşeceği yazıldı. Köşe
  assertion'ının ayırt etme gücü geçerli.

## ⚠️ Adım 2b'ye taşınan şart

`qa-engineer` ölçtü: gerçek `TableCell.onClick` kaldırıldığında **tam suite 507/507 yeşil
kalıyor**. İzole harness *deseni* pinliyor, `PlanningGridEnhanced`'in kendi TD'sini değil.
Bu boşluk e2e ile kapatılıyor (ayırt ediciliği yukarıda ölçüldü). Silme turundan sonra
`FuRowEnhanced` üzerinden bir entegrasyon testi **iyileştirme**dir, açık eksiklik değil.

## ADIM 2B — SİLME (tamamlandı, mekanizma artık CANLI)

İki satır-içi editör dalı silindi; düzenleme `EditableCell`'e devredildi. 2a'nın
`blocked-unreachable` durumu bununla kapandı.

Ölü kod kaldırıldı: `editInputRef`, iki `ref={… as any}`, 50 ms focus `setTimeout`,
parent'ın `onCellCommit` sarmalayıcısı ve `FuRowEnhanced`'in `onCellCommit` prop'u.
Ayrıca `commitCellEdit`/`decideCellCommit`/`CellCommit`/`CellCommitEffects` **silindi** —
tek üretim çağıranları `onCellCommit`'ti, kalanı yalnız kendi testleriydi. Testlerin
varlığı yolu yaratmaz; tersine ölü mekanizmayı yeşil tutarak canlı gösterir.

### Kabul ölçütü — assertion'ın ANLAMI değişti, iki yandan ölçüldü

Ürün sahibinin şartı: *"bir assertion'ın anlamı değişiyorsa, yeşil kalması bir kanıt değil."*

`grid-cells.tsx`'te `if (isOpen) setEditValue(formatForEdit(value));` kaldırıldığında
`04-grid-cell-kpi.spec.ts`:

```
silme ÖNCESİ : exit 0, 2 passed              ← assertion KÖR (değer legacy defaultValue'dan)
silme SONRASI: exit 1, not.toHaveValue('')   ← artık EditableCell'i ölçüyor
```

### Silmenin yan ürünü: bir sessiz-sıfır yazması kapandı

Legacy dal kutuyu `defaultValue={formatForEdit(value ?? 0)}` ile açıyordu. Girilmemiş
mekanik hücreleri `null` döndüğü için **taze bir planda her mekanik hücresi**: kullanıcı
açıp hiçbir şey yazmadan blur ediyordu → kutuda `0` → gerçek PATCH ile **0 kaydediliyordu**.
`EditableCell` `formatForEdit(value)` ile açıyor → `null` için `''` → `EMPTY` → `onCancel`,
yazma yok.

### Review S1 — invaryant konvansiyondan TİPE taşındı

`editingCell` `{ fuId?, skuId?, field }` idi. Review ölçtü: ikisi birden dolu bir şekilde
**iki editör açılıyor** ve ikincinin odağı birincisini blur edip **değişmemiş değerin gerçek
PATCH'ini** gönderiyor — T-112'nin kusuru koordinatörün tipinden geri giriyor.

Artık ayrık birleşim (`{ level:'FU'; fuId; field } | { level:'SKU'; skuId; field }`) — kötü
şekil **temsil edilemez**. Mutasyon: korumasız hâle dönünce **tam 1 test** kırmızı.

> Dört çağrı yerinin uyduğu bir konvansiyon invaryant değildir.

### Diğer review bulguları

- **S2/S3:** iki yorum bayatlamıştı ve biri normatifti — e2e assertion'ı "zaten kör" diye
  tarif ediyordu, oysa silme sonrası ayırt edici. Bir yorumun bir korumayı koruma altından
  çıkarması (T-084 sınıfı). İkisi de ölçümle yeniden yazıldı.
- **S4:** SKU testi `editableAt: 'FU'` kolonu sürüyordu — üretimin **açamayacağı** hücre.
  Gerçek `BASE_COLUMNS`'tan `BASE_VOL`'e geçildi.
- **S5:** kalıbın FU ucu ölçülmüyordu (`calls[0][1]` undefined mı) — o argüman hacim ile
  taktik mutasyonu arasında seçim yapıyor.
- **S6/S7/N1** → karar/kapsam gerektiriyor: [[T-117]], [[T-118]].

### ⚠️ Ve kapının kendisi hakkında bir bulgu: [[T-116]]

`npm run type-check` **hiçbir test dosyasını denetlemiyor** (`tsconfig include: ["src"]`).
Kasten üretilen tip hatasıyla ölçüldü → **exit 0**. Fixture'ın eski `editingCell` şeklini
kullanması bu yüzden tip hatası vermedi, yalnız 8 kırmızı test olarak göründü.

## ⚠️ Sıra

[[T-110]] (`formatForEdit` yuvarlaması) bununla birlikte çözülmeli: yuvarlama bugün
zararsız çünkü kutu hiç açılmıyor. Bu task onu **canlı hâle getirir**.

## Done tanımı
- [ ] `type-check` 0 · vitest yeşil · e2e yeşil
- [ ] `code-reviewer` onayı
- [ ] **Üretim çağrı yolu ölçülerek gösterildi** (bu task'ın konusu tam olarak bu)
- [ ] `touches:` güncel
