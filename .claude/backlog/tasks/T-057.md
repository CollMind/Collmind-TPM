---
id: T-057
title: Kalan tipsiz zarf çözümleri — status, auto-create, ledger yolları
epic: E-001
sprint: S-001
status: in-progress
assignee: backend-engineer
created: 2026-08-02
updated: 2026-08-02
---

## Açıklama
[[T-056]] submit yolunu yakınsıyor (`plan.service.ts:810`). Geriye `findEnvelopeByDimensions`'ı
`spendType` vermeden çağıran 4 yer kalıyor; bunlar bölünmüş boyutta [[T-019b]]'nin
`SPEND_TYPE_REQUIRED_FOR_SPLIT_DIMENSION` guard'ına takılır (sessiz hata değil, açık hata —
ama yine de çalışmaz).

| Yer | Ne yapıyor | Bölünmüş dünyada ne olmalı? |
|---|---|---|
| `plan.service.ts:910` | bütçe durumu sorgusu (okuma) | Tip bazlı kırılım (§5.6 `bySpendType`) |
| `plan.service.ts:1019` | auto-create-on-approve — **tek zarf** yaratıyor | Bölünmüş boyutta **çift** yaratmalı (tasarım 0008 §4 tuzağı) |
| `agreement-transaction.service.ts:142` | ledger kaydı | Agreement `spend_type`'ına göre; BOTH/NULL → ADR 0004 Karar 3 (400) |
| `on-invoice.service.ts:439` | ledger kaydı | Adı gereği ON_INVOICE; **varsayma, doğrula** |

## 🟡 F4 — [[T-056]] adım 6'da bulundu, buraya devredildi
`commitReservedForPlan`'ın "legacy: hiç rezerve edilmemiş plan" fallback'i (`bucket='TOTAL'` dalı,
`budget.service.ts` ~781-787 ve ~896-917) hâlâ **tipsiz** `findEnvelopeByDimensions` çağırıyor.

**Bugün ulaşılamaz (kanıtlandı):** adım 5 sonrası `/submit`, `reserveTypedForPlan`'ın kapısından
geçmeden plan PENDING_APPROVAL'a geçemiyor. Ama yukarıdaki üç yoldan biri kullanılırsa erişilebilir
hale gelir → bu task'ta birlikte ele alınmalı.

## Done Checklist (Team Lead, 2026-08-03 — hepsi sağlanmadan `done` yazılmaz)

**Kapsam maddeleri**
- [x] `plan.service.ts:910` `checkBudget` — tip kırılımı **EKLEMELİ** (mevcut alanlar birebir
      korunur). 🔴 Bu ucu frontend TÜKETİYOR (`BudgetApprovalModal.tsx:71`) — şekil değişikliği
      UI'ı kırar. Frontend'e dokunulmaz.
- [x] `plan.service.ts:1019` — T-056 adım 6'da çözülmüş olmalı; **doğrulanıp raporlanır**
- [x] `agreement-transaction.service.ts:142` — agreement `spend_type`'ına göre tipli çözüm;
      BOTH/NULL + bölünmüş boyut → `400 AGREEMENT_SPEND_TYPE_SPLIT_REQUIRED` (ADR 0004 Karar 3)
- [x] `on-invoice.service.ts:439` — **önce ölç** (her zaman ON_INVOICE mi?), sonra çöz
- [x] **F4** — `commitReservedForPlan` legacy TOTAL dalının tipsiz çözümü kapatılır

**Bağlayıcı kurallar**
- [x] Split tespiti [[T-019b]] guard'ından türetilir — **ikinci "is-split" sorgusu YOK**
      (T-056 adım 6 deseni; 0008 §5.7)
- [x] UNSPLIT boyutta davranış **birebir aynı**; mevcut hiçbir testin beklentisi gevşetilmedi
- [x] Key formatlarına dokunulmadı (T-053 kuralı); seed'e dokunulmadı

**Kanıt**
- [x] Her kapsam maddesi için **ayrı mutasyon**: tipli çözüm tipsize geri alınır → ilgili test
      KIRMIZI. Her mutasyonda `tsc` **TEMİZ**, hedef `git diff` ile doğrulanmış, **koşan test
      sayısı** raporlanmış ("N skipped" kanıt değildir)
- [x] Kırmızı üretmeyen mutasyon varsa → koruma kanıtlanmamış, **durulup raporlanmış**
- [x] Uçtan uca SQL: bölünmüş boyutta dört yolun da çalıştığı gösterilmiş
- [x] unit + e2e **3 ardışık reset'siz koşum** (flag kapalı/açık/kapalı), jest borusuz,
      **exit code'lar** raporlanmış
- [x] [[T-047]] genişletilmiş invaryantı üç koşumda PASS; `ENV-2026-NKA-Q1`/`NKA-Q2` 75.000 sabit

**Onaylar**
- [x] `qa-engineer` bağımsız doğrulaması yeşil
- [x] `code-reviewer` onayı alınmış
- [x] Team Lead bağımsız doğrulaması (mutasyonlar kendi koşumumda tekrarlanmış)

**Sonuç**
- [ ] 🔴 **SAĞLANMADI** — ADR 0004 **Karar 5 kısıtı KALKMIYOR** → [[T-065]]: `POST /budget/envelopes/:id/split` üretimde
      kullanılabilir hale gelir — bu, task'ın ürün değeri; açıkça belgelenir

## Ön koşul
[[T-056]]. Birlikte kapanınca `POST /budget/envelopes/:id/split` üretimde kullanılabilir hale
gelir (bkz. ADR 0004 Karar 5 kısıtı).

## İlgili
- [[T-019b]] · [[T-054]] · [[T-055]] · Tasarım: `docs/analysis/0008-*` §4, §5.6, §5.7


## Review turu 1 — ONAYLANMADI (3 bloklayıcı)
- **B1** split yanlış-pozitifi: guard, istenen dönemin **tam eşleşen** zarfı yoksa `createdAt DESC`
  ile alakasız dönemin tipli ikizine düşüyordu. **Team Lead ölçümle doğruladı:** seed zarflarında
  `channel` kolonu NULL (kanal yalnız metadata'da), `NKA + '2026-05'` → tam eşleşme **0**,
  yıl-fallback adayı **2**. Yani NKA-Q2 bölünürse `2026-03…12` dönemli her istek haksız 400 alırdı.
- **B2** `agreement-transaction` 400 sonrası **orphan satır** bırakıyordu (create, throw'dan önceydi);
  satır agreement cap'ini tüketiyordu — ADR 0004 Karar 2'nin kaçındığı "yarım durum".
- **B3** F4'te **özdeşlik kapısı yoktu**; `on + off ≠ fallbackAmount` sessizce eksik/fazla COMMIT.

## Review turu 2 — ✅ ONAYLANDI (bloklayıcı yok)
B1/B2/B3 kapandı; S1 (guard-hatası tekilleştirme), S2 (`sufficient` tip bazlı), S3
(`splitDimension` ek alanı), S6 (TEMP başlığı) tamam. `npm run guards` EXIT=0, allowlist'e yeni
satır yok. Değişen test dosyalarında **silinen satır 0** — commit'li hiçbir test dokunulmadı.

**B3 fixture değişikliği zayıflatma DEĞİL** (review doğruladı): eski testin gerçek yükü
"commit'ler 60/40, uydurma oran değil" iddiasıydı ve aynen duruyor; "999999 asla kullanılmaz"
iddiası ise ayrı testte **daha güçlü** biçimde kanıtlanıyor (aynı girdi 400, COMMIT çağrısı sıfır).

## Team Lead bağımsız doğrulama
- **B1 mutasyonu (kendim koştum):** `winner.period !== periodMonth` → `if (false)` → `git diff` ile
  hedef doğrulandı, `tsc` **TEMİZ**, **EXIT=1**, SP-E2E-10 `expected 200 "OK", got 400 "Bad Request"`;
  geri alınca 1 passed
- Değişen test dosyalarında silinen satır **0** (numstat ile doğrulandı)
- unit **631/631** EXIT=0 · e2e **239/239 × 3 ardışık reset'siz koşum**, üçünde de **EXIT=0**
- [[T-047]] invaryantı üç koşumda PASS · `NKA-Q1`/`NKA-Q2` 75.000 sabit · `E2E-%` artık zarf **0**

## 🔴 Neden `done` YAZILMADI
Checklist'in son maddesi **"ADR 0004 Karar 5 kısıtı kalkar"** sağlanmıyor. Review turu 2'nin üç
bulgusu (**Y2**, **B1 artık aralığı**, **Y1**) bloklayıcı sayılmadı — gerekçe açıkça
*"bugün split üretimde kullanılmadığı için erişilebilir değil"*. Kısıtı kaldırmak tam da bu
bulguları **canlı hale getirir**. → [[T-065]] açıldı; kısıt onunla birlikte kalkar.

Kapsamın 5 maddesi + tüm kanıt ve onay maddeleri **tamamlandı**; eksik olan yalnız sonuç maddesi.
