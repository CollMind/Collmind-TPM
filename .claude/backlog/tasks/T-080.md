---
id: T-080
title: "PATCH tactics tüm JSONB'yi değiştiriyor — ikinci mekaniği girmek birincisini siliyor olabilir (veri kaybı)"
epic:
sprint:
status: todo
assignee: debugger
priority: P1
created: 2026-08-05
updated: 2026-08-05
touches:
  - collmind.backend/src/modules/modes/planning-first/plan/plan.service.ts
  - collmind.frontend/src/components/planning/PlanningGridEnhanced.tsx
  - collmind.backend/test/plan-scale-validation.e2e-spec.ts
migration_seq:
---

## Açıklama

`plan.service.ts` `updateFuTactic`:

```ts
tactics: dto.tactics || planFu.tactics,
```

Bu bir **replace**, merge değil. Gönderilen `tactics` nesnesi kolonun tamamının yerine geçer.

`code-reviewer` (F2/C3 birinci tur, #14) frontend'in tek anahtar gönderdiğini işaret etti
(`PlanningGridEnhanced.tsx:1032` — tek PATCH çağrı yeri). Doğruysa: planner grid'de ikinci
mekaniği düzenlediğinde **birincisi sessizce siliniyor.**

**Bugün kimsenin fark etmemiş olması beklenir**, çünkü `plan_fus` bugün 0 satır — ürün yalnız
lokal geliştirmede koşuyor.

E2E'ler bunu görmüyor: iki anahtarı **tek istekte** gönderiyorlar
(`role-journey.e2e-spec.ts:370`), yani replace ile merge aynı sonucu veriyor. Testlerin yeşil
olması bu vaka için bilgi taşımıyor.

## Ölçüm önce (kod yazmadan) — bu task ölçümle başlar

- [ ] `PlanningGridEnhanced.tsx:1032` çevresini oku. PATCH gövdesi gerçekten **tek anahtar** mı
      içeriyor, yoksa tüm satırın tactics'i mi gönderiliyor? **Kod adına değil, gönderilen
      gövdenin şekline bak.** Bu tek soru task'ın P1 mi yoksa hiç-sorun-değil mi olduğunu
      belirler.
- [ ] Başka PATCH çağrı yeri var mı? (Bu oturumda "tek çağrı yeri" iddiası birkaç kez düşük
      çıktı — grep terimlerini raporla.)
- [ ] Backend sözleşmesi hangisi olmalı: replace mi merge mi? Bir anahtarı **silmek** için bir
      yol gerekiyor mu? (Merge'e geçilirse "bu taktiği kaldır" ifade edilemez hâle gelir —
      bu, karar gerektiren bir sonuçtur, sessizce seçilmez. Belirsizse CLAUDE.md §2.4: DUR.)

### ⚠️ T-079 ile bağımlı — karar bağımsız verilemez

`code-reviewer` (C3 ikinci tur, S1): bugünkü **replace** semantiği, T-079'un vakası için tek
kurtarma yolu. `POST /plans/:id/fus` ile diske düşen bozuk bir anahtar (T-079'da ölçüldü,
kilitlenme zinciri gerçek) bugün ancak PATCH'in tüm JSONB'yi değiştirmesi sayesinde
silinebiliyor. **T-080 "merge"e karar verirse o kaçış yolu kapanır ve bozuk anahtar silinemez
hâle gelir.** Merge seçilecekse bir "anahtar sil" yolu aynı kararın parçası olmak zorunda.

### Ek vaka: boş nesne

`dto.tactics === {}` truthy → `{} || planFu.tactics` `{}` kazanır → **tüm taktikler hatasız
silinir**. Replace semantiğinin en keskin hâli. Bugünkü frontend üretmiyor, ama sözleşme
kararının kapsamında.

### Frontend ölçümü — YAPILDI

`collmind.frontend/src/components/features/plans/PlanningGridEnhanced.tsx:1032` gerçekten
`tactics: { [mechanicCode]: value }` gönderiyor — **tek anahtar**. Tek çağrı yeri
(`updateTacticMutation`, :1017/:1154). Yani veri kaybı vakası **doğrulandı**, P1 doğru.

## Acceptance Criteria

- [ ] Ölçüm raporu: frontend gerçekten tek anahtar mı gönderiyor?
- [ ] Sözleşme karara bağlandı (replace mi merge mi) ve **kod yorumunda gerekçelendirildi**.
- [ ] Veri kaybı vakası varsa: e2e testi **iki ayrı istekte** iki mekanik yazıp ikisinin de
      hayatta kaldığını doğrular. Tek istekte iki anahtar gönderen bir test bu vakayı
      yakalamaz — mevcut testlerin kör noktası tam olarak buydu.
- [ ] Frontend tarafı gerekiyorsa `frontend-engineer`'a ayrı task.

## Done tanımı
- [ ] Testler yeşil (unit + e2e), 3× reset'siz
- [ ] `npm run guards` exit 0 · `money-float.sh --ratchet` exit 0
- [ ] `code-reviewer` onayı
- [ ] Üretim çağrı yolu var
- [ ] `touches:` güncel
