---
id: T-311
title: POST /tenants/:id/suspend HİÇBİR ŞEY YAPMIYOR — kiracı statüsü hiçbir yerde OKUNMUYOR
epic:
sprint:
status: todo
assignee: backend-engineer
created: 2026-08-27
updated: 2026-08-27
touches:
  - collmind.backend/src/modules/user/strategies/jwt.strategy.ts
  - collmind.backend/src/modules/tenant/tenant.service.ts
---

## ⛔ BULGU — `T-309`'un silmesi **YAZISIZ BİR SÖZLEŞMEYİ** de götürdü

Silinen `tenant.guard.ts` (`git show HEAD~:...:20-26`) şunu yapıyordu:
```
where: { id: tenantId, status: 'ACTIVE' }
⇒ ASKIYA ALINMIŞ bir kiracının kullanıcılarını REDDEDİYORDU
```

**Ölçüm (review `S8`):**
```
grep -rn "TenantStatus|tenant.status|tenantStatus" src   (spec/migration hariç)
→ CANLI HİÇBİR İSTEK YOLUNDA kiracı statüsü OKUNMUYOR
jwt.strategy.ts:26        yalnız user.status !== 'ACTIVE'
tenant.service.ts:168/174 statüyü YAZIYOR — KİMSE OKUMUYOR
```

⇒ **`POST /tenants/:id/suspend` bugün HİÇBİR ŞEY YAPMIYOR.** Askıya alınan kiracının
kullanıcıları API'yi **kullanmaya devam eder**.

## Guard ölüydü, silinmesi DOĞRU — ama sözleşme kayda GİRMEDİ

> `DISIPLIN`: *"Testler bir ŞARTNAMEDİR — kod silinse bile."*

Bu sözleşme ne `T-309`'a ne `Z45`'e girdi; **bu task onu kayda alır**.

📌 Ve sınıfı tanıdık: *"mekanizma var, ona giden yol yok"*un **tersi** — burada **yol
var** (`/suspend` ucu, `TENANT_WRITE`, canlı frontend tüketicisi), **mekanizma yok**.

## Acceptance Criteria
- [ ] ⛔ **Repro pini ÖNCE:** kiracıyı `SUSPENDED` yap → kullanıcısı **hâlâ `200`
      alıyor** — kusur **görülür**, sonra düzeltilir
- [ ] Kiracı statüsü **canlı istek yolunda** okunur *(muhtemel yer: `jwt.strategy.ts`,
      `user.status` kontrolünün yanında — **tek kaynak**, ikinci bir guard **değil**;
      `T-309`'un dördüncü-yol dersi)*
- [ ] Davranış pini **iki-girdi-iki-çıktı**: `ACTIVE` → `200` · `SUSPENDED` → `403`
- [ ] `SYSTEM_INVARIANTS`'a bir satır: *"askıya alınmış kiracı erişemez"* — **kanıt
      yüzeyiyle**

## Done tanımı
- [ ] Testler yeşil · `npm run guards` exit 0 · `code-reviewer` onayı
