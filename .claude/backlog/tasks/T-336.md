---
id: T-336
title: LTA bağ tekilliği ↔ TERMINATED / soft-delete uyuşmazlığı
epic:
sprint:
status: todo
assignee: backend-engineer
created: 2026-08-30
updated: 2026-08-31
touches:
  - collmind.backend/src/modules/shared/lta/lta-agreement.service.ts
  - collmind.backend/src/modules/shared/lta/lta-agreement.repository.ts
  # ⛔ migrations/ DÜŞTÜ — Q22 (iii) migration GEREKTİRMİYOR (Z78 §3)
migration_seq:
---

## Açıklama (review `S4`, `W2 DALGA-B`)
```
LTAAgreement extends BaseEntity      ⇒ @DeleteDateColumn deleted_at VAR
lta-agreement.repository.ts:143      softRemove MEVCUT
alreadyBound kontrolü (service:73-76) TypeORM findOne ⇒ SOFT-SİLİNMİŞ satırları HARİÇ TUTAR
migration 1817 CREATE UNIQUE INDEX   ⇒ WHERE deleted_at IS NULL YOK — TÜM satırları kapsar
```

**İki sonuç:**
- **(a)** Soft-silinmiş bir başlıktan sonra aynı ebeveyne yeniden bağlanma **uygulama
  kontrolünü GEÇER**, DB'de `QueryFailedError` ile patlar ⇒ kullanıcıya `409` yerine
  **ham `500`**. *(`DISIPLIN`: "`500` bu kod tabanında yaygın bir örtü.")*
- **(b)** `terminateAgreement` satırı **silmiyor** (yalnız `status`) ⇒ **sonlandırılmış**
  bir oran-şartları başlığı bağı **kalıcı işgal ediyor**; aynı anlaşma için yeniden
  müzakere edilmiş bir oran takvimi **kurulamaz**.
  ⛔ Ve `PATCH` reddinin mesajı **tam bu yolu öneriyor** (*"başlık sonlandırılır ve
  yenisi … yaratılır"*) — yani ürün **kendi önerdiği yolu kapatıyor**.

## Karar gerekiyor (ürün sahibi)
```
(i)   kısmi index:  WHERE deleted_at IS NULL
(ii)  tekillik:     status <> 'terminated' üzerinde
(iii) "bir ebeveyn = bir başlık, ÖMÜR BOYU"
```

## Acceptance Criteria
- [ ] Karar alındı ve **gerekçesiyle** kayda geçti
- [ ] `PATCH` ret mesajı ile gerçek davranış **tutarlı** *(bugün değil)*
- [ ] TOCTOU yolu `409` veriyor, `500` değil
- [ ] Migration üç durum ayrımı; dalların en az ikisi ampirik

---

## ⛔ HÜKÜM `Q22` (ürün sahibi, 2026-08-31 · `Z78 §3`) — ADAY **(iii)**, İNİYOR

**Kural: "BİR EBEVEYN = BİR BAŞLIK, ÖMÜR BOYU."** Ve **kod da ömür-boyu tekil** —
`agreementCode` kardeşi **aynı hükümle, aynı dalgada, tek pakette**.

```
alreadyBound  manager.findOne(LTAAgreement,{where:{tenantId,agreementId}})  withDeleted YOK
findByCode    repository.findOne({where:{tenantId,agreementCode}})          withDeleted YOK
DB            UQ_lta_agreements_agreement_id · IDX_lta_agreements_tenant_code
              İKİSİ DE KISMİ DEĞİL ⇒ soft-delete edilmiş satır YERİ İŞGAL EDER
⇒ soft-delete → yeniden bağlanma: ham QueryFailedError → 500
```

**İniş:** iki ön-kontrole `withDeleted: true` ⇒ **`500 → 409`, doğru mesajla.**

⛔ **MIGRATION YOK** — DB tarafı zaten ömür-boyu tekil; değişen tek şey ön-kontrolün onu
**görmesi**. `migration_seq` boş KALIR.

⛔ **`(c)` yolu AÇIK KALIR ve TESTLE PİNLENİR:** `terminate → FARKLI ebeveyne yeni başlık
→ 201`. `PATCH` mesajı **tam bunu** öneriyor. *(Bir önceki review "ürün kendi önerdiği yolu
kapatıyor" demişti — **ölçüm çürüttü**; pin, o çürütmenin kalıcı hâlidir.)*

**`T-335` hizası, `(ii)`'nin reddi:** *"yeniden müzakere edilen oran, ebeveynin ESKİ onayını
miras almaz."*
