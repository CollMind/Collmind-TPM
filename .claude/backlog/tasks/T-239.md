---
id: T-239
title: guard.sh bölüm dağılımını BASIYOR ama KONTROL ETMİYOR — ikinci, kapısız bir sayı
epic: E-001
sprint: S-001
status: done
assignee: qa-engineer
created: 2026-08-18
updated: 2026-08-18
touches:
  - docs/brd-v2/guard.sh
  - docs/brd-v2/00_PAKET_INDEKSI.md
---

## Açıklama

`Z8` turunda ölçüldü. `guard.sh`'ın kural sayımı **toplamı** denetliyor
(`indeks 375 ↔ gerçek 380` → ⛔ İHLAL, doğru çalıştı) ama **bölüm dağılımını yalnız
BASIYOR**:

```
guard.sh   toplam        → KONTROL EDİLİYOR   (kapı)
guard.sh   bölüm başına  → yalnız BASILIYOR   (çıktı)
```

**Sonuç ölçüldü:** `00_PAKET_INDEKSI.md`'nin dağılım satırı
`147 + 49 + 89 + 89 = 374` diyordu — **kendi toplamıyla (`375`) bile tutmuyordu**,
ve gerçekle (`150 + 49 + 92 + 89 = 380`) hiç tutmuyordu. Ne zaman bayatladığı
bilinmiyor; guard hiç bakmadığı için **hiçbir zaman kırmızıya dönmedi**.

📌 Bu `F8`'in (*"sayı dört yerde dört farklıydı"*) guard'ın **kör noktasındaki** hâli,
ve `CLAUDE.md`'nin *"doğrulama bir KAPIDIR — durdurmuyorsa doğrulama değildir"*
maddesinin doğrudan vakası: **basılan bir sayı okunmayı bekler; bir kapı beklemez.**

⚠️ Ve `CLAUDE.md`'nin *"bir guard düzeltildiğinde iki soru sorulur"* kuralı burada
geçerli: kör kaldığı sürede ne birikti, **ve** guard onu şimdi görebilir mi?

## Seçenekler (karar ürün sahibinin)

| şık | sonuç |
|---|---|
| **(a)** guard dağılımı da denetlesin | ikinci kapı; ama `00_PAKET_INDEKSI`'de **elle tutulan** bir sayı daha kalır |
| **(b)** dağılım satırı indeksten **kalksın** | `CLAUDE.md`: *"kural sayısı hiçbir belgeye yazılmaz — kanonik kaynak guard çıktısıdır"*. En tutarlı şık `[GEREKÇELİ]` |
| **(c)** satır kalsın, *"guard denetlemiyor"* uyarısıyla | ⚠️ **bugün bu yapıldı** — geçici; bir uyarı bir kapı değildir |

## Acceptance Criteria

- [x] ✅ Şık **`(b)`** seçildi (ürün sahibi, 2026-08-18) — gerekçe karar defteri `Z9`
- [x] `(a)` **seçilmedi** → kırmızı-kanıt koşusu gerekmiyor. Reddediliş gerekçesi `Z9`'da:
      indekste **elle tutulan bir sayı daha** kalırdı — `F8`'i çözmez, ikinci bir
      bekçiyle **erteler**
- [x] ✅ **`Z9` ÖNCE yazıldı**, sonra satır kaldırıldı (`Z1` şartı). Guard `EXIT 0`,
      `380 kural` — toplam satırı **kalıyor**, çünkü o denetleniyor. Ayrım: **denetlenen
      sayı kalır, denetlenmeyen sayı kalkar.**
- [x] ⚠️ **Kör nokta taraması YAPILDI** (2026-08-18) — ve **bir tane daha** buldu:

      | satır | guard denetliyor mu | değerlendirme |
      |---|---|---|
      | `L2` kural tanımı (`380`) | ✅ evet (`guard.sh:384`) | kalır |
      | `Açık (⛔) kural` (`0`) | ✅ evet (`guard.sh:399`) | kalır |
      | ~~Bölüm dağılımı~~ | ❌ hayır | **kaldırıldı** (`Z9`) |
      | **`Açık karar (kural dışı)` (`2`)** | ❌ **HAYIR** | ⚠️ **aynı sınıf, ama daha zayıf vaka** |

      **`EK_*` dosyalarında kural/madde sayısı iddiası: `0`** (tarandı, yok).

      📌 **`Açık karar (kural dışı)` neden hemen kaldırılmadı:** o hücre sayıyı **kendi
      listesiyle birlikte** taşıyor (*"`2` — veri ayrımı modeli · iade temsili"*), yani
      sayı **aynı hücrede doğrulanabilir**. Bölüm dağılımı ise dört sayı taşıyordu ve
      hiçbiri kendi içinde doğrulanamıyordu — üstelik toplamları kendi toplam satırıyla
      bile tutmuyordu.

      ✅ **KARAR (ürün sahibi, 2026-08-18): sayı düşer, liste kalır** — kayıt `Z10`.
      Gerekçe: *"sayının hiçbir işi yok — liste zaten iki madde gösteriyor. `2` yazmak,
      okuyucuya saymayı kolaylaştırmıyor, yalnız bir **bayatlama yüzeyi** ekliyor."*

## Done tanımı

- [x] `guard.sh` exit 0 — ⚠️ **kendi dizininden** koşulmalı (`cd docs/brd-v2 && bash guard.sh`);
      kök dizinden `EXIT 2` + *"HATA: ./03_IS_KURALLARI bulunamadı"* verir. **Sesli hata,
      sessiz geçiş değil** — doğru davranış, ama ölçüm alışkanlığına yazıldı
- [x] `(a)` seçilmedi — koşu gerekmiyor
- [x] `code-reviewer` — **gerekmedi**: değişiklik yalnız belge (iki satır kaldırma + iki karar kaydı), kod/kapı davranışı değişmedi. Guard `EXIT 0` her adımda ölçüldü.
- [x] `touches:` güncel (+`04_KARAR_KAYDI.md` — `Z9`)
