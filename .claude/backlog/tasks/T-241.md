---
id: T-241
title: user_scopes'a YAZMA YOLU YOK — bayrağın önündeki gerçek engel
epic: E-001
sprint: S-001
status: todo
assignee: backend-engineer
created: 2026-08-18
updated: 2026-08-18
touches:
  - collmind.backend/src/modules/user/
  - collmind.backend/src/database/entities/user-scope.entity.ts
---

## Açıklama

Ürün sahibinin sorusuyla ortaya çıktı (2026-08-18):

> *"`PLANNER`'ların `11/29` ve `17/29` kapsamı bugünkü **seed**'den geliyor. Gerçek bir
> tenant'ta bu satırlar **kim tarafından** yazılacak? Eğer yazma yolu yoksa, bayrak
> açıldığında yeni bir `PLANNER` **sıfır satırla doğar** ve hiçbir şey göremez —
> `T-028c`'nin korktuğu senaryo."*

**Ölçüldü — yazma yolu YOK:**

```
HTTP ucu (herhangi bir controller)   ⛔ SIFIR
POST /users → create()                ⛔ kapsam satırı YAZMIYOR
yazan tek yollar                      seed (user-scope.seed.ts) ·
                                      migration 1792 (tek seferlik backfill)
```

**Pozitif kontrol:** `User` entity'sinin yazma yolu var (`user.service.ts` +
`user.seed.ts`) — desen çalışıyor, `UserScope`'da gerçekten yok.

### ⚠️ Ve migration `1792` bunu KENDİ BAŞLIĞINDA kabul ediyor

> *"⚠️ **KAPSAM DIŞI** (release notunda belirtilmeli — **manuel atama gerekir**):
> Hiç plan açmamış bir `PLANNER` (yeni kullanıcı, henüz iş yapmamış) bu backfill'den
> satır **ALMAZ** — enforcement açıldığında `[]` görür (fail-closed, `R-2`). Bu
> kullanıcılar için **admin'in `user_scopes`'a manuel satır eklemesi gerekir**."*

📌 **Ama o "manuel" yol da YOK.** Migration, var olmayan bir kapıya adres veriyor —
admin ekranı da API ucu da yok. Yani cümle bir çözüm değil, **kaydedilmiş bir boşluk**.

## Sınıf — bu projenin imza kusuru

*"Mekanizma var, ona giden yol yok"* (`CLAUDE.md §4.2`, adıyla sayılan liste:
`T-033` · `T-036` · `T-039` · `T-046d` · `T-048` · `T-052` · `T-053` · `T-062`).

Burada mekanizma **fazlasıyla** var: `AccessScopeService` tekil, `R-1`/`R-2` semantiği
testli, pair düzleştirme tuzağı kapalı, `T-237` verinin bütünlüğünü kurdu, `T-238`
gereksiz kolonu düşürdü. **Eksik olan tek şey: satırları kimin yazacağı.**

## ⚡ Bayrağı BLOKLUYOR

```
T-238  ✅ kapandı (review'da)
T-241  ⛔ BU TASK        ← bayrak buna da bağlı
bayrak    SCOPE_ENFORCEMENT_ENABLED
Faz B     union + göç
```

⚠️ Bayrak `T-241` olmadan açılırsa: bugünkü iki `PLANNER` seed satırı taşıdığı için
**çalışır görünür**, ama tenant'a eklenen **her yeni `PLANNER` hiçbir şey görmez**.
Kusur ilk gün değil, **ilk yeni kullanıcıda** çıkar — ve yönü fail-closed olduğu için
*"sistem bozuk"* diye raporlanır.

## ✅ KARAR: **(b)** — `POST /users` akışına GÖMÜLÜ (ürün sahibi, 2026-08-18)

| şık | neden reddedildi / seçildi |
|---|---|
| (a) ayrı admin ucu | ⛔ kullanıcı **kapsamsız doğabilir**, kapsam **ikinci bir adım** olur. Unutulabilir — ve unutulduğunda **fail-closed**: yeni kullanıcı hiçbir şey görmez. `K-2.6.8a` *"boş kapsam = erişim yok"* diyor, yani boş doğmak **meşru bir durum** ama **kaza olmamalı** |
| (c) yalnız seed | ⛔ bugünkü hâl — sorunu **çözmüyor** |
| **(b) `POST /users`'a gömülü** | ✅ **rol + kapsam BİRLİKTE alınır; kapsamsız kullanıcı YARATILMAZ.** Ara durum yok |

> **`T-214`'ün deseni birebir:** *"tenant şablon seçer ve eşik ayarlar"* — **tek çağrı,
> iki bilgi.**

### Kapsam HER ZAMAN yazılır — içeriği role bağlı

```
ADMIN · FİNANS · İZLEYİCİ    joker satır   {cplId: null, categoryId: null}
PLANNER · KATEGORİ MÜDÜRÜ    açık kapsam   çağrıda VERİLİR
```

⚠️ **`İZLEYİCİ` neden joker tarafında:** `T-235 ADIM 2` (`ff1f85f`) onu
`UNRESTRICTED_ROLES` kod dalından çıkardı; artık kapsamı **joker satırdan** geliyor
(`ADIM 1`, `a25e820`). Yani kod dalı değil, **veri**.

📌 Ve bu şekil **ileriye dönük uyumlu**: `T-235 ADIM 3` `ADMIN`/`FİNANS`'ı da kod
dalından çıkaracak — o gün bu üç rolün satırları **zaten yazılıyor** olacak.

### ⚠️ `K-2.6.10` sınırı — uç bir kapsam YAZAR, bir yetki HESAPLAMAZ

> *"Yetki kararı **tek bir yerde** verilir."*

Yazma yolu **veriyi** kurar; karar yine `AccessScopeService`'te kalır. Uç
`buildScope`'un semantiğini (`R-1` pair · `R-2` fail-closed · `NULL` = *hepsi*)
**yeniden uygulamaz**.

## Ölçülmüş zemin (2026-08-18)

```
CreateUserDto            `role` ZATEN var (enum, varsayılan PLANNER) — rol alınıyor
user.service.create()    düz create+save · transaction YOK · kapsam YOK
```

⚠️ **Yani `create()` atomik DEĞİL.** Kapsam eklenirken **aynı transaction'a** alınmalı —
yoksa kullanıcı yazılıp kapsam yazılamadığında tam olarak kaçınılan delik doğar
(`K-2.2.9h`'in atomiklik gerekçesinin bu akıştaki karşılığı).

⚠️ Ve `forbidNonWhitelisted` açık: DTO'ya alan **eklenmeden** gönderilen kapsam **400**
alır — yani sözleşme değişikliği **açık** olmak zorunda, sessizce yoksayılamaz.

## Acceptance Criteria

- [ ] `CreateUserDto` kapsam alanı taşır; `POST /users` **rol + kapsam birlikte** alır
- [ ] ⚡ **ATOMİK** — kullanıcı ve kapsam satır(lar)ı **tek transaction**. Kısmi başarı
      (kullanıcı var, kapsam yok) **oluşamaz**
- [ ] **Kabul davranışı — üç girdi, üç çıktı:**
      ```
      kapsamsız PLANNER yaratma denemesi   →  400
      ADMIN yaratma                        →  joker satır OTOMATİK
      yaratılan HER kullanıcı              →  kapsam satırı ≥ 1
      ```
- [ ] Sonuncusu bir **invaryant testine** bağlanır: `users` tablosunda kapsam satırı
      olmayan kullanıcı **sıfır** (rol farkı gözetmeksizin — joker de bir satırdır)
- [ ] ⚠️ `K-2.6.10` korunur — uç `AccessScopeService`'in semantiğini **yeniden
      uygulamıyor**; `code-reviewer` bunu ayrıca denetler
- [ ] RBAC: bu ucun kendisi `@Roles(ADMIN)` (bugünkü `POST /users` ile aynı) —
      `K-2.6.4`: `YÖNETİCİ` = *"tanımlar, kural yönetimi"*
- [ ] e2e: yeni yaratılan bir `PLANNER` **bayrak açıkken** kendi kapsamını görüyor
      (bugün bayrak kapalı — test bayrağı **açarak** koşmalı, yoksa hiçbir şey ölçmez)

### ⚠️ Kapsam SINIRI — güncelleme bu task'ta DEĞİL

```
YARATMA      bayrağı AÇAR                        → BU TASK
GÜNCELLEME   bir yönetim yeteneği                → [[T-242]]
```

Ürün sahibi: *"Yaratma bayrağı açıyor, güncelleme bir yönetim yeteneği."*

## Done tanımı

- [ ] Testler yeşil (unit + ilgili e2e)
- [ ] `npm run guards` exit 0 · `money-float --ratchet` exit 0
- [ ] **Üretim çağrı yolu var mı?** — bu task'ın **konusu** tam olarak bu; uç yazıldıysa
      HTTP rotası + RBAC + e2e ile kanıtlanır
- [ ] `code-reviewer` onayı
- [ ] `touches:` güncel
