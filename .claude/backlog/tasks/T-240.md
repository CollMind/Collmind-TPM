---
id: T-240
title: ledger_entries'in 5 FK'siz kolonu — yapısal yol, canlı hata değil
epic: E-001
sprint: S-001
status: todo
assignee: data-engineer
created: 2026-08-18
updated: 2026-08-18
touches:
  - collmind.backend/src/database/entities/ledger-entry.entity.ts
  - collmind.backend/src/database/seeds/cleanup-data.ts
migration_seq: (Team Lead tahsis edecek — MIGRATION_SEQUENCE.md)
---

## Açıklama

[[T-238]]'den **ayrıldı** (ürün sahibi, 2026-08-18): farklı sınıf, ayrı tur.

```
ledger_entries   kolon DOLU olabilir, FK yok   → BÜTÜNLÜK eksik   ← BU TASK
channel_id       kolon BOŞ, hiç okunmuyor      → VARLIK gereksiz  → T-238
```

`code-reviewer` ölçümü (`607a2ab` review'u, 2026-08-18) — `main` şemasında FK'siz
`%_id` kolonları:

```
ledger_entries | cpl_id · mechanic_id · tactic_id · fu_id · reverses_entry_id
```

**Mekanizma [[T-237]]'nin kapattığının aynısı:** `cleanup-data.ts` `mechanics` ·
`tactics` · `forecasting_units` satırlarını **sert siliyor** ve `ledger_entries`'e
**hiç dokunmuyor**; o kolonlarda FK de yok → **sessiz öksüz**.

⚠️ **Bugün fiili kusur YOK** (ölçüldü: `ledger_entries` `0` satır, öksüz `0`) — bu
**yapısal bir yol**, canlı bir hata değil. Ama `ledger_entries` bir **defterdir**
(`K-2.3.4`: *"hiçbir defter kaydı silinemez"*), yani doldurulduğu an sessiz öksüz
üretmeye başlar.

📌 `sales_actuals` / `budget_envelopes` bu script üzerinden öksüz **üretmiyor** —
kategorilerden **önce** siliniyorlar (ölçüldü).

## Acceptance Criteria

- [ ] Beş kolonun her biri için karar: **FK mi, bilinçli mi**.
      ⚠️ `reverses_entry_id` **self-referential** — ayrı değerlendirilir
- [ ] Silme davranışı `T-237` gerekçesini izler (`ADR 0012` → `RESTRICT`). ⚠️ **Ama
      defter tablosunda `RESTRICT` silmeyi TÜMÜYLE engeller** — ve bu **istenen**
      olabilir (`K-2.3.4`). **Karar ürün sahibinin**
- [ ] `cleanup-data.ts`'in sert sildiği **her** tablo için hedef-tablo taraması: script
      bir öksüz kaynağı bırakıyor mu? (`T-237` turunda **yalnız `categories`** için
      yapıldı; `mechanics`/`tactics`/`forecasting_units`/`skus`/`brands` **taranmadı**)
- [ ] `RESTRICT` seçilirse `cleanup-data.ts` **patlar** — `T-237`'de olduğu gibi aynı
      dalgada düzeltilmeli, yoksa seed/e2e kırılır

## Done tanımı

- [ ] Testler yeşil (unit + ilgili e2e)
- [ ] `npm run guards` exit 0 · `money-float --ratchet` exit 0
- [ ] Migration varsa `run` → `revert` → `run` ampirik doğrulandı
- [ ] Catalogue guard'ları **şema-nitelendirilmiş**
- [ ] `code-reviewer` onayı
- [ ] `touches:` güncel
