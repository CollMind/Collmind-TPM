---
id: T-349
title: "FE'nin 40 sessiz sıfırı — Q20'nin görüntü katmanında geri alınması"
epic: E-001
sprint: S-001
status: todo
assignee: frontend-engineer
priority: P0
created: 2026-08-31
updated: 2026-08-31
touches:
  - collmind.frontend/src/components/features/plans/PlanningGridEnhanced.tsx
migration_seq:
---

## Hüküm `Z79 §8` (`W8`) — **`8b` ÖNCE**, `BL` öncesi kendi dalgasında

```
PlanningGridEnhanced.tsx   baseVolume ?? 0 / || 0   →  40 vaka   [TL ÖLÇTÜ]
```

Backend `Z77`/`Q20` ile `null` propagasyonuna geçti; istemci **hâlâ sessiz sıfıra** düşüyor.

> ### **`Q20`'NİN BÜTÜN İŞİ, GÖRÜNTÜ KATMANINDA GERİ ALINIYOR.**

⇒ `NOT_EVALUABLE` bir satır ekranda **`0`** görünüyor, *"hesaplanamadı"* olarak değil.
Ve bu, *"veri-sıfır yeşili"* riskinin **UI yüzü**: `BL` gerçek eksik-veri getirdiğinde
bu **kullanıcının gördüğü ilk şey**.

## Hedef
`NOT_EVALUABLE` ekranda **`—` / "hesaplanamadı"** olur — **`targetRoi` emsali**
(`collmind.frontend/src/utils/targetRoi.ts` — aynı ayrımı zaten yapıyor, OKU ve UYARLA).

⛔ **İkinci bir "hesaplanamadı" tanımı YAZMA** (`§7`/`F8`) — tek yardımcı, tek metin.

## Acceptance
- [ ] `baseVolume ?? 0` / `|| 0` **40 vakası** ele alındı; kalan varsa **gerekçesi yorumda**
- [ ] `baseGsv`/`baseCogs` istemci türetmeleri `null` taşıyor, `0`'a düşmüyor
- [ ] Vitest: dolu satır ↔ eksik satır **AYIRT EDİLİYOR** (`§2.7 #6` — aynı çıktı verirlerse test kör)
- [ ] `npm run type-check` + `npm run lint` exit 0
