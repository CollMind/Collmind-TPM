---
id: T-098
title: "dashboard/summary catch-and-null: T-097'nin fırlattığı hatayı tam olarak kaldırdığı davranışa çeviriyor"
epic:
sprint:
status: review
assignee: backend-engineer
priority: P1
created: 2026-08-07
updated: 2026-08-07
touches:
  - collmind.backend/src/database/transformers/decimal.transformer.ts
  - collmind.backend/src/database/transformers/decimal.transformer.spec.ts
  - collmind.backend/src/database/entities/on-invoice-batch.entity.ts
  - collmind.backend/src/modules/shared/dashboard/dashboard.service.ts
  - collmind.backend/src/modules/shared/dashboard/dashboard.service.spec.ts
  - collmind.backend/src/modules/shared/dashboard/dto/dashboard-summary.dto.ts
  - collmind.backend/src/modules/modes/actuals-first/on-invoice/on-invoice.service.ts
  - collmind.backend/src/modules/modes/actuals-first/on-invoice/on-invoice.service.spec.ts
  - collmind.backend/src/modules/modes/actuals-first/on-invoice/dto/validation-response.dto.ts
  - collmind.backend/src/modules/modes/actuals-first/on-invoice/services/on-invoice-validation.service.ts
  - collmind.backend/src/modules/modes/actuals-first/on-invoice/services/on-invoice-validation.service.spec.ts
  - collmind.frontend/src/api/endpoints/dashboard.endpoints.ts
  - collmind.frontend/src/api/endpoints/on-invoice.endpoints.ts
  - collmind.frontend/src/components/features/dashboard/DashboardPage.tsx
  - collmind.frontend/src/components/features/on-invoice/OnInvoiceUploadPage.tsx
migration_seq:
---

## Sınıf: koruma eklendi, bir katman aşağıda yutuluyor

[[T-083a]]'daki yarım düzeltmenin akrabası, ama tersi yönde: orada mekanizma vardı, mesaj
eksikti; burada mekanizma **var ve çalışıyor**, sonucu bir `catch` siliyor.

## Bulgu

`dashboard.service.ts:126-131` (`code-reviewer`, T-097 review B3; Team Lead bağımsız doğruladı):

```ts
let budgetUtilization: DashboardSummaryResponseDto['budgetUtilization'] = null;
try {
  const raw = await this.financeReportingService.getBudgetUtilization(tenantId, budgetFilters);
  budgetUtilization = { ... };
} catch (err) {
  this.logger.warn('getBudgetUtilization failed — returning null', err);
}
```

**Ölçülen zincir:**

```
GET /dashboard/summary            (dashboard.controller.ts:42 — canlı rota)
  → DashboardService
  → FinanceReportingService.getBudgetUtilization
  → budgetAllocationRepository.find({...})     (finance-reporting.service.ts:129)
  → BudgetAllocation hidrasyonu
  → DecimalTransformer.from                    (9 transformer'lı kolon)
```

`from` artık ayrıştırılamayan bir değerde `InvalidDecimalError` fırlatıyor ([[T-097]]).
Bu rotada o hata **tam olarak `null`'a dönüşüyor** — T-097'nin ortadan kaldırmak için var
olduğu davranışa.

## İki ayrı kusur, ikisi de kapatılmalı

1. **Kapsam:** `catch (err)` her şeyi yakalıyor. Beklenen hata sınıfı ne? Yalnız o yakalanmalı.
2. **§2.5:** bir para KPI'ı için sessiz `null`. `budgetUtilization: null` gövdede "veri yok"
   demek — oysa gerçek "okunamadı". Frontend ikisini ayırt edemiyor.

⚠️ Bu bir T-097 regresyonu **değil** — catch önceden de oradaydı. Ama T-097 onu görünür
kılıyor: fırlatan bir koruma eklendi ve tek etkisi log satırı oldu.

## Karar gerekiyor (§2.4 — varsayma)

`getBudgetUtilization` başarısız olduğunda dashboard **ne yapmalı**? Üç seçenek ve hiçbiri
ajan varsayımıyla seçilemez:

- (a) Tüm `GET /summary` 500 dönsün — dashboard'un tamamı çöker, ama yanlış sayı gösterilmez
- (b) `budgetUtilization` alanı **hata durumunu taşısın** (`{ status: 'unavailable', reason }`)
      — DTO değişikliği, frontend işi
- (c) `null` kalsın ama **yalnız beklenen hata sınıfları** için, `InvalidDecimalError` yukarı
      geçsin

Ölçülmesi gereken: bu catch bugün **hangi** hataları gerçekten yutuyor (yani hangi gerçek
başarısızlık için yazılmış)? Git geçmişi ve çağrı yolu bunu söyleyebilir. Cevap yoksa **DUR.**

## Acceptance Criteria

- [x] Üç rotanın hiçbirinde sessizce veri kılığına girmiyor
- [x] Karar: kapsam daraltmak yerine **hata veri kılığından çıkarıldı** — catch'in yakaladığı
      şey değil, ürettiği şey düzeltildi (gerekçe: dashboard bir özet ekranı, 500 orantısız)
- [x] Üç karar ürün sahibinden alındı ve üçü de kodda gerekçeli
- [x] **Yedi mutasyon**, ayrık kümeler (tablo aşağıda) — ve ilk iki denemem no-op çıktı,
      o da bir bulguya dönüştü
- [x] §7.1 kardeş taraması **yapıldı — "tek vaka" iddiası yine yanlış çıktı, üç vaka var**
      (ölçüm aşağıda). Bu task artık üçünü birden kapsıyor.

## Done tanımı
- [ ] Testler yeşil (unit + e2e), 3× reset'siz
- [ ] `npm run guards` exit 0 · `money-float.sh --ratchet` exit 0, korunanlar **36/54/10**
- [ ] `code-reviewer` onayı
- [ ] Üretim çağrı yolu var (`GET /dashboard/summary` canlı)
- [ ] `touches:` güncel


---

## ÖLÇÜM — 2026-08-07, Team Lead

### "Yutan tek catch bu" YANLIŞ — üç vaka

Review bir tane bulmuştu. Ölçüldü: **üç** canlı rota `DecimalTransformer` hatasını yutuyor,
ve üçü birbirinden **farklı** şekilde yanlış.

| # | yer | hata dalında ne oluyor | ağırlık |
|---|---|---|---|
| 1 | `dashboard.service.ts:129` | `budgetUtilization = null` | sessiz **yokluk** |
| 2 | `on-invoice-validation.service.ts:558` | `current: 0`, `status: RED` | sessiz **yanlış sayı** |
| 3 | `on-invoice.service.ts:543` | entry `ERROR`, `error.message` **diske yazılıyor** | veri sızıntısı |

**İkincisi en ağırı ve review'ın bulduğundan kötü.** Başarı dalında `current: current` (gerçek
kullanım); catch dalında **`current: 0`**. Kullanıcı "bu zarfın mevcut kullanımı sıfır" diye
okur — oysa gerçek "okunamadı". §2.5'in doğrudan yasağı, ve T-097'nin `Infinity` gerekçesinin
aynısı: **sessizce yanlış bir değer, sessizce geçersiz bir değerden tehlikelidir.** `null`
hiç değilse kendini ele veriyor; `0` bir bütçe rakamı olarak geçerli görünüyor.

**Üçüncüsü yeni bir eksen açıyor:** `error.message` `validationErrors` JSONB'sine
kalıcılaştırılıyor. T-097'nin hata mesajı **ham kolon değerini** taşıyor
(`Cannot read a decimal column value: "..."`). T-097'de "hiçbir global filter mesajı
yankılamıyor, değer yalnız sunucu log'una düşer" diye ölçmüştüm — **filtreleri ölçtüm,
mesajı KALICILAŞTIRAN catch'leri ölçmedim.** §7.1: kalıbın bir ucunu aradım.

### Zincirler (ölçülmüş, varsayılmamış)

```
1) GET /dashboard/summary
     → FinanceReportingService.getBudgetUtilization
     → budgetAllocationRepository.find                    (finance-reporting.service.ts:129)
     → BudgetAllocation hidrasyonu (9 transformer kolonu)

2) POST /on-invoice/:batchId/validate
     → OnInvoiceValidationService  (…:558)
     → budgetService.findEnvelopeByDimensions             (budget.service.ts:1203)
     → budgetRepository.findEnvelopeByDimensions
     → envelopeRepository.createQueryBuilder              (budget.repository.ts:136)
     → BudgetEnvelope hidrasyonu (3 transformer kolonu)

3) POST /on-invoice/... (entry işleme)  → aynı envelope zinciri  (on-invoice.service.ts:543)
```

### ⚠️ Bu ölçümün kendisi iki kez maskelendi — ikisi de kayda değer

**(i) `\bcatch\b` prose'da eşleşti.** İlk tarama 144 catch buldu; 19'u **yorum satırıydı**
("try-catch'i kendi içinde…"). Sözdizimsel forma (`} catch (…) {`) daraltınca **122**.

**(ii) Brace dengesi ilk satırda sıfırlanıyordu.** `} catch (err) {` satırında `{` ve `}`
eşit → gövde hiç okunmadı → **122/122 "throw içermiyor"** çıktı. Filtre her şeyi eşleştirdi,
yani hiçbir şey ölçmedi (§2.7 #5). Düzeltince: 122 catch, **57** throw'suz, **30**'u üretim
kodunda (`src/modules`, `src/common`).

**(iii) Hidrasyon noktası taraması yarısını görüyordu.** `envelopeRepository` ile
`.createQueryBuilder` **ayrı satırlardaydı**; tek-satır regex'i atladı. Tek satır: 9 nokta.
Çok satırlı zincirlere izin verince: **18**. Tam iki katı — ve atlananlardan biri bu task'ın
2. ve 3. vakasının tam olarak geçtiği yerdi (`budget.repository.ts:136`).

> **Ders: bir çağrı kalıbını ararken zincirin satır sonunda kırılabileceğini varsay.
> Prettier uzun zincirleri böler; tek-satır regex'i kod tabanının biçimlendirmesine bağımlıdır.**

### Review'ın sayısı da düzeltildi

Review *"throw içermeyen 122 catch bloğu var"* dedi. Ölçüldü: **122 toplam catch**, bunların
**57'si** throw içermiyor. Sayı toplamın kendisiydi, alt kümenin değil. (CLAUDE.md: dokümanda
sayı yerine niteliksel ayırt edici yaz.)

### Karar hâlâ ürün sahibinin — ama artık üç yer için

Üç vaka **aynı** cevabı almak zorunda değil:

- **1 (dashboard):** özet ekranı, tek bozuk allocation için tüm sayfayı 500'e düşürmek
  savunulabilir değil. Alan **hata durumunu taşımalı** (`unavailable`), `null` değil —
  frontend "veri yok" ile "okunamadı"yı ayırt edebilsin.
- **2 (bütçe etkisi):** `current: 0` **her hâlükârda yanlış**. Karar, 0 yerine ne geleceği:
  alanı nullable yapmak mı, satırı listeden çıkarmak mı, yoksa validation'ı komple
  başarısız saymak mı.
- **3 (entry ERROR):** davranış doğru olabilir (satır bazlı hata), ama **mesaj
  kalıcılaştırılmamalı** — ya sınıf adı yazılmalı ya da hata sınıfına göre ayrılmalı.

**Ek zorunluluk (vaka 3'ten):** T-097'nin hata mesajı ham değeri taşıdığı sürece, o mesajı
kalıcılaştıran veya kullanıcıya dönen **her** yol bir sızıntıdır. Ya mesaj değerden
arındırılmalı ya da bu yollar tek tek kapatılmalı — ve hangisi seçilirse `decimal.transformer.ts`
içindeki N2 yorumu **güncellenmeli** (bugün "yalnız log'a düşer" diyor, bu artık eksik).


---

## ✅ TAMAMLANDI — 2026-08-07 · backend `5d9a946` · `b45a943` · `a264627` · frontend `d065d7d`

Üç commit, tek task. Ürün sahibi kararları: vaka 1 ve 2 **aynı ilke** (hata veri kılığına
girmeyecek), vaka 3 **mesaj/context ayrımı tek noktada**.

### Ne değişti

| vaka | önce | sonra |
|---|---|---|
| dashboard | `budgetUtilization = null` | `budgetUtilizationStatus: 'ok' \| 'unavailable'` |
| bütçe etkisi | `current: 0`, `status: RED` | `current/after/status = null`, `dataStatus: 'unavailable'` |
| entry ERROR | `error.message` diske | `İşlenemedi (${error.name})`, tam hata log'a |
| hata nesnesi | değer mesajda | değer `context.rawValue`'da, mesaj **değişmez** |

### Ölçümün getirdiği, task'ın istemediği dört şey

**1. `budgetUtilization` veri yokluğundan HİÇ null olmamış.** `getBudgetUtilization` boş
dönemde de sıfırlardan oluşan bir özet döndürüyor — yani `null`'ın tek üreticisi yutulan
hataydı. Ve alanın kendi açıklaması *"null if no data"* diyordu. **Belge, kılığın kendisiydi.**
Bu yüzden üç değil **iki** durum var: `empty` yok, çünkü hiçbir kod yolu üretemiyor.

**2. `criticalEnvelopesCount` `status === 'RED'` sayıyor.** Okunamayan zarf RED iddiasını
bırakınca hiçbir yerde sayılmaz oldu — özet "0 kritik" derken listede okunamayan bir zarf
duruyordu. `unreadableEnvelopesCount` eklendi. **Bir toplamdan sadece eksik olmak da, bir
kademe yukarıda, hatayı yokluk diye raporlamaktır.**

**3. `Intl.NumberFormat.format(null)` → `₺0,00`.** Backend'i `null` yapıp UI'a dokunmasaydım
kılık bir katman yukarıda aynen doğacaktı. Tip değişikliği bunu **derleme hatası** hâline
getirdi — iki render noktası sessizce sıfır basmak yerine kırmızıya döndü.

**4. Dashboard testi kusuru sözleşme yapıyordu — bu oturumda dördüncü kez.**
`it('returns null budgetUtilization gracefully if service throws')`. "Gracefully", T-097'nin
"instead of NaN"ı ile aynı işi yapıyor: sessiz yutmayı **dikkatli seçim** gibi gösteriyor.

### Ayrı DTO alanı — üslup değil ölçüm

Ayrımlı birleşim (`{ status: 'unavailable' }`) **kullanılamazdı**: frontend nesne truthy olur
olmaz `budgetUtilization.total` okuyor (`DashboardPage.tsx` → `BudgetUtilizationPanel.tsx`).
Kardeş alan, varlığından habersiz bir okuyucuyu kıramaz.

### Hiçbir şey kanıtlamayan iki mutasyon — ve ortaya çıkardığı kusur

İlk denemeler: catch'teki status atamasını kaldır → **hepsi yeşil**. Initial'i değiştir →
**hepsi yeşil**. İkisi de **anlamsal no-op**'tu, çünkü kod aynı durumu **iki kez** yazıyordu.

> **Hiçbir testin göremediği tekrar, bir sonraki gerçek değişikliği saklayan tekrardır.**

Kod tekrarsız ve fail-closed hâle getirildi (initial `'unavailable'`, başarıdan sonra `'ok'`).
Ancak ondan sonra mutasyonlar anlam kazandı.

### Yedi mutasyon, ayrık kümeler

| mutasyon | kırmızıya dönen |
|---|---|
| mesaja değeri geri koy | yalnız değişmezlik testi |
| `context`'i boşalt | yalnız erişilebilirlik testi |
| `'ok'` atamasını kaldır | yalnız başarı testi |
| initial'i `'ok'` yap | yalnız hata testi |
| `current:0/RED`'i geri getir | sıfır testi **ve** RED testi |
| satırı listeden düşür | satır-hayatta-kalma testi |
| `error.message`'ı geri koy | yalnız kalıcılaştırma testi |

### Dokunulmayan kardeş — kodda yazılı

"Envelope bulunamadı" dalı da `current: 0` yazıyor. O sıfır **eksik bir zarf** hakkında bir
ifade, okunamamış bir değer hakkında değil; olmayan bir zarfın 0 mı yoksa hiçbir şey mi
raporlaması gerektiği **ürün sorusu** (§2.4). Geçerken karara bağlanmadı.

### Bu dosyanın ilk spec'i

`on-invoice-validation.service.spec.ts` yoktu. Yalnız T-098 hata yolunu kapsıyor — **servisin
geri kalanı hâlâ korumasız** ve bu bilinmeye değer.

### Kapı

backend: `tsc` 0 · `guards` 0 · `ratchet` 0 (**36/54/10** sabit, `decimal.transformer.ts` **1**)
· unit **744/744** · 3× reset'siz e2e **266/266**, T-047 PASS
frontend: `type-check` 0 · vitest **408/408**
eslint iki repoda da **tabanla birebir** — HEAD sürümüne karşı ölçüldü, yeni hata yok.
