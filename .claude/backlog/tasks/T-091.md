---
id: T-091
title: "Para akümülatörleri string birleştiriyor — biri diske yazıyor (bütçe), biri yanlış 'yetersiz bütçe' üretiyor"
epic:
sprint:
status: todo
assignee: debugger
priority: P1
created: 2026-08-05
updated: 2026-08-05
touches:
  - collmind.backend/src/modules/shared/budget/budget-allocation.service.ts
  - collmind.backend/src/modules/shared/spend-calculation/spend-validation.service.ts
  - collmind.backend/src/modules/shared/spend-calculation/spend-validation.service.spec.ts
migration_seq:
---

## ⚠️ Bu, oturumun en ciddi bulgusu — para diske yanlış yazılıyor

`budget-allocation.service.ts:421-424`:

```ts
allocation.onInvoiceReserved  -= reservation.onInvoiceAmount;   // ← DOĞRU çalışıyor
allocation.onInvoiceUtilized  += reservation.onInvoiceAmount;   // ← BİRLEŞTİRİYOR
allocation.offInvoiceReserved -= reservation.offInvoiceAmount;  // ← DOĞRU
allocation.offInvoiceUtilized += reservation.offInvoiceAmount;  // ← BİRLEŞTİRİYOR
await this.budgetAllocationRepository.save(allocation);          // ← DİSKE YAZIYOR
```

**Tip uyuşmazlığı ölçüldü:**

| Alan | Transformer | Runtime tipi |
|---|---|---|
| `allocation.onInvoiceUtilized` (`budget-allocation.entity.ts:104`) | **`DecimalTransformer` VAR** | `number` |
| `reservation.onInvoiceAmount` (`budget-transaction-log.entity.ts:40`) | **YOK** | `string` |

`number - string` → sayısal coercion → **`-=` doğru**.
`number + string` → **birleştirme** → `+=` bozuk.

**Ölçüldü (node, ampirik):**

```
utilized = 500, amount = "100.00"
500 - "100.00"  → 400          (doğru)
500 + "100.00"  → "500100.00"  (string!)
DB'ye yazılan   → 500100       (olması gereken: 600)
```

Aynı satırda `-=` doğru, `+=` bozuk — **muhtemelen bu yüzden bugüne kadar görünmemiş.**

## Canlı çağrı yolu: PLAN ONAYI

`approval-workflow.service.ts:528` ve `:1050` → `budgetService.commitAllReservedForPlan` →
`budget-allocation.service.ts` commit metodu.

Yani **bir plan her onaylandığında** o bütçe zarfının "kullanılmış" tutarı bozuluyor. Sonraki
her bütçe kontrolü (eşik uyarıları, yeterlilik kontrolü, raporlama) bu bozuk değeri okuyor.

## Nasıl bulundu

[[T-089]] review'ında `code-reviewer` (S6). T-089 `spend-validation`'da aynı kalıbı
düzeltiyordu; reviewer kalıbı repo genelinde aradı ve **para kalıcılaştıran** bir örneğini
buldu. Team Lead bağımsız doğruladı.

Bu, [[T-084]]/[[T-085]]/[[T-089]] ile **aynı kök neden** — transformer'sız `decimal` kolon
string döndürüyor. Ama diğerlerinden farkı: **bu bellekte kalmıyor, diske yazılıyor.**
Bozulan veri kalıcı.

## ✅ ÖLÇÜM TAMAM (2026-08-05) — kapsam kesinleşti, BÜYÜMEDİ

### Ö1 — Bozuk veri YOK, onarım migration'ı gerekmiyor

```
main.plans                   → hiç satır yok
main.budget_allocations      → boş
main.budget_transaction_logs → 0
```

Kusur **latent**: ilk gerçek onayda bozacak. "Doğru değeri ledger'dan yeniden türetmek mümkün
mü?" sorusu **konusuz** — türetilecek bozuk veri yok.

### Ö2 — §7.1: 10 aday nokta, **4'ü bozuk**, 6'sı ölçümle güvenli çıktı

Transformer'lı alanlara `+=`/`-=` yapan her yer sayıldı:

| Yer | İşlem | Sağ taraf | Durum |
|---|---|---|---|
| `budget-allocation:422`, `:424` | `+=` | `reservation.onInvoiceAmount` — transformer'sız **string** | 🔴 **BOZUK, diske yazıyor** |
| `spend-validation:422`, `:424` | `+=` | `pmv.calculatedSpend` — transformer'sız **string** | 🔴 **BOZUK, yetersiz-bütçe üretiyor** |
| `budget-allocation:421`, `:423`, `:472`, `:473` | `-=` | string | ✅ çıkarma coerce eder |
| `budget-allocation:526`, `:527` | `+=` | `onInvoiceDiff` — `:521`'de çıkarmayla hesaplanıyor | ✅ sayı |
| `budget-allocation:373`, `:374` | `+=` | `amounts.planned.totalOnInvoice` | ✅ **ölçüldü: sayı** |

**`:373/:374` neden güvenli — ölçüldü, varsayılmadı:** `SpendBreakdown.planned.totalOnInvoice`
zinciri `spend-calculation.service.ts:509,512,636`'da her değeri önce `*` veya `/` içinden
geçiriyor (`(baseGsv * ltaOnInvoicePct) / 100`). Çarpma ve bölme string'i **sayıya çevirir**;
sonraki `+` bu yüzden gerçek toplama. Ampirik: `("1000.00" * "10.0000")/100` → `100` (number).

Yani `reserveBudget`'ın üç çağıranını (`budget-allocation.controller:146`,
`budget.controller:85`, ve approval akışı) ayrı ayrı denetlemeye gerek kalmadı — zincir
kaynağında sayısal.

## TEK GERÇEK: **para akümülatörleri string birleştirmez**

Bu bir invariant cümlesi ve `SYSTEM_INVARIANTS.md`'ye aday.

## ZİNCİR — bu yüzden tek task

```
spend-validation:422   string üretiyor      (totalOnInvoiceSpend += calculatedSpend)
        ↓              estimatedOnInvoiceSpend olarak
checkAvailability      NaN karşılaştırması → "Insufficient budget", "Shortfall: NaN"
        ↓
budget-allocation:422  diske yazıyor        (onInvoiceUtilized += reservation.onInvoiceAmount)
```

Ayrı düzeltmek bir ucu kapatıp diğerini açık bırakır. Bu oturumda **yarım düzeltme iki kez**
oldu (T-083a okuma/yazma, T-084 kardeş karşılaştırmalar) — üçüncüsüne davetiye çıkarılmıyor.

## Eski ölçüm planı (koşuldu, cevapları yukarıda)

- [ ] `main.budget_allocations`'ta anormal büyüklükte `on_invoice_utilized`/
      `off_invoice_utilized` var mı? (Beklenen büyüklük ile karşılaştır)
- [ ] Bugün onaylanmış plan var mı? (`main.plans WHERE status='APPROVED'`) Yoksa bozuk veri de
      yok demektir ve düzeltme yeterli; varsa **veri onarımı ayrı iş.**
- [ ] Aynı kalıp (`+=` ile transformer'lı/transformer'sız karışımı) başka nerede? §7.1: ölçmeden
      "başka yok" deme. `code-reviewer` iki yer daha işaret etti:
      `finance-reporting.service.ts:285-289` ve `:589` (raporlanan spend — bellekte, diske değil).

## Acceptance Criteria

- [ ] `+=` ve `-=` her iki taraf da sayı üzerinden çalışıyor
- [ ] Test: `utilized` 500 iken 100 commit edilir → **600**, `"500100.00"` değil
- [ ] ⚠️ **İKİ ARDIŞIK onay testi.** Tek onayda `0 + "100.00"` = `"0100.00"` → `Number()` → 100,
      yani **tesadüfen doğru**. Kusur ikinci onayda çıkıyor. T-089'un rollup'ıyla aynı şekil:
      tek örnekte doğru, çoklu örnekte bozuk. Tek onaylı bir test bu kusuru **göremez.**
- [ ] Test diskteki değeri kontrol ediyor (bellekteki nesneyi değil)
- [ ] Test **string fixture ile** — sayı verirsen kusuru hiç test etmemiş olursun
      ([[T-089]]'un spec'indeki aynı ders)
- [ ] Mevcut bozuk veri ölçüldü; varsa onarım ayrı task
- [ ] **Mutasyon kanıtı:** düzeltmeyi geri al → test kırmızı
- [ ] Ratchet: `budget-allocation.service.ts` tabanı artmamalı — `src/common/numeric/`
      yardımcıları kullanılabilir. Artış gerekiyorsa **DUR ve bildir.**

## Kökten çözüm ilişkisi

[[T-090]] (`DecimalTransformer`'ı transformer'sız 22 entity'ye yaymak) bu kusur sınıfının
kökünü kapatır. Bu task **noktasal ve acil** — T-090'ı beklemez.

Ama T-090'ın ölçümüne bir veri noktası ekliyor: **transformer'ın kısmi uygulanması, hiç
uygulanmamasından tehlikeli olabilir.** `-=` doğru, `+=` bozuk — karışık tipler, tek tip
olmaktan daha zor fark ediliyor.

## Done tanımı
- [ ] Testler yeşil (unit + e2e), 3× reset'siz
- [ ] `npm run guards` exit 0 · `money-float.sh --ratchet` exit 0
- [ ] `code-reviewer` onayı
- [ ] Üretim çağrı yolu var (plan onayı)
- [ ] `touches:` güncel
