---
id: T-312
title: GET /budget/envelopes — Promise.all SNAPSHOT YARIŞI, tek zarf yüzünden liste 500 olur
epic:
sprint:
status: todo
assignee: backend-engineer
created: 2026-08-27
updated: 2026-08-27
touches:
  - collmind.backend/src/modules/shared/budget/budget.service.ts
---

## ⛔ BULGU — `Z47` review 🟡-3

`budget.service.ts:199-223` (`findAllEnvelopes`): zarf sorgusu ve özet sorgusu
`Promise.all` ile **ayrı bağlantılarda, ayrı snapshot'larda** koşuyor.

```
zarf sorgusu yeni commit'lenmiş bir zarfı GÖRÜR
özet sorgusu GÖRMEZ
⇒ summaryByEnvelopeId.get() boş → throw → TÜM LİSTE 500
   (tek bir zarf yüzünden)
```

## Neden bir TASK, dalganın parçası değil

`throw` **doğru** (`§2.5` — sessizce `0`'a düşmüyor). Ama **tetikleyicisi bir
veri-bütünlüğü çelişkisi değil, sıradan bir EŞZAMANLILIK.**

⇒ Bu bir **hata-yutma STRATEJİSİ kararıdır**: *tek zarf düşünce liste ne yapar?*
**`T-287`/`allSettled` ailesinin sorusu** ve kaydı **orada** — dolayısıyla
`Z47` dalgasına **binmez**, sahipli bekler.

📌 `findEnvelopeById` (`:225-242`) **zaten sıralı** ve doğru yönde — yani düzeltme
şekli repoda **mevcut**.

## Acceptance Criteria
- [ ] ⛔ **Repro pini ÖNCE:** yarışı **kur** (özet sorgusundan sonra zarf ekle) →
      liste `500` **görülsün** (`T-273`)
- [ ] Strateji kararı: **sıralı çağır** (önce zarflar, sonra özetler ⇒ özet kümesi
      **üst-küme**) · **tek transaction** · ya da **`allSettled` + kısmi bozulma**
      *(hangisi olursa `T-287` ailesinin kaydına bağlanır)*
- [ ] Pin **ayırt edici**: bir zarfın özeti eksikken **liste render oluyor** mu,
      yoksa **tamamı mı düşüyor** — iki alternatifi ayırt etmeli

## Done tanımı
- [ ] Testler yeşil · `npm run guards` exit 0 · `code-reviewer` onayı
