---
id: T-237
title: user_scopes.category_id FK'siz — 115/148 satır öksüz, etkisi SESSİZ erişim kaybı
epic: E-001
sprint: S-001
status: todo
assignee: data-engineer
created: 2026-08-17
updated: 2026-08-17
touches:
  - collmind.backend/src/database/entities/user-scope.entity.ts
  - collmind.backend/src/database/migrations/
migration_seq: (Team Lead tahsis edecek — MIGRATION_SEQUENCE.md)
---

## Açıklama

`T-235` ölçüm 3'ün yan bulgusu (`docs/verification/T235_OLCUM_1_VE_3.md §2`).

```
main.user_scopes                 148 satır
  category_id NULL                28    PLANNER — "her kategori", meşru
  category_id ÖKSÜZ              115    ← main.categories'te KARŞILIĞI YOK
  category_id GEÇERLİ              5
```

**Kök neden — aynı tabloda üç FK var, dördüncüsü yok:**

```
FK_user_scopes_cpl      cpl_id      → main.cpls(id)     ON DELETE CASCADE
FK_user_scopes_tenant   tenant_id   → main.tenants(id)  ON DELETE CASCADE
FK_user_scopes_user     user_id     → main.users(id)    ON DELETE CASCADE
                        category_id → ⛔ FK YOK
```

Doğuran migration `1779000000000-CreateUserScopes.ts` `(user_id, cpl_id, category_id)`
için UNIQUE index yazıyor, `category_id` için FK **yazmıyor**.

⚠️ **Entity ile katalog ayrışmış:** `user-scope.entity.ts:41-43` `@ManyToOne(() => Category)`
+ `@JoinColumn({ name: 'category_id' })` **tanımlıyor** — TypeORM ilişki olduğunu sanıyor,
PostgreSQL zorlamıyor.

## Neden `P1` — etkisi sessiz ve YÖNÜ erişim kaybı

`R-2` fail-closed olduğu için öksüz bir pair hiçbir satırla eşleşmez:

```
CM'nin 48 scope satırı → 46'sı öksüz → ETKİN kapsam 2 kategori (8'den)
```

> **Bir kategori silinip yeniden yaratıldığında (yeni `id`), o kategorinin müdürü onu
> SESSİZCE kaybeder** — hata yok, log yok, yalnız boş liste. `K-2.6.8a`'nın uyardığı
> *"neden hiçbir plan göremiyorum"* destek talebinin ta kendisi.

Birikim tarihli: `2026-07-27` (5) · `07-28` (5) · `07-29` (70) · `08-13` (35) —
kategorileri sert silen seed/e2e koşumlarının kalıntısı.

## Acceptance Criteria

- [ ] `category_id` için FK eklenir. ⚠️ **Silme davranışı KARAR gerektirir:** `cpl_id`
      `ON DELETE CASCADE` — kategori silinince scope satırı da silinsin mi (kapsam daralır,
      sessiz) yoksa `RESTRICT` mi (kategori silinemez, **görünür**)? `K-2.6.8a`'nın
      *"boş kapsam = erişim yok"* asimetrisi `RESTRICT` yönünü destekliyor `[GEREKÇELİ]` —
      ama **karar ürün sahibinin**
- [ ] **`115` öksüz satır** FK eklenmeden önce temizlenir — aksi hâlde migration
      **başarısız olur**. Temizlik `CLAUDE.md`'nin *"assert taşıyan migration ÜÇ durumu
      ayırt etmeli"* kuralına uyar: beklenen sayı → temizle+assert · `0` → no-op ·
      ara sayı → **İPTAL**
- [ ] Öksüz satırların **kaynağı** kapatılır: kategoriyi sert silen yol bulunur
      (seed/e2e cleanup) — FK olmadan yeniden birikir
- [ ] `migration:generate` bu FK'yi **artık önermiyor** (entity ↔ katalog eşitliği —
      `CLAUDE.md`: *"bir şema kararını geri alırken entity metadata'sını da geri al"*)
- [ ] ⚠️ **`T-234` ile kesişim ölçülür:** `1390` satırlık drift içinde bu FK var mı?
      Varsa iki task aynı satırı yazacak — sıra tespit edilmeli

## Done tanımı

- [ ] Testler yeşil (unit + ilgili e2e)
- [ ] `npm run guards` yeşil — exit 0
- [ ] Migration `run` → `revert` → `run` döngüsü ampirik doğrulandı
- [ ] Catalogue guard'ları **şema-nitelendirilmiş** (`nspname`/`schemaname` predicate'i)
- [ ] `code-reviewer` onayı
- [ ] `touches:` gerçekte dokunulan dosyalarla güncel
