---
id: T-028c
title: Planner CPL/Category scope enforcement (plan + agreement) — YÜKSEK RİSK
epic: E-001
sprint: S-001
status: done
assignee: backend-engineer
created: 2026-07-28
updated: 2026-07-28
---

## Sonuç
- **Feature flag** `SCOPE_ENFORCEMENT_ENABLED` **tek noktada** (`AccessScopeService.resolveScope`),
  varsayılan **kapalı** (`=== 'true'` → env yoksa false). Kapalıyken PLANNER `UNRESTRICTED` döner,
  DB sorgusu bile yapılmaz (sıfır regresyon). CM/ADMIN/FM/READONLY flag'den etkilenmez.
- **Enforcement:** PlanService create(403)/findById(404 `OUT_OF_SCOPE`)/findAll(filtre)/
  update/addFu/updateFuTactic/updateSkuVolume/removeFu/submit/delete; ApprovalWorkflow
  submitForApproval + approval-history; AgreementService aynı desen (repository QB dahil).
- **Backfill migration 1792:** geçmiş planlardan scope türetir; marker'lı `down()`;
  savunmacı `AND u.tenant_id = p.tenant_id`. Dev'de 0 satır (zaten seed'li).
- **437 unit + 142 e2e — hem flag KAPALI hem AÇIK yeşil** (davranış-değişmiyor + enforcement-çalışıyor
  ikili kanıtı).

## Code-review — 🔴 scope kaçağı yakalandı ve kapatıldı
`POST /plans/:id/calculate-kpis` ve `/recalculate` **actor almıyordu** → iç `findById` scope'suz.
Flag açıkken kapsam dışı planı **yazdırabiliyor** (KPI persist) ve **tam içeriğini** alabiliyordu
(cpl/kanal/kategori/FU/SKU/spend/GP/ROI). 13 yolda scope vardı, 2 yol atlanmıştı — test edilmediği
için kaçmıştı. Düzeltildi + `N8-RECALC`/`N8-CALCKPIS` testleri (404 kanıtlı).
Ayrıca: `kpi.getGridKpisForPlan` existence-oracle kapatıldı (PlanService.findById üzerinden).

**Dahili çağrılar bilinçli scope'suz:** addFu/updateFuTactic/updateSkuVolume/removeFu →
çağıran zaten girişte `findById(actor)` ile kontrol ediyor; iç `findById` yalnız hesap için okuyor,
response'a sızmıyor.

## Release notu (ZORUNLU)
1. Önce backfill migration çalıştır ve doğrula.
2. **Geçmişte hiç plan açmamış PLANNER'lar backfill'den satır ALMAZ** → enforcement açılınca `[]` görür;
   Admin `user_scopes`'a manuel satır eklemeli.
3. Sonra `SCOPE_ENFORCEMENT_ENABLED=true`.
- `.env.example` yazılamadı (izin) — ops deploy env'inde tanımlamalı.
- N13 cross-tenant e2e proxy test; asıl kanıt `access-scope.service.spec.ts` birim testleri.
- Agreement CM kategori-scope'u → [[T-028e]].

## Açıklama
T-028'in 3. parçası — **en riskli**. Tasarım: `docs/analysis/0004-rbac-brd-alignment.md` §4, §6, §7.

E2E kanıtı: PLANNER kendisine atanmamış CPL ile plan açtı → **201**; `GET /plans` tenant-wide
(PLANNER == ADMIN sonuç sayısı). BRD: "Planner sadece yetkili CPL+Category."

## ⚠️ Deny-by-default geçiş riski
Enforcement açıldığı anda scope satırı olmayan her PLANNER **her şeyi kaybeder** (prod/UAT dahil).
**Sıra zorunlu:** önce seed/backfill → doğrula → sonra enforcement. Feature flag ile kademeli aç.

## Acceptance Criteria
- [ ] `PlanService` create (yetkisiz CPL → **403**) / findAll (filtre) / findById (kapsam dışı → **404**) /
      update-delete yolları scope'lu; `AgreementService` aynısı
- [ ] `plan.repository.findAll` QB'sine `applyToQueryBuilder`; controller'lara `@CurrentUser` enjeksiyonu
- [ ] **Backfill migration:** mevcut PLANNER'lara geçmiş planlarının CPL/Category kümesinden scope türet;
      planı olmayan Planner için **release notunda manuel atama uyarısı**
- [ ] **Feature flag** `SCOPE_ENFORCEMENT_ENABLED` (env) — ilk deploy `false`, backfill doğrulandıktan sonra `true`
- [ ] 404 body'sinde `code: 'OUT_OF_SCOPE'` (mesaj sızdırmadan)
- [ ] Perf: OR-grubu index kullanıyor (EXPLAIN, <500ms)
- [ ] E2E: N6 (yetkisiz CPL create **403**, bugün 201), N7, N8, N9, N13 (§9)

## İlgili
- Tasarım: `docs/analysis/0004-rbac-brd-alignment.md` · Önceki: [[T-028b]]
