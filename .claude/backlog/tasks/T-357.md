---
id: T-357
title: "BL-1 ŞEMA — baseline hacim tablosu (D3): tenant × SKU × CPL × period"
epic: E-001
sprint: S-001
status: todo
assignee: data-engineer
priority: P0
created: 2026-09-02
updated: 2026-09-02
touches:
  - collmind.backend/src/database/migrations/1822000000000-*.ts
  - collmind.backend/src/database/entities/
migration_seq: 1822000000000
---

## Hüküm `Z84` (ürün sahibi, 2026-09-02) — GRAIN KİLİTLENDİ

> **Excel'in *"Base Volume · Master Data · piece"* satırının CTPM karşılığı.**
> `Faz-2`'nin **ilk gerçek-veri tablosu**.

```
GRAIN         tenant × SKU × CPL × period      UNIQUE
              ⛔ DÖRT ANAHTAR DA NOT NULL ⇒ kısmi-tuple YOK
              ⇒ NULLS NOT DISTINCT GEREKMİYOR
period        Date DEĞİL — periyot ANAHTARI
              ⛔ mevcut periodLabel konvansiyonuyla BİREBİR
              ⛔ 'YYYY-MM' char(7) mi int mi → TÜKETİCİLERİ ÖLÇ, sonra seç
              + CHECK kısıtı
base_volume   numeric · birim PIECE
              ⛔ UOM DÖNÜŞÜMÜ BU TABLODA YAŞAMAZ (gösterim/import katmanı, Excel deseni)
META          import_batch_id (FK)
              source_type      ENUM(IMPORT, COMPUTED)
              acceptance_status ACCEPTED / REJECTED + reason
              imported_at      timestamptz  ⛔ GERÇEK ZAMAN-NOKTASI — YALNIZ BURADA
FK            skus · cpls · tenants → RESTRICT   (denetim/iz ailesi, ADR 0012 ruhu)
RLS           tenant_id taşıyor ⇒ YENİ-TABLO-RLS kapısının evrenine girer
```

## ⛔ KAPSAM DIŞI — ve gerekçesi bir SINIF
```
kanal      CPL'den TÜRER
kategori   SKU'dan TÜRER
⇒ KOLON OLARAK TUTULMAZ — türetilebilir alan tutmak INV-B-009 KOPYA-KOLON sınıfıdır
```

## ⛔ İKİ ŞART
1. **`BL-2`'nin ilk import'undan ÖNCE iner.** *(Tablo yeni doğuyor, `plans=0` penceresi
   sorunu yok — ama sıra bağlayıcı: `BL-1` → `BL-2`.)*
2. ⛔ **`acceptance_status`'ün KOLON ADI ve ENUM DEĞERLERİ `BL-3`'ün tüketeceği şekilde
   BUGÜN doğar, YARIN YENİDEN ADLANMAZ** — **AD-BORCU önlemi**.
   `D4`'ün `≥%95` kapsam kapısı **buradan okuyacak**: coverage paydası **toplam evren**,
   **reddedilen satır "EKSİK"tir** (`Z79 §4`).

## ⛔ `period` TİPİ BİR TERCİH DEĞİL, BİR ÖLÇÜM
`main`'de dönem etiketi taşıyan **on tablo** var ve **hepsi `character varying`**
(TL ölçtü: `plans.period_month` · `agreements.period_month` · `budget_envelopes.period` ·
`ledger_entries.period_month` · `agreement_transactions.fiscal_period` · …).
⇒ **Tüketicileri ölç**, konvansiyondan **sapıyorsan gerekçesini yaz** (`F8` ailesi:
aynı kavramın iki temsili).

## ⚠️ KAYIT — ayrı borç, bu task'ın işi DEĞİL
`plans.start_date` / `agreements.start_date` bugün **`date`** ⇒ `T-333` ailesinden bir borç:
`date` kolonundan **UTC-getters** ile okumak **Istanbul'da (+03) yanlış ay** üretiyor
(TL ölçtü, `Z84 §3`). **Okuyucuları ölçülecek** — bu tablo o riski **taşımıyor** çünkü
`period` bir **dizge anahtarı**, `Date` değil.

## Done ek maddeleri
- [ ] `run → revert → run` ampirik · **üç durum ayrımı** (`1805`/`1808` deseni)
- [ ] Entity metadata migration ile **birlikte**
- [ ] `new-table-rls` guard'ı yeşil (tablo o kapının evrenine giriyor)
- [ ] `period` tipi **tüketici ölçümüyle** seçildi, gerekçe yazılı
