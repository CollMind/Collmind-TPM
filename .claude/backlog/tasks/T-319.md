---
id: T-319
title: Dört kabul pini — P1 ilişki-pini · P2 şablon · P3 tekrar-bastırma · P4 sessiz-düşme + yol-canlılığı
epic:
sprint:
status: review
assignee: qa-engineer
created: 2026-08-28
updated: 2026-08-28
touches:
  - collmind.backend/test/
migration_seq:
---

> **BLOKLU:** `T-318` inmeden başlamaz.

## `P1` — İLİŞKİ-PİNİ *(ürün sahibi şartı, `Z56 §4`)*

```
YANLIŞ   zarf %90'ı geçer → bildirim düşer            ← DEĞER-pini
DOĞRU    zarf KONFİGÜRE EDİLMİŞ finance_review eşiğini GEÇER → bildirim düşer
         eşiğin BİR ALTINDA → düşmez
```

⛔ **Test eşiği KONFİGÜRASYONDAN OKUR, sabit yazmaz.**
⛔ **POZİTİF KONTROL: eşik DEĞİŞTİRİLİR, pin YİNE tutar.**

📌 **Ve pozitif kontrol `50/60` vakasını HEDEFLER** (`Z57 §1a`): ilk koşum,
`50/60` dünyasında pinin **ne ölçeceğini** de kayda geçirir.
⇒ Bu, ilişki-pininin değer-pinine üstünlüğünün **somut kanıtı**: değer-pini
`50/60`'ı ölçüp **yeşil** derdi.

## `P2` — aynı desen, her olayda
`%80` ve `%90` **aynı şekilde** pinlenir. `P1` bir vaka değil, **şablon**.

## `P3` — TEKRAR-BASTIRMA
```
%89 → %91         ⇒ BİR
%91 → %92         ⇒ SIFIR
%91 → %88 → %91   ⇒ İKİNCİ
```
> *"Olay bir **GEÇİŞTİR**, durum değil."* Gerekçe: aynı geçiş her yenilemede
> bildirim doğurursa **ilk gerçek kullanıcı bildirim-yorgunluğuyla özelliği
> kapatır** — `K-2.2.7b`'nin amacı **karar-desteği**, gürültü değil.

## ⛔ `P4b` — BİLDİRİM-YOLU-CANLILIĞI PİNİ *(kabulün PARÇASI, ek değil)*

```
YETMEZ   "createNotification çağrıldı"
GEREKLİ  GET /notifications O SATIRI DÖNDÜRÜYOR      ← UÇTAN UCA, MOCK'SUZ
```

📌 **Gerekçe bugünün ölçümünde:** tablo, servis, kanal ve UI'ın **dördü de vardı
ve zincir KOPUKTU** — her parça tek tek *"var"* diye raporlanabilirdi.
`DISIPLIN`: *"bileşimsel fail-open — her parça masum, boşluk BİLEŞİMDE."*

⚠️ **Ve `P4b` `5-halka envanterinin` BİLDİRİM-HALKASI olarak `Faz-1` kapanış
denetimine girer** (`Z57 §3a`).

## Acceptance
- [ ] `P1`–`P4` yeşil · `P1`'in pozitif kontrolü (eşik değiştir) **fiilen koşturuldu**
- [ ] `P4b` **e2e**, mock'suz
- [ ] ⛔ Her pin **mutasyonla** sınandı: mutasyon **MEKANİZMAYA** düştü mü (satır **basılır**),
      testler **gerçekten koştu** mu (derleme hatası bir kanıt değil), geri alma
      `shasum -a 256 -c` ile doğrulandı mı
- [ ] `CLAUDE.md §4.2` Done listesi tam


---

## ⛔ KAPSAM EKİ — `T-047` EVRENİ İKİNCİ KEZ KÖRLEŞTİ (Team Lead, 2026-08-28)

`Z59` dalgası `notifications`'a **ilk üretim yazıcısını** kazandırdı. Sonuç:

```
tam e2e sonrası   main.notifications   BUDGET_ALERT_80 8 · BUDGET_FINANCE_REVIEW 8
                                       = 16 satır ARTIK          (hepsi 2026-08-28)
T-047 invariant                        PASS  ✅
```

**Çünkü `countRows()` ELLE YAZILMIŞ 7 tablo sayıyor** ve `notifications` içinde yok.

⛔ **VE BU İKİNCİ KEZ** — dosyanın **kendi yorumu** ilkini kaydediyor
(`e2e-row-count.js:84-90`, `T-060`): *"`approval_requests` 9.116 satır, `plans` 0 —
invaryantın **bütün bir tabloya kör** olduğunun kanıtı."*
⇒ `DISIPLIN`: *"elle yazılmış üye listesi — ölçülmüş oran dokuzda dokuz."*

### ⛔ DOĞRU DÜZELTME *"notifications ekle"* DEĞİL

`T-060` yöntemi **dosyada yazılı ve emsal**: *"hangi BAŞKA tabloların sızdığını
TAHMİN etmek yerine, **her `main.*` tablosu** tam ve değiştirilmemiş bir
`npm run test:e2e` koşumunun hemen öncesinde ve sonrasında sayıldı."*

⇒ **O ölçümü TEKRARLA.** Bu dalga bir tablo değiştirmiş olabilir, **birden fazlasını
da** — ve tahmin, `T-060`'ın bilerek reddettiği şey.

### Bu ek için acceptance
- [ ] Tam e2e öncesi/sonrası **tüm `main.*`** tablo sayımı (türetilmiş evren, elle liste **değil**)
- [ ] Delta'sı sıfır olmayan **her** tablo raporlanır — `notifications` dahil, **ve varsa başkaları**
- [ ] `countRows()` evreni ölçüme göre genişletilir · ilgili suite'lerin temizliği eklenir
- [ ] 16 artık satır temizlenir *(dev DB, hepsi bugünkü koşumdan — `created_at` ile doğrulandı)*
- [ ] ⛔ Reprodüksiyon: genişletilmiş invaryant, **artık bırakan** bir koşumda **kırmızı** verir
