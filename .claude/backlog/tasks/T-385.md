---
id: T-385
title: "`Z107` demo hizalaması **2026-09-30'da SONA ERİYOR** — altı suite o gün kendiliğinden kırmızıya döner"
epic:
sprint:
status: done
assignee: qa-engineer
created: 2026-09-08
updated: 2026-09-08
touches:
  - collmind.backend/test/role-journey.e2e-spec.ts
  - collmind.backend/test/lta-lifecycle-bond-and-base-chain.e2e-spec.ts
  - collmind.backend/test/lta-parent-lifecycle-status-gate.e2e-spec.ts
  - collmind.backend/test/formula-canon-turnover-niv-and-rag-quadrant.e2e-spec.ts
  # ⛔ cash-flow-* İKİ DOSYA F12 İLE DÜŞTÜ — aşağıdaki EK'e bak (ölçüldü 2026-09-08)
  - collmind.backend/test/helpers/server-calendar-day.ts   # YENİ — yedi kopya buraya indi
  - collmind.backend/test/helpers/seed-e2e.ts              # YEDİNCİ kopya (isoOffsetDays) kalktı
---

## Nasıl doğdu

`Z107 §2` demo dönemini **2026 Q3** (`2026-07 … 2026-09`) yaptı — *"tenant-bugünü İÇEREN"*,
yani **canlı demo**. `code-reviewer` bunun bir **son kullanma tarihi** olduğunu ölçtü:

```
A13 / A13b   role-journey — computeBudgetUtilization'ın VARSAYILAN penceresi "BUGÜN"
             zarflar 2026-07..2026-09  ⇒  2026-10-01'de 200 değil 404 döner
             (ve bu tur o assertion'ı 404'ten 200'e ÇEKTİ — F12 ile)
isoToday() / isoPlusDays() kullanan BEŞ dosya (grep'le tarandı):
   lta-lifecycle-bond-and-base-chain · lta-parent-lifecycle-status-gate:236
   formula-canon-turnover-niv-and-rag-quadrant:304,376
   cash-flow-projection-month-overflow · cash-flow-projection-months-contract
   ⇒ hepsi BUGÜNÜN AYINDA anlaşma kuruyor ⇒ hepsi Q3 zarfına BAĞIMLI hâle geldi
```

> ### ⛔ **1 Ekim 2026'da altı suite birden kırmızıya döner — ve sebebi BU COMMIT'tir.**
> ### O gün kimse buraya bakmaz; üç hafta önceki bir dönem hizalaması aranmaz.

## Sınıfı: `T-329`/`T-333`'ün **TERSİ**

```
T-329 / T-333   veri hareketli, "bugün" sabit    → test koştuğu güne göre kayar
BU VAKA         veri SABİT, "bugün" HAREKETLİ    → test takvime göre kayar
```
İkisi de aynı kökten: **bir test, koştuğu ana bağımlı olmamalıdır.**
📌 Ve `qa` turu bunlardan **yalnız birini** (`lta-parent-lifecycle`) bildirdi; evren **beş
dosya** çıktı — `DISIPLIN`: *"bir liste vermek, EVRENİ tanımlamak değildir."*

## Acceptance
- [ ] ⛔ **Önce EVREN:** *"bugün"e bağımlı fixture* kaç **farklı şekilde** yazılabilir —
      `isoToday()` · `isoPlusDays()` · `new Date()` · varsayılan pencere (**parametre
      vermeyen** çağrı, `A13`'ün şekli). Şekilleri **adlandır**, sonra tara.
- [ ] Her dosya **sabit** tarihe geçer **ya da** kendi zarfını kurar (`ensureBudgetEnvelope`
      deseni, `Z107 §3`) — ⛔ hükmü gevşetme, fixture'ı bağımsızlaştır
- [ ] ⛔ **AYIRT EDİCİLİK:** düzeltme sonrası, sistem saatini `2026-10-01`'e alan bir koşum
      (child-process + sahte saat ya da sabit fixture) **yeşil** kalmalı — bu, kapanışın
      **tek gerçek kanıtıdır**; yoksa aynı bomba yeniden kurulmuş olur
- [ ] `Z107`'ye *"bu hizalamanın son geçerlilik ayı `2026-09`"* satırı düşmüş olmalı
      (Team Lead yazdı — doğrula)

---

## ⛔ `F12` — **EVREN LİSTESİ YANLIŞTI** (Team Lead, ölçüldü 2026-09-08)

Yukarıdaki *"`isoToday()` / `isoPlusDays()` kullanan **BEŞ** dosya (grep'le tarandı)"* satırı
**yanlıştı**. *(Eski metin **silinmedi**, üstü çizildi:)*

> ~~`cash-flow-projection-month-overflow` · `cash-flow-projection-months-contract`~~

**Ölçüm:**
```
grep -rc "isoToday(\|isoPlusDays(" test/*.ts | grep -v ":0$"
  formula-canon-turnover-niv-and-rag-quadrant   7
  lta-parent-lifecycle-status-gate              7
  lta-lifecycle-bond-and-base-chain            11
  ⇒ ÜÇ dosya, BEŞ değil.

grep -n "new Date()" test/cash-flow-projection-month-overflow.e2e-spec.ts
  11: * `startDate` GÖNDERMEDEN — yani ürün `new Date()` kullanıyordu.   ← YORUM SATIRI

grep -c "new Date()\|Date.now()\|isoToday\|isoPlusDays" test/cash-flow-projection-months-contract.e2e-spec.ts
  0
```

⇒ **İki `cash-flow` dosyası bugüne BAĞIMLI DEĞİL** — ikisi de `'2026-01-31'`, `'2026-08-31'`
gibi **sabit** tarihlerle koşuyor. Zaten `T-329`/`T-333`'ün **düzeltilmiş** hâlindeler; bu
task'ın listesine girmelerinin sebebi bir `new Date()` eşleşmesinin **bir yorumun içinde**
olmasıydı.

> ### ⛔ Ve bu `§2.7 #5`'in **ters yönü**: orada desen **hiçbir şeyle** eşleşmiyordu; burada
> ### desen **yorumla** eşleşti. İkisi de aynı sonucu verir — **liste, evreni tanımlamıyor.**

📌 **Ve yön önemlidir:** bu kez **fazla** listelendi, eksik değil. Fazla listeleme ucuz
görünür (*"zararı ne"*) ama **iki** zararı ölçüldü: (i) düzeltilmiş bir dosyayı yeniden açmak
onu **bozabilir**, (ii) *"altı suite"* sayısı ürün sahibinin **önceliklendirme kararına** girdi
— yani **yanlış bir sayı bir hükme girdi**. ⛔ `DISIPLIN`: *"bir sayı, eşleşmeleri
ÖRNEKLENMEDEN raporlanamaz."*

## ⇒ DÜZELTİLMİŞ EVREN — **ŞEKİL ÖNCE, SONRA TARAMA**

```
ŞEKİL 1  isoToday() / isoPlusDays() yardımcısı           [ÖLÇÜLDÜ: grep -rc, yukarıda]
         3 dosya · ⚠️ ve yardımcı DÖRT KEZ TANIMLI (lta-parent-lifecycle'da İKİ KEZ:
         :85 ve :416) ⇒ §2.7 #8 ailesi — kopya, orijinaldeki regresyonu görmez

ŞEKİL 2  VARSAYILAN PENCERE (parametre vermeyen çağrı)   [ÖLÇÜLDÜ: Z107 review]
         role-journey A13 / A13b — computeBudgetUtilization'ın varsayılanı "BUGÜN"

ŞEKİL 3  testte doğrudan new Date() → API'ye giden TARİH ALANI
         (postingDate · effectiveDate · transactionDate · period)
         ⛔ [ÖLÇÜLMEDİ — ölçülecek]  33 dosya `new Date()` içeriyor ama çoğu
         SÜRE ÖLÇÜMÜ / optimistic-locking damgası — ZARARSIZ.
         ⇒ ŞERİT bunları AYIRACAK: "takvim alanı mı, anlık mı" — SAYI DEĞİL LİSTE

ŞEKİL 4  SEED ZARFINA bağımlılık (kendi zarfını KURMAYAN test)
         ⛔ [ÖLÇÜLMEDİ — ölçülecek]  ensureBudgetEnvelope'u ÇAĞIRMAYAN ama bütçe
         yoluna giren suite'ler. Z107 bunu ON suite'te düzeltti; DÖRT'ünü mü
         bıraktı, yoksa hepsi mi kapandı — ÖLÇÜLECEK
```

⛔ **Ve kapanışın TEK gerçek kanıtı değişmedi:** sistem saati `2026-10-01`'e alınmış bir
koşum **yeşil** kalmalı. Yoksa aynı bomba yeniden kurulmuş olur.

---

## ⛔ `F12` #2 — EVREN **ÜÇÜNCÜ KEZ** DARALDI: **ALTI → ÜÇ → BİR SUITE** (2026-09-08)

`qa` şeridi **reprodüksiyonla** ölçtü, Team Lead **bağımsız doğruladı**:

```
faketime -f "2026-10-01 12:00:00" node node_modules/.bin/jest --config ./test/jest-e2e.json <dosya>

role-journey  -t "budget-utilization"   A13 + A13b   Expected 200 / Received 404   ⛔ KIRMIZI
formula-canon-turnover-niv-and-rag-quadrant          7/7    ✅ YEŞİL
lta-parent-lifecycle-status-gate                     5/5    ✅ YEŞİL
lta-lifecycle-bond-and-base-chain                    4/4    ✅ YEŞİL
```

⇒ **Gerçek evren: BİR suite, İKİ test.** `isoToday()` kullanmak **tek başına** zamanlı-kırmızı
üretmiyor — o üç dosya bütçe-zarfı eşleşmesinden **hiç geçmiyor**; oradaki `isoToday()`
Q3 demo penceresiyle **ilgisiz**, `lta-agreement.service.ts:151`'in *"`effectiveDate` geçmişte
olamaz"* **ürün kuralını** karşılamak için var.

> ### ⛔ Üç tur, üç sayı: **altı → üç → bir.** Ve üçünü de **aynı şey** düzeltti:
> ### bir **grep** değil, bir **REPRODÜKSİYON**.
> `DISIPLIN`: *"reprodüksiyon şartı iddiayı da, düzeltmeyi de AYNI KAPIDAN geçirir"* —
> ve *"kusur var"* demek, *"kusur yok"* demek kadar bir iddiadır.

### ⛔ VE TEAM LEAD'İN ÖLÇÜMÜ ÖNCE **YANLIŞTI** — `§2.7 #4`

Team Lead bilinen-kırmızıyı bağımsız üretmek isterken **`npx jest`** kullandı ve
*"kırmızı üremedi, `200` dönüyor"* sonucuna vardı. **Ölçüm geçersizdi:**

```
faketime … node -e 'new Date()'      → 2026-10-01T09:00:00Z   ✅ sahte saat GEÇERLİ
faketime … npx node -e 'new Date()'  → 2026-09-08T08:13:35Z   ⛔ sahte saat DÜŞTÜ
```
`npx` yeni bir node çözer ve `libfaketime`'ın `DYLD` enjeksiyonunu **kaybeder** — **sessizce**.
⇒ **Kanıt kurulumu, ölçtüğü durumu YOK ETTİ** (`§2.7 #4`), ve sonuç *"kusur yok"* yönünde
yanıldı. ⚠️ `DISIPLIN`: *"beklenen yöne yanılan bir hata, ters yöne yanılandan tehlikelidir"* —
burada beklenen yön **rahatlatıcı** olandı.
📌 Ve şerit bunu **zaten ölçmüştü**; Team Lead şeridin raporunu **okumadan** önce ölçtü.

### ⛔ VE ARACIN SINIRI ÖLÇÜLDÜ — DB SAATİ SAHTELENMİYOR
`libfaketime` yalnız **Node** sürecini sahteler; Postgres **ayrı container'da, gerçek saatte**.
Bu turda sorun çıkarmadı (hiçbir assertion `NOW()`'a dayanmıyor) ama `created_at` /
optimistic-lock damgasına dayanan bir test bu yöntemle **yanlış** sonuç verir. ⛔ Bu bir
**araç sınırıdır** ve kullanan her turda yazılır.

## ⛔ AÇIK — ŞERİDİN `DUR` MADDESİ: ŞEKİL 1'in ÜÇ DOSYASI

Şerit üç dosyayı **sabit tarihe çekmeyi denedi** ve **14 test kırıldı**:
`lta-agreement.service.ts:151` → `if (effectiveDate < tenantTodayIsoDate(timezone)) throw 400`.
Sabit bir **geçmiş** tarih, bugünden sonraki **her** koşumda `400` üretir.

> ### ⇒ *"Sabit tarih"* çözümü, hüküm 17'nin kapattığı bombadan **DAHA KALICI** bir kırmızı
> ### üretirdi. Şerit değişikliği geri aldı (`git show HEAD:` + `cp`, `git diff --stat` boş,
> ### 16/16 yeşil) ve **karara bıraktı**.

```
(A) DOKUNMA   reprodüksiyon bu üç dosyayı KIRMIZI göstermedi; isoToday() orada MEŞRU
              (ürün kuralını karşılıyor). Kalan: §2.7 #8 — yardımcı DÖRT KEZ tanımlı.
(B) GERÇEK CLOCK-INJECTION   ClockService / tenantTodayIsoDate'e test-only `now`
              ⇒ ÜRÜN KODU değişir. Şerit "orantısız iş" ölçtü.
```
⛔ Bu, hüküm 17'nin (*"testte göreli tarih YASAK"*) **lafzıyla gerilimdedir** — bir
**hüküm** gerektirir, bir mühendislik tercihi değil. **Ürün sahibine gider.**

## ✅ KAPANAN
```
[ÖLÇÜLDÜ: /private/tmp/TL_known_red_v2.log]    düzeltme ÖNCESİ 2026-10-01 → 404 KIRMIZI
[ÖLÇÜLDÜ: /private/tmp/TL_known_green_v2.log]  düzeltme SONRASI 2026-10-01 → 2 passed
[ÖLÇÜLDÜ: /private/tmp/e2e_full_oct1.log]      TAM e2e 2026-10-01 → 64/64 · 887/887 · T-047 PASS
[ÖLÇÜLDÜ: /private/tmp/e2e_full_baseline.log]  TAM e2e bugün      → 64/64 · 887/887
⇒ Z83 doğum kuralı TAM: bilinen-kırmızı VE bilinen-yeşil, aynı komut, aynı saat.
```

⛔ **VE `F12` GEÇMİŞİ GERİ KONDU** (Team Lead, 2026-09-08): şerit A13'ün yorumundan
`T-270`/`Z21`'in **kök-neden kaydını** (sessiz `0`/GREEN raporu, `§2.5`) ve üstü çizili
`toBe(404)` satırlarını **sildi**. Append-only ihlali — geri kondu, ve şeridin yazdığı
*"reprodüksiyon: `T-385` **raporu**"* atfı (o an **var olmayan** bir kaynak) yerine
**çalıştırılabilir komut** yazıldı. ⇒ `Z108 §3`'ün (hüküm 20) **doğduğu turdaki ilk ihlali**.

---

## ✅ KAPANIŞ — HÜKÜM 22 (`Z108 §6`, ürün sahibi 2026-09-08)

```
ŞEKİL 1'in üç dosyası   (A) DOKUNMA — meşru, ve meşruiyet ÖLÇÜLDÜ
hüküm 17                F12: kapsam cümlesi + bu üç dosyanın ölçülmüş istisnası → DISIPLIN
(B) clock-injection     İlke 1 gereği AÇILMADI — OLAY-TETİKLİ adaya çevrildi (T-389)
§2.7 #8                 yardımcı ALTI kopya → tek test/helpers/server-calendar-day.ts
seed anahtarı           T-385'in DEĞİL — T-388 (ayrı, zamanlı, 30 Eylül'den önce)
```

**Yardımcı birleştirmesi — ölçülmüş kopya sayısı:**
```
formula-canon-turnover-niv-and-rag-quadrant   isoToday · isoPlusDays
lta-lifecycle-bond-and-base-chain             isoToday · isoPlusDays
lta-parent-lifecycle-status-gate              iso · isoToday · isoPlusDays
                                              + AYNI DOSYADA İKİNCİ KEZ (iç describe, :416)
⇒ ALTI tanım. Biri düzeltilseydi diğer beşi SESSİZCE eski hâlinde kalırdı.
```
[ÖLÇÜLDÜ] birleştirme sonrası üç suite `16/16` yeşil (öncesi 7+5+4 = **16** — birebir).

⛔ **Ve yeni yardımcı, meşruiyetini KENDİ İÇİNDE taşıyor:** dosya başlığı hüküm 17'nin
kapsamını, `faketime` ölçümünü, *"sabit tarih 14 testi kırardı"* karşı-ölçümünü ve
**sunucu-yerel ≠ tenant-TZ** sınırını yazıyor. ⇒ Bir sonraki okuyucu *"bu neden `new Date()`
kullanıyor?"* diye sorduğunda cevap **aynı dosyada**.

---

## ⛔ `🟡-3` KAPANIŞI — ŞEKİL 3 ve 4 **AYRI AYRI TARANMADI**, ve bu bilinçli

`code-reviewer` haklı olarak sordu: `status: done` ama iki şekil hâlâ
`[ÖLÇÜLMEDİ — ölçülecek]` etiketli. **Gerekçe yazılmamıştı; yazılıyor:**

```
[ÖLÇÜLDÜ: docs/verification/T385_ZAMANLI_KIRMIZI_2026-09-08.txt §1]
  faketime 2026-10-01 altında TAM e2e  →  64 suite / 887 test YEŞİL · T-047 PASS
```

⇒ Bu koşum, dört şeklin **DAVRANIŞSAL ÜST KÜMESİDİR**: hangi şekle ait olursa olsun, bir
suite `2026-10-01`'de kırmızıya dönecek olsaydı **bu koşumda görülürdü**. `qa` şeridi ayrıca
ŞEKİL 3'ün 33 eşleşmesini ve ŞEKİL 4'ün 13 adayını **tek tek** okudu; ikisi de **sıfır ek
dosya** getirdi.

⚠️ **Ama üst-küme argümanının SINIRI var ve yazılır:** o koşum `TZ=Europe/Istanbul` ve
`12:00:00` altında yapıldı. **Gün sınırına ve TZ skew'ine kör** (bkz. `server-calendar-day.ts`
`SINIR` bloğu). ⇒ *"dört şekil tarandı"* değil, ***"dört şeklin bu koşul altındaki davranışı
ölçüldü"***. `DISIPLIN`: *"bir ÖLÇÜMÜN geçerliliği de koşullarına bağlıdır."*

## ✅ Acceptance
- [x] Önce EVREN — dört şekil **adlandırıldı**; 1 ve 2 dosya dosya, 3 ve 4 davranışsal üst küme
- [x] Her dosya sabit tarihe geçti **ya da** kendi zarfını kurdu — `A13`/`A13b` (`2029-04`)
- [x] AYIRT EDİCİLİK — `faketime 2026-10-01` düzeltme öncesi **404**, sonrası **200**;
      ayrıca **mutasyon kanıtı**: fixture silindi → **404**
- [x] `Z107`'ye *"son geçerlilik ayı"* satırı → `F12` ileri-atfı olarak kondu (`Z108 §6`)
