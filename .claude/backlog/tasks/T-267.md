---
id: T-267
title: "`ADIM 3` `B2` — `59` filtresiz uç `@Roles`'a bağlanır (`B1`'in sınıflandırmasıyla)"
epic: E-001
sprint: S-001
status: review
assignee: backend-engineer
created: 2026-08-21
updated: 2026-08-21
touches:
  - collmind.backend/src/modules/
  - collmind.backend/scripts/guards/route-scope-baseline.txt
---

## Girdi — `B1` taslağı, YENİDEN TÜRETİLMEZ

`docs/process/ADIM3_B1_TASLAK.md` `59` ucun tamamını sınıflandırdı ve **her biri için
gerekçe** yazdı.

> ⛔ **`Z18`: union asla gerekçe değildir.** Her uç için gerekçe **`B1`'den gelir** —
> yeniden türetilmez, ve *"union böyle diyor"* **kabul edilmez**.

## ⛔ DÖRT ŞART (ürün sahibi)

```
girdi        B1 taslağı — 59 ucun sınıflandırması VE gerekçeleri
iki sütun    @Roles durumu · KAPSAM durumu, AYRI
yeşil        B2'nin yeşili YALNIZ @Roles sütununu kapatır (Z19b)
baseline     her uç bağlandıkça 59 → düşer, AYRI COMMIT
```

### ⚠️ *"`B2`'nin yeşili yalnız `@Roles` sütununu kapatır"* — ne demek

```
route-scope-baseline.txt   59 → 0     B2 KAPATIR
scope-a1-baseline.txt      67          B2 DOKUNMAZ — ayrı kapanış
```

**İki ratchet farklı evrenlerde** (`@Roles` `59` uç · kapsam `235` rotanın tamamı), ve
bu `Z19b`'nin **ölçülmüş** gerekçesi: `T-253`'ün `dashboard-summary`'si `@Roles`'lu
**beş rol** taşıyordu **ve kapsamsızdı**.

## `14` tüketicisiz uç — rol ATANIR

`S2`/`S3`'ün tüketicisiz uçlarına da `@Roles` eklenir.

> `T-257`'nin dersi: *"silinecekse bile **silinene kadar** açık kalamaz."*
> Ve `T-249`'un emsali: `spend-calculation`'ın kaderi `T-063`'e bağlıydı, **`@Roles`
> yine eklendi.**

⚠️ Kader kararı [[T-265]]'te ve **paralel yürür** — `B2`'yi bekletmez.

## ⚠️ `2` ALAN-GUARD'LI UÇ — AYRI KOVA, dokunma

```
reversal.controller.ts:28    @UseGuards(JwtAuthGuard, ReversalGuard)   ← CONTROLLER seviyesi
settlement.controller.ts:77  @UseGuards(SettlementGuard)               ← rota seviyesi
```

İkisi de `{ADMIN, CATEGORY_MANAGER}`'ı **guard'ın içinde** zorluyor. **`@Roles` EKLEME**
— `route-scope` onları zaten ayrı kovada tutuyor.

📌 **Bu ayrım `B4`'ün yanlış teşhisini şimdiden engelliyor:** default-deny onları
keserse kırılma `403` olur ve *"default-deny çalışıyor"* diye okunur.

## Acceptance Criteria

- [ ] `59` ucun tamamı `@Roles` taşıyor — **her biri `B1`'in gerekçesiyle**
- [ ] ⚠️ Bir uç için `B1`'in gerekçesi **yetersizse** → **DUR**, uydurma
- [ ] `route-scope-baseline.txt` `59 → 0`, ve **AYRI COMMIT** (düzeltmeden **sonra**)
- [ ] ⛔ `scope-a1-baseline.txt`'e **DOKUNULMAZ**
- [ ] Davranışsal pin: en az bir uç için **kapsam dışı rol → `403`**, ⚠️ **pozitif
      kontrol:** kapsam içi rol → `200`
- [ ] ⚠️ **Ne BOZULABİLİR:** `@Roles` eklemek bugün çalışan bir e2e'yi kırabilir —
      kırılan **her** test için: rol seçimi mi yanlış, yoksa test **yanlış rolle** mi
      giriyordu? (İkincisi bir **bulgu**, sessizce test değiştirme)
- [ ] Tam e2e yeşil

## DUR

- `B1`'in gerekçesi bir uç için yetersizse → **DUR**
- Kırılan test sayısı **beklenenden çok** (`5+`) → **DUR ve raporla**
- `scope-a1-baseline.txt` değişirse → **DUR** (bu `B2`'nin işi değil)
- `docs/` altına **yazma**

## İlgili
- `docs/process/ADIM3_B1_TASLAK.md` — girdi · `Z18` · `Z19b`
- [[T-252]] `@Roles` ratchet'i · [[T-266]] kapsam ratchet'i · [[T-265]] kader
