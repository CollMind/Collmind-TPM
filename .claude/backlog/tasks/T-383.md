---
id: T-383
title: "`\"bugün\"` ÜÇ FARKLI TABANDA yaşıyor — sunucu-yerel · UTC · DB OTURUMU; üç ayrı ŞEKİL, yedi yer"
epic:
sprint:
status: todo
assignee: backend-engineer
created: 2026-09-07
updated: 2026-09-07
touches:
  - collmind.backend/src/modules/modes/planning-first/plan/plan.repository.ts
  - collmind.backend/src/modules/modes/actuals-first/agreement/agreement.repository.ts
  - collmind.backend/src/modules/shared/dashboard/dashboard.service.ts
  - collmind.backend/src/modules/shared/lta/lta-agreement.controller.ts
---

## Nasıl doğdu

`T-375` ADIM 3 hüküm 11'i uyguladı: *"bugün" finansal yollarda **tenant saat dilimi**.*
`local-today.ts` yeniden yazıldı, `localTodayIsoDate` (sunucu-yereli) **öldü**, ve brief'in
adıyla verdiği **üç** tüketici taşındı (ajan dördüncüsünü — `lta#updateAgreement` — kendi ölçüp buldu).

**Team Lead bağımsız taraması başka bir ŞEKİL buldu** — yardımcıdan geçmeyen, `new Date()`'i
**doğrudan** takvim alanı olarak okuyan üretim yolları:

```
plan.repository.ts:401-402    generatePlanCode       new Date().getFullYear() / .getMonth()
                              → `PLAN-<yıl>-Q<çeyrek>-…`     ⛔ KALICI KİMLİK
agreement.repository.ts:275   generateAgreementCode  new Date().getFullYear()
                              → `<tip>-<yıl>-…`              ⛔ KALICI KİMLİK
dashboard.service.ts:411      period = 'ALL'         new Date().getFullYear()
                              → `${year}-01-01` .. `${year}-12-31`   ⛔ FİNANSAL SORGU PENCERESİ
lta-agreement.controller.ts:187  `date ? new Date(date) : new Date()`   ← ÖLÇ: anlık mı, takvim günü mü
```

## Neden önemli — üç ayrı şiddet

```
kimlik    yıl sınırında yanlış yıl/çeyrek taşıyan bir KOD üretilir ve KALICI olarak yazılır
          (İstanbul 01 Oca 01:00 = UTC 31 Ara 22:00 ⇒ sunucu "2025", tenant "2026")
          ⛔ Ve bir kimlik GERİ ALINAMAZ — düzeltme bir yeniden adlandırma turu ister
pencere   dashboard `period='ALL'` sorgusu bir YIL kayar ⇒ finansal ekranda yanlış kapsam
anlık     lta controller'ınki muhtemelen meşru (anlık zaman noktası) — ÖLÇ, varsayma
```

## ⛔ Ve sınıfı: `§7.1`'in bu oturumdaki **DÖRDÜNCÜ** vakası

```
1  T-373 on-invoice          kardeş yollar sayılmadı → review yakaladı
2  agreements 5/5 NULL       girdi ölçüldü → kod yazılmadan görüldü
3  PATCH /agreements/:id     create() kapandı, update() ölçülmedi → review yakaladı
4  "bugün"                   üç tüketici taşındı, DÖRDÜNCÜ ŞEKİL taranmadı  ← BU
```

⚠️ Ve dördüncüsü **Team Lead'in brief'inin eksiğidir**: brief üç tüketiciyi **adıyla** verdi,
ama *"bu soruyu BAŞKA hangi ŞEKİLDE soruyoruz"* diye sormadı. Bir liste vermek, **evreni
tanımlamak değildir** — `DISIPLIN`: *"kapsam maskelemesi — desen çalışır, EVREN eksiktir."*

---

## ⛔ EK — `code-reviewer` BAŞKA BİR ŞEKİL BULDU (2026-09-07, ADIM 3 review'ı)

Team Lead'in taraması `new Date().getFullYear()` **şeklini** gördü. Review **ikinci ve üçüncü
şekli** buldu — ve üçüncüsü **bu turun DEĞİŞTİRDİĞİ dosyanın İÇİNDE**:

```
ŞEKİL 2 · new Date() + setHours + KARŞILAŞTIRMA
  off-invoice-validation.service.ts:203-204   const today = new Date(); today.setHours(23,59,59,999)
  on-invoice-validation.service.ts:241-242    ⛔ KELİMESİ KELİMESİNE KOPYA
  → "Fatura tarihi gelecekte olamaz": SUNUCU-yereli `today` ↔ UTC-parse edilmiş `invoiceDate`
  ⛔ AYNI if'TE İKİ TABAN — bu turun düzelttiği desenin TA KENDİSİ

ŞEKİL 3 · new Date() → DB KARŞILAŞTIRMASI (ÜÇÜNCÜ BİR TABAN)
  lta-agreement.service.ts:447 · :457 · :547   date: Date = new Date() → findActiveForCPL
  lta-agreement.repository.ts:112-114          lta.effectiveDate <= :date   (date kolonu ↔ timestamptz)
  ⇒ takvim sınırı DB OTURUM TZ'sine düşer — ne tenant, ne sunucu: ÜÇÜNCÜ taban
  review ölçtü:  TZ=Europe/Istanbul → 2026-09-08T01:00+03:00
                 TZ=America/LA      → 2026-09-07T15:00-07:00
  ⛔ VE BU, ADIM 3'ÜN DEĞİŞTİRDİĞİ DOSYANIN İÇİNDE — tüketicisi: getLTAForPlanContext
     → spend-calculation.service.ts:217/487/546 ⇒ LTA ORANI SEÇİMİ
```

> ### ⇒ `"bugün"` bu kod tabanında **ÜÇ FARKLI TABANDA** yaşıyor: **sunucu-yerel · UTC ·
> ### DB oturumu.** `ADIM 3` birincisini üç (dört) tüketicide kapattı; ikinci ve üçüncü şekil
> ### **taranmadı**, çünkü tarama **liste** üzerinden yapıldı, **evren** üzerinden değil.

📌 Ve blocker'ın kendisi bir **cümleydi**: `local-today.spec.ts:14` *"sunucu yereli artık
hiçbir üretim yolunda kullanılmıyor"* — normatif, ve **yanlış**. `T-084` sınıfı: bir hatayı
belgelemek onu **koruma altına alır**; bu cümle sonraki okuyucuya sınıfı **kapalı** gösterirdi.

## Acceptance
- [ ] ⛔ **Önce EVREN**: *"şu an"dan takvim alanı okuyan* **tüm** üretim yolları taransın
      (`new Date()` + `getFullYear`/`getMonth`/`getDate`), ve her biri **meşru / kaçak** diye
      sınıflansın — ⛔ **SAYI değil LİSTE**, ve *"meşru"* diyorsan **neden** meşru olduğunu yaz
- [ ] Kaçak olanlar `tenantTodayIsoDate` / tenant-TZ tabanına taşınsın
- [ ] ⛔ **Kimlik üreten iki yol için ayrıca**: bugüne kadar üretilmiş kodlarda yanlış yıl
      taşıyan var mı — **ÖLÇ**. Varsa düzeltme bir **karar** ister (kimlik geri alınamaz)
- [ ] `T-333` harness'i (üç `TZ`, child-process) ile pin — ⛔ **emsali YENİDEN KULLAN**, kopya yazma
- [ ] ⛔ **ÜÇ ŞEKLİN ÜÇÜ DE** taransın — `new Date()` + takvim alanı · `new Date()` + `setHours`
      + karşılaştırma · `new Date()` → DB sorgusu. Şekilleri **önce adlandır**, sonra tara.
- [ ] ⛔ `off-invoice`/`on-invoice` validation'daki **kelimesi kelimesine kopya** guard tek
      mekanizmaya insin (`§2.7 #8`: kopya, orijinaldeki regresyonu görmez)
- [ ] Bir kapı adayı değerlendirilsin: *"`new Date()` + takvim alanı"* mekanik olarak taranabilir
      bir desendir. ⛔ `Z83` doğum kuralı: **bilinen-yeşil VE bilinen-kırmızı** üretilmeden doğmaz.

---

## ✅ `İŞ 1` KISMEN İNDİ — `ŞEKİL 2` KAPANDI (`DALGA-A` ŞERİT 2, 2026-09-08)

```
ŞEKİL 1  new Date() + takvim alanı      ⏸ AÇIK — üç yer YENİDEN DOĞRULANDI, aynı
ŞEKİL 2  new Date() + setHours + karş.  ✅ KAPANDI — kopya guard TEK MEKANİZMAYA indi
ŞEKİL 3  new Date() → DB karşılaştırma  ⏸ AÇIK — ve EVREN BÜYÜDÜ (aşağıya bak)
```

### `ŞEKİL 2` — kopya öldü, ve reprodüksiyon GERÇEK bir kusur gösterdi
```
[YENİ] common/validation/invoice-date-not-future.ts — isInvoiceDateInFuture(value, timezone)
       common/date/local-today.ts'in MEVCUT export'larını kullanır, yeni tarih aritmetiği YOK
[YENİ] invoice-date-not-future.spec.ts — T-333 harness'i YENİDEN KULLANILDI (üçüncü kopya YOK)
[REPRODÜKSİYON] TZ=UTC sunucu ↔ Europe/Istanbul tenant:
       tenant BUGÜNÜ olan bir fatura ESKİ guard'la YANLIŞLIKLA "gelecek" diye REDDEDİLİYORDU
```
⛔ Yani bu bir *"kopya temizliği"* değildi — **canlı bir kusurun** iki kopyasıydı.

### ⛔ VE `§1`'İN LİSTESİ KAYMIŞTI — *"liste tarihlidir"* doğrulandı
```
[ÖLÇÜLDÜ: 2026-09-08]  on-invoice-validation  :241-242 → :242-243  (DALGA-B dokundu, içerik AYNI)
                        diğer altı yer: satır numaraları AYNI
```

### ⛔ `ŞEKİL 3`'ÜN EVRENİ BÜYÜDÜ — ÜÇ YENİ YER, **SINIFLANMADI**
```
[ÖLÇÜLDÜ: ŞERİT 2 taraması]  finance-reporting.service.ts
   :238-239 · :513-514 · :1130-1131   `filters.endDate ? new Date(...) : new Date()`
   ⇒ server-instant, DB karşılaştırmasına giriyor
[ÖLÇÜLMEDİ — ölçülecek]  bunlar KAÇAK mı yoksa hesap-döngüsü bağlamında MEŞRU mu
```
⛔ Brief'in somut eylem maddesi yalnız `off-invoice`/`on-invoice`'u adlandırıyordu ⇒ bu üç
yer **bilerek** kapsam dışı bırakıldı, **sessizce değil**.
📌 Ve `lta-agreement.service.ts` → `lta-agreement.repository.ts` (**üçüncü taban**,
DB oturum TZ'si) hâlâ **açık** — bu task'ın asıl kalan işi.
