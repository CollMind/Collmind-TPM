---
id: T-225
title: "`BudgetReservation` ölü mü, unutulmuş bir bağlantı mı? — üç listenin de kaçırdığı entity"
epic: E-001
sprint: S-001
status: todo
assignee: architect
priority: P1
created: 2026-08-14
updated: 2026-08-14
touches:
  - collmind.backend/src/database/entities/budget-reservation.entity.ts
  - collmind.backend/src/config/typeorm.config.spec.ts
migration_seq:
---

## Bulgu — pin testinin İLK KOŞUMU buldu

```
budget-reservation.entity.ts:14   @Entity({ name: 'budget_reservations', schema: 'main' })
üç listenin ÜÇÜNDE de yok         typeorm.config 0 · database.module 0 · index.ts 0
tablo DB'de                       VAR · 0 satır
entity'yi import eden             0
para alanı                        1 · reserved_amount decimal(15,2), transformer YOK
                                  (ölçüldü 2026-08-14 — entity ve katalog aynı fikirde)
migration                         1704067560000-CreateBudgetReservations, hiç DROP edilmemiş
                                  (1802 bile FK'larına dokunmuş)
```

⚠️ **Ve bu, `T-224`'ün pin testinin varlık gerekçesinin kendisi.** `T-219`'daki *"fark 0"*
ölçümü onu **görmemişti** — çünkü listeleri **birbirine** karşı ölçüyordu, **diske** karşı
değil.

> **Üç kopyanın birbiriyle tutarlı olması, doğru olmaları anlamına gelmiyordu.**

📌 `§2.7`'nin *"test ile mock'u birbirine uyuyor, ikisi de üretimden sapmış"* vakasının
**liste tarafındaki** hâli.

## Soru — ve nasıl ölçülecek

> **`BudgetReservation` ölü mü, yoksa unutulmuş bir bağlantı mı?**

```
git blame + commit bağlamı        entity ne zaman doğdu, hangi niyetle
BudgetReservationService bugün    neyi kullanıyor → ledger (BudgetTransaction:
                                  RESERVE/COMMIT/RELEASE)
tablo ne zaman doğdu              1704067560000 — projenin en erken commit'leri
entity'yi import eden: 0          ama TTM'de karşılığı VAR MI?
```

⚠️ **Son satır `T-206`'nın deseni:** *"pilot profili mi, tasarım kararı mı?"* — TTM'de bir
karşılığı varsa bu bir **taslak** değil, **port edilmemiş bir yetenek** olabilir.

## ⛔ DUR

**Silme kararı çıkarsa, tablo da migration'la gitmeli — ve o `Alan A` (para), yani ayrı
bir dalga kalemi.** Bu task **karar verir**, silmez.

⚠️ Ve `(1)` seçeneği (*"`ALL_ENTITIES`'e ekle, pin yeşile dönsün"*) **reddedildi**: bir
kusuru **"düzeltme" kılığında** kapatır (`§2.7` — *bir sapmayı uygunluk diye etiketlemek*),
ve `T-224`'ün felsefesine aykırı (*"bugün doğru olanı pinle, bozuk olanı düzeltme"*).

## ✅ KARAR — ÖLÜ İSKELE, silinecek (2026-08-15)

`architect` ölçtü, Team Lead **üçünü bağımsız doğruladı**:

```
git log -S "BudgetReservation," (üç liste)  → 0     HİÇ eklenmemiş
POZİTİF KONTROL "FiscalPeriod,"              → 3    (desen çalışıyor)
entity SINIFINI import eden                  → 0
   ⚠️ ham eşleşme 14'tü — 13'ü `BudgetReservationService`, migration adı ya da
   pin spec'i. Örneklenmeseydi karar ÇÜRÜTÜLECEKTİ (`§7.1`).
INSERT INTO main.budget_reservations         → 0
POZİTİF KONTROL budget_transactions          → 1
pin unskip                                   → EXIT 1, onlyOnDisk:[BudgetReservation]
```

**Yetenek canlı, sadece başka şekilde:** rezervasyon `budget_transactions` üzerinde
append-only yaşıyor; `BudgetReservationService` entity'yi **import etmiyor**. TTM de aynı
modeli kullanıyor (35 tablo sayıldı, `reserv` adlı tablo yok). `BRD v2` `§1.4`
*"rezervasyon ✅"* diyor.

⛔ **Ve canlandırmak bir port değil, bir KURAL İHLALİ olurdu:** `L2 K-2.5.7` *"bekleyen bir
planın bütçe kaydı yoktur"* diyor; entity'nin `PENDING` başlangıç durumu bunu doğrudan ihlal
eder.

## ⚠️ AYRI BULGU — frontend'de kısmen CANLI bir yüzey

`architect` *"7 sembolün tüketicisi 0"* dedi; Team Lead ölçtü ve **enumerasyon eksik çıktı**:

| sembol | tüketici | backend rotası | karar |
|---|---|---|---|
| `useReserveBudget` | **VAR** — `ReserveBudgetDialog.tsx` | `@Post('reserve')` ✅ | **KALIR** |
| `useBudgetReservedAmount` | **2** | `@Get('envelopes/:id/reserved')` ✅ | **KALIR** |
| `useApproveReservation` | 0 | ⛔ yok → 404 | silinir |
| `useRejectReservation` | 0 | ⛔ yok → 404 | silinir |
| `useBudgetReservations` | 0 | ⛔ yok → 404 | silinir |

⚠️ **`ReserveBudgetDialog` ölü değil** — `routes/index.tsx:287` → `BudgetPage.tsx:150`,
**barrel** üzerinden. Team Lead ilk aramasında kaçırdı (`from.*ReserveBudgetDialog'`); import
`budget/index.ts`'ten geliyordu. 📌 `find-importers.sh`'ın (`T-212 Kalem 4`) doğduğu turda
tam olarak bu sınıfın bir vakası.

**Tip yalanı, canlı rotada:** backend `Promise<BudgetTransaction>` dönüyor, client
`BudgetReservation` diye tipliyor. **Bugün ısırmıyor** — ölçüldü: `useReserveBudget`'ın
`onSuccess`'i yanıt gövdesini **hiç okumuyor**. İlk okuyan alan geldiğinde ısırır.

## Silme dalgası — üç kalem

```
1805000000000   data-engineer   DROP TABLE + DROP TYPE, ÜÇ durum ayrımı, down() birebir
backend         backend-engineer entity · cleanup-data.ts:63 · budget.service.ts:1605
                                 yorumu · pin `it.skip` → `it`   ⚠️ AYNI commit
frontend        frontend-engineer 3 ölü hook + tip düzeltmesi
```

⚠️ **`cleanup-data.ts:63` (`DELETE FROM main.budget_reservations`) AYNI dalgada temizlenmeli**
— yoksa tablo düşünce `42P01`.

⚠️ **Pin'in `skip`'i silme adımıyla AYNI commit'e ait.** Tek başına kaldırılırsa kapı hep
kırmızı olur ve hiçbir şey ayırt etmez (`§2.7 #9`).

## Done tanımı
- [ ] Karar **ölçümle** verildi: ölü mü, unutulmuş mu — ve **hangi kanıtla**
- [ ] Silinecekse: migration bir **dalga kalemine** bağlandı (`Alan A`)
- [ ] **`typeorm.config.spec.ts`'teki `it.skip` KALDIRILDI ve pin YEŞİL**
      ⚠️ Bu bir `Done` şartı — askı, kusurun kabulü değildi
- [ ] `code-reviewer` onayı
