---
id: T-398
title: "declared-migrations: liste parse'ı İKİ KOPYA (guard ↔ push-order) · json_field kaçışlı boşluk · alt dizin taranmıyor · notlar"
epic:
sprint:
status: todo
assignee: backend-engineer
priority: P2
created: 2026-09-13
updated: 2026-09-13
touches:
  - scripts/guards/declared-migrations.sh
  - scripts/push-order.sh
  - scripts/run-all.sh
---

## Nasıl doğdu
Ratchet review'u (2026-09-13) · hüküm `Z111 §33` (dar düzeltme dışında kalanlar).

## Maddeler
1. **S2 — iki desen (DISIPLIN F12):** liste parse'ı guard'da ve `push-order.sh`'ta ayrı yazılmış, farklı davranıyor (işaretçi sayısı, biçim dışı
   satır, değer listesi push-order'da elle `case`). ⇒ guard'a `--list-lines` modu; push-order onu çağırır ve rc'sini okur.
2. **S4 — iki ayrıştırıcı:** `json_field` yalnız `\"` ve `\\` açıyor; sebep `"\t\n "` guard'da DOLU, harness'ta (jq) BOŞ → K2 zıt sonuç
   [reviewer ÖLÇTÜ]. ⇒ guard da `jq` ile okur — tek ayrıştırıcı.
3. **S6 — evren:** guard `find -maxdepth 1`, TypeORM `src/database/migrations/**/*.ts` [reviewer ÖLÇTÜ: typeorm.config.ts:63]. Bugün alt dizin 0.
   ⇒ `**` tarama ya da alt dizinde dosya varsa ÖLÇEMEDİM.
4. **N2 — ön filtre:** anahtar kelime ön filtresi `export * from` ve yalnız `_REASON` export eden dosyayı ts-node'a göndermiyor.
5. **N3 — run-all:** exit 2'yi 1'e katlıyor (bu turdan önce de) — ÖLÇEMEDİM ile KIRMIZI zincir sonucunda ayırt edilemiyor.
6. **N5 — self-test temizliği trap'siz** (kesilirse mktemp artığı).

## ⭐ İkinci review'den eklenenler (ratchet brief §10.2, 2026-09-13)
7. **Baseline biçim delikleri:** `EFFECT 3` (EXPORT=DEĞER dışı) → yanıltıcı "bayat baseline" rc=1 · `FOO=BAR 0` (tanınmayan tür) → rc=0 ·
   aynı tür iki satır eşit sayıyla → rc=0 · boş baseline "0 beyanlı" ile ayırt edilemiyor, `# total:` karşılaştırılmıyor [reviewer ÖLÇTÜ]
8. **Fixture izolasyonu:** self-test senaryolarının çoğu `DECLARED_MIGRATIONS_BASELINE` vermiyor, GERÇEK baseline'ı okuyor — gerçek baseline
   bozulursa fixture'lar yanlış sebeple düşer; self-test kırmızısı "guard bozuk" ile "repo kırmızı"yı ayırt etmiyor
9. **S1 self-test senaryosu yok:** `push-order-self-test.sh`'ta beyan bloğu / OKUNAMADI senaryosu geçmiyor [reviewer ÖLÇTÜ: grep boş]
10. 🔵 OKUNAMADI satırı stderr'e (log yönlendirmesinde başlık/mesaj ayrılır) · `cp … || true` yazma hatasını yutuyor · push-order'da
    "TANINMAYAN beyan satırı" bilgi satırı (S2 ile kapanır) · AZALDI→0 yolunun self-test senaryosu yok

11. **Ön filtre yorum-kör olsun** (`Z111 §36`, ilk gerçek tetiklenme): 1835 migration'ının bir YORUMUNDA geçen `REVERSIBILITY`/`EFFECT`
    kelimesi dosyayı ön filtreye takıp ts-node (yavaş) yoluna soktu; iki okuyucu uyuştu, sonuç doğru (rc=0) [ÖLÇÜLDÜ: 90 dosyadan 1'i].
    ⇒ ön filtre yorum satırlarını (`//`, `*`, `/*`) dışlar — ya da ölçüm sonucu ikinci okuyucunun her dosyada koşması seçilir.

## Acceptance Criteria
- [ ] Her madde için bilinen-kırmızı çıkış kodunu değiştirir (DISIPLIN F04) · self-test beklentileri hükme atıf taşır (F03)
- [ ] `bash scripts/run-all.sh` · `push-order-self-test.sh` exit 0

## Done tanımı
CLAUDE.md §4.2.
