---
id: T-062
title: 🔴 LUMPSUM_SPEND hiçbir toplama katılmıyor — distributeSpendToSKUs hiç çağrılmıyor
epic: E-001
sprint: S-001
status: done
assignee: backend-engineer
created: 2026-08-03
updated: 2026-08-03
---

## Açıklama
[[T-056]] adım 5 sırasında bulundu (kapsam dışı olduğu için dokunulmadı — doğru davranış).

`spend-calculation.service.ts:165-167`: `LUMPSUM_SPEND` tipi mekanikler `calculateAllSpendsForSKU`'da
**her zaman 0** döndürüyor, çünkü dağıtımı yapacak `distributeSpendToSKUs` **hiçbir yerden
çağrılmıyor**. Aynı durum `calculateAllSpendsForFU` için de geçerli → hem recalc hem
`submitForApproval` hem (adım 5 sonrası) canlı `/submit` yolu etkileniyor.

**Sonuç:** lumpsum (götürü) harcama içeren bir plan, o harcamayı **bütçeden hiç düşürmüyor**.
Sessiz eksik rezervasyon.

**Nasıl ortaya çıktı:** `CPP_ON_PCT + VIS_LS` fixture'ı hiçbir zaman `off > 0` üretmiyordu;
A17'nin yazarı bunu adım 1'de fark edip yorumlamış, adım 5'te bağımsız ampirik ölçümle
doğrulandı. Fixture'lar kanıtlı kombinasyona (`MEC-DISCOUNT` + `CPP_OFF_PCT`) geçirildi — aksi
halde adım 5'in yeni assertion'ları hep `off=0` görüp anlamsız kalacaktı.

Oturumun ana temasının sekizinci örneği: **mekanizma var, ona giden çağrı yok**
([[T-033]], [[T-036]], [[T-039]], [[T-046d]], [[T-048]], [[T-052]], [[T-053]], ve mimari
ölçekte [[T-056]]/ADR 0005).

## Kararlar (ürün sahibi, 2026-08-03 → `docs/decisions/0006-lumpsum-dagitimi.md`)
- **Karar 1:** SKU'lara **dağıtılır** (yalnız FU toplamına eklenmez) — SKU ROI/KPI de düzelsin.
- **Karar 2:** dağıtım tabanı **base hacme göre orantılı** — `0001`'in "null base'e pay yok"
  ifadesiyle tutarlı tek okuma.

## 🔴 Uygulama kısıtı — iki ayrı dağıtım implementasyonu
`spend-calculation.service.ts:226` (`distributeSpendToSKUs`, üretimde çağrılmıyor) **ve**
`spend-distribution.service.ts` (`DistributionMethod.LUMPSUM` dalı; modüle kayıtlı, controller'a
enjekte). İkisi birleştirilmeli veya sınırları netleştirilmeli — hangisi kanonik, **ölçülerek**
belirlenmeli. Bu, oturumun tekrar eden hata sınıfı ([[T-049]]/[[T-052]]/[[T-053]]).

## Yapılacak
- `distributeSpendToSKUs`'in neden çağrılmadığını **ölç** — hiç mi bağlanmamış, yoksa bir
  refactor'de mi düştü? Git geçmişine bak, varsayma.
- BRD'de lumpsum'ın nasıl dağıtılması gerektiğini doğrula (hacme göre mi, eşit mi?) —
  **kanıt yoksa ürün sahibine sor**, uydurma.
- Düzeltince: lumpsum içeren planın on/off kırılımına ve `totalSpend`'e doğru katıldığını
  e2e ile kanıtla; `total === on + off` özdeşliği ([[T-056]] adım 4) bozulmamalı.
- **Mutasyon kanıtı:** dağıtımı geri al → test KIRMIZI, `tsc` temiz kalarak.

## İlgili
- Bulgu: [[T-056]] adım 5 · Tek türetim kuralı: `docs/analysis/0008-*` §5.7
- Kardeşi: [[T-061]] (on/off üçüncü türetim noktası)


## Sonuç (2026-08-03) — DONE

**Kırmızı önce üretildi:** 3 yeni unit test, ilk koşum `3 failed / 0 passed`.

**İki implementasyon sorusu ÖLÇÜLEREK çözüldü (varsayılmadı):**
- `distributeSpendToSKUs` — `git log --all -S` ile ilk commit'e (`d40ca16`) kadar izlendi:
  **hiç** üretim çağrısı olmamış, doğduğundan beri ölü. **Silindi** (kendi ölü testleriyle).
- `SpendDistributionService` — gerçek bir REST ucundan erişilebilir ama frontend'de **sıfır**
  çağrı, **sıfır** test, KPI/bütçe boru hattından kopuk (farklı tablolara yazıyor). Üstelik
  kendi null-base fallback'i (base 0 → planlanan hacim → eşit bölme) **ADR 0006 Karar 2'yi
  ihlal ediyor**. Birleştirme kapsamı büyütürdü → **dokunulmadı**, JSDoc'ta işaretlendi.
  → Takip kararı gerekiyor ([[T-063]]).

**Uygulama:** `computeLumpsumDistribution` — FU başına **bir kez** çağrılıyor (hem
`calculateAllSpendsForFU` hem `recalculatePlanWithKpiEngineLocked`), base-hacim orantılı,
null/0 base → 0 pay, kuruş artığı en büyük base'li SKU'ya (toplam birebir eşit).

**Sıfıra bölme kararı:** FU'daki tüm SKU'ların base'i 0/null iken lumpsum girilirse **sessiz 0
değil**, `400 LUMPSUM_DISTRIBUTION_NO_BASE_VOLUME` (ADR 0005 K3 emsali).

**Uçtan uca kanıt (A20):** `on=30.00, off=100.00, total=130.00`; `tactic_spend` → null-base SKU
`0`, LUMPSUM-A `43.33`, LUMPSUM-B `86.67` (toplam 130.00 birebir); submit sonrası
`RESERVE ON_INVOICE=30.00`, `OFF_INVOICE=100.00` — **T-062 öncesi off 0 olurdu**.
**A21:** tüm SKU'lar null base → 400, `plan.total_spend=0`, bütçeye hiç dokunulmadı.

**Gizli fixture çakışması ortaya çıktı:** perf regresyon testi 52 SKU'luk FU'da `VIS_LS: 1000`
kullanıyordu ama hiçbirinin base hacmi yoktu — düzeltme öncesi sessizce 0 dağıtıldığı için
görünmüyordu. Hatanın ne kadar sessiz olduğunun ek kanıtı. Fixture, round-trip sayacını
etkilemeyecek şekilde düzeltildi (24 sabit, tavan 30).

## Team Lead bağımsız doğrulama
- **Seed dokunuşu denetlendi** ([[T-054]] tuzağı): seed **idempotent**
  (`findOne({ where: { code, tenantId } })`), SKU'lar `E2E-` önekli ve **katalog** kaydı
  (plan satırı değil) → invaryant taban çizgisi bozulmadı: `plan_skus=0`, `agreements=3`,
  `envelopes=4`, zarflar 75.000/75.000
- **Perf testi zayıflatılmamış:** değişiklik saf eklemeli, round-trip tavanına/sayacına
  dokunulmamış
- **Mutasyon 1** (`raw = 0`): `git diff` doğrulandı, `tsc` **TEMİZ**, **EXIT=1**
  (`Expected: 333.33, Received: 0`)
- **Mutasyon 2** (null-base'e pay ver, `|| 0` → `|| 1`): `git diff` doğrulandı, `tsc` **TEMİZ**,
  **EXIT=1**, `Expected: 0, Received: 33.33` (1 failed / 21 — filtre eşleşti)
- unit **609/609** EXIT=0 · e2e **233/233 × 3 ardışık reset'siz koşum**, üçünde de **EXIT=0**,
  [[T-047]] invaryantı üçünde PASS

## ⚠️ Operasyonel not
Yeni seed SKU'ları (`SKU-E2E-LUMPSUM-A/B`) yalnız local dev DB'de mevcut — branch'i çeken
geliştiriciler/CI `npm run seed` çalıştırmalı (idempotent, güvenli).
