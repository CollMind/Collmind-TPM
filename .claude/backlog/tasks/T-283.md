---
id: T-283
title: 211 satırlık rota→hücre tablosu REPODA YOK — B3b-1 devrinin ön koşulu
epic:
sprint:
status: todo
assignee: architect
created: 2026-08-24
updated: 2026-08-24
touches:
  - docs/process/B3A_ESLEME_TABLOSU.md
  - docs/process/B3B_RATCHET_TABANI.md
---

## Açıklama

`B3B1_DEVIR_BRIEF.md` `§2` sonraki thread'e şunu şart koşuyor:

> *"Dosyayı okuyarak değil ÖLÇEREK devral: üç listeyi repo'dan yeniden türet
> (`route-scope.awk` + harita), dosyayla karşılaştır."*

**Bu adım bugün UYGULANAMAZ.** Ölçüldü (2026-08-24):

| girdi | mekanik mi |
|---|---|
| rota → `@Roles` kümesi | ✅ `route-scope.awk` |
| hücre → rol kümesi | ✅ `capabilities.ts` |
| **rota → HÜCRE** | ⛔ **YOK** |

`B3A_ESLEME_TABLOSU.md:307` açıkça yazıyor: *"Tam `211` satırlık tablo **ajanın
raporundadır**"* — yani bir transcript'te, repoda değil.

### Dizinden türetilemiyor — ölçüldü

```
aile dağılımı (dizin)   modes 68 · master-data 64 · shared 42 · customer 17
                        user 9 · tenant 8 · admin 2 · notification 1   = 211 ✓
B3a hücre sayıları      SHARED_READ 32 + SHARED_WRITE 7 = 39     ↔ dizin 42   ✗
```

⇒ Dizin+yöntem bir **yaklaşıklık**, eşleme değil. `B3a`'nın eşlemesi **yargı içeriyor**
(`SUMMARY_READ`'in `13` rotası, `B`/`C` kova ayrımı) ve o yargı **kayıtlı değil**.

## Neden şimdi

Bu, `§ BİR ŞARTIN SAĞLAYICISI YOKSA, ŞART BİR ERTELEME DEĞİL BİR KİLİTTİR` sınıfının
kayıtlı **dördüncü** vakası — ve öncekilerden farkı: sağlayıcı **var olabilir**, yalnız
üretilmemiş. Yani kilit **bugün açılabilir**.

⚠️ Ve brief'in kendisi *"devir bayatlığı bu projede iki kez ölçüldü"* diyor. Bu **üçüncü**
olurdu, ve brief'in **kendi adımında**.

## Acceptance Criteria

- [ ] `211` satırlık tablo repoda, **makine-okunur** biçimde
      (`<dosya>|<YÖNTEM>|<yol>|<hücre>` — anahtar `route-scope.awk` ve kapsam
      baseline'larıyla **aynı**, `223/223` eşleşme zaten kanıtlı)
- [ ] `B3B_RATCHET_TABANI.md`'nin üç listesi (göçebilir · karar-bekler ·
      istisna-kayıtlı) o tablodan **türetilerek** yazılı — elle değil
- [ ] Türetimin **pozitif kontrolü**: bilinen sayıları yeniden üretiyor
      (`MODES_APPROVE 6` · bloke `75` · toplam `211`)
- [ ] Yargı içeren satırlar (`SUMMARY_READ` üyeliği · `B`/`C` kova) **gerekçe sütunu**
      taşıyor — `Z18 §4`'ün *"her çift için ayrı cümle"* şartı

## Done tanımı

- [ ] `code-reviewer` onayı
- [ ] Üretim çağrı yolu: **yok — belge/araç task'ı**, `blocked-unreachable` DEĞİL
- [ ] Bağlayıcı koşul guard'a bağlandı: tablo ↔ `route-scope.awk` anahtar eşleşmesi
      bir kontrole bağlanır (`223/223` bozulursa kırmızı)
