---
id: T-344
title: Q14 DALGASI — B3 ölümü + uyarı taşıma + rota konsolidasyonu + S9 etiketi
epic:
sprint:
status: review
assignee: backend-engineer
created: 2026-08-31
updated: 2026-08-31
touches:
  # --- backend: rota konsolidasyonu + davranış taşıma ---
  - collmind.backend/src/modules/modes/planning-first/plan/plan.controller.ts
  - collmind.backend/src/modules/modes/planning-first/plan/plan.controller.spec.ts
  - collmind.backend/src/modules/modes/planning-first/plan/plan.service.ts
  - collmind.backend/src/modules/modes/planning-first/plan/plan.service.spec.ts
  - collmind.backend/src/modules/modes/planning-first/plan/approval-workflow.service.ts
  - collmind.backend/src/modules/modes/planning-first/plan/approval-workflow.service.spec.ts
  - collmind.backend/src/common/plan-submission/submission-checks.ts        # YENİ (modes/ DONDURULMUŞ)
  - collmind.backend/src/common/plan-submission/submission-checks.spec.ts   # YENİ
  - collmind.backend/scripts/guards/money-float-domain-a.txt   # Alan A üyeliği TAŞINDI
  - collmind.backend/src/modules/modes/planning-first/plan/dto/submit-plan.dto.ts
  - collmind.backend/src/modules/modes/planning-first/plan/dto/index.ts
  - collmind.backend/src/modules/modes/planning-first/plan/dto/submit-for-approval.dto.ts       # SİLİNDİ
  - collmind.backend/src/modules/modes/planning-first/plan/dto/submit-for-approval.dto.spec.ts  # SİLİNDİ
  - collmind.backend/src/common/kpi/target-roi.ts
  - collmind.backend/src/common/authorization/capabilities.ts     # yalnız F12 yorumu
  - collmind.backend/src/modules/shared/budget/budget.service.ts  # yalnız F12 yorumu
  - collmind.backend/src/modules/shared/budget/budget.service.spec.ts  # yalnız test adı
  - collmind.backend/test/role-journey.e2e-spec.ts
  - collmind.backend/scripts/guards/scope-b.txt
  - collmind.backend/scripts/guards/mode-split-baseline.txt
  - collmind.backend/scripts/guards/money-float-baseline.txt      # ratchet: 112 -> 107
  - collmind.backend/scripts/guards/lint-ratchet-baseline.txt     # ratchet: 183 -> 181 tuple
  # --- frontend: sözleşme pini + B3 ölümü + S9 etiketi ---
  - collmind.frontend/src/api/endpoints/plans.endpoints.ts
  - collmind.frontend/src/utils/targetRoi.ts                                    # YENİ
  - collmind.frontend/src/components/features/plans/SubmissionFeedback.tsx      # YENİ
  - collmind.frontend/src/components/features/plans/useTargetRoiThreshold.ts    # YENİ
  - collmind.frontend/src/components/features/plans/PlanDetailPage.tsx
  - collmind.frontend/src/components/features/plans/PlanApprovalsPage.tsx
  - collmind.frontend/src/components/features/plans/PlanApprovalDetailModal.tsx # B3 kopya 4 + FU seviyesi (kopya 6-7)
  - collmind.frontend/src/components/features/plans/GrandTotals.tsx             # B3'ün 5. KOPYASI
  - collmind.frontend/src/components/finance/widgets/BudgetAtRiskWidget.tsx
  - collmind.frontend/tests/utils/targetRoi.test.ts                                      # YENİ
  - collmind.frontend/tests/components/features/plans/SubmissionFeedback.test.tsx        # YENİ
  - collmind.frontend/tests/components/features/plans/PlanApprovalCard.test.tsx
  - collmind.frontend/tests/components/features/plans/PlanDetailPage.submit-wiring.test.tsx  # YENİ (ŞART 2 kablolama pini)
  - collmind.frontend/tests/utils/targetRoi.test.ts
migration_seq:  # YOK — davranış/sözleşme dalgası
---

## `A` · ROTA KONSOLİDASYONU (`Z73 §1`, `ADR 0005 K2` `F12`)
```
/submit                 YAŞAR   — FE'nin çağırdığı, DOĞRU ADLI yüzey
                        KAZANIR — SubmissionResult (validations + warnings)
/submit-for-approval    ÖLÜR
```
> **submit'in NE İÇİN olduğu ROTANIN işi, ADININ değil.**

⛔ `ADR 0005 K2` **geri alınmadı — koşulu doldu** (`Z73 §2`): gerekçe-1 karşılandı
(`Q13` ürün kararı + bu dalganın UI karşılığı), gerekçe-2 **korunur** (tüm `Q13`
katmanı `warnings`, **bloklamaz** — `K-2.2.7c`).

### ⛔ ŞART 1 — ÖLEN ROTANIN **TAM EVREN** TARAMASI
FE-sıfır **ölçüldü**, ama evren `src/` değil: **`test/` · `script` · `doc` DE**.
📌 **`E1` dersi TAZE:** *bir alanı/rotayı **taşıyıcı olarak** kullanan kod, tüketici
aramasında **bulunmaz**.* `rg -i` **tam evren**; **ölü-link / ölü-buton kalıntısı** dahil
(`T-307 m2` usulü). ⛔ Kapanış kanıtı bir grep değil, **tam suite'in yeşili**.

## `B` · UYARI TAŞIMA — bu dalganın **VAR-OLUŞ CÜMLESİ**
> **"Uyarı kullanıcıya ULAŞIR."**

### ⛔ ŞART 2 — FE tarafı **SÖZLEŞME-PİNLİ**
`PlanDetailPage.tsx:107`'nin yeni dönüş şeklini **okuduğu** ve uyarıları **render ettiği**
bir test. ⛔ Yoksa uyarılar rotaya taşınır **ama ekranda yine ölü doğar** — ve o, bu
dalganın **tam olarak düzeltmek için var olduğu** kusurun tekrarı olur.
**Pin, cümlenin kendisini ölçmeli.**

## `C` · `B3` ÖLÜMÜ — hardcode'lu ikinci implementasyon
```
PlanApprovalsPage.tsx:149   plans.filter(p => toNumberOrZero(p.overallRoi) < 20)   ⛔
                    :388-389 const isLowRoi = roi < 20;                            ⛔
                    :455-463 "ROI hedefin altında ({roi} < %20)…"
```
Üç kusur birden: **(1)** eşik **koddan**, konfigürasyondan değil (`§2.3`) ·
**(2)** `toNumberOrZero` ⇒ **`null` ROI `0` sayılıyor** ⇒ *"hesaplanamadı"* olan **her**
plan uyarı alıyor (`§2.5`) · **(3)** RAG rengi **hiç okunmuyor** ⇒ `AMBER`/`RED`/`LTA_ONLY`
planlar da bannerı görüyor — `isBelowTargetRoi`'nin **engellemek için yazıldığı** çifte sayım.

⇒ `evaluateTargetRoi`/`isBelowTargetRoi` sözleşmesine **bağlanır**; eşik
`GET /master-data/kpis`'ten (`targetRoiThreshold`).

### ⛔ ŞART 3 — **BEKLENEN-DEĞİŞİM LİSTESİ**
Banner'ın bugün **yanlış** gösterdiği planlar yarın **banner-sız**: `null`-ROI'liler ·
`AMBER`/`RED`/`LTA_ONLY`'ler. Bu bir **düzeltme** ama **görünür değişim**.
⚠️ **Finance gözüyle *"uyarılar azaldı"* okunabilir — listede ADIYLA dursun.**

## `D` · ⛔ ŞART 4 — `S9` TANIM GENİŞLEMESİ **ÜÇ KATMANDA GÖRÜNÜR**
`totalAtRisk` artık below-target harcamasını **da** içeriyor ⇒ `riskPercentage`
**aynı adla farklı sayı**.
```
1  beklenen-değişim listesi   "at-risk tanımı genişledi: RED + AMBER + below-target"
2  Z kaydı                     ✅ Z72 §3 (yazıldı)
3  UI ETİKETİ/TOOLTIP          "Risk = zarar + kârsız-büyüme + hedef-altı"   ← BU DALGA
```
⛔ **Ad-borcu DEĞİL** — ad doğru, **tanımı büyüdü**.
> **Görünmez büyüyen bir tanım, SESSİZ-SÜRPRİZ sınıfıdır.**

## ⛔ TEK-DOĞRULUK-KAYNAĞI İLKESİ — dalganın kabul ölçütü
Dalga sonunda **BİR** submit yolu, **BİR** below-target implementasyonu kalır:
*konfigüre edilebilir · kadran-farkında · `null → NOT_EVALUABLE`* olan.

## Done tanımı
`CLAUDE.md §4.2` + ⛔ ilk madde (hayalet `tpm`) · `tsc` BE **ve** FE 0 · unit BE+FE ·
**hedefli** e2e *(tam koşumu Team Lead)* · `guards` 0 · ratchet'ler 0 · `improved:` yok ·
**üretim çağrı yolu** *(bu dalganın KONUSU tam da bu)*

## Kaynak
`Z73` · `Z72` · `Z71` · `ADR 0005 K2` `F12` · `T-058` (revize notu) ·
`docs/DISIPLIN.md`: *"gerekçenin çürümesi ile karşılanması ayrı sınıftır"* ·
*"bir alanı taşıyıcı olarak kullanan kod tüketici aramasında bulunmaz"*


---

# KAPANIŞ (backend-engineer, 2026-08-31)

## Yapılan
```
A  /submit-for-approval SİLİNDİ (controller + servis metodu + DTO dosyası + spec)
   /submit  →  SubmissionResult döner, ön doğrulama + Q13 uyarı katmanını TAŞIDI
   davranış KOPYALANMADI: submission-checks.ts (saf, tek türetim noktası)
B  FE sözleşme pini: SubmissionFeedback.tsx + PlanDetailPage:107 → uyarılar RENDER
C  B3 ÖLDÜ — ve DÖRT kopyada (brief üçünü sayıyordu; §7 taraması dördüncüyü buldu)
D  S9 etiketi: "Risk = zarar + kârsız-büyüme + hedef-altı" (BudgetAtRiskWidget)
```

## ⛔ AÇIK ÜRÜN KARARI — `ADR 0005 K2` gerekçe-2 SINIRI

> *"Kullanıcının bugün submit edebildiği plan yarın da edebilmeli."* (`Z73 §2`: **KORUNUR**)

Ölen rotanın bloklayan kalemleri ikiye ayrıldı, çünkü **ölçüldü**:

| kalem | `/submit` BUGÜN | seçilen |
|---|---|---|
| "FU yok" | reddediyor (400) | **BLOKLAR** (200 + `success:false`) |
| "yetersiz bütçe" | reddediyor (400, `reserveTypedForPlan` kapısı) | **BLOKLAR** |
| **"FU'da mekanik/taktik yok"** | **KABUL EDİYOR** | **UYARI** (muhafazakâr) |

Üçüncüsü bloklayan olarak taşındığında `plan-review-decision` + `plan-escalate-to-finance`
**sekiz e2e testi düştü** — ikisi de "FU var, taktik yok" planı kuruyor. Yani etkilenen
sınıf egzotik bir uç değil, **sıradan bir taslak plan**.

⛔ Fixture'ları "düzeltip" değişikliği yeşile boyamak **reddedildi** (`§2.7 #4`).
Ürün sahibi *"boş plan gönderilemesin"* derse değişiklik **tek satır**:
`submission-checks.ts#collectPlanStructureWarnings` → `warnings.push` yerine
`validationErrors.push`.

## ⛔ AÇIK ENGEL — `route-cell-map` G7 (bende yetki yok)
`npm run guards` exit 1, **tek sebep**: `docs/process/B3A_EK3_ROTA_HUCRE_ESLEMESI.tsv`
ölen rotanın satırını taşıyor. `docs/process/` bu ajanın **yasak** listesinde.
Gereken: o dosyadan **tek satır** silinmesi —
`...plan.controller.ts\tPOST\tplans/:id/submit-for-approval\t-\tMODES_SUBMIT\tZ35\tCAP\tMODES_SUBMIT`
(taze üretimle diff alındı; **başka sapma yok**).


---

# KAPANIŞ — II. TUR (`B3` gerçekten öldü)

## Ne kaçmıştı ve neden
```
I. TUR taraması   rg 'roi < 20|isLowRoi'            → "0" dedi
GERÇEK            fuRoi < 20 · isFuLowRoi           ← `fu` ÖN EKİ deseni bozdu
                  gpRoi < targetRoi                 ← eşik bir YEREL SABİTİN adı arkasında
```
> **Bir sınıfı ararken, o sınıfın DEĞİŞKEN ADLARINI değil, KARARINI ara.**

Toplam **beş** kopya vardı (brief üç sayıyordu, ben dördü buldum, koordinatör
beşinciyi + FU'daki iki render sitesini). Hepsi tek karar noktasına bağlandı:
`utils/targetRoi.ts#resolveBelowTarget`.

## Tek doğruluk kaynağı
`resolveBelowTarget` = **1** uygulama · **5** çağrı sitesi / **4** dosya.
Karar-tabanlı tarama + iki-biçimli pozitif kontrol raporda.


---

# ⛔ BEKLENEN-DEĞİŞİM LİSTESİ — **KONSOLİDE, TEK YER**

> `RAPOR < BELGE < KAPI < PİN` hiyerarşisinin **tersi** burada geçerli:
> görünür değişim listesi bir **İNSANA** gider. Kanıtlar test adlarına ve kod
> yorumlarına dağılmıştı; okunacak tek yer **burasıdır**.

## `API` — sözleşme değişimleri ⚠️ Swagger

| uç | önce | sonra |
|---|---|---|
| `POST /plans/:id/submit` | `Plan` döner | **`SubmissionResult`** döner (`validationErrors` + `budgetCheck.warnings`) |
| `POST /plans/:id/submit` · yetersiz bütçe | `400` | **`200 + success:false`** |
| `POST /plans/:id/submit` · FU yok | `400` | **`200 + success:false`** |
| `POST /plans/:id/submit` · FU'suz **ve** bayat sürüm | — | **`409`** *(sürüm kapısı doğrulamadan ÖNCE — `🟡-2`)* |
| `POST /plans/:id/submit` · gövde | `{version}` | `{version, submissionNotes?}` |
| `POST /plans/:id/submit-for-approval` | `200` (deprecated) | **`404` — KALDIRILDI** |

## `FE` — kullanıcının GÖRECEĞİ değişimler

| yüzey | değişim |
|---|---|
| `PlanDetailPage` | **YENİ YÜZEY**: submit sonrası **kalıcı** geri-bildirim paneli (uyarılar sarı/bloklamaz · hatalar kırmızı/bloklar) |
| `PlanDetailPage` | submit notu **gerçekten gidiyor** ⇒ ⚠️ **onaylayıcılar bugünden sonra NOT GÖRMEYE BAŞLAR** (dün toplanıyor, atılıyordu) |
| `PlanDetailPage` | **boş** not artık `''` olarak kalıcılaşmıyor (`🟡-6`) |
| `PlanApprovalCard` | GP ROI `null` ⇒ `"%0,0"` → **`"—"`** |
| `PlanApprovalsPage` | kart başlığı `"ROI < %20"` → `"ROI < %{eşik}"` · eşik yoksa **`"HEDEF ALTI"` + `"—"`** |
| `PlanApprovalDetailModal` | **ÜÇ yüzey**: plan ROI + FU rozeti + FU tablo hücresi |
| `GrandTotals` | `"Target: %20"` → yapılandırılmış eşik · eşik yoksa **`"Hedef ROI tanımlı değil"`** |
| **Finance** `BudgetAtRiskWidget` | `S9`: *"Risk = zarar + kârsız-büyüme + hedef-altı"* — **`totalAtRisk`/`riskPercentage` aynı adla BÜYÜMÜŞ sayı** |

## Banner/rozet DÜŞEN plan & FU sınıfları (dün görüyordu, bugün görmüyor)
```
null / okunamaz ROI      → "hesaplanamadı" ≠ "%0 ROI"          (§2.5)
RAG = RED                → kadran zaten konuşuyor              (çifte sayım)
RAG = AMBER              → aynı
RAG = null / LTA_ONLY    → "değerlendirilmedi" ≠ "kötü"        (S1)
eşik tanımsız            → uydurulmuş hedefe göre yargı YOK    (§2.5)
eşik ≠ 20 olan tenant    → artık DOĞRU eşiğe göre
```
⚠️ Finance gözüyle **"uyarılar azaldı"** okunabilir. **Azalmadı — yanlış olanlar düştü.**

## ⛔ DÜZELTİLMEDİ — ve bu bilinçli (`§7.1 T-084`: belgelenen kusur korunur)
```
PlanApprovalsPage:150  avgRoi = Σ toNumberOrZero(overallRoi) / plans.length
```
> **AYNI EKRANDA İKİ KART ARTIK ÇELİŞİK BİR EVREN ANLATIYOR:**
> *"hedef altı"* kartı `null` ROI'yi **DIŞLIYOR**, *"Ort. ROI"* kartı onu
> hâlâ **`0` sayıyor** ve ortalamayı aşağı çekiyor.

Düzeltilmedi çünkü *"ortalama hangi popülasyon üzerinden"* bir **ürün
sorusudur** (`§2.4`), ve bir sayıyı sessizce değiştirmek Finance'a haber
vermeden ikinci bir `S9` üretirdi. ⛔ **Bu bir eksiklik değil, açık bir kalem.**

**Diğer düzeltilmeyen eşik kararları (adıyla):**
`AgreementApprovalsPage.tsx:388` `totalSpend > 100000` · `BudgetEnvelopeCard:43-44` ·
`BudgetSummaryCard:31-32` · `BudgetEnvelopeList:28-29` (`80/95/100` — bilinen
`§2.3` borcu, `T-101`/`T-108`) · `plan.service.ts:2915` `20.0` fallback.


---

# KAPANIŞ — III. TUR (review blocker'ı)

```
🔴-1  ŞART 2 pini CÜMLEYİ ölçmüyordu    → PlanDetailPage.submit-wiring.test.tsx
      mutasyon: `.then(res => res.data)` düştü → YENİ pin KIRMIZI (2 test),
                                                  ESKİ pin YEŞİL kaldı ✅ teşhis doğrulandı
🟡-6  boş not `''` kalıcılaşıyordu       → `note?.trim() || undefined` + pin
🟡-1  "en az bir FU" iki mesaj           → `PLAN_MUST_HAVE_FU_MESSAGE` tek kaynak
                                           + kilit-altı dal "YARIŞ KORUMASI" diye yazılı
                                           + ayrı kod: `PLAN_FU_REMOVED_CONCURRENTLY`
🟡-2  409 doğrulamanın ARKASINDA kalmıştı → sürüm kapısı ÖNE alındı (kilitsiz okuma;
                                           otorite hâlâ kilit-altı ikizi)
                                           mutasyon: kapı kaldırıldı → 2 test KIRMIZI
🟢    "vaka vaka aynalar" abartısı        → "sınıf sınıf" + eksik 4 ayırt edici eklendi
```
⚠️ `🟡-2` düzeltmesi `money-float` ratchet'ini `32 -> 33` kırdı (yeni bir
`Number()` para yolunda) — guard **haklıydı**, `toFiniteDecimal`'e çevrildi.
