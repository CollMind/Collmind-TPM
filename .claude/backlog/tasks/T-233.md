---
id: T-233
title: "`capabilities` · `role_capabilities` — `0056-K3(b)` ile ÖLÜ YAPIYA düştü; düşürülsün mü?"
epic: E-001
sprint: S-001
status: todo
assignee: architect
priority: P2
created: 2026-08-16
updated: 2026-08-16
touches:
  - collmind.backend/src/database/migrations/
migration_seq:
---

## Bulgu — kararın ürettiği sonuç

`0056-K3` **`(b)`** ile kapandı (2026-08-16, kayıt `Z4`): yetenekler **kodda** tanımlanacak
(`const CAPABILITIES`), **tabloda değil**.

Ama iki tablo `B` dalgasında **zaten indi** — *"`T-165` ile dolacak"* gerekçesiyle. Ölçüldü
(Team Lead, 2026-08-16, canlı DB):

```
capabilities        10 kolon ·  0 satır
role_capabilities    9 kolon ·  0 satır
roles               11 kolon ·  5 satır      ← rol kataloğu İNDİ ve DOLU, o kalıyor
Capability entity dosyası                     0
```

> **`T-225`'in (`BudgetReservation`) sınıfı: yapı var, yol yok.** Fark şu ki orada yapı bir
> **unutulmuştu**; burada bir **kararla** öksüz kaldı — yani sebebi biliniyor ve kayıtlı.

## Karar gerektiren

| yol | sonuç |
|---|---|
| (a) **düşür** (migration) | Şema, kararın gerçeğiyle hizalanır. `T-225`'in deseni: ölü yapı silinir, gerekçe kural metnine yazılır. ⚠️ `0056-K3` bir gün `(a)`'ya dönerse tablolar yeniden yazılır — ama o zaten yeni bir karar olur |
| (b) **bırak** | ⚠️ `T-225`'in tam olarak ölçtüğü tehlike: yapı durur, kimse dokunmaz, ve bir gün biri *"demek ki CBAC vardı"* diye okur. Ve entity pini (`T-224`) onları **görmez** — çünkü `@Entity` sınıfı yok |
| (c) **ertele** | ⚠️ `0056-K3` zaten kapandı; erteleme yalnız kararı **görünmez** kılar |

## ⚠️ Ve bir ölçüm gerekli — `roles` KALIYOR

`roles` tablosu **5 satır** taşıyor (`K-2.6.4`'ün rol kataloğu) ve `(b)` kararı onu
**etkilemiyor** — yetenek koda taşındı, **rol** değil. Düşürme kapsamı **yalnız** iki
yetenek tablosu.

⛔ **Ve FK yönü ölçülmeli:** `role_capabilities` muhtemelen `roles`'a FK ile bağlı. Düşürme
sırası ve `down()`'ın birebir geri kurması `T-225`'in `1805` deseniyle aynı titizlikte
olmalı (üç durum ayrımı: beklenen · zaten uygulanmış · **satır VAR → İPTAL**).

## Done tanımı
- [ ] Yol seçildi, **gerekçesiyle**
- [ ] (a) ise: migration numarası Team Lead'den, `down()` birebir, **üç durum ayrımı**
- [ ] `EK_C`'nin ilgili satırı sonuca göre güncellendi (⚠️ **donmuş belge** — karar defterine kayıtla)
- [ ] `code-reviewer` onayı
