---
id: T-049
title: 🔴 CANLI UI HATASI — Planning Grid veri hücreleri başlığa göre 2 kolon kaymış
epic: E-001
sprint: S-001
status: todo
assignee: frontend-engineer
created: 2026-08-01
updated: 2026-08-01
---

## Açıklama
[[T-016]] Playwright altyapısı kurulur kurulmaz bulundu. **Team Lead kodda birebir doğruladı.**

`PlanningGridEnhanced.tsx`:
| Yer | Kod |
|---|---|
| **Başlık** `:1284` | `visibleColumns.filter(c => c.code !== 'ITEM_NAME' && c.code !== 'ITEM_CODE').map(...)` |
| **FU satırı** `:1470` | `columns.map(...)` — **filtresiz** |
| **SKU satırı** `:1594` | `columns.map(...)` — **filtresiz** |

Satırlar başlıkta karşılığı olmayan **2 hayalet hücre** (ITEM_NAME, ITEM_CODE) basıyor →
**her veri hücresi başlığına göre 2 kolon sağa kayıyor.**

Ekran görüntüsüyle doğrulanmış örnek: *"Base Volume (pcs)"* başlığı altında **List Price ₺100**,
*"Planned Volume (pcs)"* altında **COGS ₺60** görünüyor.

**Etki:** ürünün kalbindeki ekran (planlama grid'i), **tüm planlar, tüm kullanıcılar**. Planner
yanlış sütunda veri okuyor — hacim sanılan yerde fiyat var. Finansal karar hatasına doğrudan yol
açabilir.

**İkinci sapma:** satırlar `columns`, başlık `visibleColumns` kullanıyor → kolon görünürlüğü
değiştirilebiliyorsa orada da ayrışırlar.

## Neden bugüne kadar görünmedi
Backend e2e (209 test) API doğruluğunu kanıtlıyor, **DOM hizasını değil**. Frontend birim
testleri veriyi mock'layıp bileşeni izole ediyor. Bu sınıf **yalnız gerçek tarayıcıda** görünür —
[[T-016]]'nın var oluş sebebi tam olarak bu.

## ⚠️ Mevcut testler bu hatayı YAKALAMIYOR
T-016'nın `tests/e2e/support/grid.ts` içindeki `dataCell()` helper'ı offset'i **telafi ediyor**
(hedefi 409/KPI davranışıydı, hiza değil). Yani suite yeşil ama hata duruyor.
**Düzeltmeyle birlikte:** telafi silinmeli **ve** hizayı doğrulayan bir test eklenmeli — aksi
halde hata geri gelirse yine sessiz kalır.

## Acceptance Criteria
- [ ] Satır ve başlık **aynı** kolon dizisini kullansın (filtre + `visibleColumns` tutarlılığı)
- [ ] `grid.ts`'teki offset telafisi **kaldırılsın**
- [ ] **Hizayı doğrulayan** Playwright testi: bilinen bir SKU için "List Price" başlığının altındaki
      hücre gerçekten liste fiyatını göstersin
- [ ] **Mutasyon kanıtı:** filtreyi tekrar ayrıştır → test KIRMIZI olsun (derlemeyi bozmadan)
- [ ] Frontend unit suite ve T-016 senaryoları yeşil kalsın

## İlgili
- Tespit: [[T-016]] · Aynı turda bulunan diğer hata: [[T-050]]
