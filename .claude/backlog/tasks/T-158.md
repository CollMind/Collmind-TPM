---
id: T-158
title: "`EXPIRED` durumu ve 7 günlük zaman aşımı ailesi yok — ve gece işi altyapısı da yok"
epic: E-001
sprint:
status: todo
assignee: architect
priority: P3
created: 2026-08-10
updated: 2026-08-10
touches:
  - collmind.backend/src/database/entities/plan.entity.ts
  - collmind.backend/src/database/entities/agreement.entity.ts
migration_seq:
---

## Bulgu ([[T-143]] turu 8 / `docs/analysis/0026 §3.2`)

Addendum **H3 Action 3.3** normatif:

```
Rule: Approvals pending >7 days auto-expire
  → status = 'EXPIRED', expired_at, expiry_reason = 'No action for 7 days'
  → NOTIFY requester + approver
Grace Period: 14 gün içinde yeniden gönderilebilir, sonra silinir
```

**Ölçüm — durum enum'ları:**

```
plan.entity.ts       DRAFT · PENDING_APPROVAL · PENDING_FINANCE_REVIEW · APPROVED · REJECTED
agreement.entity.ts  DRAFT · PENDING · APPROVED · ACTIVE · CLOSED · REJECTED · CANCELLED
```

- **`EXPIRED` ikisinde de yok** · `expired_at` / `expiry_reason` yok
- **`CANCELLED` planlarda yok** (anlaşmalarda var) — H3'ün tablosu ikisini de kapsıyor

⚠️ **Ve gerektirdiği altyapı bizde yok:** *"Nightly Job"*. `SYSTEM_INVARIANTS §10` iki
maddeyi zaten bu yüzden `CI` diye etiketlemeyi reddetmişti; **bu üçüncüsü** ([[T-157]] ile
aynı kök).

## ⚠️ Ölçülmedi — iddia yazılmadı

H3: *"❌ **PENDING → DRAFT** (must cancel first)"* — **geçersiz** geçiş.

Bizde `plan.service.ts` `returnToDraft()` var. **Hangi durumdan çağrıldığı ölçülmedi.**
`REJECTED → DRAFT` ise uyumlu; `PENDING_* → DRAFT` ise **doğrudan sapma**.

- [ ] `returnToDraft`'ın kabul ettiği kaynak durumları **ölç** (§7.1: her iki uç)

## Yapılacak
- [ ] Zaman aşımı ailesi **ürün kararı**: uygulanacak mı, ertelenecek mi
      (`03_Candidate_Log`'da karşılığı var mı — CLAUDE.md §2.1.1, **tüm pakete** yay)
- [ ] Uygulanacaksa: `EXPIRED` + `expired_at` + `expiry_reason`, plan `CANCELLED`,
      ve **zamanlanmış iş altyapısı** ([[T-157]] ile birlikte)
- [ ] `PENDING → DRAFT` ölçümü ve sapma varsa kararı

## Done tanımı
- [ ] Karar gerekçeli; ertelenirse `SYSTEM_INVARIANTS`'a "guard yok" olarak kaydedilir
- [ ] `returnToDraft` ölçümü yapılmış

---

## ⚠️ YENİDEN ÇERÇEVELENDİ — *"UAT'ye ertelenmiş edge case"* ([[T-143]] turu 30 / `0048 §3`)

`03_Candidate_Log` **CANDIDATE-004** (Source: **Addendum §H3**):

```
Status: 🟠 Partially Implemented (Phase 1) — happy path complete,
        edge cases pending UAT validation
Deferral:
  ❌ Core flow defined in BRD v1.0 (sufficient for Phase 1 happy path)
  ❌ Edge cases depend on real usage patterns (cannot be fully enumerated
     without production data)
```

> **Kaynak zaman aşımı ailesini *"edge case"* sayıyor ve *"pending UAT validation"* diyor.**
>
> Bu task *"karşılanmamış Phase 1 gereksinimi"* **değil** — [[T-167]] (RLS, `§7.7`'de
> Phase 1 ✅) ile **tam zıt** bir statü sınıfında.

**Öncelik P2 → P3.**

⚠️ **Ölçümün sınırı:** CANDIDATE-004'ün *"edge cases"* tanımının **7 günlük zaman aşımını
kapsadığı doğrulanmadı** (adayın doğrulama tablosu okunmadı). *"Kapsıyor"* denmiyor;
**statü sınıfının** farklı olduğu söyleniyor.

- [ ] Doğrulama tablosu okunduğunda kapsam netleşsin
