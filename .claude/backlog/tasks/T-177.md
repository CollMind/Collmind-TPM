---
id: T-177
title: "⛔ Plan/FU seviyesindeki `GP_ROI_PCT` formülün kendisi DEĞİL — null SKU'lar elenmiş ağırlıksız ortalama"
epic: E-001
sprint: S-001
status: in-progress
assignee: architect
priority: P1
created: 2026-08-10
updated: 2026-08-10
touches:
  - collmind.backend/src/modules/shared/kpi-engine/kpi-engine.service.ts
migration_seq:
---

## Açıklama

**ADR 0011** paydayı `TOTAL_PLANNED_SPEND` yaptı, gerekçesi *"dört eşik bu paydaya göre
kalibre"*. Ama o eşikler **plan seviyesinde** okunuyor, ve plan seviyesindeki değer
formülün kendisi değil.

**Ölçüldü (2026-08-10):**

```ts
// kpi-engine.service.ts:307-309
case AggregationMethod.WEIGHTED_AVG:
  // Default to simple average if no weights      ← yorum kendisi itiraf ediyor
  return values.reduce((a, b) => a + b, 0) / values.length;

// kpi-engine.service.ts:112-116
const values = skuResults
  .map((sr) => sr[kpi.kpiCode]?.value)
  .filter((v): v is number => v !== null && v !== undefined);   ← null'lar SESSİZCE eleniyor
```

`GP_ROI_PCT.aggregationMethodFu = WEIGHTED_AVG` (`kpi.seed.ts`).

| | hesaplanan |
|---|---|
| **olması gereken** | `Σ INCR_GP / Σ TOTAL_PLANNED_SPEND` |
| **bugün olan** | `mean(SKU başına ROI)`, **ve yalnız null olmayan SKU'lar üzerinden** |

Bu matematiksel olarak **başka bir büyüklük**. Küçük harcamalı bir SKU'nun uç ROI'si,
büyük harcamalı bir SKU'nunkiyle **eşit ağırlık** taşıyor.

### İkinci kayıp: null'lar sessizce eleniyor

`docs/analysis/0016`: SKU'ların büyük çoğunluğu null ROI üretebiliyor (COGS eksikse).
Ortalama **kalan alt kümenin** ortalamasıdır ve kullanıcı hangi alt küme olduğunu görmez.

> `§2.5`'in toplama tarafındaki hâli: sayı **uydurulmuyor**, ama **hangi kümeden** geldiği
> kayboluyor. Bir plan, SKU'larının %3'ü üzerinden "yeşil" görünebilir.

## Neden P1

`§10.2 Gate 3` ve `§10.1` ölçütü: *"planların **%70'i** yeşil (ROI ≥%20)"*. Bu kapı
**plan seviyesinde** ölçülür. Bugünkü haliyle o kapı, ağırlıksız ve eksik bir ortalamayı
ölçüyor — yani ADR 0011'in düzelttiği şey **SKU seviyesinde doğru**, kapının okuduğu yerde
değil.

## Yapılacak

1. **`WEIGHTED_AVG` gerçekten ağırlıklı olsun.** Ağırlık nedir — karar:
   - **Doğal aday:** payda (`TOTAL_PLANNED_SPEND`). O zaman ağırlıklı ortalama
     cebirsel olarak `Σ INCR_GP / Σ TOTAL_PLANNED_SPEND`'e eşittir.
   - **Daha sade alternatif:** oran KPI'ları için aggregation'ı hiç kullanma —
     pay ve paydayı ayrı ayrı topla, oranı **üst seviyede yeniden hesapla**. Bu, ağırlık
     kavramını tamamen ortadan kaldırır.
2. **Null'lar sessizce elenmesin.** `§2.5`: eksik girdi bir karardır.
   Seçenekler: tümü null → sonuç null · kısmi null → sonuç + **kapsama oranı** taşınır.
   *"Sessizce kalanların ortalaması"* seçeneği **değildir**.
3. `aggregate()`'in diğer tüketicileri **sayılsın** (`§7.1`) — kaç KPI `WEIGHTED_AVG`
   kullanıyor, hepsi aynı sorundan etkileniyor mu?

## Acceptance Criteria
- [ ] `WEIGHTED_AVG` tüketicilerinin tam listesi **komut çıktısıyla** yazıldı
- [ ] Plan seviyesindeki `GP_ROI_PCT`, SKU toplamlarından türeyen orana **eşit**
- [ ] Kısmi null bir plan, tam veriyle **ayırt edilebiliyor** (`§2.7 #6`)
- [ ] Test fixture'ı SKU'lara **farklı harcama** veriyor — eşit harcamada iki yöntem aynı
      sonucu verir ve test hiçbir şey ölçmez (`CLAUDE.md`, T-101 dersi)

## Notlar
Bulan: `code-reviewer`, T-163 diff review'ı (🟡-3). Team Lead **bağımsız doğruladı**
(`kpi-engine.service.ts:307-309`, `:112-116` okundu).
Kardeşleri: [[T-171]] (hardcoded hedef) · [[T-172]] (null ROI → `0`/`BELOW_TARGET`).
Üçü de aynı ailenin üyesi: **ROI'nin tüketim ucu**.

---

## Kapsam ölçüldü (2026-08-11) — **üç oran KPI'sı, hepsi aynı kusurdan etkileniyor**

```
WEIGHTED_AVG kullanan KPI'lar (kpi.seed.ts):
  UPLIFT_PCT      (PLAN_VOL - BASE_VOL) / BASE_VOL * 100
  GP_ROI_PCT      INCR_GP / TOTAL_PLANNED_SPEND * 100
  GP_MARGIN_PCT   PLANNED_GP / PLANNED_TO * 100
```

> **Üçü de oran.** Ve bir oranın ortalaması, oranların toplamının oranı **değildir** —
> yani kusur `GP_ROI_PCT`'ye özel değil, **`WEIGHTED_AVG`'ın kendisine** ait.

Bu, "daha sade alternatif"i güçlendiriyor: oran KPI'ları için aggregation'ı hiç kullanma —
**pay ve paydayı ayrı topla, oranı üst seviyede yeniden hesapla**. O zaman ağırlık kavramı
üç KPI için birden gereksizleşir.

⚠️ Ve `aggregationMethodFu` **üç ayrı yerde** tanımlı ([[T-178]]'in aynı üçlüsü):
`kpi.seed.ts` · `kpi.service.ts:480,:665,:684` · migration'lar. Değişiklik hangisine
uygulanırsa uygulansın **üçü birden** taranmalı — bu turun dersi ([[T-163]]).

---

## ⚠️ Null elemesinin ağırlığı — ürün sahibinin vurgusu (2026-08-11)

İki katmandan **ikincisi bugünkü veriyle daha ağır**:

```
COGS dolu SKU:  4 / 170        (docs/analysis/0016)
```

> **Bir plan, SKU'larının %3'ü üzerinden "yeşil" görünebiliyor** — ve kullanıcı hangi alt
> kümeye baktığını göremiyor.

Yani sıralama içi öncelik: **önce null semantiği, sonra ağırlık.** Ağırlıksız ortalama
yanlış bir sayı üretir; sessiz eleme **hangi sorunun cevaplandığını** değiştirir.

- [ ] Kısmi null bir planın çıktısı, tam verili bir plandan **ayırt edilebilir** olmalı —
      kapsama oranı taşınmalı ya da sonuç null olmalı. Karar ürün sahibinin, ama
      *"sessizce kalanların ortalaması"* seçenek değil (`§2.5`).

---

## ✅ Uygulandı — iki commit, `review` (2026-08-11)

**Ürün sahibi kararı:** *"Değer + kapsama oranı göster, ve kapsama düşükse rengi geri çek."*
Eşik **uydurulmadı** — bugünkü kural: `coverageRatio === 1` değilse RAG **yok**, değer
gösterilir. Eşik bir gün gelirse konfigürasyon katmanına ([[T-156]]) bağlanır.

| commit | ne |
|---|---|
| 1 — null semantiği | `CalculationResult.coverageRatio` · RAG yalnız tam kapsamada · `aggregate()` **değişmedi** |
| 2 — ağırlık | oran KPI'sı **üst seviyede yeniden hesaplanıyor** (`Σpay/Σpayda`), `WEIGHTED_AVG` dalı artık **throw** |

**Bağımlılık ölçümü (zorunluydu, yapıldı):** altı bağımlılığın (`PLAN_VOL` · `BASE_VOL` ·
`INCR_GP` · `TOTAL_PLANNED_SPEND` · `PLANNED_GP` · `PLANNED_TO`) **hepsi `SUM`**, ve her
birinin `calculationOrder`'ı kendi oranından düşük.

**Team Lead doğrulaması:** `WEIGHTED_AVG` her iki çağrı noktasında (`:127` FU, `:297` Plan)
`aggregate()`'ten **önce** yakalanıyor → `:414`'teki throw savunma amaçlı, canlı yolda
tetiklenmiyor (1077 test yeşil).

### ✅ Kapsama seviye bazında — ürün sahibinin şartı birebir

> *"FU'nun kapsaması SKU'larından, planın kapsaması FU'larından."*

Uygulanan tam olarak bu. Ajan bunu bir *"bilinen sınır"* diye raporladı (plan kapsaması,
altındaki SKU'ların tamamının dolu olduğu anlamına gelmiyor) — **sınır değil, şartname**.
Çarpımsal/geçişli kapsama ayrı bir ürün kararıdır ve istenmedi.

### Turun ürettiği iş
- [[T-185]] **P1** — actuals-first anlaşma KPI'ları **uydurulmuş** bir SKU'dan hesaplanıyor
- 📌 `calculatePlan`'ın `calculationLevel === PLAN` dalında `result.value || 0` — aynı
  sessiz-sıfır sınıfı, ama **üretim çağrı yolu yok** (üç kaynakta da `calculationLevel:
  PLAN` taşıyan KPI yok, doğrulandı). Task açılmadı; T-172 kapsamında değerlendirilsin.

### Kalan
- [ ] `qa-engineer` testleri (bu tur test yazmadı — 1077 sabit)
- [ ] `code-reviewer`
- [ ] Frontend: kapsama gösterimi — [[T-172]] ile **birleşiyor**. Orada iki durum vardı
      (*"hesaplanamadı"* ↔ *"hedefin altında"*); bu **üçüncüsünü** ekliyor:
      **"kısmen hesaplandı"**.

---

# 🔴 BLOCKER — `5bc2787` şartnameyi ihlal ediyor, ve **kötüleştiriyor** (2026-08-11)

`code-reviewer` buldu, Team Lead **bağımsız doğruladı** (`kpi-engine.service.ts:467-472`).

```ts
for (const [code, result] of Object.entries(results)) {
  contextMap[code] = result.value;   // her bağımlılık KENDİ kapsamı üzerinden toplanmış
}
const value = formula.execute(contextMap);
```

`INCR_GP` **4 SKU'dan** (COGS olanlar), `TOTAL_PLANNED_SPEND` **170'inden**. Oran iki
**farklı alt kümenin** bölümü — hiçbir kümenin gerçek oranı değil.

| | değer |
|---|---|
| rapor edilen | **%0,588** |
| dürüst alt küme (4 SKU) | **%25** |
| **kaldırdığımız `mean(oran)`** bu veride | **%25** |

> **42× sapma — ve kaldırdığımız kusur bu veride daha doğru sonuç veriyordu.**
> Düzeltme, düzelttiği şeyden kötü.

Ve plan seviyesinde her FU bir değer ürettiği için `coverageRatio === 1` kapısı açılıyor →
o çarpık sayıya **renk atanıyor** (`RED`) ve `plans.ragStatus`/`overall_roi`'ye
**kalıcılaşıyor** (`plan.service.ts:2607-2608`, `:2640`).

⚠️ Ve `Math.min(depCoverages)` kapsamaların **farklı olduğunu biliyor**, kaydediyor, ve
oranı yine de uyumsuz kümeler üzerinden hesaplıyor. **Bilgi elde, kullanılmıyor.**

## Bu bir açık karar DEĞİL — ürün sahibinin şartnamesi zaten böyle

> *"value: Σ INCR_GP / Σ TOTAL_PLANNED_SPEND **(hesaplanabilen SKU'lar üzerinden)**"*

*"Hesaplanabilen SKU'lar üzerinden"* **her iki toplama** uygulanır.

**Doğru çözüm — kesişim:** pay ve payda, formülün **tüm** bağımlılıklarının çözüldüğü
çocuk kesişimi üzerinden toplanır. O zaman `coverageRatio = |kesişim| / toplam` **tek ve
iyi tanımlı** olur ve `Math.min` **gereksizleşir**.

> `Math.min` bir **semptom düzeltmesiydi** — farklı kapsamları uzlaştırmaya çalışıyordu.
> Kesişim onları **eşitliyor**.

## S1 — aynı turda, ayrı gidemez

`coverageRatio`'nun **üretim tüketicisi yok**: `plan.service.ts:2543-2551` JSONB alanlarını
tek tek sayıyor ve onu **yazmıyor**; `calculateKpis` (`:2686-2698`) geri kurarken de almıyor.

Yani kararın *"kapsama göster"* yarısı **yok**, *"rengi geri çek"* yarısı **var ve
kalıcılaşıyor**. İlk deploy'da bir COGS'suz SKU'su olan her FU'nun rengi **kaybolur** ve
kullanıcı **hiçbir açıklama görmez**.

> `T-098`/`T-101`'de kurduğumuz ilkenin ihlali: **sonuç geri çekiliyor, sebep taşınmıyor.**
> Ve bu oturumda üçüncü kez — ayırt edici üretip **okuyucusuz** bırakmak.

## S4 — belgelenmemiş davranış değişikliği, karara bağlanacak

FU seviyesindeki oran RAG'i *"çocukların en kötüsü"* (`:196-202`) → *"değere eşik uygula"*
(`:146`). Savunulabilir (bir oran için worst-of-children anlamsız) ama **commit mesajında,
task notunda ve kod yorumunda yok**.

⚠️ Ve yeni spec bunu **tesadüfen** pinliyor — fixture'da SKU RAG'leri null olduğu için.
*"Bir SKU RED, FU toplamı GREEN"* vakası **adıyla** eklenmeli.

## Durum
- `5bc2787` **staging'de**. Kalıcı hasar **yok**: `main.plans = 0`, `plan_skus = 0`,
  `overall_roi dolu = 0` (ölçüldü). Risk **ileriye dönük**.
- Kapsam dışına alınan: **S2** → [[T-190]]

---

## ✅ FU seviyesi kapandı — `341e2b2` + `5743c6e` (2026-08-11)

**Ölçüm** (üretim veri şekli, 170 SKU / 4'ünde COGS):

```
önce  %0,291        (karışık popülasyon)
sonra %47,272727    = elle hesaplanan dürüst kesişim oranı, birebir
coverageRatio 4/170
Math.min(depCoverages) → KALDIRILDI
```

`Math.min` bir **semptom düzeltmesiydi** — farklı kapsamları uzlaştırmaya çalışıyordu;
kesişim onları **eşitliyor**.

📌 Ve kesişim, eksik bağımlılığı olan çocuğu **paydadan da** düşürüyor — yalnız paydan
değil. Yeni test tam bunu ayırt ediyor (eski min-yaklaşımı `2/3`, kesişim `1/3`).

**S1** ✅ `coverageRatio` `calculatedKpis` JSONB'sine yazılıyor (`plan.service.ts:2564`),
geri okunuyor (`:2715`), `plan.entity.ts:304`'te tipli. Şema değişmedi.
**S4** ✅ gerekçesiyle iki çağrı noktasına yazıldı.

**Testler:** 1087 → **1089**, iki mutasyonla kanıtlandı.

## ⛔ Ama plan seviyesi AÇIK → [[T-191]]

`code-reviewer` ikinci turda blocker verdi: kesişim **FU'lar** üzerinde alınıyor, B1 ise
**SKU'lar** üzerindeydi. FU sonuçları zaten karışık popülasyon (`:188-191` SUM dalı her
`kpiCode` için ayrı filtreliyor) — FU kesişimi bunu geri alamaz.

> Blocker'ın sebebi **kod değil, iddia**: yorum ve commit mesajı *"plan da düzeldi"*
> diyordu. Üçü de düzeltildi (`5743c6e`), ve yanlış plan değeri artık `⛔ BU DEĞER
> YANLIŞ` + *"geri alınmamalı"* notuyla pinli.

## Kalan
- [ ] [[T-191]] — plan seviyesi kesişimi (**P1**, tasarım kararı)
- [ ] Frontend: kapsama gösterimi → [[T-172]] ile birleşiyor

---

## 📌 Dördüncü tanık: `Section_08 §8.1 Report 4` — kaynak **aritmetiğiyle** Σnum/Σden diyor (2026-08-11, `docs/analysis/0061 §1`)

`Section_08` `0059`'un envanter turuna kadar ⚪ ("gerekçeyle atla") işaretliydi; yeni ölçütle
okundu ve bu turun ilk bulgusu doğrudan bu task'ın kararına ait.

**Metrik adı:** *"Average GP ROI % (**weighted by spend**)"*
**Örnek çıktı, üç sayı birlikte:**

```
Total Planned Spend:  2.450.000 TL
Total Incremental GP:   598.000 TL
Weighted Avg GP ROI:       24.4% 🟢
```

**Ölçüm:** `598.000 / 2.450.000 = %24,41` → **24.4**. Yani kaynağın kendi sayısı
**ratio-of-sums**'a oturuyor; *"weighted by spend"* ile `Σ iGP / Σ spend` aynı ifadedir
(ağırlık payda olduğunda ağırlıklı ortalama, oranların toplamına indirgenir).

> **Adım 2'nin (`5bc2787`) uyguladığı okuma kaynakta hem kelimeyle hem sayıyla var.**

⚠️ **Ama bu task'ın açık sorusunu kapatmıyor.** `Section_08` **toplama şeklini** veriyor;
**kısmi null** bir planın ne göstereceğini söylemiyor. `Section_05 §5.3 Edge Case Handling`'in
`NULL` kuralları (`BASE_VOL = 0` · `TOTAL_PLANNED_SPEND = 0`) **satır** seviyesinde yazılmış
ve toplama düzeyine taşınmamış. `OPEN_DECISIONS` → [[T-177]] satırı **açık kalıyor**.
