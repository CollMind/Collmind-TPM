---
id: T-360
title: coverage payı ile paydası AYNI EVRENİ konuşmuyor — ratio > 1 mümkün
epic:
sprint:
status: todo
assignee: backend-engineer
created: 2026-09-03
updated: 2026-09-03
touches:
  - collmind.backend/src/modules/master-data/baseline-volume/services/baseline-volume-coverage.service.ts
---

## Açıklama

`code-reviewer` bulgusu `S2` (2026-09-03, `BL-4a` review'u).

```
PAY    baseline_volumes içinde tenant'ın TÜM ACCEPTED satırları
       — dönem filtresi YOK, PASİF SKU/CPL satırları DAHİL
PAYDA  aktif SKU × aktif CPL × 12
       — pasif HARİÇ, 12 döneme SABİT
```

⇒ Veri 12 dönemden fazlasına yayıldığında **ya da** pasif SKU/CPL satırları olduğunda
`coverageRatio > 1` ve **sahte `GREEN`** üretilebilir.

⚠️ **Bugün görünmüyor** çünkü pay `0` — `DISIPLIN`: *"verinin yokluğu örter"* (`T-273`
körlüğü). Yani kusur **veri geldiği gün kendiliğinden doğar**, bir regresyon turu olmadan.

📌 Kod bu turda **değişmedi**; ama `BL-4` onu ilk kez HTTP'ye açtı ⇒ kusur **erişilebilir**
oldu ve sayı **kullanıcıya gösteriliyor**.

## Ölçülmesi gerekenler (önce ÖLÇ, sonra düzelt)

- [ ] Reprodüksiyon: pasif bir SKU'ya ait `ACCEPTED` satır + 12'den fazla döneme yayılan
      satırlar kur ⇒ `coverageRatio > 1` **GÖRÜLSÜN** (görülmezse hipotez elenir, `Z-`
      reprodüksiyon şartı yönsüzdür)
- [ ] `12` sabiti nereden geliyor — bir ürün kararı mı, bir varsayım mı? (`Z90` okunacak)
- [ ] Payın evreni paydaya **eşitlenecek**: aynı aktiflik filtresi + aynı dönem penceresi

## Done tanımı
- [ ] `docker ps --filter "label=com.docker.compose.project=tpm"` boş
- [ ] Testler yeşil (unit + e2e), `npm run guards` exit 0, `money-float --ratchet` exit 0
- [ ] `code-reviewer` onayı
- [ ] Üretim çağrı yolu var (`GET /baseline-volumes/coverage` — VAR)

## ⚠️ EK (2026-09-03) — YENİ ÇAPRAZ-DOĞRULAMA TESTİ BU KUSURU YAKALAYAMAZ

`test/baseline-volume-diagnostics-surface.e2e-spec.ts:506+` coverage'ı **bağımsız SQL** ile
çapraz doğruluyor — iyi bir test. **Ama payı servisle AYNI şekilde türetiyor:**

```sql
SELECT COUNT(*) FROM main.baseline_volumes
WHERE tenant_id = $1 AND acceptance_status = 'ACCEPTED'    -- dönem filtresi YOK, aktiflik filtresi YOK
```
```ts
const expectedUniverse = activeSkuCount * activeCplCount * 12;   -- 12 SABİTİ KOPYALANDI
```

⇒ `§2.7 #8` sınıfı: **test, sınadığı kontrolün bir KOPYASINI çalıştırıyor.** Servisin
tek başına regresyona uğramasını yakalar; **ortak yanlış varsayımı yakalamaz.**
`ratio > 1` günü geldiğinde bu test **yeşil kalır**.

📌 `T-360` düzeltilirken **testin de bağımsız bir kaynaktan türetilmesi** gerekir —
ör. payın hangi (sku, cpl, period) üçlülerine denk geldiğini **sayarak**, bir formülü
kopyalayarak değil.
