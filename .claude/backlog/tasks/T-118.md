---
id: T-118
title: "Ölü `PlanningGrid.tsx` kapattığımız kusurların tam kopyasını taşıyor"
epic:
sprint:
status: todo
assignee: frontend-engineer
priority: P3
created: 2026-08-09
updated: 2026-08-09
touches:
  - collmind.frontend/src/components/features/plans/PlanningGrid.tsx
migration_seq:
---

## Bulgu (T-109 2b review, ölçüldü)

`src/components/features/plans/PlanningGrid.tsx` **hiçbir yerden import edilmiyor**
(grep: `from '...PlanningGrid'` → 0 eşleşme). `PlanDetailPage.tsx` `PlanningGridEnhanced`'i
`PlanningGrid` **takma adıyla** alıyor — isim benzerliği bu dosyayı canlı gösteriyor, değil.

İçinde bugün duranlar:

- `parseFloat(value)` (`:449`) — CLAUDE.md: *"`parseFloat` para yolunda asla doğru araç
  değildir — hata vermez, kırpar"*
- `<input type="number">` (`:765`) — tr-TR değeri **tutamaz**, tarayıcı temizler ([[T-106]])
- `if (isNaN || < 0) { setEditingCell(null); return; }` — okunamayan girdiyi **sessizce atma**
  (§2.5, [[T-112]])
- `Escape → onCellSave(currentValue)` — Escape'in **yazması** ([[T-112]]: gerçek PATCH,
  version bump, audit satırı, başkasına 409)
- 50 ms focus `setTimeout` — [[T-109]] 2b'de ölü kod olarak kaldırılan desen

Yani T-106 · T-109 · T-110 · T-112'de kapatılan **her kusur** bu dosyada bir arada duruyor.

## Neden P3 değil de kaydedilmeli

§7'nin doğrudan konusu: *"Bu projede aynı yetenek birden çok kez yazıldı."* Ölü bir dosya
zarar vermez — **kopyalanana kadar.** Ve bu dosya, adı ve şekli itibarıyla tam olarak
kopyalanmaya davetiye: yeni bir grid gerektiğinde "zaten bir `PlanningGrid` var" denip
buradan türetilirse, dört task'ın kapattığı kusurlar tek seferde geri gelir.

Ayrıca ikinci bir doğruluk kaynağı: bir okuyucu hangi dosyanın canlı olduğunu isimden
ayırt edemiyor.

## Yapılacak

- [ ] Dosyanın gerçekten ölü olduğu **yeniden** doğrulansın (import + dinamik `lazy()` +
      route tanımları — "grep boş" tek başına yeterli değil, `React.lazy`/string yolu ara)
- [ ] Silinsin. Tarihsel değeri git geçmişinde duruyor
- [ ] Silme sonrası `npm test -- --run`, `npx playwright test`, `npm run type-check` yeşil
      (⚠️ type-check bugün testleri kapsamıyor — bkz. [[T-116]])

## Done tanımı
- [ ] Ölülük ölçüldü (yalnız grep değil), dosya silindi
- [ ] Kapılar yeşil, exit kodları boruya sokulmadan
- [ ] `code-reviewer` onayı
