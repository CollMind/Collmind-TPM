---
id: T-342
title: RAG iki-eksen kadran inişi + LTA-only planlarda TANIMLI-YOKLUK
epic:
sprint:
status: review
assignee: backend-engineer
created: 2026-08-31
updated: 2026-08-31
touches:
  - collmind.backend/src/common/kpi/rag-quadrant.ts            # YENİ — kanonik tek nokta
  - collmind.backend/src/modules/shared/kpi-engine/kpi-engine.service.ts
  - collmind.backend/src/modules/shared/kpi-engine/kpi-engine.service.spec.ts
  - collmind.backend/src/modules/modes/planning-first/plan/plan.service.ts
  - collmind.backend/src/database/entities/plan.entity.ts      # yalnız JSONB tip yorumu
  - collmind.backend/test/formula-canon-turnover-niv-and-rag-quadrant.e2e-spec.ts
  - collmind.frontend/src/utils/ragCoverage.ts
  - collmind.frontend/src/api/endpoints/plans.endpoints.ts
  - collmind.frontend/src/components/features/plans/grid-cells.tsx
  - collmind.frontend/src/components/features/plans/PlanningGridEnhanced.tsx
  - collmind.frontend/src/components/features/plans/PlanList.tsx
  - collmind.frontend/src/components/features/plans/GrandTotals.tsx
  - collmind.backend/src/common/kpi/rag-quadrant.spec.ts               # YENİ (S3)
  - collmind.backend/src/database/migrations/1819000000000-AddRagExclusionReasonToPlans.ts
  - collmind.backend/src/modules/shared/finance-reporting/             # DTO + service
  - collmind.backend/src/modules/modes/planning-first/plan/approval-workflow.service.ts
  - collmind.backend/src/modules/modes/actuals-first/agreement/agreement.service.ts  # N3 yorumu
  - collmind.frontend/tests/utils/ragCoverage.test.ts                  # YENİ (S3)
  - collmind.frontend/src/components/finance/widgets/PlanPerformanceWidget.tsx
  - collmind.frontend/src/utils/export.ts
migration_seq: 1819000000000  # B2 / Z71 §2 — plans.rag_exclusion_reason
# ⚠️ ilk turda "YOK — hesaplanan-değer davranışı (Z68 §1)" yazıyordu ve `Q7`
# kadranı için DOĞRUydu; `S1`'in üç yüzeye inişi (Z71 §2) kolonu şart yaptı.
---

## `A` · `Q7` KADRAN İNİŞİ (`Z66 §2` hükmü, `Z68 §1` iniş kararı)
```
Red     iTO ≤ 0
Amber   iTO > 0  ∧  iGP ≤ 0        ← "satış var, kâr yok"
Green   ikisi de > 0
```
⛔ **Migration YOK** — hesaplanan-değer davranışı. `plans=0` penceresi **gerekmiyor**.
✅ Dört kadran fixture'ı `formula-canon-…-quadrant.e2e-spec.ts`'te **HAZIR**; her hücre
**literal `liveRag` sabiti** taşıyor ⇒ **bu iniş o sabitleri BİLEREK kıracak.**

## ⛔ `A0` · ÖN-ÖLÇÜM — **`AMBER` İLK KEZ DOĞUYOR**
Bugün hiç üretilmeyen bir durum yarın üretilecek. **Tüketicileri tanıyor mu?**
Team Lead ön-ölçümü *(rigorous hâlini SEN yap)*: `AMBER` tip birleşimlerinde
(`plans`/`dashboard`/`finance-reporting`/`budget`/`on-invoice` endpoints), renk
haritalarında (`PlanPerformanceWidget:67`, `BudgetUtilizationWidget:39`) ve
`BudgetAtRiskWidget`'te **tanınıyor görünüyor**.
⚠️ **YAN BULGU — ölç:** `ragAmberThreshold` (`kpi.endpoints.ts:25,53`,
`kpi.repository.ts:101`) **tek-eksen eşiğinin konfigürasyon alanı**. İki-eksen kadranda
**ölü mü kalıyor, hâlâ okunuyor mu?** Ölü kalıyorsa `@deprecated` **bir niyet beyanıdır,
bir ölçüm değil** — okuyanları say.

## `B` · `S1` — **RENKSİZLİK ÖLÜR, TANIMLI-YOKLUK DOĞAR** (`Z68 §2`)
LTA-only plan bir **promosyon değerlendirmesi değildir** (incremental ekseni yok; plan,
baseline'ın **sözleşmeli hâli**).
```
RAG'ı ZORLA üret  → ANLAMSIZ SAYI    ⛔      boş bırak → SESSİZ BOŞLUK  ⛔
⇒ ÜÇÜNCÜ DURUM
```
| yüzey | şekil |
|---|---|
| API | `ragStatus: null` **+** `ragExclusionReason: 'LTA_ONLY'` sınıfı **açık alan** |
| UI | gri/nötr **"Değerlendirme dışı — LTA"** rozeti + tooltip'te **tek cümle** gerekçe |

⇒ Filtre/rapor tüketicileri **"Red değil"** ile **"değerlendirilmedi"**yi ayırt eder.
📌 `T-323` UI dersinin RAG hâli: *"yetkin yok ≠ kimse yok"* neyse, *"kötü değil ≠
değerlendirilmedi"* o.

⛔ **`ragExclusionReason` bir ENUM/sınıf olmalı**, serbest metin değil — ve `LTA_ONLY`
**tek üyesi** olarak doğar. `İlke 1`: bugün ihtiyaç olmayan üye **açılmaz**.

## Acceptance Criteria
- [ ] `determineRagStatus` **iki-eksen kadran**; `iTO`/`iGP` kaynağı **tek noktadan**
- [ ] Dört kadran pini **bilerek kırıldı ve yeni sabitlere çevrildi** *(kırılma **görülmeden**
      sabit değiştirilmez — reprodüksiyon şartı)*
- [ ] ⭐ **BEKLENEN-DEĞİŞİM LİSTESİ — ampirik önce/sonra** (`T-334` emsali): hangi hücre
      hangi renkten hangi renge, **ölçülmüş değerlerle**
- [ ] `AMBER`'ın **ilk doğuşu** raporda: tüketici taraması + `ragAmberThreshold`'un âkıbeti
- [ ] `ragExclusionReason` **açık alan**; `null` RAG ile **birlikte** dönüyor
- [ ] **Frontend renk eşlemesi aynı dalgada doğrulandı** — rozet + tooltip
- [ ] Bir tüketici `AMBER`'ı ya da `ragExclusionReason`'ı **tanımıyorsa** ⇒ **DUR ve bildir**

## Done tanımı
`CLAUDE.md §4.2` + ⛔ ilk madde (hayalet `tpm`) · `tsc` BE **ve** FE 0 · unit BE+FE ·
**hedefli** e2e *(tam koşumu Team Lead)* · `guards` 0 · ratchet'ler 0 · `improved:` yok

## Kaynak
`Z66 §2` · `Z68` · `docs/research/A1_KPI_ESLEME.md` · `T-334` kapanış raporu


---

## KAPANIŞ ÖLÇÜMLERİ (`T-342`, 2026-08-31)

### `A0` · `AMBER` tüketici taraması — ⛔ **ÖNCÜL DÜZELTİLDİ (`Z71 §0`)**

> **YAZILAN:** *"`AMBER` İLK KEZ DOĞUYOR"* — **YANLIŞ.**
> **ÖLÇÜM (canlı `main.kpis`):** `GP_ROI_PCT` `rag_green=20 · rag_amber=10`
> ⇒ eski tek-eksen model `10 ≤ ROI < 20` aralığında **`AMBER` ÜRETİYORDU**.
> **DOĞRUSU:** *"**negatif ROI'den** `AMBER`"* — eşik modelinin
> **ÜRETEMEYECEĞİ** renk (`HÜCRE 2`: ROI `-%60,13`).
>
> ⛔ **Bedeli bir yazım hatası değil, bir EKSEN KİLİTLENMESİ:** aşağıdaki
> tarama *"yeni bir renk tanınmıyor mu"* ekseninde yapıldı; **gerçek risk
> `RED→GREEN`**'di ve o eksende **hiç tarama yoktu**. `B1`/`Q13` turunda
> kapatıldı. *(Öncülü Team Lead ölçtü, ürün sahibi hüküm-şartı yaptı,
> ajan sorgulamadı — üç taraf da.)*
Aranan (`rg -i`, poz. kontrol: `amber-` Tailwind sınıfları da eşleşti ⇒ desen kör değil):
BE `src` 37 eşleşme · FE `src` 79 eşleşme.
**Karar noktaları ayrı ayrı ölçüldü** (tip birleşiminde ad geçmesi *"tanıyor"* sayılmadı):

| yüzey | kanıt | sonuç |
|---|---|---|
| risk raporu kovası | `finance-reporting.service.ts:872` filtre `['RED','AMBER']` · `:920` `else if === 'AMBER'` | ✅ |
| risk raporu DTO/öneri | `risk-report.dto.ts:27,65,81` · `:952` öneri metni | ✅ |
| plan performans filtresi | `plan-performance.dto.ts:68,72` `@IsEnum([...,'AMBER',...])` | ✅ |
| FE renk haritaları | `PlanPerformanceWidget:67` · `ragColorUtils:4,10` · `grid-cells:579,588` · `PlanList:72` · `GrandTotals:52` · `PlanningGrid:77` | ✅ |
| FE dışa aktarım | `export.ts:99` (`AMBER → 'RİSKLİ'`) · `:219,231` | ✅ |

⛔ **`default` dalı taraması sonucu — TEK BULGU (ürün kararı gerekiyor, DEĞİŞTİRİLMEDİ):**
`approval-workflow.service.ts:146` submit öncesi **yalnız `RED`** için uyarı üretir.
Kadran öncesi `iGP ≤ 0` olan her plan `RED`di (ROI negatif ⇒ eşik `RED`); kadran sonrası
`AMBER` oluyor ⇒ **o planlar submit uyarısını KAYBEDİYOR.** Bu bir hüküm sonucudur,
bir hata değil — ama `DISIPLIN`'in *"hata sınıfı daha mı sessiz oldu?"* sorusuna
**evet** diyen tek yüzey. Kararı ürün sahibi verir (`§2.4`).

### `A0b` · Eşik alanlarının âkıbeti — **ASİMETRİK** (⇒ `T-343`'ün girdisi)
| alan | davranışsal okuyucu | sonuç |
|---|---|---|
| `ragGreenThreshold` | `kpi-engine` eşik yolu **+** `plan.service.ts:2892` **Target ROI** (`ABOVE_/ON_/BELOW_TARGET`) | **ÖLÜ DEĞİL** — RAG'dan bağımsız ikinci tüketici var |
| `ragAmberThreshold` | yalnız `kpi-engine.service.ts:456` (`determineRagStatus`) | **tüketicisiz** — yol canlı ama bugün girdisi yok |

Eşik yolu **silinmedi**: taşıyıcı dışındaki KPI'lar için okunmaya devam ediyor.
Bugün kataloğda eşiği olan tek KPI taşıyıcının kendisi (`kpi.seed.ts:537-538`) ⇒
`ragAmberThreshold` bugün **ulaşılabilir ama beslenmiyor**. Admin CRUD yüzeyi
(`KpiManagementPage`, DTO, repository) hâlâ onu yazıyor.
⛔ Kaldırma **bu turun işi değil** — ayrı task olarak bildirildi.

### ⭐ BEKLENEN-DEĞİŞİM LİSTESİ — AMPİRİK ÖNCE/SONRA
Fixture: `price=100 · cogs=60` (DB'den okundu, `FU-E2E-GRID-SINGLE-SKU`).

| hücre | `iTO` | `iGP` | `INCR_PROMO_SPEND` | `GP_ROI_PCT` | ÖNCE | SONRA |
|---|---:|---:|---:|---:|---|---|
| 1 | **+87.475,80** | **+27.475,80** | 3.701,40 | %742,31 | `GREEN` | `GREEN` (değişmedi) |
| 2 | **+2.269,20** | **−9.730,80** | 16.182,00 | %−60,13 | `RED` | ⭐ **`AMBER`** |
| 3 | **−21.083,10** | **−15.083,10** | 12.136,50 | %−124,28 | `RED` | `RED` (değişmedi) |
| 4 | **−11.700,00** | **+300,00** | **0,00** | `null` | `null` | `null` + **`LTA_ONLY`** |

### ⛔ REPRODÜKSİYON — pin kırılması GÖRÜLDÜ
```
1  değişiklik ÖNCESİ koşum   exit 0 · 6/6 yeşil                 (poz. kontrol: suite gerçekten koşuyor)
2  kadran indi, SABİT YOK    exit 1 · HÜCRE 2  Expected "RED" / Received "AMBER"
3  sabitler yenilendi        exit 0 · 6/6 yeşil
```

### `S1`'in KADRAN DÜNYASINDAKİ YENİ TETİKLEYİCİSİ (mutasyon kanıtlı)
`T-334`'te `HÜCRE 4` renksizdi çünkü payda `0` ⇒ `GP_ROI_PCT = null` ⇒ **yan etki**.
Kadranda iki eksen de **dolu** (`−11.700` / `+300`) ⇒ kadran **`RED`** üretir.
Mutasyon (`if (false && incrPromoSpend === 0)`, satır basıldı, `shasum -c` ile geri alındı):
```
kapı KAPALI  → HÜCRE 4: Expected null / Received "RED"     ← kapı GERÇEKTEN tutuyor
kapı AÇIK    → HÜCRE 4: null + ragExclusionReason 'LTA_ONLY'
```
⇒ Yeni tetikleyici **`INCR_PROMO_SPEND === 0`** (tam sıfır; `null`/`undefined`
sessizce sıfır sayılmaz — o eksik VERİ sınıfına düşer).

### ✅ KAPANDI — plan seviyesinde sebep artık taşınıyor (`Z71 §2`, `B2`)
İlk turda `plans.rag_exclusion_reason` kolonu yoktu ve rapor *"Faz-1: yalnız
grid"* diye bir not düşmüştü. ⛔ Review o notu **`T-084` problemi** olarak
adlandırdı: *bir kusuru belgelemek onu koruma altına alır.* Migration
**`1819000000000`** kolonu getirdi (ölçüldü: `main.plans` **0 satır** ⇒ maliyet
sıfır) ve ayrım artık **dört yüzeyde** konuşuyor: `PlanList` · `GrandTotals` ·
`finance-reporting` (risk raporu + plan-performans DTO) · `approval-workflow`.

⚠️ Çıkarım yolu ölçüldü ve **kapalı** kaldı: `ragStatus null && coverageRatio 1`
bileşimi *"eksenlerden biri kısmi kapsamalı"* durumuyla da eşleşiyor ⇒ kolonsuz
ayrım **sessiz bir tahmin** olurdu.

### ✅ KAPANDI — `approval-workflow` `AMBER` bulgusu (`Z70 §1` · `Z71 §1`)
İlk turun *"tek bulgu, ürün kararı gerekiyor"* kalemi hükümle kapandı ve
**üç dilim birden** ele alındı — bkz. `T-343` kapanış notu.
