---
id: T-279
title: "`GET /agreement-transactions?status=` SESSİZCE yok sayılıyor — frontend gönderiyor"
epic: E-001
sprint: S-001
status: todo
assignee: backend-engineer
created: 2026-08-24
updated: 2026-08-24
touches:
  - collmind.backend/src/modules/modes/actuals-first/agreement-transaction/agreement-transaction.controller.ts
---

## Bulgu — `T-277` turunun yan bulgusu, lint kapsamı genişleyince göründü

```
backend   agreement-transaction.controller.ts:118
            @Query('status') status?: string     ← BİLDİRİLİYOR
          gövde                                   ← HİÇ KULLANILMIYOR
          (ESLint AST analizi: 'status' is defined but never used)

frontend  off-invoice.endpoints.ts:177   status?: string     ← filtre tipinde VAR
                                  :181   { params: filters } ← GÖNDERİYOR
                                         → apiClient.get('/agreement-transactions')
```

> **Kullanıcı statüye göre filtreliyor, sunucu filtresiz liste dönüyor.**

## Sınıf — `T-274`/`T-276` ailesi

```
T-274   dashboard cplIds'i HESAPLAYIP geçiriyor, tüketici HİÇ OKUMUYOR
T-276   /approvals/pending userId'yi ALIYOR, WHERE'e KOYMUYOR
T-279   status QUERY'de ALINIYOR, filtreye KOYULMUYOR      ← bu
```

📌 `§2.5`'in **sessiz no-op** yüzü: hata yok, boş sonuç yok — **yanlış sonuç**.
Ve `T-242a`'nın `scope` için verdiği karar burada da geçerli: *"sessiz no-op'u AÇIK bir
hataya çevirir."*

## Nasıl göründü — ve bu kayda değer

Parametre **pre-existing**; `T-277` controller'a dokununca dosya **`npm run lint`
kapsamına girdi** ve hata yüzeye çıktı.

⚠️ `T-100`'ün deseni: *"kapının kapsamı dinamik"* — `changed-ts.sh` yalnız değişen
dosyaları lint'liyor. **Bu kusur, dosyaya dokunulmadığı sürece görünmüyordu.**

📌 `lint-ratchet-baseline:112` onu **zaten tanıyor** (`no-unused-vars 1`) — yani ratchet
**bilerek taşıyordu**, ama *"neden unused"* sorusu hiç sorulmamıştı.

## Karar gerekiyor — üç şık

| # | seçenek | not |
|---|---|---|
| **a** | filtreyi **uygula** | `status` gerçek bir kolon mu? `agreement_transactions`'ta **`status` alanı YOK** (`T-277` ölçümü) — o hâlde neyi filtreleyecek? |
| **b** | parametreyi **kaldır** | API yüzeyi daralır; frontend'in `status?:` alanı da temizlenir. ⚠️ `§ serileştirme`: bir alanın düşmesi hem regresyon hem **kusur kapanması** olabilir |
| **c** | `400` ile **açık ret** | `T-242a` deseni — *"sessiz no-op'u açık hataya çevir"* |

⛔ **`(a)` ölçüm gerektiriyor:** `agreement_transactions` şemasında filtrelenecek bir
statü var mı? `T-277` *"ara statü YOK"* diye ölçmüştü — o hâlde parametrenin **karşılığı
olmayabilir**, ve `(b)`/`(c)` doğru olur.

## Acceptance Criteria
- [ ] `agreement_transactions`'ta filtrelenebilir bir statü **var mı** — katalogdan ölç
- [ ] Karar verildi ve gerekçesi yazıldı
- [ ] **Davranışsal:** `?status=X` gönderen istemci ne alıyor — **iki girdi iki çıktı**
- [ ] ⚠️ **Çapraz-repo:** `(b)` seçilirse frontend'in `status?:` alanı **aynı turda**
      temizlenir (`T-277`'nin eşzamanlılık dersi)
