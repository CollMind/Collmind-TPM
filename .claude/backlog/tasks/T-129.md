---
id: T-129
title: "İki parser'ın dört getter'ı byte-for-byte aynı — §7 turu yarım kaldı"
epic:
sprint:
status: todo
assignee: backend-engineer
priority: P2
created: 2026-08-10
updated: 2026-08-10
touches:
  - collmind.backend/src/common/row-parsing/
  - collmind.backend/src/modules/modes/actuals-first/on-invoice/services/on-invoice-file-parser.service.ts
  - collmind.backend/src/modules/modes/actuals-first/agreement-transaction/services/off-invoice-file-parser.service.ts
migration_seq:
---

## Bulgu ([[T-126]] review, gövde karşılaştırmasıyla programatik doğrulandı)

`getDateValue` · `getNumberValue` · `getStringValue` · `getOptionalString` —
`off-invoice` ≡ `on-invoice`, **byte-for-byte**.

Ve `getDateValue`'nun kendi dokümanı bunu **itiraf ediyor**: *"Mirrors
`OnInvoiceFileParserService.getDateValue` **exactly**"*.

⚠️ [[T-126]] `FieldParseError`'ı §7 gerekçesiyle ortak yere taşıdı (*"about to become a
second/third copy"*) ama **davranışı** ikinci kopya olarak bıraktı.

## ⚠️ Ve bunun bedeli aynı turda ölçüldü

Review'ın B1'i (boşluklu hücrenin yokluk testi) **iki kopyada da** vardı — tek bir kusur, iki
yerde düzeltilmek zorunda kaldı. Bir sonraki kusur da öyle olacak.

> §7'nin gerekçesi bu: kopya, kusuru de kopyalar.

## Yapılacak

- [ ] Dört getter'ı `src/common/row-parsing/` altına taşı (`field-parse-error.ts`'in yanına)
- [ ] ⚠️ **Taşımadan önce farkları ölç** — "byte-for-byte aynı" iddiası `getDateValue`/
      `getNumberValue`/`getStringValue`/`getOptionalString` için ölçüldü; **diğer getter'lar
      için ölçülmedi** (`getFiscalPeriod` bilinçli olarak farklı: on-invoice fırlatıyor,
      off-invoice topluyor — [[T-126]]'da gerekçesiyle pinlendi, **birleştirilmemeli**)
- [ ] `customer` parser'ının ikizleri de aynı sınıfta mı — **ölç** (§7.1: kalıbın her ucu)
- [ ] Mutasyonla: ortak getter'da bir kusur → **her iki importer'ın** testleri kırmızı

## Done tanımı
- [ ] Tek tanım, iki tüketici
- [ ] `getFiscalPeriod` asimetrisi **korunmuş** ve pini hâlâ ayırt edici
- [ ] `npm run guards` 0 · `ratchet` 0 (**36/54/10**) · e2e yeşil
- [ ] `code-reviewer` onayı
