---
id: T-388
title: "SEED IDEMPOTENCY ANAHTARI **DÖNEM TAŞIMIYOR** — hüküm 18'in göreli-seed'i SESSİZCE etkisiz kalır"
epic:
sprint:
status: todo
assignee: data-engineer
created: 2026-09-08
updated: 2026-09-08
deadline:            # ⛔ KALKTI — 1 Ekim ÖLÇÜLDÜ, risk YOK (aşağı bak)
touches:
  - collmind.backend/src/database/seeds/budget-envelope.seed.ts
  # ⛔ ÖLÇÜLDÜ 2026-09-08: AYNI SINIFTA — ama DÜZELTİLMEDİ, AÇIK BULGU.
  # "Ölçüldü" ile "düzeltildi" AYNI ŞEY DEĞİLDİR. 1 Ekim riski: aşağıdaki bloğa bak.
  - collmind.backend/src/database/seeds/agreement.seed.ts
---

## Nasıl doğdu

`T-385` şeridi, brief'in `§4` (demo-seed ölçümü) başlığı altında **brief'te olmayan** bir şey
buldu — ve bu, ölçmesi istenen üç sorunun **hiçbiri değildi**:

```
[ÖLÇÜLDÜ: budget-envelope.seed.ts:187,189-190]
  code = `ENV-2026-${categoryCode}`                       ⛔ DÖNEM BİLGİSİ TAŞIMIYOR
  if (existing) { created.push(existing); continue; }      ⛔ ve SESSİZ
```

> ### ⛔ Hüküm 18 (`Z108 §1.1`) *"demo-seed tenant-bugüne göre içinde bulunulan çeyreği
> ### üretir"* diyor. **Anahtar dönem taşımadığı için, seed kodu göreli yapılsa bile
> ### `existing` bulunur ve satır HİÇ GÜNCELLENMEZ — hüküm SESSİZCE etkisiz kalır.**

## Sınıfı — `1/3-doğru-seed` ailesi

```
mekanizma   DOĞRU     (göreli dönem üretimi)
anahtar     YANLIŞ    (döneme duyarsız)
kapı        YOK       ← seed "başarıyla" koşar, çıktısı YEŞİL görünür
```
📌 `T-273` ailesiyle akraba: orada **verinin yokluğu** örtüyordu, burada **bir eşleşmenin
varlığı** örtüyor — `existing` bulundu diye iş yapılmadı, ve **yapılmadığı söylenmedi**.

## İKİ DÜZELTME **BİRLİKTE** (ayrılamaz — hüküm 23, `Z108 §6`)

```
1  ANAHTAR DÖNEME ÖZGÜ
   `ENV-${period_from}-${cat}`   ya da   (code, period_from) BİLEŞİK TEKİLLİK
   ⛔ hangisi — ÖLÇÜMLE seçilir: `code` bugün başka nerede REFERANS alınıyor?
     (e2e fixture'ları · migration'lar · `1832`'nin taşıdığı satırlar)
     Bir kod bir KİMLİKTİR (hüküm 13) — biçimini değiştirmek GEÇMİŞİ etkiler mi, ÖLÇ.

2  İDEMPOTENT SEED NE ATLADIĞINI **BASAR**
   ⛔ `continue` SESSİZ OLAMAZ (`§2.5`) — atlanan her satır, SEBEBİYLE birlikte yazılır
   ⇒ "seed-sözleşme-kapısı" adaylığının ÜÇÜNCÜ üyesi
```

## ⛔ SIRA VE TARİH — İKİSİ DE BAĞLAYICI

```
SIRA    anahtar  →  göreli seed        (hüküm 18 bu anahtar İNMEDEN uygulanmaz)
YER     DALGA-A'dan SONRA, tek KÜÇÜK tur
TARİH   ⛔ 2026-09-30'dan ÖNCE — demo canlılığı (T-385 ile aynı takvim sınırı)
```

## Acceptance
- [ ] ⛔ **ÖNCE EVREN:** `code` alanının **kaç referansı** var — e2e · migration · seed ·
      üretim kodu. **SAYI DEĞİL LİSTE.** Biçim değişikliği bir **kimlik** değişikliğidir.
- [ ] Anahtar döneme özgü · `continue` **konuşur** — ikisi **aynı turda**
- [ ] ⛔ **AYIRT EDİCİLİK:** dönemi değişmiş bir seed'i **yeniden koş** → satır **güncellenmeli**
      (ya da açıkça *"atlandı, çünkü …"* basmalı). Düzeltme öncesi bu koşum **sessizce hiçbir
      şey yapmalı** — bilinen-kırmızı budur (`Z83`)
- [ ] `K-2.2.1b` kesişme invaryantı: göreli üretim **çakışan aralık** üretmiyor
      (ardışık çeyrekler örtüşmez — [ÖLÇÜLDÜ: `L2_01…md:486`] — ama **yeniden doğrula**)
- [ ] Migration gerekiyorsa ⛔ **DUR** → Team Lead → numara tahsisi (`CLAUDE.md §3`)
- [ ] Bu iner inmez **hüküm 18** (göreli seed) uygulanır — **aynı turda ya da hemen ardından**

---

## ✅ `budget-envelope.seed.ts` KAPANDI (2026-09-08) — ama **KARDEŞİ AÇIK**

### Tasarım kararı — ⛔ `code` FORMATI **DEĞİŞMEDİ**, ve gerekçesi ÖLÇÜM

Task'ın `1` numaralı maddesi *"anahtar döneme özgü: `ENV-${period_from}-${cat}` **ya da**
`(code, period_from)` bileşik tekillik — **hangisi ÖLÇÜMLE seçilir**"* diyordu. Şerit ölçtü
ve **ikincisini** seçti:

```
[ÖLÇÜLDÜ: rg -n "ENV-2026-" src/ test/ scripts/ → 64 satır, 15 dosya]
  dört MIGRATION (1827 · 1829 · 1831 · 1832) bu kodları LİTERAL olarak taşıyor
[ÖLÇÜLDÜ: 1832000000000-DemoPeriodQ3Realignment.ts:103-117]
  sekiz zarfı TAM BU literal kodlarla bulup Q2→Q3'e taşımış
[ÖLÇÜLDÜ: budget-envelope.entity.ts:44]  @Index(['tenantId','code'], { unique: true })
```
> ### ⛔ Koda dönem gömmek, `1832`'nin **zaten Q3'e taşıdığı** satırın yanına **aynı
> ### kategori + aynı dönemde İKİNCİ bir satır** insert etmeye çalışırdı ⇒ `K-2.2.1b`
> ### **kesişme trigger'ına** (`1831`) çarpar, ve seed **bugün kırılırdı**.

⇒ Seçilen: `code` **kimlik olarak sabit** (hüküm 13); idempotency artık
`(code, periodFrom, periodTo)` karşılaştırır. **`[ÖLÇÜLDÜ: Team Lead, pg_trigger]`** —
trigger canlı, gerekçe doğrulandı.

### `Z83` — üç durum, üçü de ölçüldü
```
bilinen-KIRMIZI  (düzeltme ÖNCESİ, stale Q2 durumda)  "✅ Seeded 10 budget envelopes"
                 ⇒ BAŞARILI görünüyor, satırlar 2026-04..2026-06'da KALIYOR, HİÇ LOG YOK
bilinen-YEŞİL    "ENV-…-SAC-BOYASI dönemi GÜNCELLENDİ (2026-04..2026-06 → 2026-07..2026-09)
                  — tenant-bugün (2026-09-08) çeyreği değişti (hüküm 18)"
idempotent NO-OP  "… zaten GÜNCEL dönemde (…) — ATLANDI."   ⛔ SESSİZ DEĞİL
```
```
[ÖLÇÜLDÜ: Team Lead, docker exec psql]
  Σ=2300000.00 · satır=8 · dönem=2026-07..2026-09   ⇒ imzalı tablo AYNEN
```

---

## ⛔ AÇIK BULGU — `agreement.seed.ts` **AYNI SINIFTA**, ve **1 EKİM RİSKİ TAŞIYOR**

```
[ÖLÇÜLDÜ: agreement.seed.ts:493-503]
  STA-2026-0001 · STA-2026-0002 · LTA-2026-0001 sabit `agreementCode` ile bulunuyor;
  bulununca tarih/dönem HİÇ KARŞILAŞTIRILMADAN sessizce created.push(existing)
```
Şerit bunu **ölçtü ama düzeltmedi** — gerekçesi kayıtlı (tarih-kaydırma algoritması
gerektiriyor, *"tek küçük tur"*un dışında). ⛔ **Ama risk ölçülmedi ve gerçek:**

> ### `1 Ekim` sonrası bir **yeniden kurulum**da zarflar **göreli seed** ile **Q4**'e
> ### gider, anlaşmalar **Q3**'te kalır ⇒ **`Z107 §1`'in TAM TEKRARI**: seed'in dönem
> ### kararı ile fixture'ın dönemi ayrışır, ve aradaki uyuşmazlığı bu kez **hiçbir
> ### bulanık eşleşme örtmez** — çünkü onu `DALGA-B` öldürdü.

```
⛔ [ÖLÇÜLMEDİ — ölçülecek: faketime 2026-10-01 altında TAM RESEED]
   zarflar Q4 · anlaşmalar Q3 olduğunda kaç e2e suite düşüyor?
   ⇒ Bu, hüküm 18'in YARIM uygulanmasının bedelidir ve ölçülmelidir.
```
📌 Ve sınıf tanıdık: **göreli seed bir yarıya uygulandı, diğer yarı sabit kaldı** —
`Z107 §1`'i doğuran asimetrinin ta kendisi, yalnız tarafları değişmiş.

## `touches` — DÜZELTME (şeridin kendi notu)
`agreement.seed.ts` satırı **`⛔ ÖLÇÜLDÜ: AYNI SINIFTA — ama DÜZELTİLMEDİ, AÇIK BULGU`**
olarak işaretlenir. *"Ölçüldü"* ile *"düzeltildi"* aynı şey değildir.

---

## ⭐ `1 EKİM` ÖLÇÜLDÜ — VE **RİSK YOK**. İDDİAM ÇÜRÜDÜ (Team Lead, 2026-09-09)

Bu task'a şunu yazmıştım ve **ölçmemiştim**:

> ~~*"1 Ekim sonrası bir yeniden kurulumda zarflar Q4'e gider, anlaşmalar Q3'te kalır
> ⇒ `Z107 §1`'in TAM TEKRARI"*~~

⛔ **ÖLÇÜLDÜ — İKİ DÜNYA + POZİTİF KONTROL**, `faketime 2026-10-01`, TAM e2e, `npx` YOK:

```
[ÖLÇÜLDÜ: faketime -f "2026-10-01 12:00:00" node node_modules/.bin/jest --config ./test/jest-e2e.json --runInBand]

DÜNYA A   zarflar Q4 (2026-10..12) · anlaşmalar Q3'TE KALIR
          → 64 suite / 887 test  ✅  DÜŞEN: HİÇBİRİ

DÜNYA B   zarflar Q4 · anlaşmalar DA Q4 (+3 ay)
          → 64 suite / 887 test  ✅  DÜŞEN: HİÇBİRİ

POZİTİF   seed zarfları TAMAMEN kaldırıldı (status=ARCHIVED)
KONTROL   → 62 suite / 883 test · EXIT 1 · DÜŞEN:
             FAIL test/variance-analysis-comparison-type-contract.e2e-spec.ts
             FAIL test/budget-variance.e2e-spec.ts
```

> ### ⇒ **e2e, seed zarflarının VARLIĞINA bağlı — DÖNEMİNE DEĞİL.**

Sebep ölçülmüş bir tarihte yatıyor: **`Z107 §3`** her ilgili e2e'yi **kendi zarfını
sabit dönemde kuracak** hâle getirdi (`ensureBudgetEnvelope`). O tur, bu riski **zaten
bağışıklamış**.

### ⇒ HÜKÜM İÇİN: **GÖRELİ SEED'İN KAPSAMI = YALNIZ ZARF**
```
zarf      göreli olmalı   ✅ İNDİ (bu task)
anlaşma   göreli OLMASINA GEREK YOK — ölçüldü, e2e etkilenmiyor
plan      aynı gerekçe (ölçülmedi ama aynı sınıf — aşağı bak)
```
⛔ `agreement.seed.ts`'in *"aynı sınıfta"* olması **doğru** (kod ölçüldü), ama
**bedeli yok**: dönemi kaymış bir anlaşma **hiçbir testi düşürmüyor**.
⇒ Kalem **kapanmıyor**, ama **tarihli olmaktan çıkıyor** — `1 Ekim` bir **eşik değil**.

---

## ⛔ VE BU ÖLÇÜMÜN KENDİSİ İKİ KUSUR ÜRETTİ

### `1` · GÜN TAŞMASI — **kendi kuralımın sınıfı** (`T-328`)
```
UPDATE agreements SET end_date = end_date + INTERVAL '3 months'   (dünya B)
UPDATE agreements SET end_date = end_date - INTERVAL '3 months'   (geri alma)
[ÖLÇÜLDÜ: snapshot diff]  STA-2026-0002  2026-08-31 → 2026-08-30   ⛔ TERSİNİR DEĞİL
   2026-08-31 +3ay → 2026-11-30 (Kasım 30 çeker, CLAMP) → -3ay → 2026-08-30
```
⇒ Snapshot'tan **açıkça** geri yazıldı; sonra **birebir** doğrulandı.
📌 `T-328`'in (`addMonthsClamped`) tam sınıfı — ve bu kez **veri geri alırken**.
⛔ **Ders:** bir geri alma **ters işlemle** değil, **SNAPSHOT'tan** yapılır.

### `2` · ÇÖKEN BİR KOŞUM ARTIK BIRAKIR — ve `T-047` GÖREMEZ
```
[ÖLÇÜLDÜ] pozitif kontrol koşumu EXIT 1 verdi ⇒ baseline_volume_import_batches 2 satır,
          _batch_rows 6 satır KALDI
[ÖLÇÜLDÜ] T-047 TEMİZ koşumda PASS — çünkü invaryant KOŞUM-İÇİ (öncesi/sonrası),
          ve bir sonraki koşumun TABANI artığı SOĞURUR
```
⇒ Artık **bir sonraki temiz koşumun teardown'ı** tarafından silindi (ölçüldü: `0`/`0`).
📌 Ama sınıf gerçek ve `T-377`'nin ailesinden: **`T-047` koşumlar ARASI artığı görmez.**

---

## KAPANIŞ — ORTAM
```
[ÖLÇÜLDÜ: snapshot diff]  ENV 10 satır + AGR 5 satır  →  ✅ BİREBİR
[ÖLÇÜLDÜ: temiz e2e]      64/887 · [T-047 invariant] PASS
```
