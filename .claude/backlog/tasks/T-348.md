---
id: T-348
title: "plan_mechanic_values.plan_sku_id — S1/Z74'ün taşıyıcısı (FU-varsayılan + SKU-override)"
epic: E-001
sprint: S-001
status: todo
assignee: data-engineer
priority: P0
created: 2026-08-31
updated: 2026-08-31
touches:
  - collmind.backend/src/database/migrations/1821000000000-*.ts
  - collmind.backend/src/database/entities/plan.entity.ts
migration_seq: 1821000000000
---

## Hüküm `Z79 §2` (`W2`) — aday **(a)**

```
plan_sku_id NULL  = FU değeri geçerli
plan_sku_id dolu  = o SKU için EZME
çözümleme          TEK RESOLVER — SKU satırı varsa O, yoksa FU  (targetRoi deseni)
```

## ⛔ İKİ BAĞLAYICI ŞART

**1 · UNIQUE kısıtı `K-2.2.8c` dersiyle** (`L2_01:561-569` — *"en spesifik kayıt kazanır"*):
**kısmi-tuple eşitliği düşünülmüş olsun** ⇒ **`NULLS NOT DISTINCT`** + resolver'da
**AÇIK ÖNCELİK**. ⛔ **Gizli tie-break YOK** (`CLAUDE.md §2.5`).
Bugünkü `UNIQUE(plan_fu_id, mechanic_id)` **revize edilir**.

**2 · `plans=0` PENCERESİNDE iner.** Pencere kapanırsa iş bir **veri taşıma** işine
dönüşür — ve o **BAŞKA bir karardır**, bu migration'ın kapsamı değil.
⇒ İnmeden önce **ölç**: `SELECT count(*) FROM main.plans;` **`0` değilse DUR ve bildir.**

## Reddedilen adaylar — bir daha tartışılmasın

| aday | red gerekçesi |
|---|---|
| `plan_fus.tactics` jsonb | **tipsiz** — `SKUContext` markasıyla kurulan tip-zorlaması ailesine aykırı |
| `mechanic_spend_breakdown` | eksen **hazırdı** (`UNIQUE(plan_sku_id, mechanic_id)`) — **tuzak buydu**: çıktı tablosuna girdi koymak bir satırı **hem kaynak hem sonuç** yapar (`Z79 §2`) |

## Done ek maddeleri
- [ ] `run → revert → run` ampirik; **üç durum ayrımı** (`1805`/`1808` deseni)
- [ ] `plans=0` penceresi migration ÖNCESİ ve SONRASI ölçüldü
- [ ] Entity metadata migration ile **birlikte** (`DISIPLIN`: şema kararını geri alırken metadata'yı da geri al)
