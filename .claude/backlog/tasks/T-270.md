---
id: T-270
title: 🔴 Canlı bütçe doğrulaması ÖLÜ veri modelini okuyor — her plan "yetersiz bütçe"
epic: E-001
sprint: S-001
status: in-progress
assignee: architect
created: 2026-08-23
updated: 2026-08-23
touches:
  - collmind.backend/src/modules/shared/spend-calculation/spend-validation.service.ts
  - collmind.backend/src/modules/shared/budget/budget-allocation.service.ts
---

## Bulgu — `T-265` ölçüm turunun yan bulgusu

```
main.budget_allocations   0 satır     ← ÖLÜ model
main.budget_envelopes     4 satır     ← CANLI model (frontend /budget/envelopes çağırıyor)
```

Ve canlı doğrulama **ölü olanı** okuyor:

```
GET /spend-calculation/validate-budget/:planId        ← CANLI ROTA (rota tablosunda doğrulandı)
  spend-validation.service.ts:550
    budgetAllocationService.checkAvailability(...)
      findMatchingAllocation(...)  →  null
        budget-allocation.service.ts:308-317
          return { onInvoiceAvailable: 0, onInvoiceSufficient: false, ... }
```

⇒ **Her planda "Insufficient On-Invoice budget"** — gerçek zarf bütçesi ne olursa olsun.

## ⛔ VE BU BİR SİLME UYARISIDIR — `T-265` ile kesişiyor

`budget_allocations`'a **üretimde tek yazma yolu** `POST /budget-allocations`, ve o uç
`T-265`'in `14`'ünün **DIŞINDA**. Controller **tümüyle** silinirse:
- tabloyu doldurmanın tek yolu kalkar → kusur **düzeltilemez** hâle gelir
- ⛔ `test/t254-empty-scope-budget-utilization.e2e-spec.ts:111` o ucu **çağırıyor** → e2e kırılır

> **`T-265`'te yalnız `5` uç silinmeli, controller DEĞİL.**

## `İlke 4` — iki bütçe müsaitlik implementasyonu

| implementasyon | model | çağıran |
|---|---|---|
| `BudgetAllocationService.checkAvailability` | **ölü** (`budget_allocations`) | 1 (`spend-validation:550`) |
| `budgetRepository.checkBudgetAvailability` | **canlı** (zarf) | `budget.service`'te 5 + approval-workflow |

## ⚠️ Ve `T-265` görev dosyasındaki bir premise YANLIŞTI — Team Lead'in

Dosyada *"`BudgetAllocationService` CANLI → spend-validation **ve finance-reporting**
kullanıyor"* yazıyordu. Ölçüm:

```
finance-reporting.service.ts   enjeksiyon 1  ·  ÇAĞRI 0
POZ.KONTROL  aynı dosyada this.accessScopeService. → 2 çağrı   ✅ desen çalışıyor
```

`CLAUDE.md`'nin *"ENJEKSİYON kullanım değildir"* kuralının yeni vakası — ve kuralı yazan
Team Lead ihlal etti. Gerçek tüketim **tek bir çağrı**.

## Karar gerekiyor
- [ ] Canlı doğrulama zarf modeline mi taşınır, yoksa `budget_allocations` mı doldurulur?
- [ ] `İlke 4`: iki implementasyondan hangisi kanonik, diğeri ne olur?
- [ ] ⚠️ **Ne bozulabilir:** doğrulama düzelince bugün geçen planlar **reddedilmeye
      başlayabilir** — ya da tersi. Önce/sonra ölçümü zorunlu


---

## 2026-08-23 — ÖLÇÜLDÜ, ve SEVERITY YÖN DEĞİŞTİRDİ

### ✅ Ürün sahibinin hipotezi ÇÜRÜDÜ — gönderim etkilenmiyor

> *"Bugün bir `PLANNER` bütçesi olan bir planı bile gönderemeyebilir."*

**Hayır.** Zincirin ucunda **kapı yok**:

```
spend-validation.service.ts:550  →  validateBudgetImpact  (:463)
                                 →  validateBeforeSubmission (:659)
                                 →  spend-calculation.controller.ts:232  ← TEK tüketici, bir @Get
SpendValidationService'i enjekte eden: YALNIZ kendi controller'ı (:99)
```

Gerçek gönderim kapıları **zarf modelini** kullanıyor:
`plan.service.ts:882` `reserveTypedForPlan` · `approval-workflow.ts:165`
`checkPlanBudgetAvailability`. İkisinde de `BudgetAllocationService` **geçmiyor**.

⚠️ Yanlış pozitif elendi: `grep 'validationService\.'` altı çağrı daha buldu — üçü **başka
sınıf** (`OffInvoice…` · `OnInvoice…` · `SalesActuals…`). Aynı değişken adı, farklı tip.

### 🔴 AMA daha kötüsü var — ve BRIEF'İN DIŞINDAYDI

Aynı ölü tablo, **canlı bir dashboard'a** besleniyor:

```
finance-reporting.service.ts:160  this.budgetAllocationRepository.find(...)
  içinde: getBudgetUtilization (:146)
    ← GET /finance-reporting/budget-utilization
    ← dashboard.service.ts:142  →  GET /dashboard/summary
        →  DashboardPage.tsx:165 → BudgetUtilizationPanel   ← KULLANICI EKRANI
        →  utils/export.ts:123                              ← EXCEL ÇIKTISI
```

**Davranışsal, Team Lead bağımsız doğruladı:**

```
DB   main.budget_envelopes  →  4 zarf · toplam ₺1.600.000 · spend_type NULL: 4/4
API  GET /dashboard/summary →  budgetUtilizationStatus = 'ok'
                               total = {allocated:0, utilized:0, reserved:0,
                                        available:0, utilizationPercent:0, status:'GREEN'}
```

> **₺1.6M zarf bütçesi dururken dashboard `₺0 · GREEN · "ok"` basıyor.**
> `§2.5` (sessiz sıfır) **ve** `§2.3` (RAG) ihlali, **canlı ve kullanıcıya görünür** bir
> finansal ekranda. Verinin yokluğu bir **iş yargısına** çöküyor.

⚠️ **Ve `unavailable` koruması bunu GÖRMÜYOR — üstelik ürün bunu zaten yazmış.**
`dashboard-summary.dto.ts:76-82` (`T-098` notu) *"`getBudgetUtilization` boş listede
sıfır raporlar, yani `budgetUtilization` veri yokluğundan ASLA null olmadı"* diyor, ve
aynı DTO *"NEVER render unavailable as zero"* diye uyarıyor. **Koruma doğru, kapı hiç
ateşlemiyor** — `T-098` boş tabloyu *teorik* sanmış; bugün **fiilî** durum o.

### ⚠️ Ve bir ölçüm hatam bu bulguyu KAPATIYORDU

Önceki turda *"`finance-reporting` enjekte ediyor, çağırmıyor → tek tüketici"* yazmıştım.
`budgetAllocationService` için **doğru** (çağrı `0`). Ama soru **tablo**ydu ve tabloya
`budgetAllocationRepository` üzerinden erişiliyor. **Ölçüm doğru, SORU dardı** — ve
çürüten bir ölçüm yanıldığı için gerçek bir kusuru kapatıyordu. `CLAUDE.md`'ye kural
olarak eklendi.

### ⛔ DUR TETİKLENDİ — bu bir TAŞIMA değil, DAVRANIŞ DEĞİŞİKLİĞİ

| boyut | ölü model | zarf modeli |
|---|---|---|
| eşleştirme | `cplId` + `channel` + `category` + tarih **ARALIĞI** | `channelCode` + `periodMonth` + `spendType` · **`cplId` YOK, `category` `undefined`** |
| dönem | aralık örtüşmesi | tek ay **string**'i |
| para kaynağı | **denormalize kolon** | `v_budget_summary` — **defterden** türetilmiş |
| On/Off | tek satırda iki kolon | **iki ayrı zarf** + `UNSPLIT`/`SPLIT` (ADR 0004 K2) |
| **sıfır bacak** | ⛔ `sufficient:false` — sıfır harcayan plana *"Shortfall: 0.00"* | ✅ `sufficient:true` |
| çoklu eşleşme | ⛔ `.getOne()` + `DESC` — **sessiz tie-break** (`§2.5`) | zarf boyutları tekil |

### 📌 Ve `budget_allocations` bir GÖÇ KALINTISI DEĞİL — brief'in okuması TERSTİ

```
CreateBudgetEnvelopes            1704067500000   2024-01-01   ← ZARF ÖNCE
UpdateBudgetAllocationStructure  1771169825000   2026-02-15   ← tahsis createTable
AddMetadataToBudgetAllocations   1786000000000   2026-08-06   ← ⚠️ İKİ HAFTA ÖNCE
```

Zarf modelinden **~14 ay sonra** doğdu ve **hiç benimsenmedi** — bir kalıntı değil,
**ölü doğmuş paralel bir model**. Ve **hâlâ bakım alıyor**: sıfır satırlık bir tabloya
iki hafta önce migration yazıldı. Yani modelin ölü olduğu **bilinmiyor** — kusurdan ayrı
bir sinyal.

### `§2.7` bulgusu — bugün koşan TEK dal test edilmemiş

```
budget-allocation.service.spec.ts  describe('checkAvailability')
  :222 · :258   getOne.mockResolvedValue(mockAllocation)      ← DOLU dal
  getOne…mockResolvedValue(null)  →  0 eşleşme
  POZ.KONTROL   aynı spec'te getOne mock'u → 7 eşleşme  ✅
```

`null` dalı — **üretimde bugün koşan tek dal** — hiçbir testle kapsanmıyor. Fixture'lar
tam da ayırt etmesi gereken tarafta veri taşıyor (`§2.7 #4`).

---

## ÖNERİ — üç aşama, ve ikincisi ürün sahibi kararı

### `A1` · ACİL, model kararını BEKLEMEZ

`getBudgetUtilization` boş kümede **sıfır raporu üretmemeli** → `unavailable`.
Sözleşme `dashboard-summary.dto.ts:76-95`'te **zaten yazılı ve doğru**; eksik olan onu
tetikleyen dal. Ve `status: 'GREEN'` **girdi yokken hesaplanmamalı**.

> Bu düzeltme her iki dünyada da doğru: tablo doldurulsa da, doğrulama zarfa taşınsa da,
> *"boş küme → sıfır rapor"* yanlıştır.

⚠️ **NE BOZULABİLİR:** `DashboardPage.tsx:165` `summary?.budgetUtilization &&` koşuluyla
panel **tümüyle kaybolur**, ve `utils/export.ts:123` Excel'den bir **sayfa düşer**.
Kullanıcıya **regresyon gibi görünür** — ama bu bir **kusur kapanmasıdır** ve öyle
yazılmalı (`§ serileştirme`). Önce/sonra ekran kanıtı şart.

### `A2` · MODEL KARARI — ⛔ ÜRÜN SAHİBİ

| seçenek | bedel |
|---|---|
| zarfa taşı, `cplId`/`category` boyutlarını **düşür** | CPL/kategori bazlı bütçe **yeteneği kaybolur** — BRD'ye karşı doğrulanmalı |
| zarfa taşı, boyutları **zarfa ekle** | `budget_envelopes` şema değişikliği · `v_budget_summary` etkilenir |
| `budget_allocations`'ı **doldur** | ölçüm **desteklemiyor**: canlı para akışının tamamı zarfta, ve iki model **mutabakatsız** olur — bugün olmayan bir mutabakat yükümlülüğü doğar |

### `A3` · `spend-validation:550` — `A2`'ye bağımlı

⚠️ Her hâlükârda yanlış olan: `:308-317` `sufficient:false` + `shortfall:0` —
**sıfır talep için "yetersiz" demek** bir semantik hatadır ve ADR 0004 K2'ye aykırıdır.

---

## `T-265` KESİŞİMİ — bağımlılık TEK YÖNLÜ ve DAR

`budget-allocation.controller.ts` **11 uç** (ölçüldü):

```
🟢 KALMALI    POST /budget-allocations  (:50)   tek yazma yolu · t254 e2e çağırıyor  ← DOĞRULANDI, bayat değil
🟡 KALMALI    PATCH :id (:129)                  1'in tamamlayıcısı
🟡 A2'yi BEKLE check-availability · reserve · commit · release · adjust
🔴 ŞİMDİ SİL  GET liste (:86) · GET :id (:112) · reports/utilization (:257) · reports/forecast (:283)
```

> **`T-265`'in `4` ucu (`2`,`3`,`10`,`11`) ŞİMDİ silinebilir** — hiçbiri `T-270`'in karar
> uzayını daraltmıyor. Yalnız `check-availability` `A2`'yi bekler.

## Kabul kriterine yazılacak FIXTURE (`§` sağlayıcı şartı)

```
FIXTURE  e2e: 1 tenant + 1 CPL + 1 plan + ≥1 plan_fu + ≥1 plan_mechanic_value
         + 1 budget_envelope (spend_type NULL = UNSPLIT)
         T-254 deseni · kalıcı seed DEĞİL (T-047)
KANIT    önce/sonra GET /dashboard/summary → budgetUtilizationStatus
         önce = 'ok' + ₺0     sonra = 'unavailable'
```


---

## 2026-08-23 — `A2` KARARI VERİLDİ (`Z21`)

> **Zarfa taşı, `cplId` boyutunu DÜŞÜREREK.** Ve *"bedel"* çerçevesi **reddedildi**:
> o boyut bir yetenek değil, bir **eksen ihlaliydi**.

Üç dayanak — ikisi ürün sahibinin, üçüncüsü ölçümden:

```
K-2.2.1   zarf = Kanal × Kategori × Dönem     ← CPL yok
          L2_01'de 'cpl' 2 geçiş, İKİSİ DE K-2.2 bloğunun DIŞINDA (:76 hiyerarşi, :82 FK)
A7        kapsam = kanal + müşteri + kategori  ← CPL'in yeri KAPSAM katmanı
K-2.2.3   "Zarf çözümlemesi TÜM YOLLARDA AYNI boyut kümesini kullanır.
           Farklı bir yol farklı bir boyut kümesiyle zarf ARAYAMAZ."
          Gerekçe: "fark SESSİZDİR."      ← findMatchingAllocation TAM BU
```

📌 Üçüncüsü en güçlüsü: ilk ikisi *"bu yetenek hiç olmadı"* diyor, üçüncüsü **kural bu
şekli adıyla yasaklamış** diyor. `budget_allocations` benimsenmemiş değil — **doğduğu
andan beri `K-2.2.3` ihlali**.

## ⛔ TAMAMLANMA — dört şart (`Z21`)

- [ ] **1 · Davranışsal pin ÇİFTİ:** gerçek veri (4 zarf · ₺1.6M) dashboard'da **görünür**
      ∧ boş-zarf durumu **`unavailable`/GRİ** döner (`GREEN` **değil**). İki girdi, iki çıktı
- [ ] **2 · Üç davranış farkı TEK TEK pinlenir:**
      sıfır-bacak (sıfır harcayan plan *"Shortfall: 0.00"* **üretmemeli**) ·
      dönem aralık↔ay · para kaynağı denormalize↔defter
- [ ] **3 · ⛔ `POST /budget-allocations` çelişkisi çözülür.** *"Tabloyu ölü ilan edip canlı
      yazma yolu bırakmak, ölü modeli yeniden dolduran bir MUSLUK bırakmaktır."*
      `T-265` o ucu tutuyorsa: **tüketicisi kim, zarf yoluna göçü ne?**
      *"POST kalır"* ancak **geçiş dönemi + kapanış tarihi kaydıyla** kalabilir
- [ ] **4 · Tablo:** karar defterine **`F12` deseni** kayıt (iki hafta önce migration'la
      doğdu — **geri alış iziyle**). ⛔ **`3.` şart çözülmeden SİLİNMEZ**

## `A1` onaylandı — ve gerekçesi bir üst kata bağlandı

Boş küme → `unavailable`. **Model kararından bağımsız doğru, çünkü GRİ kuralının kod
hâlidir**: `allocated: 0 + GREEN` sessiz-yeşilin **finansal ekran** hâli, ve
*"kapsama yoksa renk yok"* kuralının kodda ihlali.

## `spend-validation:550` — artık bekleyen değil

`A2` verildi → `check-availability` **zarf yoluna işaret ediyor**. Ve her hâlükârda
yanlış olan `:308-317` `sufficient:false` + `shortfall:0` şart `2` ile kapanıyor.
