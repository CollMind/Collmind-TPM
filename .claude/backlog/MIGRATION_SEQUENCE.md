# Migration numara tahsisi

Ajan kendi numarasını SEÇMEZ. Team Lead buradan tahsis eder ve satırı işaretler.
Sebep: T-030/T-028'de 1790 iki kez alındı (elle yakalandı).

## ⛔ HER ŞEMA-DOKUNUŞLU MIGRATION'IN KABUL KRİTERİ (emsal: `1811000000000`, `Z24`)

**1 · `up()` ÜÇ durumu ayırt eder** (`1805`/`1808`/`1809`/`1810`/`1811` hepsi ampirik):

```
beklenen durum   →  işlemi yap + assert
zaten uygulanmış →  NO-OP, sessizce geç      ← taze/prod DB'de TIKANMAMALI
beklenmeyen      →  ⛔ İPTAL — küme değişmiş, sessizce geçme
```

**2 · `down()` BYTE-BİREBİR doğrulanır** — ⛔ *"`down()` yazıldı ✅"* bir **niyet
beyanıdır**, ölçüm değil:

```bash
pg_dump --schema-only  >  /tmp/before.sql     # DROP/ALTER ÖNCESİ
npm run migration:run
npm run migration:revert
pg_dump --schema-only  >  /tmp/after.sql
diff /tmp/before.sql /tmp/after.sql            # ⛔ BOŞ OLMALI
```

⚠️ **Ve `down()` elle eski migration metinleri birleştirilerek YAZILMAZ** — **canlı
katalogdan** alınan DDL ile yazılır. `1811` böyle yazıldı ve PK/index/FK adları dahil
birebir tuttu; elle birleştirme bu adları **sessizce kaybeder**.

**3 · Dalların en az ikisi AMPİRİK koşturulur** — yazılmış bir dal, çalıştığı anlamına
gelmez.

**4 · `migration:generate` sessiz mi** — `T-101`: entity'de kalan bir tanım bir sonraki
`generate`'te **gerekçesiz** geri gelir. ⚠️ Bilinen gürültü: `T-234`'ün ~1300 satırlık
pre-existing drift'i — **kendi kalemini ondan ayırt et**, throwaway dosyayı **sil**.

**5 · Katalog sorguları ŞEMA-NİTELENDİRİLİR** (`nspname`/`schemaname` predicate'i).
Ve bir nesnenin **yokluğunu** iddia etmeden önce **iki katalogu da** sorgula —
`@Index({unique:true})` bir **index** yaratır, constraint değil.

---

| Numara | Task | Durum |
|---|---|---|
| 1795000000000 | AddSpendTypeToBudgetDimensions | kullanıldı |
| 1796000000000 | ADR 0007 F2/C1 — SplitPlanMechanicEnteredValue (expand) | kullanıldı |
| 1797000000000 | ADR 0007 F2/C2 — DropEnteredValue (contract) | **kullanıldı** — `1803000000000-BDalgasiSemaKalemleri.ts`, run/revert/run boş VE seed'li ortamda doğrulandı |
| 1798000000000 | T-095 — budget_transaction_logs.idempotency_key kısmi UNIQUE (`WHERE key IS NOT NULL`) | kullanıldı |
| 1799000000000 | T-101 — budget_alert_configurations: threshold_percent aralık CHECK (**yalnız CHECK**; kısmi UNIQUE taslağı kapsam dışı olduğu için çıkarıldı → T-108) | kullanıldı |
| 1800000000000 | [[T-141]] / ADR 0009 — mechanics.max_combined_discount_percentage: `CHECK (IS NULL OR > 0)` | **kullanıldı** — `1803000000000-BDalgasiSemaKalemleri.ts`, run/revert/run boş VE seed'li ortamda doğrulandı |
| 1801000000000 | [[T-163]] / **ADR 0011** — GP_ROI_PCT paydası `INCR_SPEND` → `TOTAL_PLANNED_SPEND` (1780 geriye dönük düzenlenmez) | kullanıldı |
| 1802000000000 | **ADR 0012** / [[T-188]] — finansal FK'lar: `purge → agreement_id FK → RESTRICT (⛔ kovası + tenants + */users) → budget_envelopes.deleted_at (zaten vardı, aksiyon yok)`. ⚠️ `tenants` DAHİL — T-188 "Migration issue gövdesi" not 3, offboarding yolu [[T-195]]'te tanımlanana kadar `tenants` de `RESTRICT` | kullanıldı — `1802000000000-FinancialFkRestrictAndLedgerOrphanPurge.ts`, run/revert/run doğrulandı |
| 1803000000000 | [[T-211]] / **`B` dalgası** — `EK_C §B dalgası — kanonik kalem listesi`'nin `S1`–`S14` + `R1`–`R3` kalemleri. ⚠️ **TEK migration, TEK `down`** (ürün sahibi kısıtı: tek geri dönüş noktası). Seed ayrı ve **atomik**. `S3` DÜŞTÜ, uygulanmaz | kullanıldı — `1803000000000-BDalgasiSemaKalemleri.ts`, run/revert/run (boş + seed'li ortamda) doğrulandı. `S13` migration'da yok (servis işi, [[T-207]]). S10 tablo-seçimi (`budget_transactions` vs `ledger_entries`) data-engineer yorumu — Team Lead onayına açık, final rapora bkz. |
| 1804000000000 | [[T-218]] — `plans.coverage_ratio` `numeric(9,4)` nullable. Değer ZATEN hesaplanıyor (`kpi-engine.calculatePlan`), `plan.service.ts:2621-2659` onu atıyor. ⚠️ `calculated_kpis` JSONB **açılmaz** — bugün ihtiyaç olmayan şema esnekliği (`İlke 1`) | kullanıldı — `1804000000000-AddCoverageRatioToPlans.ts`, run/revert/run içerikten doğrulandı; `plan.entity.ts` + `plan.service.ts` (GP_ROI_PCT.coverageRatio pass-through, recompute değil) aynı PR'da. ⚠️ mode-split guard'ı `plan.service.ts`'i BÜYÜDÜ diye işaretliyor (2977→2990, +13 satır) — bu task'ın ürün sahibi talimatının doğrudan sonucu; baseline güncellemesi Team Lead kararına bırakıldı, ben commit etmedim. |
| 1805000000000 | [[T-225]] — `DROP TABLE main.budget_reservations` + `DROP TYPE`. Karar: **ölü iskele** (architect ölçümü 2026-08-15: entity hiçbir listeye HİÇ eklenmemiş, 0 INSERT yolu, TTM'de karşılığı yok, `K-2.5.7` ile uyumsuz). ⚠️ `Alan A` — `money-float --ratchet` koşulur. ⚠️ `down()` tabloyu `1704067560000` ile **birebir** geri kurmalı. ⚠️ `cleanup-data.ts:63` AYNI dalgada temizlenmeli, yoksa `42P01` | tahsis edildi |
| 1806000000000 | `0056-K3` ikinci kararı — `users.permissions` ölü `jsonb` kolonu DÜŞÜRÜLÜR. Ölçüldü 2026-08-16: entity 1 · DB 1 · **kodda okuyan 0**. `0056`: *"ölü bırakılırsa yetkinin ÜÇÜNCÜ olası yeri olarak kalır."* ⚠️ `down()` kolonu geri kurar (nullable `jsonb`, varsayılan yok) | tahsis edildi |
| 1807000000000 | [[T-233]] / `0056-K3(b)` — `role_capabilities` + `capabilities` DÜŞÜRÜLÜR (ölü yapı: 0 satır, üretim tüketicisi yok. ⚠️ *"entity yok"* İFADESİ YANLIŞTI — sınıflar `role.entity.ts` içindeydi, kayıt `Z5`). ⚠️ **Sıra:** `role_capabilities` ÖNCE (o `capabilities`'e CASCADE FK ile bağlı). ⚠️ `roles` KALIR — 5 satır, karar onu etkilemiyor. `down()` `1803000000000`'in ilgili bölümünü **birebir** geri kurar. Üç durum ayrımı zorunlu (`1805` deseni) | tahsis edildi |
| 1808000000000 | [[T-237]] — `user_scopes.category_id` **FK yok** (aynı tabloda üç FK var: cpl/tenant/user, hepsi `CASCADE`). İki karar (ürün sahibi 2026-08-17): **`ON DELETE RESTRICT`** (`ADR 0012` deseni — *"sessiz destruction'ı tespit edilebilir bir hataya çevirir"*; `K-2.6.8a` kapsam tarafında varsayılan KISITLI olduğu için sessiz kayıp en kötü sonuç) + **`115` öksüz satır SİLİNİR** (`NULL`'lamak `PLANNER` deseni = *tüm kategoriler*, erişimi GENİŞLETİR). ⚠️ **Üç durum ayrımı zorunlu:** `115` → sil+assert · `0` → **no-op** · ara sayı → **İPTAL**. ⚠️ Silme sonrası **hiçbir kullanıcı `0` satıra düşmemeli** — assert (bugün `2/2/1` kalıyor, ölçüldü). ⚠️ `entity` ilişkiyi ZATEN tanımlıyor (`@ManyToOne` + `@JoinColumn`) → `migration:generate` bunu artık önermemeli. ⚠️ [[T-234]] drift'iyle kesişim ÖLÇÜLMELİ | **kullanıldı** — `1808000000000-AddCategoryFkToUserScopesAndPurgeOrphans.ts`. Öksüz `115` bağımsız ölçüldü (Team Lead'in sayısıyla birebir eşleşti), silme sonrası `2/2/1` doğrulandı (hiçbir kullanıcı `0`'a düşmüyor). `run→revert→run` ampirik: ikinci `run` `0` dalını (no-op) fiilen tetikledi (öksüzler geri gelmiyor — bilinçli `down()` asimetrisi). `RESTRICT` iki farklı girdiyle sınandı: kapsam satırı olan kategori → hata (`FK_user_scopes_category`), olmayan → başarılı. `migration:generate` artık bu FK'yi hiç önermiyor (`foreignKeyConstraintName` ile entity↔katalog tam eşit) — üç kez ölçüldü (migration'dan önce/sonra/entity reformat sonrası). **`T-234` kesişimi ÖLÇÜLDÜ:** bu FK, migration'dan önceki `1390` satırlık drift'in İÇİNDEYDİ (eşleşmeyen `ADD CONSTRAINT`, `NO ACTION`) — bu migration onu kapattı, `T-234` artık bunu görmeyecek. **Ek bulgu + düzeltme (kapsam dışı değil, `Öksüz satırların kaynağı kapatılır` kriteri):** `cleanup-data.ts`'in `DELETE FROM main.categories`'i `user_scopes`'u hiç temizlemiyordu — öksüzlerin ölçülen kaynağı buydu. `DELETE FROM main.user_scopes WHERE category_id IS NOT NULL` kategorilerden ÖNCE eklendi, `npm run seed:cleanup-and-seed` uçtan uca doğrulandı (33 satır, 0 öksüz). `npm test` (1128) + `npm run test:e2e` (301, T-047 invariant PASS) + `npm run guards` + `money-float --ratchet` hepsi yeşil. |
| 1809000000000 | [[T-238]] — `user_scopes.channel_id` **DROP** (FK değil). Karar (ürün sahibi 2026-08-18): `K-2.1.4` kanalı `cpl_id`'den **garantili** türetiyor (`cpls.channel_id NOT NULL`, tekil FK, DB seviyesinde) → kolon **ikinci ve zorlanmayan** bir kaynak; `İlke 4` (*"aynı olgunun iki temsili zamanla ayrışır"*) + `İlke 1` (ihtiyaçsızlık **ölçüldü**: kod okuyan 0 · yazan 0 · DB'de dolu satır 0/37). ⚠️ **Entity'den de kaldırılır** (`T-101`: yoksa `migration:generate` geri getirir). ⚠️ `@ManyToOne(() => Channel)` ilişkisi **YOK** (ölçüldü — düz `@Column`), yani `T-237`'nin entity↔katalog ayrışması burada **doğmuyor**. ⚠️ `UQ_user_scopes_user_cpl_category` `channel_id` **içermiyor** → tekillik etkilenmez. ⚠️ `down()` kolonu geri kurar (`uuid`, `nullable`) — **veri asimetrisi YOK**, kolon `37/37 NULL`. Üç durum ayrımı (`1805`/`1808` deseni): kolon var → düşür · yok → **no-op** · dolu satır varsa → **İPTAL** | **kullanıldı** — `1809000000000-DropChannelIdFromUserScopes.ts`. **Üç dal da Team Lead tarafından ampirik koşturuldu:** DROP (`revert`→`run`, `DROP COLUMN` ×1) · **NO-OP** (kolon yokken kayıt silinip `run` → `EXIT 0`, `DROP COLUMN` ×0, kontrol sorgusu ×2 — taze/prod DB'de tıkanmaz) · **İPTAL** (sahte dolu satır → `EXIT 1`, kolon **bozulmadan** kaldı, kayıt girmedi). `down()` **simetrik** (`uuid/YES`) — `1808`'den farkı, orada veri asimetrisi vardı. Katalog: 4 FK ve UQ index **aynı**. `T-234` kesişimi **YOK** (ölçüldü). tsc 0 · 1129 unit · 301 e2e · T-047 PASS · guards 0 |
| 1810000000000 | [[T-245]] / `T-242a` `B1` — `UQ_user_scopes_user_cpl_category` **NULL'ları ayırt ETMİYOR**. Ölçüldü (code-review, canlı katalog): `(user, cpl, NULL)` iki kez → **ihlal yok**; `(user, NULL, cat)` iki kez → **ihlal yok**; pozitif kontrol `(user, cpl, cat)` iki kez → `23505`. `R1/A5` gereği her `CATEGORY_MANAGER` çifti `cplId=null`, her `PLANNER` çifti `categoryId=null` taşır → **kuralın kapsadığı çiftlerin ezici çoğunluğunda index hiç ateşlemiyor.** Karar (ürün sahibi 2026-08-20): `COALESCE` ya da `NULLS NOT DISTINCT` ile kapatılır. ⚠️ **Entity'den de güncellenir** (`T-101`: yoksa `migration:generate` geri getirir; `@Index`'e **ad yaz**). ⚠️ Üç durum ayrımı (`1805`/`1808`/`1809` deseni): yinelenen satır **yok** → düz değiştir · **var** → İPTAL (temizlik ayrı karar) · index yok → no-op. ⚠️ Canlı DB'de bugün yinelenen satır **0** (ölçüldü, pozitif kontrollü) | **kullanıldı** — `1810000000000-UserScopesUniqueNullsNotDistinct.ts`. **Yol: `NULLS NOT DISTINCT`** (server 16.13, `SHOW server_version` ile ölçüldü; emsal `budget_policies`/`1803000000000` aynı deseni kullanıyor — `COALESCE` sentinel'i reddedildi, `İlke 4`). `data-engineer` bulguyu bağımsız tekrarladı (transaction+ROLLBACK: DENEY1/DENEY2 ihlal yok, POZ.KONTROL `23505`) — code-reviewer'ın sayısıyla eşleşti. **Dört durum da (üç zorunlu + bir idempotency no-op) fiilen tetiklendi:** (1) yinelenen-yok→alter — gerçek `migration:run`, `GROUP BY...HAVING>1` 0 grup, `DROP+CREATE...NULLS NOT DISTINCT`; (2) yinelenen-var→İPTAL — gerçek bir dup satır eklenip gerçek `migration:run` çalıştırıldı, `EXIT=1`, `ROLLBACK`, index bozulmadan plain kaldı, `migrations` tablosuna girmedi (sonra dup satır silindi, tekrar `run` başarılı); (3) index-yok→no-op (`up`) — gerçek migration sınıfı bir throwaway transaction'da çağrıldı (index `DROP` edilip `up()` invoke edildi, index geri gelmedi, sonra `ROLLBACK`); (4) index-yok→no-op (`down`) — aynı transaction'da `down()` da çağrıldı, aynı sonuç. İki-girdi-iki-çıktı: `(user,cpl,NULL)` tekrar → RET (`23505`); farklı `(user,cpl,category)` çifti → KABUL. `down()` gerçek `migration:revert` ile koşturuldu, plain UNIQUE'e simetrik döndü (katı→gevşek geçiş her zaman güvenli). **Entity:** `@Index(['userId','cplId','categoryId'],{unique:true})` KALDIRILDI (`budget-policy.entity.ts` deseni — TypeORM `NULLS NOT DISTINCT`'i ifade edemiyor). **`migration:generate` — TAM SESSİZ DEĞİL:** T-234'ün bilinen büyük pre-existing drift dosyasında (1386 satır) bu index 2 satır olarak görünüyor (`DROP` + `CREATE UNIQUE INDEX ... (plain)`) — AMA **`budget_policies`'in emsal index'i BİREBİR AYNI ŞEKİLDE** aynı dosyada görünüyor (satır 212/1178, benimki 216/1174). Bu, TypeORM'un raw-SQL-only index'leri (entity'de tanımsız) her tam drift-regen'de "fazlalık" sayıp DROP önermesinin YAPISAL sonucu — NULLS NOT DISTINCT'i decorator'la ifade etmenin imkânsızlığından geliyor, benim eklediğim YENİ bir drift sınıfı değil. Throwaway dosya commit edilmedi, silindi. **`updateScope` reaktivasyon gerekçesi ETKİLENMEDİ** (ölçüldü: index hâlâ non-partial, yalnız NULL semantiği değişti; kod zaten `targetKey`'de NULL'ları uygulama katmanında eşit sayıyordu). `npm test` 1143 · `npm run test:e2e` 330 (T-047 PASS, boru olmadan `EXIT_CODE=0` ayrıca doğrulandı) · `npm run guards` exit 0 · `money-float --ratchet` exit 0 · `tsc --noEmit` exit 0. DB son durum: 37 satır (test öncesi/sonrası birebir), `indnullsnotdistinct=true`. |
| 1811000000000 | **`Z24`** / `Z21` şart 3+4 — `DROP TABLE main.budget_transaction_logs` **SONRA** `main.budget_allocations`. ⚠️ **SIRA ZORUNLU:** logs → allocations (`FK_d803327caaf6745f39ccc8729da` logs'tan allocations'a bakıyor). Karar: ölü model — `K-2.2.3` ihlali olarak doğdu (`Z21`), tüketicisi kalmadı (`T-265`: `POST`'un controller dışında **sıfır** çağıranı; `BudgetAllocationService`'in **sıfır** dış çağıranı; `BudgetTransactionLog`'un tek tüketicisi o servis). İki tablo da **`0` satır** (ölçüldü). ⚠️ `down()` **üç** migration'ın ilgili bölümünü birebir geri kurmalı: `1771169825000` (allocations yapısı) · `1786000000000` (metadata kolonları) · `1798000000000` (`budget_transaction_logs.idempotency_key` **kısmi UNIQUE** `WHERE key IS NOT NULL`). ⚠️ **Üç durum ayrımı zorunlu** (`1805`/`1808`/`1809`/`1810` deseni): tablo var+boş → düşür · yok → **no-op** · **satır varsa → İPTAL**. ⚠️ `cleanup-data.ts:61-62` AYNI dalgada temizlenmeli, yoksa `42P01`. ⚠️ `app_runtime` GRANT'ları aynı commit'te düşer (`K-2.6.13` birebirlik) | **kullanıldı** — `1811000000000-DropBudgetAllocationsAndTransactionLogs.ts`. **DÖRT dal da ampirik koşturuldu:** DROP (`run` exit 0, katalog doğrulandı) · `down()` (`revert` exit 0, ve `pg_dump` çıktısı drop ÖNCESİ dump ile **byte-birebir aynı** — PK/index/FK adları dahil; `down()` elle üç migration birleştirilerek DEĞİL, canlı katalogdan alınan DDL ile yazıldı) · NO-OP (tablo yokken `up()` — hata yok, `DROP` denenmedi) · İPTAL (gerçek satır → `run` exit 1, `ROLLBACK`, iki tablo da geri geldi). `migration:generate` **sessiz** (throwaway drift 1308 satır = `T-234` ailesi; bu iki tablo için **0** satır). `GRANT`'lar aynı commit'te düştü, `app_runtime`'da kalan **yok**. 1127 unit · 378 e2e (`T-047` PASS) · guards 0 · tsc 0 · lint 0. Rota 230→223. |
| 1812000000000 | **`Z30 H8`** / `Z35 §3` — `UNRESTRICTED` terfisi şık **(c)**: `ADMIN` + `FINANCE` kullanıcılarına **joker `user_scopes` satırı** (BACKFILL-EMNİYETİ). ⚠️ **KURMA DEĞİL EMNİYET:** yaratma katmanı (`user.service.ts` `resolveScopeRowsToWrite`, `WILDCARD_ON_CREATE_ROLES` — *`H8` ile yeniden adlandırıldı, eski adı `WILDCARD_SCOPE_ROLES`*) bu satırları **zaten yazıyor** — ölçüldü 2026-08-24, canlı DB'de boşluk **0** (`ADMIN 1/1` · `FINANCE 2/2` · `READONLY 1/1`). Migration'ın işi **bu DB değil, HER DB**: `T-241` öncesi doğmuş kullanıcılar. ⚠️ **ÜÇ DURUM AYRIMI ZORUNLU** (`1805`/`1808`/`1809`/`1810`/`1811` deseni): eksik satır **var** → yaz + assert · **yok** → **no-op** (taze/prod DB'de tıkanmaz) · **beklenmeyen ara durum** → **İPTAL** (sessiz-tamir yasağının migration hâli — sessizce tamamlamaz, DURUR). ⚠️ **ATOMİKLİK:** bu migration `UNRESTRICTED_ROLES` kod dalının kaldırılmasıyla **AYNI dalgada** iner; satır gelmeden dal ölürse `buildScope` `rows.length===0` → `SCOPED{pairs:[]}` → `ADMIN`/`FINANCE` **FAIL-CLOSED düşer** (`resolveScope:169` satırları OKUMADAN kısa devre yapıyor — ölçüldü). ⚠️ `down()`: **yalnız bu migration'ın YAZDIĞI satırları** siler (zaten var olanlara dokunmaz) — aksi hâlde geri alma `READONLY` desenli meşru satırları siler. ⚠️ Joker satır `is_active=true` ve `(cpl_id, category_id) = (NULL, NULL)`; `UQ` `NULLS NOT DISTINCT` (`1810`) yürürlükte, yani ikinci bir joker satır `23505` verir → **idempotency bu index'ten gelir, ayrıca kontrol edilir** | **kullanıldı** — `1812000000000-BackfillWildcardScopeForAdminFinance.ts`. **BEŞ kova** (üç zorunlu + `deleted_at` evren uyuşmazlığı + joker-yanında-dar), hepsi throwaway transaction + `ROLLBACK` ile **fiilen tetiklendi**: eksik→yaz · pasif joker→İPTAL · soft-silinmiş joker→İPTAL (`UQ` partial DEĞİL, anahtarı hâlâ işgal ediyor → ham `23505` yerine adlandırılmış hata) · joker+dar bir arada→İPTAL. Gerçek `run→revert→run→revert→run` (5 adım) exit 0, DB 37 satırda sabit. ⚠️ **Bu DB'de `up()` NO-OP'tur** (boşluk `0`) — yazma dalı gerçek `migration:run`'da KOŞMADI, ayrı ölçüldü (`code-reviewer`: `ADMIN`'in joker satırı silinip `SELECT` koşturuldu → `missing` kovası, `INSERT 0 1`, `ROLLBACK`). `down()` **sentinel** (`created_by = 00000000-…-1812`) ile izole — `created_by`'de FK yok (katalogdan, `nspname='main'`), gerçek `createdBy` her zaman gerçek bir `users.id`, ve reaktivasyon sentinel'i **kaybettiriyor** → `down()` meşru satıra dokunamaz. `migration:generate` bu tablo için **0** satır (kalan `T-234` ailesi). tsc 0 · unit 1136 · e2e 404 (`T-047` PASS) · guards 0 |
| 1813000000000 | **`T-308` / `Z45 §1`** — `main.v_budget_summary` `security_invoker = true`. Bulgu: view `app_migrate` sahipliğinde, `security_invoker` YOK, `app_runtime` SELECT hakkı var → RLS politikası yazılsa bile view atlar (bugün RLS hiçbir tabloda yok, ölçüldü — davranışsal etki bugün sıfır, ama `RLS` paketinden ÖNCE gelmesi zorunlu, `Z45 §1`). Envanter: `main` şemasında **tek** view (`v_budget_summary`) — hepsi invoker'lı olacak beklentisi doğrulandı. ⚠️ Üç durum ayrımı (`1805` deseni): zaten invoker → NO-OP · değilse → ALTER + assert · view yok → İPTAL | **kullanıldı** — `1813000000000-SecurityInvokerOnBudgetSummaryView.ts`. Üç dal da ampirik koşturuldu: ALTER (`run`, reloptions `NULL`→`{security_invoker=true}`) · `down()` (`revert`, reloptions **byte-birebir** `NULL`'a döndü) · NO-OP (throwaway transaction + `ROLLBACK`: `up()` ikinci kez çağrıldı, `ALTER VIEW` sorgu logunda **basılmadı**, reloptions değişmedi). `app_migrate`-owner + `app_runtime`-SELECT dokunulmadı. |
| 1814000000000 | **`INV-B-009` / `Z47`** — `main.budget_envelopes.available_amount` **DÜŞÜRÜLÜR**. Karar (ürün sahibi, 2026-08-27): şık **(ii) türev** — kolon hiçbir reserve/commit/release yolu tarafından senkronlanmıyordu (yalnız `createEnvelope`/`splitEnvelope`'ta yazılıyordu), canlı DB'de 4 zarfın 2'si `v_budget_summary`'den ayrık. Kanonik kaynak `v_budget_summary.available_amount` (sorgu anında türetilir). ⚠️ `ADR 0012` ihlali YOK — satır silinmiyor, yalnız bayat türev kolon düşüyor. ⚠️ Üç durum ayrımı (`1805`/`1808`/`1809`/`1810`/`1811`/`1812`/`1813` deseni): kolon yok → NO-OP · kolon beklenen şekilde (`numeric(15,2) NOT NULL`, default yok) → DROP · beklenmeyen şekil → İPTAL. Dört-yüzey okuyucu taraması (`src`/`test`/`collmind.frontend/src`/`scripts`, pozitif kontrollü) zorunlu | **kullanıldı** — `1814000000000-DropAvailableAmountFromBudgetEnvelopes.ts`. **Üç dal da ampirik koşturuldu:** DROP (`run`, `pg_dump` diff'i yalnız kolon satırının eksikliğini gösterdi, başka fark yok) · `down()` (`revert`, tip/precision/scale/nullable/default **byte-birebir** aynı — ama ⚠️ ordinal pozisyon KORUNMAZ, bu PostgreSQL'in `ADD COLUMN`'ı her zaman sona eklemesinin yapısal sonucu, dosya başı yorumunda belgelendi; `down()` ayrıca **bilinçli asimetrik** — geri kurulan değer `0`'dır, gerçek değer değil, çünkü kolonun gerçek değeri zaten güvenilmezdi) · NO-OP (throwaway transaction: kolon zaten yokken `up()` çağrıldı, `DROP COLUMN` sorgu logunda basılmadı) · **İPTAL** (throwaway transaction: kolon `nullable` olarak yeniden eklenip `up()` çağrıldı, beklenen ASSERT hatası fırlatıldı, kolon bozulmadan kaldı). **Okuyucu taraması (dört-yüzey, poz. kontrollü) BEKLENMEYEN bir okuyucu buldu:** `budget.service.ts#findAllEnvelopes/#findEnvelopeById` — entity'yi çıplak döndürüyordu, `GET /budget/envelopes(/:id)` üzerinden `collmind.frontend`'in envelope liste/dashboard bileşenlerine besleniyordu; kolon düşseydi frontend `envelope.availableAmount.toLocaleString(...)` üzerinde ÇÖKERDİ. Her iki metot `v_budget_summary`'den zenginleştirmeye taşındı (JSON alan adı korunur). Diğer düzeltilen okuyucular: `on-invoice-validation.service.ts` (ayrışma-alarmı KALDIRILDI, tek kaynağa döndü — INV-B-009 unit testleri tek-kaynak sözleşmesini ölçecek şekilde yeniden yazıldı) · `agreement-transaction.controller.ts:214,216` (view'a döndü). Yazma yolları (`createEnvelope`/`splitEnvelope`) kolona artık yazmıyor; seed (`budget-envelope.seed.ts`) `availableAmount` alanını kaldırdı. `migration:generate` bu kolon için **sessiz** (throwaway drift dosyası — yalnız bilinen `T-234` ailesi kalıntısı, `budget_envelopes.available_amount` için sıfır satır). **`SYSTEM_INVARIANTS.md`:** `INV-B-009` `RESOLVED` (`F12`/`0006-R`, eski `VIOLATED` metni silinmedi, üstüne çözüm izi yazıldı), yeni ifade *"`available` hiçbir yerde SAKLANMAZ; her okuma DEFTER-TÜRETİMİDİR"*; `D-18` `ÇÖZÜLDÜ` olarak işaretlendi. **`money-float --ratchet`:** iki dosyada bulgu arttı (`agreement-transaction.controller.ts` 2→3, `budget.service.ts` 7→9 — üçü de `Number(summary.availableAmount)`, ADR 0007 Karar 3b'nin hedeflediği eşitlik/aritmetik sınıfı DEĞİL, view→JS-number passthrough, aynı dosyalarda zaten baseline'da olan desenin yeni örnekleri); ~~baseline aynı dalgada güncellendi (114→117)~~ ⛔ **REDDEDİLDİ ve GERİ ALINDI** (Team Lead, `Z47` review): üç `Number(summary.availableAmount)` bir **no-op**tu — `BudgetSummaryView.availableAmount` `@ViewColumn({transformer: DecimalTransformer})` ile **zaten `number`** (canlı probe: ORM yolu `number`, ham SQL `string` ⇒ ayırt edici). Üstelik **nötr değil, LATENT BİR SESSİZ SIFIRDI**: `parseFiniteOnRead` `null`'ı olduğu gibi döndürür, `Number(null)` → **`0`** (`§2.5`, ve bir RAG yolunda). Üçü **kaldırıldı**, baseline **114'te KALDI**, `--ratchet` **exit 0**. ⇒ **Ratchet'i yeşile döndürmenin iki yolu vardı: baseline'ı YÜKSELTMEK ya da NO-OP'u SİLMEK.** `tsc --noEmit` 0 · unit 1160/1160 · e2e 801/801 (`T-047` PASS) · `npm run guards` 0 · `lint-ratchet --ratchet` 0 (iki pre-existing eslint hatası — `budget-envelope.entity.ts` unused `OneToMany`, `agreement-transaction.controller.ts:140` unused `status` — bu diff'ten ÖNCE vardı, ratchet baseline'da, dokunulmadı). |
| 1815000000000 | **`K1a` / `Z52 §1`/`§2`** — `admin_audit_logs.tenant_id` FK `ON DELETE CASCADE` → `RESTRICT` (denetim izi, iz sürdüğü nesnenin yaşam döngüsüne tabi olamaz — `ADR 0012`'nin denetim-katmanı kardeşi) VE `tenant_id` `NOT NULL` → `NULLABLE` (`NULL` = platform-seviyesi eylem, katman-bilgisi). `admin_id`/`admin_email` NOT NULL KALIR. Bulgu pakette yoktu (`Z51 §4`) — bir tenant silinince TÜM denetim geçmişi siliniyordu, `CLAUDE.md §2.3` ("audit immutable") ihlali. Üç durum ayrımı: (CASCADE,NOT NULL)→UYGULA · (RESTRICT,NULLABLE)→NO-OP · başka herhangi bir kombinasyon→İPTAL. `down()`: NULL tenant_id satırı varken NOT NULL'a dönmeyi REDDEDER (İPTAL — sessiz veri kaybı yasağı). Constraint adı `FK_df6424756dc2edd5ef911dbe777` korunur (canlı katalogdan ölçüldü). | **kullanıldı** — `1815000000000-RestrictAndNullableTenantOnAdminAuditLogs.ts`. **Üç dal da ampirik koşturuldu:** UYGULA (`run`, gerçek `migration:run`, `ALTER COLUMN DROP NOT NULL` + `DROP/ADD CONSTRAINT ... RESTRICT` + `COMMENT ON COLUMN`) · NO-OP (migrations satırı silinip `run` tekrar çağrıldı — yalnız SELECT'ler koştu, hiçbir ALTER/DROP/ADD/COMMENT basılmadı) · **İPTAL** (`down()`, gerçek NULL-tenant fixture satırı eklenip `migration:revert` çağrıldı — `EXIT=1`, `ROLLBACK`, katalog bozulmadan RESTRICT/NULLABLE kaldı, `migrations` satırı silinmedi). `down()` ayrıca normal yol (`revert`, fixture yokken) ile de koşturuldu — `pg_dump --schema-only` diff'i ÖNCESİYLE **byte-birebir** (yalnız `pg_dump`'ın rastgele `\restrict` token'ı farklı). **Üç davranışsal pin:** RESTRICT (izole iki-tenant fixture'ı, `T-047` etkilenmeden `ROLLBACK` içinde — denetim satırı olan tenant silme → `FK_df6424756dc2edd5ef911dbe777` ile RET) · nullable-tenant (tenant_id NULL satır YAZILABİLİYOR, admin_id NULL hâlâ RET — iki-girdi-iki-çıktı) · `app_operator` (rolsuper=f, rolbypassrls=t, rolcanlogin=t, rolconfig={log_statement=all} — `app_runtime`'ın rolconfig'i BOŞ, canlı sapma temizlendi) · `NOCREATEROLE` (CREATE ROLE denemesi RET). `migration:generate` — İLK turda FK/yorum drift'i görüldü (entity'de `comment` metadata'sı yoktu), `@Column({comment:...})` eklenip İKİNCİ turda **sessiz** (yalnız kalan `created_at`/index-adı `T-234` ailesi — index sayısı admin_audit_logs için değişmedi, FK `ON UPDATE NO ACTION` gürültüsü proje genelinde 190/189 FK'de aynı, benzersiz değil). `tsc --noEmit` 0 · unit 1151/1151 · `npm run guards` exit 0 (`migration-schema` bulgusu `pg_constraint` sorgusuna `n.nspname=$2` predikat'i eklenerek kapatıldı) · `money-float --ratchet` temiz (yeni dosya `Number()`→`parseInt(...,10)`, 113→112) · `lint-ratchet --ratchet` temiz (prettier format düzeltmesi) · e2e **iki kez tam koşum**: 1. tur 787/790 (3 başarısızlık, izole tekrar koşumda ÜÇÜ de PASS — sıraya-bağlı, pre-existing flake, bu değişiklikle ilgisi ölçülüp elendi) · 2. tur **790/790**, `T-047` PASS her iki turda da. Frontend: `type-check` 0 · `eslint db-cleanup.ts` 0 · unit 536/536. **DB nihai durum:** 39 satır (öncesi/sonrası birebir), FK `RESTRICT`, `tenant_id` nullable, `app_operator` canlı GRANT'lerle çalışıyor (`03-operator-grants.sql`). |
| 1816000000000 | **`Z57`** / [[T-317]] — bildirim şeması: `NotificationType` enum'una **`BUDGET_FINANCE_REVIEW`** + **zarf-başına "son bilinen kademe"** (`P3` tekrar-bastırma: *"olay bir GEÇİŞTİR, durum değil"*). ⚠️ Enum bugün `%90` karşılığı **taşımıyor** (ölçüldü: `BUDGET_ALERT_80`/`BUDGET_ALERT_100` var, arası yok). ⚠️ **`collmind.frontend/src/types/notification.types.ts` enum'u backend'le BİREBİR** — aynı dalgada hizalanır. ⚠️ `ALTER TYPE ... ADD VALUE` **geri alınamaz olabilir** — `down()` yolu **ölçülür**, mümkün değilse gerekçe dosya başına yazılır ve Team Lead'e bildirilir. ⚠️ Son-kademe için **kolon mu tablo mu** ölçerek karar verilir (`İlke 1`: bugün ihtiyaç olmayan esneklik açılmaz). ⛔ **`7/14` için HİÇBİR ŞEY EKLENMEZ** (`Z57 §2a`: ertelendi). Üç durum ayrımı zorunlu (`1805`+ deseni) | **kullanıldı** — `1816000000000-AddBudgetFinanceReviewNotificationTypeAndEnvelopeTier.ts`. **PARÇA A** (`NotificationType.BUDGET_FINANCE_REVIEW`): `ALTER TYPE ... ADD VALUE` server 16.13'te (ölçüldü) TypeORM'un varsayılan TEK-transaction sarmalayıcısı İÇİNDE çalıştı — `transaction=false` GEREKMEDİ (repo emsali `1775`/`1776`'nın aksine); değer AYNI transaction'da kullanılmadığı için PG12+ kısıtına takılmadı. **PARÇA B** (`budget_envelopes.last_notified_tier`): **KOLON** seçildi, TABLO değil — ölçülen gerekçe: `K-2.2.7a` TEK monoton merdiven (zarf her an TEK basamakta), `P3`'ün üç örneği de tarihçe değil EN SON durumu gerektiriyor, `İlke 1` (emsal `T-218`) bugün ihtiyaç olmayan esnekliği kapattı. **Üç durum ayrımı HER İKİ parça için ayrı ayrı, DÖRT dal ampirik koşturuldu** (throwaway transaction + `ROLLBACK`, script silindi): NO-OP+NO-OP (ikinci `up()` çağrısı, zaten uygulanmış) · İPTAL (Parça B kolonu `DROP NOT NULL` ile bozulup `up()` çağrıldı → hata, kolon bozulmadan kaldı) · İPTAL (Parça A `down()`, fixture `notifications` satırı `type='BUDGET_FINANCE_REVIEW'` ile eklenip çağrıldı → "1 satır taşıyor" hatası) · TAM BAŞARILI down() (0 satırken, tip yeniden oluşturuldu — 0 satır bırakan izole transaction). **Gerçek `run→revert→run` de ayrıca koşturuldu:** `run` (7. enum değeri + kolon eklendi, 4 mevcut zarf `NONE`'a düştü) · `revert` (`pg_dump --schema-only` diff'i revert-öncesi/sonrası arasında YALNIZ rastgele `\restrict` token'ı ve migration'ın kendi eklediği iki blok farkı gösterdi — enum 6 değere, kolon yokluğuna **byte-birebir** döndü) · `run` (ikinci kez, temiz). `migration:generate` bu iki kalem için **sessiz** (throwaway drift dosyası 1304 satır — bilinen `T-234` ailesi, silindi). **Frontend aynı dalgada hizalandı:** `notification.types.ts` + `NotificationItem.tsx` (yeni `case`, `default`'a düşmüyor artık — brief'in "UI bilinmeyen türe düşer" uyarısı kapatıldı), `type-check` 0. **İzolasyon:** `git worktree` (`T-316` paralel koşuyordu, ana ağaçta o sırada `budget-policy.service.ts` vb. commit edilmemiş değişiklikler VARDI — ölçüldü, hiç dokunulmadı); doğrulama tamamen worktree'de yapıldı, dosyalar sonda `cp` + `shasum -a 256` ile ana ağaca senkronlandı (checksum birebir eşleşti). `npm run guards` ilk turda `route-cell-map` G7 ve `lint-ratchet --ratchet` ile KIRMIZI çıktı — ikisi de teşhis edildi: G7 worktree'nin `../docs` sembolik bağlantısı olmadığı için SETUP HATASI'ydı (meta-repo dosyası, submodule'de yok — symlink kurulunca `route-cell-map: temiz`); `lint-ratchet` benim YENİ migration dosyamın prettier-formatsız doğmasıydı (`eslint --fix` ile düzeltildi, sonra `temiz`). Ayrıca bir kez `app-runtime-grants-self-test`'te "Broken pipe" ile flaky kırmızı görüldü — izole tekrar koşumda temiz (benim değişikliğimle ilgisiz, grants.sql'e hiç dokunulmadı). **Nihai `npm run guards` exit 0** (money-float --ratchet temiz 112, lint-ratchet --ratchet temiz 184, route-cell-map temiz, single-mechanism temiz). `tsc --noEmit` 0 (backend + frontend) · unit 1151/1151 · e2e 790/790 (`T-047` PASS) · `docker ps --filter label=com.docker.compose.project=tpm` boş. **DB nihai durum:** migration UYGULANMIŞ (enum 7 değer, `budget_envelopes.last_notified_tier` 4 satırda `NONE`). |
| 1817000000000 | **`Z64 §1-3`** / [[T-293]]+[[T-291]] (`W2` `DALGA-B`) — `agreements`(type=LTA) ↔ `lta_agreements`/`lta_rates` **BAĞI** + `T-291`'in dört `|| 0` düşüşünün kaldırılması. ⛔ **Tablolar BUGÜN BOŞ** (`lta_agreements 0` · `lta_rates 0` · `lta_plan_overrides 0`) ⇒ `T-273` ailesi: **kalıcı değer taşıyan FIXTURE ŞART**, yoksa pin `0`'larla yeşil geçer. ⚠️ Üç durum ayrımı zorunlu (`1805`+ deseni) · katalog sorguları **şema-nitelendirilmiş** · doğrulama **izole `git worktree`'de** (`DALGA-A` paralel koşuyor) | **kullanıldı** — `1817000000000-BindLtaAgreementsToAgreements.ts`. Kolon `main.lta_agreements.agreement_id` `uuid NOT NULL` + FK `FK_lta_agreements_agreement` → `main.agreements(id)` `ON DELETE RESTRICT` + `UNIQUE INDEX UQ_lta_agreements_agreement_id`. `NOT NULL` gerekçesi `Z38 §3(a)` (*"oran kademesi ebeveyne BAĞLI DOĞAR"*); nullable bir kolon bugünkü kırık durumu şemaya yazardı. **Dört dal da AMPİRİK koşturuldu:** UYGULA (gerçek `migration:run`, ADD COLUMN + FK + UNIQUE + COMMENT + assert) · `down()` (gerçek `migration:revert`, `pg_dump --schema-only` diff'i revert-öncesi/sonrası arasında YALNIZ `pg_dump`'ın rastgele `\restrict` token'ını gösterdi — **byte-birebir**) · **NO-OP** (`migrations` satırı silinip `run` tekrar çağrıldı → `EXIT 0`, `ALTER/CREATE/COMMENT` sorgu logunda **0 kez**, yalnız 8 `SELECT`) · **İPTAL ×3** (throwaway transaction + `ROLLBACK`: `up()` kolon-yok+satır-var → İPTAL · `up()` yarım-uygulanmış (kolon var, FK yok) → İPTAL · `down()` satır-var → İPTAL) + `down()` kolon-yok → NO-OP. `run→revert→run` **hem SEED'Lİ** (canlı `collmind_tpm`) **hem BOŞ** ortamda (sıfırdan `collmind_tpm_1817` DB'si, 75 migration, sonra revert+run; katalog doğrulandı: `is_nullable=NO`, `confdeltype=r`, UQ index var; DB sonra düşürüldü) koşturuldu. **FK davranışı iki-girdi-iki-çıktı:** bağlı ebeveyni silme → `FK_lta_agreements_agreement` ile RET · bağsız başka bir ebeveyni silme → İZİN VERİLDİ (poz. kontrol). **`migration:generate` TAM SESSİZ** — ilk turda entity'de `comment` metadata'sı eksik olduğu için `COMMENT ON COLUMN ... IS NULL` + FK DROP/ADD çifti görüldü (`1815` dersinin tekrarı); `@Column({comment:...})` eklendikten sonra ikinci turda throwaway drift dosyası **1304 satır** (bilinen `T-234` ailesi, `1816` turunda ölçülenle **aynı sayı**) ve benim kalemlerim için **0 satır** (poz. kontrol: aynı dosyada `IDX_lta_agreements_status` **2 kez** geçiyor). Throwaway dosyalar silindi. ⚠️ `app_runtime` `main.agreements` üzerinde SELECT'e **zaten** sahipti (`02-runtime-grants.sql` tur 1) — yeni GRANT gerekmedi, ölçüldü; `app-runtime-grants` guard'ı 0 bulgu. Aynı dalgada [[T-291]]'in dört `|| 0` düşüşü kaldırıldı. tsc 0 · unit 1241/1241 · hedefli e2e 64/64 (`T-047` PASS) · `npm run guards` exit 0 · `money-float --ratchet` temiz (112) · `lint-ratchet --ratchet` temiz (183) · `improved` satırı **0**. Doğrulama izole `git worktree`'de yapıldı (`DALGA-A` paralel), dosyalar `cp` + `shasum -a 256 -c` (13/13 OK) ile ana ağaca senkronlandı. ⚠️ **REVIEW KAPANIŞ TURU (2026-08-30):** `src/modules/shared/lta` `money-float-domain-a.txt`'e eklendi (Team Lead) — ratchet bu modülü **hiç ölçmemişti** (`§2.7 #9`); ekleme `EXIT 1` verdi (`lta-calculation.service.ts` `Number(value)`, `ADR 0007 Karar 8.2`), düzeltme **baseline değil KOD**: `Number()` no-op olduğu ölçülüp kaldırıldı (`parseFiniteOnRead` zaten finite `number` döndürüyor), `Number.isFinite` doğrudan uygulanıyor. Ayrıca `updateAgreement`'ın `cplId` sessiz-yoksayması kapatıldı (reprodüksiyon: mutasyonla `200` görüldü) ve `BASE_TO` assertion'ına `Z65 §1`/`T-334` **beklenen-değişim** şerhi yazıldı. Kapanış turu kapıları: tsc 0 · unit **1242/1242** · hedefli e2e 64/64 (`T-047` PASS) · `guards` exit 0 · `money-float --ratchet` **exit 0** · `lint-ratchet --ratchet` exit 0 · `improved:` **0**. |
| 1818000000000 | **`Z65`+`Z66`** / [[T-334]] — **FORMÜL-KANON DÜZELTMESİ** (`Q2`+`Q3`+`Q5`+`Q8`+`Q6`, **9+1 kalem TEK PAKET**): `TO`/`NIV` kavram-ayrıştırma (`BASE_NIV`/`PLANNED_NIV`/`INCR_NIV` **doğar**, `*_TO` gerçek `TO` semantiğine **döner** — `1780`'in formülleri) · GP tabanı `TO` · off-invoice tabanı `NIV` (`LTA_Off` düşülmez) · kalem-22 (`PLANNED_LTA_OFF` tabanı) · ROI paydası **BÖLÜNÜR** (`TOTAL_PLANNED_SPEND` bütçede **KALIR**, ROI `INCR_PROMO_SPEND` sınıfı okur — `ADR 0011` `F12`). ⛔ **`plans=0` PENCERESİ**: bugün yalnız `kpi`-TANIM tablosu, ilk müşteri planıyla **veri-migration'ına döner**. ⚠️ Üç durum ayrımı (`1805`+ deseni), dalların en az ikisi ampirik; `run→revert→run` boş VE seed'li. ⚠️ **Beklenen-değişim listesi ZORUNLU** (RAG renkleri + `e2e:436` şerhli assertion + GP değerleri) | **kullanıldı** — `1818000000000-FormulaCanonTurnoverNivSplit.ts`. **DÖRT dal da AMPİRİK:** `APPLY` (exit 0, kpi 27→32) · **`NO-OP`** (`migrations` kaydı silinip yeniden koşuldu → `INSERT` 0 / `UPDATE` 0, exit 0) · **`İPTAL`** (`BASE_TO` formülü kasten bozuldu → exit 1, açık mesaj, **32 satır bozulmadan kaldı**; `mechanics` metni için de ayrı üç-durum) · **`down()`** (`revert` sonrası `main.kpis` dökümü T-334 ÖNCESİ snapshot ile **byte-birebir**, `mechanics.calculation_formula`+`description` de geri döndü; `pg_dump --schema-only` diff'i yalnız pg_dump'ın rastgele `\restrict` nonce'unda farklı — **DDL YOK, veri migration'ı**). ⚠️ `transaction` **varsayılan** bırakıldı: ilk koşumda `transaction=false` ile İPTAL dalı **yarım bir `BASE_NIV` satırı BIRAKTI** (ölçüldü) — atomiklik ŞART. ⚠️ Yazma ifadelerinin **dönüş değerine** dayalı sayım TERK EDİLDİ: `queryRunner.query()` `INSERT…RETURNING` için satır dizisi, `UPDATE…RETURNING` için `[rows,count]` döndürüyor (ölçüldü) ⇒ doğrulama **katalogu okuyarak**. `migration:generate` **1304 satır** = `T-234`'ün bilinen drift'i; bu migration **0 satır** ekledi, throwaway silindi. `npm run seed` sonrası kpi dökümü **değişmiyor** (seed migration hedefini geri almıyor). tsc 0 (BE+FE) · 1244 unit · 536 FE unit · 249 hedefli e2e (11 suite, `T-047` PASS) · `npm run guards` exit 0 · `money-float --ratchet` 0 · `lint-ratchet --ratchet` 0. ⛔ `Q7` (RAG kadranı) **uygulanmadı** — canlı model tek-eksen ölçüldü, `eşleşen-sapmalı` olarak kayda girdi, hüküm bekliyor. ⭐ **ÜRÜN-GÖRÜNÜR YAN SONUÇ (`S1`, review'da eklendi):** payda `INCR_PROMO_SPEND` olduğu için **promo mekaniği olmayan (LTA-only) planlarda payda tam `0`** ⇒ `GP_ROI_PCT` `null` ⇒ **RAG rengi de `null`** — o plan sınıfı ekranda **renksiz** kalır. Hükmün doğrudan sonucu, hata değil; kadran `HÜCRE 4` `liveRag: null` sabitiyle **pinli**; gösterim kararı ürün sahibinde. 🔁 **REVIEW KAPANIŞ:** `down()`'ın `kpis` yarısına da **üç durum ayrımı** eklendi (`S4`: hedef hâl ⇒ geri al · `PRE` ⇒ NO-OP · başka ⇒ İPTAL, + geri alma sonrası katalogdan assert) — dosyanın iki yarısı artık aynı disiplinde. |
| 1819000000000 | **`Z71 §2`** / [[T-342]] `B2` — `plans.rag_exclusion_reason` kolonu (**TANIMLI-YOKLUK üç yüzeye taşınır**). ⛔ *"Faz-1: yalnız grid"* notu **T-084 problemi** olurdu (yarım durumu belgeleyip **korumak**); `plans=0` penceresi her e2e sonrası açık ⇒ **bugün bedelsiz**. Üç durum ayrımı; `down()` gerçek | tahsis edildi 2026-08-31 |
| 1820000000000 | **`Z71 §3`** / [[T-343]] — `rag_amber_threshold` **ÖLÜR** (tüketicisiz, ölçüldü) + `rag_green_threshold` → **`target_roi_threshold`** YENİDEN ADLANDIRILIR (tüketicisi RAG değil **Target ROI**, `plan.service:2892`; **below-target uyarısının okuduğu kalem TAM O**). ⛔ **CANLIYKEN ölen kolon** (emsal `E2`/`tier_roles` ama o ölü doğmuştu) ⇒ `down()` yolu **daha da önemli**. Rename: veri **taşınır**, kolon **düşürülüp yeniden yaratılmaz** | tahsis edildi 2026-08-31 |
| 1821000000000 | [[T-348]] / `Z79 §2` (`W2` hükmü) — `plan_mechanic_values`'a **nullable `plan_sku_id`** eklenir: `NULL` = **FU değeri geçerli**, dolu = **o SKU için EZME** (`S1`/`Z74`'ün doğal taşıyıcısı). ⛔ **UNIQUE kısıtı `K-2.2.8c` DERSİYLE kurulur** (`L2_01:561-569`): kısmi-tuple eşitliği düşünülmüş olsun — **`NULLS NOT DISTINCT`** + resolver'da **açık öncelik**, gizli tie-break YOK (`§2.5`). Bugünkü kısıt `UNIQUE(plan_fu_id, mechanic_id)` **revize edilir**. ⛔ **`plans=0` PENCERESİNDE iner** — ürün sahibi şartı; pencere kapanırsa migration **veri taşıma** işine dönüşür ve bu bir BAŞKA karardır. ⚠️ Reddedilen adaylar kayda: `plan_fus.tactics` jsonb (**tipsiz**, tip-zorlaması ailesine aykırı) · `mechanic_spend_breakdown` (eksen HAZIR ama **çıktı tablosu** — girdiyi oraya koymak bir satırı **hem kaynak hem sonuç** yapar) | tahsis edildi |
